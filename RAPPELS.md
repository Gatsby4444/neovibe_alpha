# Rappels — NeoVibe

Fichier de mémoire de Jay. **Claude doit le tenir à jour** : y consigner tout
point que Jay demande de « garder pour plus tard » ou de « rappeler », et le
ressortir quand le contexte s'y prête (release de prod, chantier concerné).
Ne rien y supprimer sans validation explicite de Jay.

---

## Avant la mise en production

| # | Sujet | Détail | Depuis |
|---|-------|--------|--------|
| 65 | 🟢 **PING v2 — le GPS oriente, le BLE prouve (v0.9.130)** | **Renversement d'architecture décidé par Jay le 2026-08-25** : le BLE cesse d'être la techno de base pour devenir un **outil de vérification**. Conception complète : `docs/proximite-v2-gps-et-ble.md`. ⚠️ **Correction apportée à la proposition initiale** : elle visait un rayon **GPS de 20 m**, ce que le GPS ne sait pas faire (4-10 m plein ciel, 10-30 m en ville, **20-100 m en intérieur** — or c'est là qu'on se rencontre). À ce seuil le signal est sous le bruit **dans les deux sens** : des gens côte à côte invisibles, des gens d'un autre bâtiment affichés. Le GNSS bi-fréquence ne sauve pas ce cas (ne traverse pas un toit, pas sur tous les appareils, et l'utilisateur peut n'accorder que l'approximatif). **Retenu** : le GPS ne sert qu'à une **cellule d'environ 1 km**, le BLE fournit les 20 m — l'architecture devient **indifférente à la qualité du GPS**. 🔑 **Le mur d'échelle disparaît** : il venait de l'obligation de se CONNECTER pour savoir qui est là (une connexion GATT par inconnu, plafond ~7 sur Android). Le serveur faisant la résolution d'identité, le BLE n'a plus qu'à **écouter** — passif, un-vers-tous, des centaines d'émetteurs. **Zéro connexion GATT dans tout le ping public.** 🔒 **Le serveur ne rend que des JETONS OPAQUES** : ni profils, ni identifiants, ni compte. Sans être physiquement là, la liste n'apprend rien. Et **un profil n'est révélé que si les DEUX se sont entendus** — même propriété anti-traque que le croisement d'amis (#55). ✅ **PROUVÉ EN BASE** sous identité utilisateur, sécurité active, en rollback : unilatéral → 0 paire et personne de visible ; mutuel → paire, profil, messagerie. Huit cas négatifs passent (écouter sans s'annoncer → liste vide ; messagerie sans proximité refusée ; carreau éloigné vide ; jeton d'un bloqueur retiré ; antidatage, jeton inconnu et son propre jeton → 0). ⚠️ **NOUVELLE PERMISSION** : `ACCESS_FINE_LOCATION` est **débornée** (elle était limitée à Android 11 pour le scan BLE). 🟢 **« Pendant l'utilisation » seulement** — aucune position n'est lue app fermée, et **toujours aucune `ACCESS_BACKGROUND_LOCATION`** (vérifié sur le manifeste fusionné *release*). ⚠️ **Android 12+ permet d'accorder l'approximatif (~3 km)** : insuffisant pour un carreau d'1 km. L'app le DIT (`LocationBlocker.approximate`) au lieu d'afficher une liste vide — sans quoi ce serait indiscernable de « personne autour ». 🔴 **RIEN N'EST SUPPRIMÉ** : l'écran Ping porte les DEUX sections, la v2 et l'ancien chemin BLE, pour comparaison sur appareil. La suppression ne viendra qu'après validation (règle 8). 🟡 **Reste à trancher** (§9 du document) : taille de cellule, cadence, plafond de liste, règles du moment partagé, rétention des confirmations. | 2026-08-26 |
| 63 | ✅ **PROTOCOLE v4 — L'ANNONCE PORTE UN TYPE : public et privé ne partagent plus ni format ni chemin** | **Consigne de Jay, 2026-08-25**, après le premier test à deux appareils où il a vu **« 13 détections »** là où il ne pouvait y en avoir qu'une : *« l'identifiant public ne doit pas être la même chose qu'un jeton d'ami privé, et il devrait y avoir deux modules de réception et traitement propres à chaque type »*. 🔎 **La cause, vérifiée dans le code** : un appareil crie **un jeton par ami** (`AdvertSchedule.perSlot`), et jusqu'au protocole v3 tous partaient comme **16 octets indifférenciés**. Un ami recevait donc les jetons destinés aux **autres** amis de l'émetteur sans pouvoir les distinguer d'un inconnu : **Charles a 5 amis (vérifié en base)**, donc son téléphone apparaissait chez mimi **une fois comme ami et cinq fois comme inconnu**, à chaque tour de rotation. ✅ **Format v4** : `[0..1] "NV" · [2] version=4 · [3] TYPE · [4..19] jeton` — 20 octets, 26 sur les 31 de la trame. `TYPE_PUBLIC = 0x01`, `TYPE_FRIEND = 0x02`. ✅ **Deux chemins à la réception** : un jeton **public** reste une découverte d'inconnu (c'est son rôle d'être capté sans être reconnu) ; un jeton **privé non reconnu se JETTE** — ce n'est pas un inconnu, c'est le jeton d'une autre paire. Et **seul le public ouvre un lien**. ✅ **Auto-filtre ajouté** (`BleEngine.ownTokens`) : le jeton de paire est **symétrique** (`HMAC(secret_DH, "nv-pair-slot")`, identique des deux côtés), donc sans filtre un appareil qui capte sa propre annonce **se reconnaît comme l'ami à qui il crie** — c'est ce qui affichait « mimi tout près » alors que le téléphone de mimi était éteint. Intuition de Jay, confirmée : il n'existait **aucun** filtre, ni en Kotlin ni en Dart. ✅ **Chemin mort supprimé** : `updateAdvert` (Dart + pont + service), reste d'avant le plan d'annonces, sans appelant et incapable de porter le type — deux sens relevés (règle 8). 📊 **Trois compteurs au diagnostic** : `selfScans`, `foreignTokenScans` (natif) et `foreignTokenScans` (Dart). ⚠️ **CHANGEMENT DE PROTOCOLE : les deux appareils doivent être mis à jour ENSEMBLE**, sinon `otherVersionScans` monte et ils ne se voient pas. 🧪 3 tests de garde dans `test/peer_network_test.dart`. 🔴 **NE RÈGLE PAS #62** (la session qui meurt de faim à 30 s) ni la rotation d'adresse à 400 ms — voir #64. | 2026-08-25 |
| 64 | 🔴 **L'ÉMISSION REDÉMARRE TOUTES LES 400 ms — et chaque redémarrage change l'adresse BLE** | **Vérifié dans le code le 2026-08-25** : `ProximityService.cycleMillis = 400`, et `BleEngine.updateAdvert` fait `stopAdvertising()` **puis** `startAdvertising()`. Android attribue une **nouvelle adresse aléatoire** à chaque démarrage d'annonce — c'est une protection de vie privée. 🔎 **Deux conséquences mesurées chez Jay** : ① **le bandeau « tu n'es pas annoncé » CLIGNOTE** — `advertising` passe à `false` à l'arrêt et ne repasse à `true` qu'au rappel asynchrone de la puce, donc l'état publié dit « pas annoncé » **2,5 fois par seconde**. Ce n'est pas un défaut d'affichage : c'est l'état réel, publié fidèlement. ② **les sessions se multiplient** — `presence.observe()` range une session **par adresse BLE et rien d'autre** (`peer_session.dart:447`), donc chaque nouvelle adresse crée une détection. Le Xiaomi de Jay a fermé deux sessions sur **deux adresses différentes** pour la même tablette. ⚠️ **Ça ne passe pas à l'échelle** : avec N amis, un tour complet prend N × 400 ms, soit **8 s avec 20 amis** avant qu'un ami donné soit reconnu. C'est le chantier « émission multi-annonces » (#54 ①), qui attendait un relevé pour être tranché. 🟡 **À trancher par Jay**, avec les chiffres du prochain test. | 2026-08-25 |
| 62 | 🔴 **LA SESSION MEURT DE FAIM À 30 s — cause trouvée au PREMIER test à deux appareils, correctif EN ATTENTE D'ARBITRAGE** | **Premier test réel de la refonte proximité, 2026-08-25, v0.9.128, Xiaomi (Android 13) + tablette Lenovo (Android 10), à moins de 30 cm.** ✅ **Ce qui marche** : la radio, parfaitement — **1 346 et 1 332 annonces NeoVibe reçues**, `otherVersionScans = 0`, `protocolVersion = 3` des deux côtés. Et le chemin pré-Android 12 rétabli le jour même est vert sur la tablette : `needsLocation`, `fgsLocationType`, `locationEnabled` tous à `true`. 🔴 **Ce qui échoue** : `trames applicatives livrées = 0` **des deux côtés**, pour 6 et 3 poignées de main abouties. 🔎 **Cause, mesurée** : les incidents tombent à **exactement 30-31 s d'intervalle** (21:00:27, 21:00:58, 21:01:28) et `PresenceRules.forgetAfter` vaut **30 s**. `lastHeard` n'est nourri que par une **annonce attribuée à la session** ou une **trame reçue** (`noteTraffic`, appelé uniquement à la réception, `peer_network.dart:262`). Une fois le canal chiffré établi, **plus rien ne circule** tant que personne n'écrit : la session meurt de faim, l'entretien la ferme, le pair s'en aperçoit et se reconnecte — **cycle infini de 30 s**. Le canal ne vit jamais assez longtemps pour porter quoi que ce soit. ⚠️ **Distinguer le mesuré du supposé** : la périodicité, l'égalité avec `forgetAfter` et les zéros sont **mesurés** ; que le pair cesse d'émettre pendant qu'il est connecté (fréquent, les piles BLE ont peu de créneaux d'annonce) reste une **hypothèse** — mais le correctif n'en dépend pas. 🟡 **DEUX OPTIONS, À TRANCHER PAR JAY** : ① un **battement de cœur** sur le canal établi (simple, mais du trafic radio pour rien et un réglage de plus) ; ② **ne pas oublier une session qui a un lien vivant** — le départ est déjà signalé par la coupure GATT. *Recommandation : ②, elle supprime la cause au lieu d'ajouter un garde-fou, et le code l'énonce déjà (« un lien est en soi une preuve de présence », `peer_session.dart:468`) — mais seulement à l'ouverture, jamais ensuite.* ⚠️ **Contrepartie de ②** : tant que le lien tient, la **distance** ne se rafraîchit plus (elle vient du RSSI des annonces). ⛔ **Ne rien changer sans l'accord de Jay** : `PresenceRules` porte ses décisions du 2026-08-18. | 2026-08-25 |
| 75 | ✅ **LE TAMPON Ed25519 DE L'APPAREIL EST RETIRÉ — tranché par Jay le 2026-08-27** | Chaque appareil portait **deux** clés : la **X25519** (fabriquer le jeton BLE qu'un ami donné est seul à reconnaître) et l'**Ed25519** (signer ce qui voyageait par la radio). ⚠️ **NE PAS CONFONDRE** : `device_keys.x25519_pub` reste le cœur du ping et est **intacte**. 🔎 **Pourquoi le tampon ne servait plus** : il signait le mini-profil, le certificat de croisement et la demande d'ami co-signée — tous partis avec le transport BLE. Restait une chaîne complète sans consommateur : l'appareil générait la clé, la publiait, et deux fonctions serveur la relisaient que **plus personne n'appelait**. 🔎 **L'argument qui la maintenait en vie est tombé** : je l'avais gardée pour l'attestation contre l'usurpation de pseudo hors ligne (#2). **Cette prémisse est morte** — ce chantier n'existait que parce qu'un pseudo pouvait être annoncé en BLE ; la radio ne crie plus que des jetons opaques. Règle 6 de `CLAUDE.md`, appliquée à une décision que j'avais moi-même justifiée douze heures plus tôt. ✅ **Supprimé** (migration `20260827190000`) : `report_encounter`, `submit_ble_connection`, `private.verify_ed25519`, la colonne `device_keys.ed_pub` ; côté client la paire Ed25519, `edPublicKey`, `sign`, `verify`, et `FriendKeys.edPublicKey`. ⚠️ **La graine privée est EFFACÉE du Keystore**, pas ignorée — ajoutée à `_deadKeys`, avec un test qui le prouve. Un secret orphelin reste un secret. ⚠️ **Réversible** : rien de signé n'est conservé, aucune donnée ne devient illisible. 🔴 **Un client antérieur à v0.9.136 casse sa synchro** (il `upsert` une colonne disparue) : **les deux appareils doivent être mis à jour ensemble**. | 2026-08-27 |
| 74 | ✅ **LES DEUX RECULS D'INTERFACE SONT RATTRAPÉS** | **① L'état d'une demande sortante** se lit dans `connection_requests` via `etatDemandeProvider` — trois états, dérivés du flux temps réel existant, **sans requête nouvelle**. Un seul bouton partagé pour les deux tuiles. **② « Croisés récemment » est REVENUE le 2026-08-27**, sur une source qui marche : `croisesRecemmentProvider` = **la différence entre deux vues qui existaient déjà** — les paires fraîches du serveur moins ceux qu'on entend. **Aucune requête de plus.** L'ancienne lisait les certificats BLE, qui n'ont jamais abouti : elle était vide en permanence. 📏 **Fenêtre : 10 min, celle de `fenetre_rencontre()`**, et c'est raisonné — au-delà le serveur refuse la demande d'ami, donc afficher des gens croisés il y a six heures ferait une liste de boutons qui ne peuvent que dire non. Ces dix minutes **sont** la définition utile de « récemment » : le temps qu'il reste pour ajouter quelqu'un. ⚠️ **Le bouton « Écrire » disparaît sur ces tuiles** : le canal se ferme dès qu'on ne s'entend plus, le garder afficherait un bouton dont le seul effet possible est un refus. ⚠️ **La section ne s'affiche que si elle a quelque chose à dire** — une section vide en permanence apprend à l'œil à ne plus la regarder. | 2026-08-27 |
| 73 | 🔴 **`BLUETOOTH_CONNECT` RETIRÉE LE 2026-08-27 — le SEUL changement du chantier qui ne se prouve que sur l'appareil** | Elle partait avec le bloc GATT : sur Android 12+ elle ne sert qu'à ouvrir une connexion ou un serveur GATT, jamais à annoncer ni à scanner (`BLUETOOTH_ADVERTISE` et `BLUETOOTH_SCAN` s'en chargent). Retirée du manifeste **et** de `BlePermissions.required()`, et sa traduction `Permission.bluetoothConnect` retirée de `ping_screen`. ✅ **Gain réel** : deux permissions Bluetooth à demander au lieu de trois, une de moins à justifier à la Play Console. 🔴 **À LIRE AU PROCHAIN RAPPORT DE DIAGNOSTIC** : si `rawScans` vaut 0 ou si l'émission ne démarre pas, **c'est la première ligne à remettre** — dans le manifeste ET dans `required()`, les deux ensemble. Le reste du chantier est prouvé par `flutter analyze`, 178 tests et la compilation Kotlin ; **celui-ci ne l'est pas**, et il ne lèvera aucune erreur : la radio se taira, simplement. ⚠️ **Le type `connectedDevice` du service de premier plan reste déclaré** alors qu'aucun appareil n'est plus connecté — plus large que la vérité, à revoir avec #71. | 2026-08-27 |
| 76 | ✅ **UNE DEMANDE D'AMI VIT SEPT JOURS — tranché par Jay le 2026-08-27** | La durée de vie était écrite à **trois endroits**, avec deux valeurs : `request_connection_from_proximity` posait `now() + 7 days`, le **défaut de la colonne** `expires_at` valait `now() + 90 secondes`, et un minuteur client (`ProximityRepository._refresh`) réécrivait à **90 s** toute demande dont le destinataire était à portée — dans les 30 s suivant l'envoi. Son commentaire disait *« on prolonge »* : il **raccourcissait**. 🔴 **Et il ne masquait pas, il DÉTRUISAIT** : le cron `neovibe_purge` passe toutes les 5 min et bascule en `expired` toute demande `pending` échue. Une demande était donc **perdue définitivement** 90 secondes après que les deux personnes se soient séparées. 🔎 **Prémisse morte, pas règle fausse** : « la demande expire à la sortie de portée » (spec 4.2) était juste tant qu'il fallait **être à portée pour répondre** — la réponse voyageait dans le canal BLE. Depuis le 2026-08-27 c'est un appel serveur : on accepte de n'importe où. ✅ **Livré** : minuteur supprimé, `liveOutgoingRequestsProvider` supprimé avec lui (son unique lecteur), défaut de colonne porté à 7 jours (migration `20260827150000`, **appliquée et vérifiée sur la colonne**). ⚠️ **La barrière n'est pas affaiblie** : elle est vérifiée **à l'émission** (paire mutuelle < 10 min), et elle le reste. Ce qui change est le temps laissé pour répondre. ✅ Écrit dans `docs/guide-utilisateur.md`. | 2026-08-27 |
| 77 | ✅ **BLOQUER COUPE VRAIMENT — et retirer un ami emporte ce qu'il avait ouvert** | 🔴 **Deux défauts, trouvés le 2026-08-27.** ① **`private.is_blocked` n'était consulté QU'À UN SEUL ENDROIT** : `ping_shortlist`. Bloquer quelqu'un l'empêchait donc de vous **découvrir par le ping**, et rien d'autre — il continuait de voir profil, avatar, stories, et de vous écrire. **Un bouton « Bloquer » qui ne bloque presque rien est pire que pas de bouton.** ② **Retirer un ami ne coupait pas non plus** : reproduit en base en EXÉCUTANT les fonctions, 13 h après un retrait, l'ex-ami voyait encore profil et stories publiques — la ligne `encounters` survivait au lien, et `can_view_profile` la teste **sans limite de temps**. ✅ **Deux corrections de NATURE différente** (migration `20260827180000`) : **bloquer** pose un **garde** en tête de `can_view_profile`, `can_view_stories`, `can_write_in_conversation` et `ping_nearby` — le croisement a bien eu lieu, le blocage le surpasse ; **retirer un ami** **supprime** la ligne `encounters` et les `sightings` de la paire, par un **trigger** sur `connections` — le dérivé n'a plus de raison d'être, et un trigger ne se contourne pas (le retrait est un `delete` écrit dans trois écrans). ⚠️ **`ping_pairs` n'est PAS touchée** : une proximité physique constatée n'est pas un dérivé de l'amitié. ⚠️ **Bloquer ne coupe pas un GROUPE** : la règle d'écriture ne vaut que pour `direct` et `proximity`. 📏 **Éprouvé** : blocage → profil/stories/ping **faux dans les deux sens**, déblocage → l'accès revient ; retrait d'ami → croisement 1→0, constats 31→0, stories coupées ; **deux amis réels se voient toujours** (non-régression). 🟡 **Ce qui RESTE ouvert, et que Jay doit savoir** : après un retrait d'ami, le profil reste visible **par la branche « vous partagez une conversation »** — vérifié, c'est elle et pas le croisement. C'est la sémantique voulue (`conversation_members`), mais elle veut dire qu'**une personne avec qui on a déjà parlé voit son profil pour toujours**, sauf blocage. **Question produit : retirer un ami doit-il aussi fermer la conversation directe ?** | 2026-08-27 |
| 78 | ✅ **LE CANAL DE PROXIMITÉ SE FERME — livré le 2026-08-27** | 🔴 **Ce qu'il y avait avant** : le bandeau *« ce canal se fermera sans échange mutuel »* était une promesse que **rien** ne tenait, dans les trois couches — `_Composer` n'avait aucun paramètre `enabled`, la politique `messages_insert_member` ne vérifiait qu'appartenance et TTL, et aucune purge ne touchait aux conversations. Un canal ouvert une fois servait **pour toujours**, à des kilomètres : la barrière de présence physique se contournait en l'ouvrant une seule fois. ✅ **Décision de Jay** : *« On ferme le canal et le chat reste en lecture seule et après 24 h sans échange il disparaît de la liste (lorsqu'il n'y a plus aucun contenu). »* ✅ **Livré** — migration `20260827160000` : `private.can_write_in_conversation` (exécutable par `authenticated`, piège de 2026-08-11 évité) branchée dans `messages_insert_member` ; cron `neovibe_purge_proximity_conversations` toutes les 5 min. Côté écran : `canalProximiteOuvertProvider`, composer en lecture seule, bandeau réécrit, canaux vides retirés de la liste. 📏 **Seuil : 10 minutes, le MÊME que l'ouverture du canal et la demande d'ami.** Prendre les 30 s du bandeau aurait produit une contradiction (rouvrir un canal qu'on n'a pas le droit d'utiliser) et des refus injustes — le BLE perd des annonces en permanence. 📏 **Reproduit en base sous identité, sécurité active** : paire de 14 h → **refusé** ; paire fraîche → **accepté** ; 10 min 30 s → **refusé** ; conversation **directe** → **toujours acceptée** (non-régression de la politique, qui gouverne TOUTES les écritures de messages) ; purge → épargne un canal avec contenu, supprime un canal vide, cascade vérifiée. État remis à l'identique après le test. ⚠️ **Divergence assumée** : ping coupé = canal fermé côté écran, alors que le serveur accepterait encore pendant au plus 10 min. L'écran est donc **plus strict** que la règle — jamais l'inverse. | 2026-08-27 |
| 79 | ✅ **LA PRÉSENCE SE CONSTATE EN LOCAL — 4 à 7× moins d'appels serveur, et un écran plus rapide** | 🔎 **Question de Jay du 2026-08-27** : *« pourquoi on redemande toujours au serveur une fois qu'une connexion a été vérifiée ? Cela consomme beaucoup de réseau et de batterie. »* Il avait raison. 📏 **Mesuré avant** : par téléphone et par minute, **8 appels seul** (dont 6 pour demander « qui est autour ? » alors que la radio n'entendait rien) et **14 avec quelqu'un**. 🔎 **La cause** : `ping_nearby` ne rendait pas le **jeton** du pair. Le téléphone entendait « le jeton X » sans savoir que X était Bob — il devait donc redemander au serveur toutes les 10 s si Bob était là, alors que sa radio le lui criait dix fois par seconde. ✅ **Livré** (migration `20260827170000` + client) : `ping_nearby` rend le jeton ; l'écran juge la présence **en local** (`ecouteLocaleProvider`, 10 s de silence radio) ; l'interrogation du serveur ne part plus que pour **trois raisons** — un jeton entendu qu'on ne sait pas nommer, un changement de créneau, un geste de l'utilisateur ; le dépôt des jetons passe de 10 s à **60 s**, mais un jeton **jamais entendu** déclenche un dépôt immédiat, ce qui garde la découverte à ~15 s. 📏 **Résultat** : **2 appels/min** dans les deux cas, et la disparition à l'écran passe de 30-45 s à **~10 s**. ⚠️ **Filet obligatoire** : quand le jeton est inconnu (balise expirée, créneau qui vient de tourner), on retombe sur la date serveur pendant 2 min — sans quoi **tout le monde disparaîtrait de l'écran toutes les 15 minutes**. ⚠️ **Ce que le client ne décide PAS** : le droit d'écrire reste arbitré par le serveur. Un client qui mentirait sur sa présence ne mentirait **qu'à lui-même**. C'est ce qui permet de faire confiance au local sans risque. ✅ 4 tests neufs qui **comptent** (`ping_nearby_feed_test.dart`). | 2026-08-27 |
| 80 | 📏 **DEUX FENÊTRES, DEUX QUESTIONS — les « 10 minutes » n'étaient une règle nulle part** | 🔎 **Question de Jay du 2026-08-27** : *« pourquoi 10 min, je trouve que c'est beaucoup, à quoi correspond cette grâce ? »* **Réponse honnête : à rien.** Relevé dans les migrations : elles apparaissent le **2026-08-25**, une seule fois, dans `ping_nearby`, avec ce commentaire — *« Bornée large : la vue affine. Au-delà, la ligne n'a plus de sens. »* C'était une **borne grossière de liste**, écrite pour être affinée en aval. 🔴 **Elle a été recopiée trois fois sans être rediscutée** : ouvrir un canal (2026-08-25), demander en ami (2026-08-27), écrire (2026-08-27 — par moi, et je l'ai **justifiée après coup** par « un seul nombre »). Une borne d'affichage devenue règle de sécurité par copier-coller. ✅ **Remplacées par deux fonctions nommées** : `private.fenetre_canal()` = **3 min** (« sommes-nous encore ensemble ? » — ouvrir ET écrire, la même, sinon on pourrait rouvrir un canal qu'on n'a pas le droit d'utiliser ; 3 min = deux battements de 60 s manqués) et `private.fenetre_rencontre()` = **10 min** (« l'ai-je rencontré récemment ? » — demander en ami, geste délibéré posé APRÈS la rencontre). `ping_nearby` garde ses 10 min, qui sont son sens d'origine. ⚠️ **Des fonctions, pas des littéraux** : un `interval '10 minutes'` écrit à quatre endroits ne peut pas être discuté, il ne peut qu'être copié une cinquième fois. 📏 **Reproduit en base sous identité** : écrire à 2 min 30 → accepté ; à 3 min 30 → refusé ; demander en ami à 5 min → accepté ; conversation directe → toujours acceptée. État remis à l'identique. | 2026-08-27 |
| 61 | ✅ **CLÉ DE SIGNATURE PROPRE À LA MACHINE — RÉSOLU PAR ROTATION DE CLÉ (v0.9.128)** | 🔑 **Jay a retrouvé l'ancienne clé de debug sur l'ancien PC**, et son empreinte correspond exactement au certificat de la v0.9.119 installée. On a donc fait une **rotation de clé** (APK Signature Scheme v3) plutôt qu'une désinstallation : l'APK porte la preuve cryptographique que l'ancienne clé **autorise** la nouvelle, et s'installe par-dessus comme une mise à jour ordinaire. **Aucune perte** — enregistrements, clé d'appareil, tout reste. 📦 **L'APK porte DEUX signataires**, un par plage : **V3.1** (API 33+, le Redmi) avec la nouvelle clé `CN=NeoVibe` ; **V3.0** (API 24-32, la tablette Android 10) avec l'ancienne `Android Debug`. Sur le Redmi la mise à jour passe par la rotation, sur la tablette parce que la signature est identique à l'installée. ⚠️ **La rotation ne s'applique qu'à partir de l'API 33** (défaut d'`apksigner`, choisi par Google car la rotation avait des défauts avant) : la tablette reste donc liée à l'ANCIENNE clé tant qu'elle est sous Android 13. Il faut par conséquent **continuer à signer avec les deux clés**. 🛠️ **`tool/build-release.sh` fait la construction ET la signature.** ⚠️ **Ne plus jamais publier un APK produit par `flutter build apk --release` seul** : il est signé par la nouvelle clé **sans la preuve de rotation** et ne s'installera pas — sans que rien ne le signale, l'APK se construisant parfaitement. 🔴 **Quatre fichiers hors dépôt sont désormais vitaux** : `docdev/neovibe-release.jks`, `docdev/keystore-password.txt`, `docdev/ancienne-cle-debug.keystore`, `docdev/lineage-neovibe.bin`. *(Constat d'origine, conservé :)*  **Panne constatée chez Jay** : `Impossible d'installer (-7) — Signatures d'application en conflit`, juste après avoir réglé le `versionCode` (#60). **Mesuré sur les APK** (`apksigner verify --print-certs`), pas déduit : v0.9.119 installée = certificat `4D:F8:A0:44…`, nouvelle build = `35:A8:03:CC…`. Les deux portent `CN=Android Debug` — mais de **deux machines différentes**. 🔎 **Cause** : `build.gradle.kts` signait les release avec `signingConfigs.getByName("debug")`, et `~/.android/debug.keystore` est généré **par machine**. Le changement de PC du 2026-08-25 a donc rendu toute mise à jour impossible sur les appareils de test. ⚠️ **C'ÉTAIT ÉCRIT ET PRÉVU** : #13 le signalait depuis le **2026-07-26**, en préparant justement une bascule de machine. La dette a été rouverte trois fois sans être traitée, et elle est tombée au premier test réel. ⚠️ **Le paquet de transfert (`RSUNA.md`) listait QUATRE fichiers hors-dépôt et pas la clé** — c'est le trou. ✅ **Corrigé (v0.9.127)** : keystore permanent créé, voir #13. Les deux clés de debug présentes sur la machine du 2026-08-25 (`35:A8:03:CC…` et `FA:C6:17:45…`) ne correspondaient à aucune release ; l'ancienne clé est restée sur l'ancien PC. 💡 **Contrôle avant chaque release**, avec celui du `versionCode` : `apksigner verify --print-certs <apk>` doit afficher **`CN=NeoVibe`**, jamais `CN=Android Debug`. | 2026-08-25 |
| 60 | 🔴 **NE JAMAIS MÉLANGER APK COMPLET ET APK DÉCOUPÉ — le `versionCode` change sans qu'on le demande** | **Panne constatée chez Jay le 2026-08-25** : `Impossible d'installer (-25) — Impossible de rétrograder les applications`, en installant la v0.9.125 par-dessus la v0.9.119. **Mesuré sur les APK eux-mêmes** (`aapt dump badging`), pas déduit : l'installé portait `versionCode = 2209`, le nouveau `215`. 🔎 **Cause** : les releases **v0.9.114 → v0.9.121** ont été publiées avec `--split-per-abi`, et Flutter ajoute alors un décalage au `versionCode` — **1000** pour armeabi-v7a, **2000** pour arm64-v8a, **4000** pour x86_64. Le `0.9.119+209` livré valait donc **2209**. À partir de la **v0.9.122** les APK sont redevenus complets, donc sans décalage : 212, 213, 214, 215 — **tous inférieurs à 2209**. ⚠️ **Les QUATRE releases du 2026-08-25 (122, 123, 124, 125) étaient donc ininstallables**, et rien ne l'a signalé : l'APK se construit parfaitement, c'est l'INSTALLATION qui échoue, chez Jay, à la toute fin de la chaîne. ✅ **Correctif (v0.9.126)** : `versionCode` porté à **3000**, au-dessus du plus haut jamais livré (2211 pour la v0.9.121). **Ne JAMAIS le redescendre.** 3000 laisse la place à un retour au découpage (arm64 vaudrait 2000 + 3000 = 5000, toujours croissant). ⚠️ **La leçon dépasse le nombre** : `versionName` (`0.9.125`) et `versionCode` (`215`) sont deux choses distinctes, et **Android ne lit que le second**. Changer la forme de livraison change le second **sans qu'on le demande** — c'est exactement `feedback-dont-change-how-work-is-delivered`, dont le coût s'est révélé ici. 🟢 **Contrôle à faire avant CHAQUE release** : `aapt dump badging <apk>` \| `head -1` et vérifier que le `versionCode` est bien supérieur à celui de la release précédente — le seul contrôle qui prouve quelque chose. | 2026-08-25 |
| 59 | 🔴 **AVANT LA PROD — `FOREGROUND_SERVICE_LOCATION` exige une déclaration Google Play** | Ajoutée au manifeste le 2026-08-25 avec le retour à `minSdk = 29` (#57). **Pourquoi elle est là** : à partir d'Android 14, tout type déclaré sur un `<service>` exige la permission correspondante, **que le type soit utilisé ou non à l'exécution** ; `ProximityService` déclare `connectedDevice\|location` pour Android 10/11, donc sans elle un appareil Android 14+ refuserait le service. ⚠️ **Elle n'est PAS bornée par `maxSdkVersion`** — contrairement à `ACCESS_FINE_LOCATION` — et c'est délibéré : c'est justement sur Android 14+ qu'elle est exigée. Elle est de niveau **normal** : aucune invite, aucune entrée dans les permissions de localisation de l'app. 🔴 **Ce qu'il faudra faire** : Google Play demande une **déclaration d'usage de la localisation en arrière-plan** pour cette permission, avec justification et parfois une vidéo de démonstration. Notre justification est réelle et défendable : détecter les appareils NeoVibe à proximité, jamais localiser l'utilisateur — et le type n'est **jamais** passé à `startForeground` au-dessus d'Android 11 (`ProximityService.foregroundTypeUsed`). ⚠️ **La sortie de secours, si Google refuse** : remonter `minSdk` à 31 et tout redevient inutile. Ne pas découvrir ce choix au dépôt du premier AAB. | 2026-08-25 |
| 72 | ✅ **LE TRANSPORT BLE A ÉTÉ RETIRÉ — 2026-08-27** | **Fait.** ~3 700 lignes en moins : côté Dart `peer_link`, `secure_channel`, `proximity_protocol`, `transport_trace` (supprimés), plus la moitié de `peer_network`, de `proximity_controller`, de `ping_store` et de `proximity_journal` ; côté natif tout le bloc GATT de `BleEngine.kt` (~300 lignes, serveur ET client), les ordres `connect`/`disconnect`/`send` du pont, et les événements `link`/`frame` du flux. **Le côté receveur est parti aussi** — il n'avait été gardé que le temps que le parc se mette à jour, et il n'y a que deux appareils de développement, tous deux à jour. 🔎 **Vérifié à la source avant de couper, pas déduit** : ① `report_sightings` écrit lui-même dans `encounters`, et rien ne lit `proof` — `can_view_profile` et `can_view_stories` ne testent que l'EXISTENCE de la ligne ; ② la seule ligne de `encounters` porte `mutual_sighting` alors que le défaut de la colonne est `certificate`, donc **le certificat BLE n'a jamais rien produit en deux mois** ; ③ `BleConstants.SERVICE_UUID` n'apparaissait que dans le GATT — l'annonce ne porte que des `manufacturerData`. ✅ `flutter analyze` propre, **178 tests Dart au vert**, Kotlin compilé et ses tests au vert. ⚠️ **Ce qui n'est PAS prouvé sans un appareil** : le retrait de `BLUETOOTH_CONNECT` (voir #73). | 2026-08-27 |
| 71 | 📍 **LE SERVICE DE PREMIER PLAN DOIT PORTER LE TYPE `location` — sinon le ping meurt dès que l'app passe en arrière-plan** | 🔴 **Panne constatée par Jay le 2026-08-26** : *« quand je quitte l'app sur mon téléphone, la tablette ne me voit plus et me supprime de la liste »*. 📏 **Prouvé par les compteurs, pas déduit** : `fgsLocationType` valait **`false`** sur le Xiaomi (Android 13) et **`true`** sur la tablette (Android 10) ; l'incertitude annoncée du Xiaomi passait de **129 m à 449 m** en arrière-plan, et sa balise n'était plus republiée (**158 s d'âge** contre 18 s pour la tablette). Trente secondes plus tard, il disparaissait de l'écran d'en face. 🔎 **La cause était une décision PÉRIMÉE, pas un oubli** : le code refusait le type `location` sur Android 12+, avec ce commentaire — *« se déclarer service de localisation sur un appareil récent serait une affirmation fausse »*. C'était **juste** tant que le service ne faisait que du BLE. **Le ping v2 a changé la prémisse le 2026-08-26** : le service lit maintenant une vraie position, donc l'affirmation est devenue **vraie**. C'est la règle 6 de `CLAUDE.md` — *après un changement d'architecture, rejouer les décisions qui en dépendaient*. ✅ **Corrigé** : le type est déclaré sur toutes les versions. ⚠️ **À AFFINER AVANT LA PLAY CONSOLE**, qui exige une justification par type : le service tourne aussi pour le seul croisement d'amis, qui ne lit aucune position — le déclarer alors est plus large que la vérité. Le correctif juste serait de **repromouvoir** le service quand le ping public démarre. ⚠️ Aucune `ACCESS_BACKGROUND_LOCATION` n'est requise pour autant : avec ce type, « pendant l'utilisation » suffit. | 2026-08-27 |
| 70 | 🔓 **LA BARRIÈRE DE PRÉSENCE PHYSIQUE A CHANGÉ DE NATURE — elle était PHYSIQUE, elle est désormais UNE RÈGLE SERVEUR** | **Décision de Jay du 2026-08-27** : *« on n'utilise plus la poignée de main GATT, la messagerie et les demandes d'amis passent par le serveur, le BLE ne sert qu'à valider et authentifier la proximité réelle »*. Aboutissement du renversement du 2026-08-25 (#65), dont la suppression était annoncée « après validation » — validation obtenue le 2026-08-26 (première paire réelle). 🔴 **CE QU'IL FAUT AVOIR EN TÊTE** : « on ne peut demander en ami que quelqu'un qu'on a physiquement rencontré » était garanti **par la radio** — la demande voyageait dans un canal BLE, donc il fallait être à portée, et le serveur n'avait rien à vérifier. C'est désormais un appel réseau ordinaire : **la garantie est devenue une règle, et une règle peut être contournée si on l'oublie**. ✅ Elle vit dans `request_connection_from_proximity`, qui exige une paire mutuelle fraîche (< 10 min) et refuse entre gens déjà connectés. **Prouvé en base** sous identité, 7 cas : sans proximité → refus ; avec → accepté ; deux appels → une seule demande. 🔴 **PORTE OUVERTE À REFERMER** : la politique `requests_insert_sender` autorise encore l'insertion **directe** dans `connection_requests`, donc de demander n'importe qui sans proximité. Ce n'est pas une régression (elle existait, et aucun code ne s'en servait — `sendRequest` supprimée le 2026-08-16), mais c'est à refermer une fois l'inventaire des chemins légitimes fait : **les recommandations A→B→C en ont un**, ne pas les casser. ⚠️ **Le code de transport BLE n'est PAS supprimé**, seulement plus déclenché par la découverte : `ensureChannel` sert encore aux gestes explicites et au certificat de croisement entre amis (`proximity_controller`). Son retrait est un chantier à part — relever les DEUX sens avant de couper. | 2026-08-27 |
| 69 | 💸 **LA LISTE D'ÉCOUTE COÛTE 48 Ko QUAND ELLE EST PLEINE — et le levier est le FORMAT, pas le plafond** | 📏 **Mesuré le 2026-08-26**, pas estimé : `ping_shortlist` à 500 jetons rend **49 000 octets** de JSON (~98 octets par ligne pour **16 octets utiles** — le jeton voyage en hexadécimal, avec ses guillemets et son créneau). À un appel par minute, liste pleine : **~2,9 Mo/h**. ⚠️ **Ce coût vaut zéro aujourd'hui** (0 balise en base) et ne se paie que si 500 personnes ont le ping actif dans le rayon — il faudrait **1 767 utilisateurs actifs/km²**, soit 9 % de la population de Paris, app ouverte au même instant. Atteint en revanche **immédiatement** dans un stade ou un festival. ✅ **Ne PAS toucher au plafond de 500** : depuis le tri par distance (2026-08-26), la troncature ne retire que des gens plus lointains que les 500 premiers — pour qu'un voisin à 20 m soit coupé, il faudrait **plus de 500 personnes dans un rayon de 20 m**, soit 2,5 personnes au mètre carré (fosse de concert). 💡 **Le jour où le réseau devient un sujet mesuré, le levier est le FORMAT** : jeton en binaire (`bytea`) plutôt qu'en hexadécimal, ou envoi différentiel depuis le dernier appel. **Facteur 6 disponible sans toucher au nombre.** ⚠️ Ne pas l'écrire avant d'avoir la mesure : optimiser un coût qui vaut zéro est une dette gratuite. ✅ **Ce qui A été fait le 2026-08-26** : la troncature ne ment plus. Le client passe sa limite explicitement et l'écran écrit « **Plus de** 500 personnes » quand la liste est pleine — un plafond affiché comme un total était une limite d'instrument présentée comme une mesure. | 2026-08-26 |
| 68 | 📏 **LA PORTÉE GARANTIE DU VOISINAGE EST D'UN CARREAU (~1 km), PAS DE 3 km** | ⚠️ **Mesuré le 2026-08-26, contre ce que le code affirmait et ce que j'ai répété à Jay.** `ping_shortlist` interroge 3×3 carreaux, ce qui fait 3 km **de large** — mais autour de SOI la garantie n'est que d'**un carreau**, soit 0,79 km en longitude à 45° et 1,11 km en latitude. Contre-exemple vérifié en base : deux points distants de **1 490 m** tombent aux carreaux 4885 et 4887, donc **hors** du voisinage ±1. 🔴 **Conséquence produit** : en position approximative (Android répond à ~3 km), la découverte n'est pas dégradée, **elle est impossible**. Le bandeau d'avertissement n'est donc pas une précaution de style. ⚠️ **Ne pas « corriger » en rétrécissant le carreau** : plus petit, il ne rend rien plus précis (la précision vient du BLE), il réduit encore la tolérance à l'imprécision et rend plus discriminant ce que le serveur garde. Si la portée devient un problème mesuré, le levier est le **voisinage** (±2 carreaux), pas la taille de la maille. | 2026-08-26 |
| 67 | 📡 **LE MODE D'ÉMISSION DÉCIDE DE LA MOITIÉ DES CROISEMENTS — `advertMode` est la première chose à lire au diagnostic** | 🔴 **Défaut trouvé le 2026-08-26, jamais relevé avant, et il ne lève rien** : le service émettait **un jeton à la fois**, en tournant toutes les 400 ms. Avec N jetons à crier (un par ami + l'identifiant public du ping), le jeton d'un ami donné n'est en l'air que **1/N du temps** — à dix amis, 10 %. Quelqu'un qu'on croise trois secondes dans un couloir pouvait n'être **jamais** vu : **le mode cycle ne passe pas à l'échelle en nombre d'amis**, et le défaut s'aggrave à mesure qu'on réussit. ✅ **Corrigé** : `BleEngine.applyAdverts` émet tout le créneau **simultanément** via `startAdvertisingSet` (API 26+, plancher 29), en **mode legacy délibéré** — une annonce étendue n'est pas vue par un scan legacy, et passer en étendu nous rendrait invisibles d'une partie du parc **sans lever la moindre erreur**. Effets en cascade : plus de brassage d'adresses (au changement de créneau on remplace les données **sans arrêter** le jeu), et le bandeau « Tu n'es pas annoncé » cesse de clignoter. ⚠️ **`maxParallelSets = 6` est RAISONNÉ, PAS MESURÉ** : aucune API n'expose le nombre de jeux qu'un contrôleur accepte. On tente, et le premier refus fait revenir au cycle **pour tout le monde** (tout-ou-rien : un parallèle à moitié démarré laisserait certains amis annoncés et d'autres muets). 🔴 **Le repli n'a jamais été déclenché sur un vrai appareil.** Au prochain test : lire `advertMode` dans le rapport de diagnostic — s'il dit `cycle`, c'est que l'appareil a refusé, et la ligne de LECTURE dit alors le coût exact. | 2026-08-26 |
| 66 | 📍 **LA POSITION EXACTE EST DÉSORMAIS CONSERVÉE CÔTÉ SERVEUR — décision de Jay du 2026-08-26** | Jusque-là le serveur recevait la position brute, l'arrondissait à un carreau d'~1 km et **jetait le reste**. Jay a tranché : *« je te demande de récupérer la position exacte envoyée par le téléphone et de ne pas l'arrondir au km »*. ✅ **Livré** : colonnes `ping_beacons.lat/lon/acc` (NOT NULL), `ping_shortlist` filtre par distance réelle et **trie par distance** (la troncature à 500 sacrifie donc les plus lointains, plus les moins récents). ⚠️ **CE QUI EST ASSUMÉ, ET QU'IL FAUT RELIRE AVANT LA PROD** : la table contient la position exacte des utilisateurs actifs. Elle est purgée à **15 minutes** (`purge_ping`) et **aucune politique RLS ne l'expose** — seules les fonctions `security definer` y touchent (vérifié en base le 2026-08-26 : RLS active, zéro politique). 🔴 **Le jour où cette rétention augmente, la nature de la donnée change** : ce n'est plus un instantané, c'est un historique de déplacements. 🔴 **Deux garde-fous posés avec Jay, à ne pas lever par inadvertance** : ① **rien de dérivé n'est rendu au client** (ni distance, ni direction) tant que la parade anti-trilatération n'est pas conçue — c'est exactement la faille de Happn (envoyer trois fausses positions, lire trois distances, retrouver l'adresse d'une cible) ; ② la purge doit être **vérifiée**, une tâche planifiée qui échoue est silencieuse. ⚠️ **Cadre légal à traiter À LA CONCEPTION** : #36 vise explicitement les groupes scolaires — position exacte + mineurs est la combinaison la plus encadrée qui existe (base légale, durée de conservation, minimisation). | 2026-08-26 |
| 58 | 🔑 **L'ACCÈS SUPABASE PASSE PAR LE PAT, PAS PAR LE MCP — et ce PAT EXPIRE vers le 2026-09-17** | **Décision de Jay du 2026-08-25** : *« on n'a pas besoin du MCP au final, je crois qu'il n'avait pas fonctionné et donc on avait décidé de faire un PAT »*. Confirmé par les rapports : MCP « opérationnel » le 2026-07-11, puis « non authentifié » le 2026-07-15 ; le PAT a pris le relais et est tracé depuis le 2026-08-18. ✅ **Vérifié le 2026-08-25** : le PAT de `docdev/PATsupabase30days.txt` (gitignoré) répond **HTTP 200** sur l'API Management, et `POST /v1/projects/dvixmhvqqjvbrpsckmyi/database/query` exécute bien du SQL (8 profils en base de dev). La clé publishable de `env.dart` répond aussi (200, et RLS renvoie `[]` en anonyme — comportement attendu). ⏳ **L'ÉCHÉANCE EST LE VRAI SUJET** : le fichier dit 30 jours et il est tracé au 2026-08-18 — **seule et unique occurrence dans tous les rapports**, donc c'est une date de création *au plus tard*, pas exacte. **Expiration au plus tard vers le 2026-09-17, possiblement avant.** La date réelle se lit sur https://supabase.com/dashboard/account/tokens (l'API ne l'expose pas) — à relever une bonne fois plutôt que de la re-déduire. Le jour où il tombe, la panne ne dira pas « jeton expiré » : elle ressemblera à une perte d'accès à la base. **Y penser AVANT** — en régénérer un sur https://supabase.com/dashboard/account/tokens et remplacer le contenu du fichier, sans jamais le committer. ⚠️ **Ce PAT ouvre QUATRE projets** (`neovibe_alpha`, `sonlite-server`, `sondage`, `nailsit`). Jay a dit le 2026-08-25 que l'accès aux autres ne le dérange pas — mais la consigne de `CLAUDE.md` (« jamais de manipulation de données de production ») **reste entière** : sur les trois autres projets, lecture seule et seulement s'il le demande. Ce n'est plus une barrière technique, c'est une discipline — donc écrite ici. 🟡 **`.mcp.json` déclare encore `supabase` et `code-review-graph`** ; le second n'est même pas installé sur la machine du 2026-08-25. À nettoyer si Jay le veut. | 2026-08-25 |
| 18 | 📧 **SMTP PERSONNALISÉ OBLIGATOIRE EN PROD — le service d'email intégré plafonne à 2 mails/heure** | **Mesuré dans les journaux d'auth le 2026-08-16**, pas déduit : deux inscriptions passent (12h43 et 12h44, `mail.send` confirmé dans le journal), **toutes les suivantes tombent en `429 over_email_send_rate_limit`** — y compris depuis un autre appareil, une autre adresse et un autre réseau. La limite porte sur les **endpoints qui envoient un mail** (`/auth/v1/signup`, `/auth/v1/recover`, changement d'adresse), elle est **comptée par projet** et **non relevable** : la doc Supabase précise « Custom SMTP Only ». ⚠️ **Ce n'est donc pas une gêne de dev, c'est un blocage de production** : une app publiée qui plafonne à 2 inscriptions par heure ne peut pas ouvrir. **À faire avant la prod** : brancher un SMTP personnalisé (Resend, Postmark, SES…) et remonter `rate_limit_email_sent`. ⚠️ **Deuxième effet, à connaître pour les tests** : quand le 429 tombe, **l'inscription est annulée en entier** — aucun compte n'est créé (vérifié : trois tentatives sur la même adresse portent trois identifiants différents, tous abandonnés). Un 429 en test ne laisse donc **pas** de compte fantôme, contrairement à un 200 dont le mail n'arrive pas. 💡 **Contournement de dev** : décocher **Authentication → Sign In / Providers → Email → « Confirm email »** — aucun mail n'est alors envoyé et la limite disparaît. 🔴 **Si cette case est décochée, la remettre avant la prod** : sans elle, n'importe qui s'inscrit avec une adresse qui ne lui appartient pas. | 2026-08-16 |
| 17 | 📦 **En production on publie un AAB, pas un APK** | Le Play Store n'accepte **pas** un APK pour une nouvelle app (obligatoire depuis août 2021) : il faut un **App Bundle** — `flutter build appbundle --release`. On envoie **un seul** fichier contenant toutes les architectures, et Google fabrique lui-même le paquet de chaque appareil (par processeur, par densité d'écran, par langue). **Mesuré le 2026-08-14 : AAB 78,6 Mo, dont l'utilisateur télécharge ~30 Mo.** ⚠️ **Conséquence à ne pas rater : la question « quelles architectures inclure ? » ne se pose pas en prod.** On les inclut TOUTES — un appareil arm64 ne reçoit que l'arm64, garder armeabi-v7a et x86_64 (Chromebooks) ne coûte rien à personne. L'arbitrage n'existe **que** pour les APK de test distribués à la main. 🔴 **Bloqué par #13** : un AAB signé avec la clé de debug est refusé. | 2026-08-14 |
| 16 | 🗑️ **Les octets des contenus expirés ne sont JAMAIS supprimés du coffre** | La base est propre depuis le correctif du 2026-08-13 (décisions #22) : supprimer le `contents` emporte story, clé, autorisations et vues. **Mais `storage.objects` n'a aucune clé étrangère vers le contenu**, et la suppression directe y est refusée par le trigger `storage.protect_delete` — c'est pourquoi `purge_expired_library_vibes` porte le commentaire « pas de suppression dans storage.objects ». **Conséquence : les octets de chaque story expirée, de chaque Vibe purgée, restent dans le coffre indéfiniment.** Sans conséquence de sécurité (plus aucune politique n'autorise la lecture, faute de ligne pour l'appuyer) mais **fuite de stockage sans borne, et facture qui monte**. Relevé le 2026-08-13 : 293 objets pour un contenu vivant nul. **Deux voies** : une Edge Function planifiée qui passe par l'API Storage avec la clé `service_role` (la seule qui a le droit), ou le ménage côté serveur au moment de la bascule sur VPS (#11). ⚠️ **À trancher avant la prod** : « éphémère » ne peut pas vouloir dire « invisible mais conservé pour toujours ». | 2026-08-13 |
| 1 | **Durée max des vidéos** | Fixée à **61 s** en dev. Jay veut repenser cette limite avant la prod. | 2026-07-12 |
| 2 | **Usurpation de username hors ligne** | En BLE, le mini-profil est signé par la clé de l'appareil mais **pas attesté par le serveur** : un pair jamais vu en ligne pourrait annoncer un username qui n'est pas le sien. Parade prévue : attestation serveur signée du couple (userId, username), distribuée avec les clés d'appareil. | 2026-07-13 |
| 3 | **Consommation batterie du scan BLE en arrière-plan** | Le scan tourne app fermée (service de premier plan, validé par Jay). À **mesurer et optimiser** (scan par intervalles / duty cycle) dès les premiers tests réels à deux appareils, et avant la prod. | 2026-07-13 |
| 19 | 🛠️ **OUTILS DE TEST DU 2026-08-16 — trois choses à retirer, dont une PERMISSION** | Livrés à la demande de Jay pour raccourcir la boucle de test. **À retirer avec la section Développeur** (ligne 4), mais ils ajoutent trois choses qui ne sont PAS dans le dossier `settings/sections/` et qu'on oublierait : ① la permission **`REQUEST_INSTALL_PACKAGES`** au manifeste — Google Play la restreint fortement et exige une justification ; ② le **`FileProvider` `.updates`** et `res/xml/file_paths_updates.xml` ; ③ la table **`public.dev_reports`** en base, et `NativeInstall.kt`. ⚠️ **Le jeton GitHub** éventuellement collé par Jay vit dans le Keystore de l'appareil, pas dans le dépôt — rien à nettoyer côté code, mais à révoquer côté GitHub le jour venu. | 2026-08-16 |
| 4 | **Section Développeur des Réglages** | À retirer avant la prod : interrupteur anti-capture, diagnostic caméra, déclenchement BeReal manuel. ✅ **Beaucoup plus simple depuis le 2026-08-13 (v0.9.60)** : tout est réuni dans un **dossier Développeur** unique (`lib/features/settings/sections/developer_*.dart`) avec trois sous-dossiers. Le retrait devient **une opération unique et vérifiable** — supprimer le dossier et sa tuile dans `settings_screen.dart` — au lieu d'aller chercher des entrées dispersées au milieu des vrais réglages. **À retirer avec** : `lib/core/diagnostics/diagnostic_bundle.dart`, `NativeDiagnostics.kt` + canal `neovibe/diag`, `VideoOpenTrace` et `video_timing_screen.dart`, `CamLog.kt` (ligne 8), `AppLog`. | 2026-07-11 |
| 6 | **RÉACTIVER L'ANTI-CAPTURE (FLAG_SECURE)** | Il est **désactivé par défaut** pendant le développement (demande de Jay 2026-07-14 : pouvoir prendre des captures d'écran pour montrer les bugs). Le code fonctionne et reste testable via Réglages → Développeur → « Anti-capture ». **Repasser le défaut à ACTIF avant la prod** (`DevSecureEnabled` dans `lib/core/prefs.dart` + `main.dart`). | 2026-07-14 |
| 7 | **Limite d'upload Supabase (50 Mo/fichier)** | Cause du `StorageException 413` sur une vidéo Mono. Contourné en plafonnant le débit vidéo à 3,5 Mbit/s (61 s ≈ 28 Mo). Si la qualité vidéo doit remonter, il faudra un plan Supabase supérieur (limite d'upload plus haute) ou une compression côté client. **Depuis le Oneshot filmé (v0.9.20), le moteur GPU utilise le MÊME plafond** (il était à 6 Mbit/s pour les tests locaux) : une card Oneshot vidéo = **2 fichiers** de ~28 Mo, soit ~56 Mo à envoyer par card. Chaque fichier passe, mais le volume double — à surveiller sur le quota de stockage et sur les réseaux lents. | 2026-07-14 |
| 10 | **VIDÉO HD — à activer quand l'hébergement le permettra** | Le bouton **HD** (ajouté le 2026-07-26) ne vaut que pour les **photos** (1440×2560 au lieu de 900×1600). En vidéo il ne change rien : le débit reste plafonné à 3,5 Mbit/s à cause de la limite d'upload Supabase (50 Mo/fichier, ligne 7). **Consigne de Jay** : « à noter de permettre la vidéo HD à l'avenir, quand on augmentera nos ressources d'hébergement. » Le levier est prêt : `recorder()` dans `NativeCamera.kt` (qualité + `VIDEO_BITRATE`) ; il suffira de router l'état HD jusqu'à `startVideo`. Ordre de grandeur : 1080p à 5 Mbit/s = 61 s ≈ 38 Mo, à plein débit ≈ 76 Mo (donc impossible aujourd'hui). | 2026-07-26 |
| 11 | **HÉBERGEMENT — direction TRANCHÉE par Jay le 2026-08-13 : couche serveur maison sur VPS en prod** | ⚠️ **Décision de Jay (2026-08-13)** : « on utilise Supabase en tant que serveur et avec le plan gratuit, c'est juste pour le développement ; à l'avenir pour la prod on créera notre propre couche serveur et on hébergera sur un VPS. » La question « managé ou auto-hébergé » posée ci-dessous est donc **tranchée en faveur de l'auto-hébergé**. **Ce que ça ne change pas** : les mises en garde ci-dessous restent valables et sont désormais des **tâches à planifier**, pas des objections — sauvegardes ET tests de restauration, TLS, auth (le poste le plus risqué : une auth maison est une source de fuite), supervision, correctifs. **Ce que ça rend URGENT** : la ligne 12 (couche d'abstraction des accès aux données) — 85 appels directs au client Supabase dans 31 fichiers Dart. C'est le coût de sortie réel, et il grandit à chaque chantier. **À ressortir avant tout chantier qui multiplierait les accès.** ⚠️ **Mesure du 2026-08-13, à ne pas oublier au moment du choix du VPS** : le projet de dev est en **eu-west-1 (Irlande)**, soit 20 à 40 ms d'aller-retour depuis la France — ce n'est **pas** ce qui explique les temps d'ouverture vidéo en cache. Un VPS plus proche gagnerait quelques dizaines de millisecondes, pas des centaines. *Texte d'origine ci-dessous.* Question posée par Jay le 2026-07-26. **Ce qu'on a déjà** : Supabase n'est PAS qu'une base — c'est Postgres + API REST générée + auth + stockage + temps réel + cron + Edge Functions. Le code serveur EXISTE et vit dans `supabase/migrations/` : **71 politiques RLS** (toute la sécurité du graphe social), **47 fonctions SQL**, **6 triggers**, **3 tâches cron** (dont le TTL 24 h). **Le vrai axe n'est pas « VPS ou Supabase » mais « managé ou auto-hébergé »** : un VPS ne donne pas plus de puissance, il transfère l'exploitation (Postgres, sauvegardes ET tests de restauration, TLS, auth maison = risque de fuite, supervision, correctifs). **Ce qui limitera vraiment** : le stockage et la bande passante SORTANTE, pas le SQL. La limite de 50 Mo/fichier est une limite de PLAN, pas d'architecture. Le TTL 24 h borne naturellement le stockage — **le feed local (chantier #10) fera sauter cette borne** : c'est là qu'est la bascule économique. **Ordre de migration, par morceaux et jamais d'un bloc** : (1) les médias vers un stockage objet à egress bon marché (Cloudflare R2, Backblaze B2) ; (2) les traitements lourds (transcodage, fan-out de notifications) vers un service de conteneurs (Fly.io, Railway, Render) plutôt qu'un VPS nu ; (3) Postgres en dernier — Supabase étant du Postgres standard, les migrations se rejouent ailleurs, il n'y a pas d'enfermement au niveau de la base. **Trois déclencheurs à surveiller** : la facture stockage/bande passante devient le premier poste ; le feed local arrive avec du contenu durable ; un besoin que Postgres + Edge Functions ne couvrent pas. | 2026-07-26 |
| 12 | **Couche d'abstraction des accès aux données** | ⚠️ **RE-MESURÉ le 2026-08-16 : 129 appels dans 25 fichiers** (motif compté : `.from(` · `.rpc(` · `.storage.` · `.auth.` dans `lib/`). Contre 85 dans 31 fichiers le 2026-07-26 — **+52 % en trois semaines**. La prédiction de cette entrée (« coûte peu aujourd'hui, cher plus tard », « il grandit à chaque chantier ») est donc **vérifiée par la mesure**, pas seulement plausible. Les cinq premiers fichiers concentrent 65 des 129 appels (`cards_repository` 22, `conversations_repository` 14, `library_repository` 12, `stories_repository` 10, `proximity_repository` 7) : la couche par domaine aurait donc **la moitié du travail faite en cinq fichiers**. ⚠️ Le motif de comptage est écrit ci-dessus **exprès** — sans lui, la prochaine mesure ne serait pas comparable et on croirait à une variation qui n'existe pas. *Texte d'origine :* Corollaire du point 11, mesuré le 2026-07-26 : **85 appels directs au client Supabase répartis dans 31 fichiers Dart**. C'est LÀ qu'est l'enfermement réel — pas dans Supabase, qui est du Postgres portable. Parade : faire passer les accès par une couche par domaine (cards, profils, connexions), seule à connaître Supabase. **Coûte peu aujourd'hui, cher plus tard** — à faire avant que le nombre d'appels ne double. Non urgent, mais à ressortir avant tout chantier qui multiplierait les accès (feed local, quiz). | 2026-07-26 |
| 13 | ✅ **KEYSTORE DE SIGNATURE — CRÉÉ LE 2026-08-25. 🔴 À SAUVEGARDER HORS DE LA MACHINE, MAINTENANT.** | **Créé sur décision de Jay du 2026-08-25**, après la panne d'installation `-7` (#61). RSA **4096**, valable **10 000 jours**, alias `neovibe`. Fichiers, tous **gitignorés** : `docdev/neovibe-release.jks` (la clé), `android/key.properties` (les identifiants), `docdev/keystore-password.txt` (le mot de passe), `docdev/keystore-README.txt` (le mode d'emploi). Empreinte SHA-256 du certificat : **`B0:AA:3F:C7:9E:5D:DB:44:33:73:73:D6:2D:3D:9E:9C:C6:96:54:A1:89:57:BE:5F:E9:5B:B2:98:2C:50:84:74`** — à comparer avec `apksigner verify --print-certs <apk>` en cas de doute. 🔴 **CE FICHIER EST IRREMPLAÇABLE.** Google Play ne resigne pas une application : le perdre après publication **interdit définitivement toute mise à jour**, il faudrait republier sous un autre nom de paquet et tous les utilisateurs seraient perdus. **Le sauvegarder hors de cette machine** (clé USB, gestionnaire de mots de passe) et **le joindre au paquet de transfert** — son absence du paquet est exactement ce qui a cassé la bascule du 2026-08-25. 🟡 **Avant la publication** : activer **Play App Signing**, Google garde alors une copie de la clé et celle-ci redevient une simple clé d'envoi, remplaçable. ⚠️ **Le repli sur la clé de debug reste possible** si `key.properties` manque (pour qu'un clone frais compile), mais il est **bruyant** : Gradle écrit un avertissement. Un APK signé debug ressemble en tout point à un APK officiel — seule l'empreinte les distingue. *(Texte d'origine, conservé :)*  Constaté le 2026-07-26 en préparant la bascule de machine : **aucun keystore n'existe** (`*.jks` et `android/key.properties` absents), les APK de test sont donc signés avec la **clé de debug**. C'est sans conséquence aujourd'hui, mais le Play Store exige une clé de release. **Le jour où ce keystore sera créé, il devient irremplaçable** : le perdre interdit DÉFINITIVEMENT toute mise à jour de l'app publiée (Google ne resigne pas). Il est gitignoré (règle `*.jks` / `*.keystore` du `.gitignore`), donc **aucune sauvegarde automatique** — à conserver hors dépôt, en double, et à ajouter au manifeste de transfert de `RSUNA.md` §1.0. | 2026-07-26 |
| 14 | **BOTS DE TEST À SUPPRIMER AVANT LA PROD** | Cinq comptes créés le 2026-08-02 en base de **dev** : `Lea`, `Malik`, `Chloe` (amis), `Yanis`, `Sofia` (croisés), emails `*.bot@neovibe.dev`. Ils portent cards, stories, publications, conversations et messages, plus 30 fichiers dans le bucket `cards`. **À supprimer avant toute bascule en prod** (`delete from auth.users where email like '%.bot@neovibe.dev'` — la cascade emporte profils, cards, stories et messages ; les fichiers du bucket restent orphelins, voir ligne 5). ⚠️ **MOT DE PASSE PERDU — corrigé le 2026-08-12.** Cette ligne disait « le redemander à Claude » : **c'est faux et ça a coûté un aller-retour**. Le mot de passe n'a jamais existé ailleurs que dans le contexte d'une session, qui ne survit pas à sa fermeture — et Jay n'a jamais pris le contrôle de ces comptes. **Le seul chemin est la réinitialisation par SQL** (éditeur SQL Supabase, sous l'identité du projet de dev) : `update auth.users set encrypted_password = crypt('<nouveau>', gen_salt('bf')), updated_at = now() where email like '%.bot@neovibe.dev';` — puis Jay le note **hors dépôt, de son côté**. Leçon générale : **ne jamais consigner qu'un secret « est chez Claude »** ; un secret qui n'existe que dans un contexte de session est un secret perdu. Script de regénération des médias : `tool/seed_bot_media.dart` (réécrit le 2026-08-12 : l'ancien téléversait des PNG **en clair**, d'avant le chiffrement). | 2026-08-02 |
| 15 | **Onglet « Jeux » — placeholder en production interdite** | La barre est passée à cinq onglets le 2026-08-01, avec un onglet **Jeux** qui n'affiche que « Bientôt ici ». C'est volontaire (réserver la place du chantier #11 sans refaire la navigation deux fois), mais **un onglet vide ne peut pas partir en prod** : soit le chantier quiz/mini-jeux est livré, soit l'onglet est masqué au moment de la release. | 2026-08-01 |
| 5 | **Fichiers orphelins du Storage** | Les fichiers des cards supprimées restent dans le bucket (`delete from storage.objects` est interdit par Supabase). Prévoir une edge function de purge physique. | 2026-07-12 |
| 8 | **Fiabilité dual-cam : ~~repli universel~~ + test multi-appareils** | Le double flux dépend du matériel (pas universel). Pour que le Oneshot soit fiable pour TOUS : (a) ~~**repli séquentiel automatique** sur appareils incapables (étape 5 du chantier GPU)~~ → **FAIT et validé en v0.9.22** (mis à jour le 2026-08-01) : `openGlDual` échoue → `dualFailedThisSession` → capture séquentielle, sans re-sondage du matériel ; écart mesuré entre les deux faces **1093-1129 ms** ; et (b) **test multi-appareils** avant la prod (Firebase Test Lab ou bêta-testeurs variés) — **SEUL POINT ENCORE OUVERT**, on ne valide toujours que sur le Redmi Note 10 Pro, et le repli n'a jamais tourné sur un appareil réellement incapable (seulement simulé par Réglages → Développeur → « Forcer la vue simple Oneshot »). Voir `docs/strategie-multiplateforme.md`. | 2026-07-24 |
| 9 | **Sens du miroir de la frontale en double flux GPU** | Sur le Redmi Note 10 Pro, le flux GL de la frontale arrive **déjà mirroré** (matrice de la `SurfaceTexture`), contrairement au flux CameraX du mode simple qui arrive brut. Le code compense par une inversion explicite dans `_dualPreviewFrame` (`mirrorFront = !_selfieMirror`). **Ce sens dépend du matériel** : à revérifier sur chaque nouvel appareil testé, en même temps que le point 8. Correctif éventuel = une ligne. | 2026-07-25 |

## Chantiers promis, à ne pas oublier

| # | Sujet | Détail | Depuis |
|---|-------|--------|--------|
| 57 | 🔄 **RENVERSÉ LE 2026-08-25 — `minSdk` EST REDESCENDU À 29 (Android 10)** | La prémisse ci-dessous était fausse sur un point décisif : les alternatives ne revenaient **PAS** à demander « Autoriser la localisation tout le temps ». Un service de premier plan de **type `location`** fait compter l'app comme « au premier plan » pour la localisation ; `ACCESS_FINE_LOCATION` en « **pendant l'utilisation** » suffit alors, interface fermée et écran éteint. `ACCESS_BACKGROUND_LOCATION` ne sert qu'à **DÉMARRER** un scan sans interface — ce que nous ne faisons jamais : relevé sur le manifeste **fusionné** le 2026-08-25, le seul receveur `BOOT_COMPLETED` est celui du plugin `flutter_foreground_task`, **désarmé par `autoRunOnBoot: false`** (`lib/main.dart:120`), et `ProximityService` ne part que de l'écran Ping. 🔎 **Ce qui a permis de le voir** : Jay a demandé « est-ce qu'on ne pourrait pas juste demander la permission si la version est < 12 ? ». La réponse a obligé à séparer deux choses que #57 confondait — **la permission** (une seule, non dissuasive) et **le type du service** (le vrai manquant). ✅ **Rétabli le 2026-08-25** : `LocationOff` de bout en bout (Kotlin, sérialisation, texte de notification, canal `openLocationSettings`, `RadioLocationOff`, bandeau, action, ligne de diagnostic), `BlePermissions.required()` à deux branches, `ACCESS_FINE_LOCATION`/`BLUETOOTH`/`BLUETOOTH_ADMIN` en `maxSdkVersion="30"`, `ProximityService` en `connectedDevice\|location`. ✅ **VÉRIFIÉ SUR LE MANIFESTE FUSIONNÉ *RELEASE*** le 2026-08-25 : `minSdkVersion=29`, les trois permissions bornées à 30 (donc **inexistantes sur Android 12+** : la propriété obtenue le 2026-08-20 est intacte), **aucune `ACCESS_BACKGROUND_LOCATION`**, service en `connectedDevice\|location`. ⚠️ **Pourquoi 29 et pas 26** : Android 8/9 ont un troisième modèle (COARSE suffit, les types de FGS n'existent pas avant l'API 29) — une branche de plus que **la tablette d'Android 10, seul appareil de test pré-12, ne pourrait pas exercer**. On ne code pas un chemin qu'on ne peut pas éprouver : c'est exactement ce qui a produit le défaut du 2026-08-20. 🔴 **PAS ENCORE VALIDÉ SUR APPAREIL** — à faire sur la tablette d'Android 10 (v0.9.123). Les trois prérequis sont exposés au diagnostic (`needsLocation`, `fgsLocationType`, `locationEnabled`) parce qu'aucun ne lève d'erreur quand il manque. *Texte d'origine ci-dessous, conservé : il reste la meilleure description du piège.* | 2026-08-25 |
| 57 bis | ✅ **`minSdk` EST PASSÉ À 31 (Android 12) — le trou du scan en arrière-plan est fermé** | **Décision de Jay du 2026-08-20**, option ①, appliquée le jour même. **Le problème** : sur Android 10 et 11, un scan BLE exige `ACCESS_FINE_LOCATION`, et le système ne considère l'app comme « au premier plan » pour la localisation que si son service de premier plan a le type `location` — le nôtre est `connectedDevice`. Interface fermée, `onScanResult` n'aurait rien remonté, **sans erreur ni trace**, et toute la reconnaissance native aurait été inutile sur ces versions. **Les alternatives** revenaient à demander « Autoriser la localisation tout le temps » — l'invite la plus dissuasive d'Android, sur une app dont la thèse est la confiance. On a supprimé la cause. ✅ **Ce qui a été retiré dans la foulée** (règle 8, les deux sens relevés avant de couper) : les permissions `BLUETOOTH` / `BLUETOOTH_ADMIN` / `ACCESS_FINE_LOCATION` du manifeste ; la branche `else` de `BlePermissions.required()` ; l'état **`LocationOff` de bout en bout** — côté Kotlin (le membre, sa sérialisation, `isLocationEnabled`, le texte de notification), le canal `openLocationSettings`, et côté Dart (`RadioLocationOff`, `BleRadio.openLocationSettings`, le bandeau du ping, son action, la ligne du diagnostic) ; et `Permission.locationWhenInUse` de la demande de permissions. ✅ **VÉRIFIÉ SUR LE MANIFESTE FUSIONNÉ** (`processDebugMainManifest`), donc **plugins compris** et pas seulement sur notre fichier : `minSdkVersion="31"`, et **aucune permission de localisation** ne subsiste. C'est le seul contrôle qui prouve quelque chose — un plugin peut réintroduire une permission à la fusion. ⚠️ **Ne pas redescendre `minSdk` sans relire cette entrée** : ce nombre porte une décision, pas une contrainte de compilation. Et le faire après la mise en production retirerait l'app à des gens qui l'avaient. 🟡 **Reste à balayer** : ~10 gardes de version `SDK_INT >= O/N/P/Q` dans le natif sont désormais toujours vraies (4 dans `BleEngine`, 4 dans `ProximityService`, 2 dans `NativeInstall`). Sans danger immédiat, mais une branche morte finit par être lue comme vivante — à nettoyer au checkup #52. | 2026-08-20 |
| 56 | ✅ **LE NATIF RECONNAÎT SEUL — le croisement ne dépend plus d'un Dart vivant** | Livré le 2026-08-20, dernier morceau de #54 ②. Le service reçoit une **table de reconnaissance** (`setRecognitionTable`) et accumule ses constats (`takeSightings`), que le Dart récupère à son retour et verse dans le même journal que les constats faits en direct — **un seul chemin vers le serveur**. 🔒 **Le natif n'apprend AUCUNE identité** : la table associe un jeton à un **rang** (0, 1, 2…). Seul le Dart sait qui est le rang 3, et il le sait pour *cette* table — d'où le `tableId` renvoyé avec chaque constat. Table périmée → constat **jeté**, jamais attribué au hasard : un croisement faux vaut moins que pas de croisement. 🔒 **Un jeton rejoué hors de son créneau est refusé** — sans cette fenêtre, enregistrer une annonce le matin suffirait à fabriquer un croisement le soir. ✅ **La logique est PURE (aucune dépendance Android) et testée sur la JVM** : 11 tests dans `android/app/src/test/kotlin/.../SightingBookTest.kt`. C'est la seule façon de valider du code qui tourne quand l'interface est morte — il ne peut pas l'être « à l'usage ». ⚠️ **Les constats natifs vivent en MÉMOIRE.** Si Android tue le *processus* (et pas seulement l'activité), ils sont perdus. Assumé : les écrire sur le disque depuis le natif poserait hors du Dart une trace de qui a été croisé, pour rattraper un cas rare. ⚠️ **La bande se calcule côté Dart**, jamais dans le natif : dupliquer le modèle de distance en Kotlin garantirait qu'un jour les deux ne disent plus la même chose. Le natif remonte le RSSI brut. ⚠️ **Rien n'est validé sur appareil.** | 2026-08-20 |
| 55 | ✅ **LA RÉCIPROCITÉ SERVEUR EST EN PLACE — un croisement n'existe que si les DEUX se sont vus** | **Décision de Jay du 2026-08-20** (option ②), implémentée le jour même. Chacun dépose ses **constats** unilatéraux (`report_sightings`), et le serveur ne crée un croisement que si le constat inverse existe au même créneau ± 1. **Plus aucune connexion BLE vivante n'est nécessaire** pour un croisement. 🔒 **La propriété anti-traque est structurelle** : un constat unilatéral ne produit RIEN — pas de croisement, pas de notification, pas d'historique. On ne peut pas observer sans être observé, et ce n'est pas une règle appliquée quelque part, c'est la seule façon dont une ligne peut naître par ce chemin. ✅ **Prouvé EN BASE**, sous `set local role authenticated` + `request.jwt.claims`, tout en `rollback` : constat unilatéral → 0 croisement ; constat mutuel à un créneau d'écart → croisement `proof = 'mutual_sighting'` ; non-ami refusé ; antidatage (−500 créneaux) et futur (+40) refusés ; soi-même refusé ; **écriture directe dans la table refusée** (aucune politique d'INSERT, le seul chemin est la fonction). ⚠️ **`encounters.proof` distingue les deux niveaux de preuve** : `certificate` (deux signatures en direct, infalsifiable) et `mutual_sighting` (deux constats concordants, que deux complices peuvent fabriquer). Un constat mutuel **ne rétrograde jamais** un certificat. Ne pas fusionner les deux : ce serait faire gagner la règle la plus permissive, en silence. ⚠️ **Purge à 48 h**, job cron **séparé** `neovibe_purge_sightings` (jobid 10) — jamais greffé sur `neovibe_purge`. 48 h alors que `encounters` purge à 24 h : c'est voulu, un constat doit survivre assez longtemps pour que son miroir arrive même si le téléphone d'en face est resté hors ligne une journée. 🔴 **CE QUI MANQUE ENCORE POUR L'APP FERMÉE** : le natif diffuse seul (point H corrigé) mais ne **reconnaît** toujours pas — l'appariement jeton→ami reste en Dart. Donc app **fermée**, aucun constat n'est produit. La réciprocité serveur enlève l'exigence de **connexion**, pas encore celle d'un **Dart vivant**. Voir #54 ②. | 2026-08-20 |
| 54 | 🟠 **CE QUI RESTE À FAIRE SUR LE SECRET PAR PAIRE — quatre chantiers ouverts** | Le socle est livré (voir #53), mais quatre choses ne le sont pas et ne doivent pas être redécouvertes au test. 🔴 **① L'émission multi-annonces n'est PAS implémentée.** La sonde de capacités existe (`advertCapabilities` : `multipleAdvertisement`, `extendedAdvertising`, `maxAdvertisingDataLength`, `periodicAdvertising`), et le natif fait tourner les jetons **en temps partagé sur un seul jeu d'annonces**, à `cycleMillis = 400 ms` — **valeur raisonnée, jamais mesurée**. Donc la latence de détection vaut aujourd'hui N×400 ms. Écrire le chemin multi-set **après** avoir relevé ce que l'appareil de Jay annonce, pas avant : c'est du Kotlin non testable sans matériel. ✅ **② FAIT le 2026-08-20** — le natif reconnaît seul, voir #56. 🟠 **③ `x25519_pub` est NULLABLE en base**, le temps que les appareils de test republient. À passer `not null` une fois que c'est fait. 🟡 **④ La republication après réinstallation n'est pas accélérée** : elle marche (le secret se dérive de la clé publique lue en base, donc rien à invalider), mais l'ami ne la reçoit qu'à **sa** prochaine synchro. Une notification silencieuse la rendrait immédiate — même levier que pour #55. | 2026-08-20 |
| 53 | ✅ **LE SECRET PAR PAIRE EST IMPLÉMENTÉ (points A et H) — rien n'est validé sur appareil** | Livré le 2026-08-20 sur décision de Jay (#51). ✅ **Ce qui a changé** : `ProximityIdentity` porte une paire **X25519** ; `pairSecret()` dérive `S_AB` (X25519 + HKDF, cache indexé **par la clé publique** — c'est ce qui rend une réinstallation indolore) ; `advert_plan.dart` calcule le plan d'émission et la table de reconnaissance ; `AdvertSchedule.kt` déroule le plan côté natif. La clé de diffusion, sa rotation de 7 jours, `broadcast_key_prev` et la révocation-par-rotation **n'existent plus**. ✅ **Migration `20260820120000_pairwise_secret.sql` APPLIQUÉE sur le projet de dev** (`dvixmhvqqjvbrpsckmyi`, colonnes vérifiées : `x25519_pub` ajoutée, `broadcast_key` / `broadcast_key_prev` / `rotated_at` supprimées). Sens entrant relevé avant de couper : `report_encounter` et `submit_ble_connection` ne lisent que `ed_pub`, les politiques ne nomment aucune colonne. ✅ **Rupture de protocole assumée** : version **3** portée dans chaque annonce (`ProximityIdentity.protocolVersion` et `BleConstants.PROTOCOL_VERSION`, à garder égales), charge utile passée de 18 à 19 octets. Une annonce d'une autre version est **comptée** (`otherVersionScans`) au lieu de disparaître en silence. **Les deux téléphones de test doivent être mis à jour ensemble.** ✅ 177 tests verts, `flutter analyze` propre, `compileDebugKotlin` réussi. ⚠️ **Aucun test sur appareil.** Ce qui reste à faire : #54. | 2026-08-20 |
| 52 | ✅ **DISSOCIATION ACQUISITION / USAGE — AUDIT *ET* CORRECTION FAITS LE 2026-08-25** | 🛠️ **Corrigé sur consigne de Jay** : *« corrige tous les bugs de dissociation et dissocie tout ce qui peut l'être au niveau acquisition des données »*. **Les six défauts sont traités**, état complet au §6 de `docs/checkup-acquisition-usage.md`. 🔑 **La cause racine était unique** : en Dart l'égalité d'une collection est **l'identité**, et **aucun modèle de `core/models/` n'avait d'égalité de valeur** — toute comparaison de liste était donc vaine. 11 classes en portent une désormais. ⚠️ **`Message.isExpired` est volontairement HORS de son `==`** : il dépend de `DateTime.now()`, l'inclure ferait dépendre l'égalité de l'instant du test. 🧰 **Trois primitives créées** : `DerivedList`/`DerivedSet` (`core/derived_list.dart`) pour une vue **sans paramètre**, `ValueList<T>` pour une vue **paramétrée** — Riverpod 3 sans codegen **n'a pas de `Notifier` familial**, constaté le 2026-08-25 — et `expiryClockProvider` (`core/clock.dart`) pour tout ce qui périme. ⚠️ **`.select` reste à 2 dans tout le projet et ce n'est PAS un échec** : il répond à « je ne veux qu'un champ », pas à « ne me réveille que si le résultat change » — il compare lui aussi les listes par identité. 📉 **Avant → après** : écrans contenant de l'acquisition **12 → 3** (les 3 restants vérifiés légitimes un par un) · providers dérivés sans garde **10 → 0** · péremption traitée dans l'acquisition **2 endroits → 0** · réveils/heure du chat à l'arrêt **360 → 0** · chemins vers une même card **2 → 1** · tests **188 → 199**. 🔎 **Le cas le plus parlant** : `chat_screen.dart` avait sa propre copie de `cardByIdProvider`, mot pour mot — deux chemins, deux caches, rien pour le signaler. ⚠️ **L'invalidation de cache appartient désormais à l'ÉCRITURE**, pas à l'appelant : deux écrans qui écrivent la même table doivent laisser le lecteur dans le même état. 🟡 **Deux points laissés à Jay** : `wavesProvider` garde son filtre d'heure côté serveur (le rendre vivant coûterait une requête réseau périodique — arbitrage produit) ; **`receivedDeliveriesProvider` n'a AUCUN consommateur** et n'a pas été supprimé (règle 8). ⚠️ **La leçon de méthode** : **trois fois** pendant ce chantier, un compteur à zéro a semblé prouver l'absence de défaut alors qu'il ne prouvait que le silence de l'instrument (délai nul, flux `broadcast` sans abonné, `const` canonicalisé). D'où `_sourceArrivee()` et l'horloge **pilotée** dans les tests. *(Constat d'audit d'origine, conservé :)*  📄 **Résultat complet : `docs/checkup-acquisition-usage.md`.** Preuve exécutable : `test/dissociation_connections_test.dart` — **3 tests qui ÉCHOUENT volontairement**, ils comptent des reconstructions, pas des affichages (188 autres tests passent, l'app se comporte correctement — elle coûte seulement plus qu'elle ne devrait). 🔢 **Le chiffre d'ensemble** : 160 fichiers / 40 419 lignes · **192 `ref.watch`, dont 2 `.select` (1 %)** — et les deux sont dans le fichier de référence · 128 appels Supabase dans 25 fichiers · **12 écrans contiennent leur propre acquisition** · 10 providers dérivés refabriquent une collection · **5 providers dépendent de `DateTime.now()` sans jamais se réévaluer** · 5 flux temps réel dont 1 sans filtre. 🔴 **Six défauts, dont trois MESURÉS** : ① une connexion *partielle* qui bouge réveille `fullConnectionsProvider`, observé par **7 écrans de 6 modules** (attendu 0, obtenu 1) ; ② deux émissions au contenu identique réveillent quand même (obtenu 1) ; ③ une connexion partielle **expirée reste affichée** tant que personne n'écrit en base — *défaut déjà rencontré et corrigé sur les messages le 2026-07-13, jamais porté ici* ; ④ ce correctif-là (`Timer.periodic` 10 s dans l'acquisition) est lui-même un défaut : **360 réveils/heure chat ouvert et inactif**, sur `chat_screen.dart` (1 311 lignes, 17 `ref.watch`, 0 `select`) ; ⑤ chaîne d'amplification entre modules — une personne croisée dans la rue fait recalculer les **deux bandeaux de stories** ; ⑥ les 12 écrans de D6, prérequis de la couche d'abstraction Supabase (#12). ⚠️ **La leçon de méthode vaut plus que le résultat** : le checkup a produit **deux fausses réussites avant un seul chiffre juste** — un délai nul ne traverse pas un `StreamController`, et un flux `broadcast` ne rejoue pas ce qui précède l'abonné. Dans les deux cas **un zéro ressemblait à une réussite**. D'où `_sourceArrivee` dans le test : prouver que la source est arrivée AVANT de compter. ⏳ **Aucun correctif appliqué — en attente de l'arbitrage de Jay** ; ordre de traitement proposé au §4 du document. *(Texte d'origine, conservé :)*  Demandé par Jay le 2026-08-20, en même temps que la règle ajoutée à `CLAUDE.md` (« dissocier l'ACQUISITION de l'USAGE »). Passer **tout** le code en revue avec la question de contrôle : *« si j'ajoute un champ à cet écran, dois-je toucher au code qui parle à la radio / au réseau / au disque ? »*. Chercher en particulier : ① les objets d'état qui mélangent deux sources de rythmes différents (le défaut du point C, corrigé pour la proximité seulement — les autres features n'ont **pas** été regardées) ; ② les couches basses qui filtrent « pour économiser » et prennent donc une décision d'affichage ; ③ les écrans qui observent un objet entier là où ils n'utilisent qu'un champ. ⚠️ Rendre le résultat **chiffré** (nombre de reconstructions, de lectures disque) et non en appréciation : ce défaut n'affiche jamais rien de faux, il ne se voit qu'en comptant. Référence de ce à quoi ça doit ressembler : `lib/features/proximity/presence_feed.dart` + `test/presence_feed_test.dart`. | 2026-08-20 |
| 51 | 🟢 **DÉCISION D'ARCHITECTURE — LE SECRET PAR PAIRE (Diffie-Hellman) REMPLACE LA CLÉ DE DIFFUSION** | **Approuvé par Jay le 2026-08-20**, en remplacement du point A de l'audit (#48). ⚠️ **Rien n'est encore implémenté.** Principe : plus de secret unique partagé avec tous les amis ; chaque **paire** dérive son propre secret `S_AB = X25519(ma privée, sa publique)`, jamais transmis, jamais publié. La primitive est déjà dans le projet (`secure_channel.dart:100`). L'appareil émet **un jeton par ami**, `HMAC(S_AB, créneau)`, en cycle. Ce que ça supprime : le trou de 7 jours (#46, #48-A), le coût de la révocation (#46 ②), le secret global, et la dépendance au réseau après l'amitié. **Décisions prises par Jay en même temps** : ① **architecture adaptative** — au démarrage, l'app relève les capacités de l'appareil (annonces BLE simultanées, `isMultipleAdvertisementSupported`) et s'adapte, **comme le double flux du Oneshot** ; ② **aucun plafond d'amis pour l'instant**, mais la latence ∝ N est connue et assumée — voir #50 ; ③ la fuite du **nombre d'amis** (compter les jetons distincts) est acceptée, sans importance ; ④ la rupture de protocole est acceptée : on change **maintenant**, avant la production. ⚠️ **Reste à concevoir** : la **republication des clés publiques après réinstallation** (piste de Jay : pousser la nouvelle clé publique à chaque ami, chacun renvoyant la sienne à réception). | 2026-08-20 |
| 50 | 🎯 **DEUX CHOSES DISTINCTES QUI PARTAGENT LA MÊME TECHNO — le MODE PING et le CROISEMENT ENTRE AMIS** | Consigne de Jay, 2026-08-20 : *« garde en tête que le mode ping et le croisement entre amis c'est deux choses différentes qui utilisent la même techno »*. En conséquence l'appareil émet **deux choses** : ① **en continu**, un jeton par ami (secret par paire, #51) — c'est le **croisement**, qui alimente la notification « vous vous êtes croisés » ; ② **seulement quand le mode ping est activé**, un **identifiant public** — c'est le ping, qui permet aussi de se faire voir d'inconnus. ⚠️ **Ne jamais refondre l'un dans l'autre** : ils n'ont ni le même cycle de vie, ni le même consentement, ni le même public. 🔭 **Cap produit lié** : Jay veut un **second cercle** qui ne soit pas un cercle d'amis, et plus généralement des **paliers de proximité** — « comme les cercles d'accès d'un OS, où le summum est le kernel ». À concevoir avant de figer les droits attachés à une relation. | 2026-08-20 |
| 49 | 🟠 **POINT H — L'IDENTIFIANT ÉMIS PEUT SE FIGER QUAND ANDROID TUE L'ACTIVITÉ (non vérifié)** | Trouvé le 2026-08-19 en déroulant la chaîne, **pas mesuré sur appareil** (règle 7 : à reproduire avant tout correctif). Deux rotations coexistent : la **clé** (7 j) et l'**identifiant émis** (créneau de 15 min, `ProximityIdentity.slotDuration`), les amis tolérant `[slot-1, slot, slot+1]`. Or le minuteur qui pousse le nouvel identifiant vit **en Dart** (`proximity_supervisor.dart:169`), et le natif ne le calcule jamais lui-même (`BleEngine.kt:242` rejoue `desiredAdvertId`). `MainActivity` n'utilise pas de `FlutterEngine` en cache : le Dart meurt avec l'activité, pendant que `ProximityService` garde la radio. **Conséquence déduite** : identifiant figé → passé ~30 min il sort de la fenêtre et **plus aucun ami ne reconnaît l'appareil**, même avec la bonne clé ; et l'identifiant devient constant, ce que la rotation par créneau vise précisément à éviter. ⚠️ **Indépendant du changement d'architecture #51** : le problème est le cycle de vie, pas la cryptographie — il se reposera à l'identique avec les jetons par paire. **Test à faire** : laisser un appareil une heure app fermée, écran éteint, et voir si l'autre le retrouve. | 2026-08-19 |
| 48 | 🔴 **AUDIT DU 2026-08-18 — six points relevés sur la refonte du ping, aucun corrigé** | Audit demandé par Jay après l'incident de contexte. 🔴 **A — `broadcast_key_prev` ne change AUCUN résultat de reconnaissance** : la chaîne déroulée jusqu'au bout montre que `currentRotatingId()` n'utilise que la clé COURANTE, donc dès la rotation on ne diffuse plus que K2 ; l'ami qui n'a pas resynchronisé n'a ni K2 ni la publication de K1, et celui qui a resynchronisé a K2 — la précédente ne sert dans aucun des deux cas. Elle coûte une colonne, un champ client et **le double de l'indexation**, pour zéro effet ; et l'aveuglement qu'elle prétend éviter existe toujours (**à chaque rotation de 7 jours, tout ami qui n'a pas rouvert l'app cesse de nous voir**). ⚠️ **Décision de Jay attendue** : supprimer la précédente, ou retarder la bascule de diffusion (fenêtre de grâce) — la révocation gardant sa bascule immédiate. 🟠 **B** — `PeerNetwork.dispose()` ne coupe pas la radio, contrairement aux deux autres chemins de fermeture : le natif garde des liens GATT que le Dart a oubliés (le piège déjà documenté : `connect()` réussit immédiatement sans événement quand un lien existe déjà). 🟠 **C** — bande, tendance et distance ne se rafraîchissent qu'au changement de `{adresse, stade}` : la demande de Jay du 2026-08-16 (distance en temps réel) n'est pas satisfaite pour un pair identifié immobile. **Pas une régression du 18** : l'ancien code n'émettait déjà que sur changement de stade. 🟡 **D** — le `_publish()` du `catch` de `_open()` est sans effet (`connecting` encore à `true`). 🟡 **E** — `hasEstablishedChannel`, `contactDuration`, `identifiedCount` : publics, appelés uniquement par les tests. 🟡 **F** — `tool/__pycache__/*.pyc` suivis par git et absents du `.gitignore`. Détail complet et deux points écartés après vérification en base : `rapports-de-sessions/2026-08-18_08-28.md`. | 2026-08-18 |
| 47 | 🔴 **LES VALEURS DE `PresenceRules` SONT RAISONNÉES, PAS MESURÉES — à confronter au terrain** | La refonte du ping (#45) pose **trois durées** qui décident de tout ce qui est visible : `freshFor` **5 s** (« il est là » — affichage, envoi d'un message, certificat, barrière du produit), `stableAfter` **10 s** + 5 annonces (avant d'ouvrir un lien vers un **inconnu**), `forgetAfter` **30 s** (oubli de la session et fermeture du transport). ⚠️ **Aucune n'a été mesurée sur un appareil.** Le raisonnement : l'advertising tourne à ~100 ms, donc 5 s de silence = une cinquantaine d'annonces manquées d'affilée. **Ce raisonnement suppose un scan continu**, et c'est précisément ce qui n'est pas garanti **écran éteint** — Android bride les scans en arrière-plan, et le service de premier plan n'en exempte pas de tout. **Le risque concret** : si le scan est bridé, un pair pourtant présent cesserait d'être « frais » toutes les 5 secondes, et l'utilisateur verrait sa liste clignoter. **À faire au premier test à deux appareils** : relever `rawScans`/`neoScans` (Réglages → Développeur) écran allumé PUIS écran éteint, et remonter `freshFor` si l'intervalle réel entre deux annonces reçues dépasse ~2 s. Lié à #3 (batterie). | 2026-08-18 |
| 46 | 🔑 **LA CLÉ DE DIFFUSION TOURNE DÉSORMAIS — deux conséquences à ne pas découvrir en prod** | Corrigé le 2026-08-18 (voir #45) : elle était écrite une fois à l'installation et **jamais** régénérée, donc un ex-ami nous reconnaissait **à vie**. ✅ **① La migration `20260818090000_broadcast_key_rotation.sql` est APPLIQUÉE sur le projet de dev** (`dvixmhvqqjvbrpsckmyi`, le 2026-08-18, via l'API Management ; colonnes vérifiées, les 2 lignes existantes ont reçu `rotated_at`). ⚠️ **Elle reste à appliquer sur tout autre environnement avant d'y déployer l'app** : sans les colonnes `broadcast_key_prev` et `rotated_at`, l'`upsert` de `device_keys` lève, et comme il est la PREMIÈRE chose que fait la synchro, le `catch` avale tout — plus aucune clé publiée, plus aucune clé d'ami téléchargée, et la file d'envoi (croisements, connexions, waves) ne part jamais. L'app est alors **plus cassée qu'avant**, pas seulement privée d'une fonctionnalité. ⚠️ **② La révocation a un coût visible** : retirer un ami fait tourner notre clé **sans conserver la précédente**, donc nos autres amis ne nous reconnaissent plus **jusqu'à leur prochaine synchronisation** (lancement de l'app, ou croisement). C'est le prix assumé pour reprendre un secret déjà distribué — mais si Jay observe « après avoir supprimé un ami, plus personne ne me voit pendant un moment », **ce n'est pas un bug**. Si la gêne est réelle, le levier est de publier la précédente pendant quelques minutes seulement, pas de renoncer à la révocation. | 2026-08-18 |
| 45 | ✅ **REFONTE DU PING — une session par pair, sur consigne de Jay (2026-08-18)** | **Consigne** : *« tu ne lis pas bien tout […] au lieu de construire quelque chose de simple et robuste tu compliques tout et tu bidouilles […] corrige tous les bugs et erreurs de conception »*. Le reproche était fondé, et **ce fichier le documentait déjà** (#40 : *« c'est la troisième fois qu'un correctif de symptôme fabrique le défaut suivant »*). 🔴 **Cause racine** : `peer_network.dart` tenait **neuf collections indexées par adresse** à nettoyer ensemble à la main — tous les défauts du chantier sont *la collection X a été nettoyée, la Y non*. **Preuve** : la boucle `takeMergedAway` existait **en deux exemplaires dont les corps avaient divergé**. ✅ Remplacées par **un objet par pair** (`PeerSession` + `PeerRegistry`, `peer_session.dart`) ; `release()` défait tout en un geste ; le stade est **dérivé**, plus stocké ; plusieurs adresses pointent vers la même session (fin des « adresses abandonnées »). `presence_tracker.dart` supprimé. 🔴 **Trois défauts trouvés par la lecture, jamais signalés à l'usage** : ① la règle « il est là » n'existait pas — `sendMessage` acceptait un souvenir de 25 s, infini pour un pair relié, alors que le commentaire promettait le contraire (**troisième commentaire faux du chantier**) ; ② `ProximityIdentity` construite avec `new` à **cinq** endroits sans verrou d'initialisation — **mesuré : 4 clés d'appareil créées au lieu d'une au premier lancement**, d'où « profil non authentifié » intermittent (reproduit avant correction, règle 7) ; ③ le **changement de compte** n'effaçait pas l'identité — les amis de A voyaient « A est là » pendant que B utilisait le téléphone. ✅ **156 tests verts**, `flutter analyze` propre. ⚠️ **Rien n'est validé sur appareil** — voir #47. Détail complet : `rapports-de-sessions/2026-08-18_04-16.md`. | 2026-08-18 |
| 44 | ✅ **CINQUIÈME CAUSE DE MESSAGE FANTÔME — la livraison demandait à la PRÉSENCE qui était le pair (v0.9.120)** | **Trouvé par l'instrumentation, dans le relevé de Jay en v0.9.118** : `message reçu avant le profil — ChatMessage, présence ABSENTE`, deux fois. **Deux `ChatMessage` perdus.** ⚠️ **Le mot qui change tout est « absente »** : pas `identifying`, pas « sans profil » — il n'y avait plus **aucune** entrée de présence pour cette adresse, alors que le canal déchiffrait parfaitement (le message était arrivé jusqu'à `_onMessage`). **Cause** : `_onMessage` lisait `presence.byAddress(linkId)?.snapshot`. Or la présence parle de **proximité**, et ses entrées vont et viennent — fusion d'adresses quand Android renouvelle sa MAC, remise à zéro quand la radio s'arrête, élagage. Une conversation en cours ne doit pas dépendre de ça. ✅ **Correctif — l'identité appartient à la SESSION** : `PeerNetwork._identities`, rempli à la révélation du profil signé, vidé avec le lien. La livraison ne consulte plus la présence. *C'est la règle du matin, un cran plus bas : la présence dit OÙ, jamais QUI.* ✅ **Second versant** : la fusion retirait l'entrée tout de suite mais ne fermait le lien qu'au battement suivant — **jusqu'à 3 secondes** pendant lesquelles une adresse sans identité gardait un canal vivant, et le pair parlait dans le vide. Le nettoyage se fait désormais **dans le même geste que l'observation** (`_closeMergedAway`), ce qui supprime la fenêtre **et prévient le pair**. **Reproduit avant correction** (`peer_network_test.dart` : « un message arrive même si la présence a oublié l'adresse »). ⚠️ **Un second chemin vers le même défaut existait, jamais soupçonné** : `presence.clear()` à l'arrêt de la radio vide la présence **sans fermer les canaux**. | 2026-08-17 |
| 43 | ✅ **CHAQUE ACTION ABOUTIT À UN ÉTAT NOMMÉ — chantier du 2026-08-17 (v0.9.118)** | **Objectif de Jay** : *« toute action doit être vérifiée et validée pour éviter les fausses réponses de l'app, chaque fonction doit aboutir à quelque chose de défini, et pas de nœud/fonction orphelin qui aboutit dans le vide »*. Traité par **inventaire scripté**, pas au jugé, et **réitéré jusqu'à zéro** sur les deux axes. **① Orphelines : 6 → 0.** `sendToGroup` (morte depuis la refonte « 1 contenu = 1 format »), `getOrCreateProximity` (le chat ping est 100 % local), `storeOwnMedia`/`tryOwnMedia` + `_mediaFile` devenu orphelin dans la foulée (règle 8), `totalOf`. Et **`displayColor` branchée** au lieu d'être supprimée : la règle de lisibilité en thème clair existait, documentée, et n'était **appliquée nulle part** (4 sites). **② Actions qui pouvaient mentir : 0 restante.** Trois suppressions annonçaient un succès sans l'avoir vérifié (Enregistrements, catégorie du Cercle, publication — celle-ci **fermait l'écran** dans tous les cas, ce qui *dit* que c'est fait) ; un bouton « Message » n'aboutissait à **rien** en cas d'échec ; une fin de session pouvait laisser la visionneuse suspendue sans état. **③ Le circuit d'une demande d'ami, refait.** 🔴 **Défaut trouvé** : recevoir un refus appelait `removeRequest`, qui retire une demande **ENTRANTE** — le mauvais magasin. L'émetteur n'apprenait donc jamais qu'on lui avait dit non, et si le pair lui avait *aussi* écrit, c'est **sa demande à lui** qui disparaissait. Le commentaire affirmait exactement le contraire de ce que le code faisait. ✅ Nouveau magasin `outgoing_requests.json` : trois états nommés (**envoyée / refusée / acceptée→disparaît**), pas de doublon, et le bouton du ping affiche l'état au lieu de se réafficher à l'identique. Un refus **marque** au lieu d'effacer — une demande qui s'efface toute seule est indiscernable d'une demande perdue. Erreurs **nommées** (`FriendRequestRefused`) : « déjà connectés » ne dit plus « rapproche-toi ». **6 tests.** ⚠️ **Et la promesse la plus forte du produit protégée** : si la file d'envoi abandonne une **connexion** après 8 tentatives, l'utilisateur avait lu « Vous êtes connectés » et l'amitié n'existait nulle part. Il en est désormais **notifié**. | 2026-08-17 |
| 42 | 🔒 **UNE ACCEPTATION D'AMI N'ÉTAIT PAS VÉRIFIÉE — corrigé le 2026-08-17 (v0.9.117)** | Trouvé en répondant à la question de Jay « il n'y a rien d'autre comme défauts ? », par **inventaire des retours silencieux**, pas par un symptôme. `_onFriendAccept` ne vérifiait **rien** : elle rangeait l'émetteur dans le carnet d'amis sur la seule foi du message. **N'importe quel pair ayant abouti la poignée de main — donc n'importe qui de physiquement à côté — pouvait envoyer une `FriendAcceptMessage` jamais demandée et s'inscrire lui-même comme ami.** 🔴 **Effets locaux immédiats** : affiché comme ami (icône de lien, plus de bouton « demander »), **la règle anti-spam cesse de s'appliquer à lui**, et il devient reconnaissable en silence à son identifiant rotatif. Le serveur, lui, aurait refusé la connexion (`submit_ble_connection` vérifie les deux signatures) — mais le carnet local ne l'apprend qu'à la synchro suivante, **donc jamais tant qu'on est hors ligne**. ⚠️ **Pour une app dont toute la thèse est qu'être à côté ne suffit pas à être ami, c'est la barrière fondatrice qui tombait.** ✅ **Correctif sans aucun état nouveau** : le record porte **NOTRE propre signature** (`sigFrom`) ; on la vérifie avec notre clé d'appareil. Un tiers ne peut pas la fabriquer, donc il ne peut pas fabriquer l'acceptation d'une demande qu'on n'a pas faite. C'est exactement ce que fait le serveur — on ne le faisait pas de notre côté. Règle extraite dans `FriendAcceptMessage.refusalFor()` pour être **testable** (3 tests : légitime, jamais demandée, destinée à quelqu'un d'autre). 🔎 **Leçon de méthode** : ce défaut n'a produit aucun symptôme et n'aurait jamais été signalé par un test manuel. Il est sorti d'un **inventaire des chemins silencieux**, pas d'une observation. | 2026-08-17 |
| 41 | 🔑 **LE SOCLE TEMPS RÉEL GARDAIT UN JETON MORT — corrigé le 2026-08-17 (v0.9.116)** | **Trouvé dans le journal de Jay, pas signalé par lui** : `RealtimeSubscribeException — "InvalidJWTToken: Token has expired 4847 seconds ago"`. Le jeton d'accès est renouvelé automatiquement pour les appels REST, mais le **socket temps réel garde celui avec lequel il s'est ouvert** — ici mort depuis **80 minutes**. 🔴 **Ce que ça cassait, sans le moindre symptôme** : au bout d'environ une heure d'app allumée, les abonnements temps réel (`connection_requests`, `connections`, `cards`, `conversations`) tombent en erreur **et n'en sortent plus**. Une demande de connexion reçue n'arrive jamais à l'écran ; l'utilisateur voit simplement… rien. ⚠️ **Deux moitiés, et la seconde est celle qu'on oublie** : ① le socket doit recevoir le jeton frais (`realtime.setAuth`) ; ② **un `StreamProvider` en erreur y reste** — le renouvellement ne change ni l'identifiant de l'utilisateur ni le client, donc Riverpod n'a aucune raison de reconstruire. D'où `realtimeEpoch`, surveillé par les **cinq** abonnements temps réel de l'app (inventaire fait). ⚠️ **À appliquer à tout nouvel abonnement temps réel** : il doit surveiller `realtimeEpochProvider`, sinon il retombera dans le même trou. | 2026-08-17 |
| 39 | 🐛 **PIÈGE DE THÈME — `Size.fromHeight` donne une largeur INFINIE, et tout `FilledButton` dans une `Row` est cassé** | **Prouvé le 2026-08-17 par un test** (`test/filled_button_row_test.dart`, rouge). `theme.dart` pose `minimumSize: const Size.fromHeight(52)` sur le style des `FilledButton`. Or **`Size.fromHeight(52)` vaut `Size(double.infinity, 52)`** — une largeur minimale **infinie**, pas « seulement une hauteur ». Le nom du constructeur dit le contraire de ce qu'il fait, et c'est ce qui a permis au défaut de vivre des semaines. **Dans une `Row`**, le bouton impose `BoxConstraints(w=Infinity)` à son parent : en debug Flutter lève, **en release `debugAssertIsValid` est compilée hors du binaire** et la mise en page continue — le bouton est peint à droite de l'écran, **sans un mot**. 🔴 **Ce qu'il a coûté** : la carte « X veut se connecter avec toi » affichait bien ses DEUX boutons dans le code, et Jay n'a vu que « Refuser » → **une demande d'ami impossible à accepter**. ⚠️ **AMPLEUR — j'avais d'abord écrit « tout `FilledButton` dans une `Row` est cassé », c'est FAUX** : la largeur infinie ne casse que si le parent laisse la largeur **non bornée**. Un `Expanded`, un `Flexible` ou un `SizedBox` la borne. Inventaire du 2026-08-17 : sur **49** `FilledButton`, 8 sont dans une `Row`, et **un seul est nu** — `ping_screen.dart:400`, celui que Jay a trouvé. Les 7 autres tiennent parce que leurs auteurs ont mis un `Expanded` **par habitude, pas parce qu'une règle l'imposait**. ✅ **Correctif retenu (révisé après inventaire)** : **garder** `Size.fromHeight` — la largeur infinie n'est pas un accident, c'est ce qui rend pleine largeur les ~40 gros boutons de l'app sans qu'on l'écrive ; la retirer les rétracterait tous. Donc : ① `Expanded` sur les deux boutons de la carte, ② **un commentaire au point de définition dans `theme.dart`** qui énonce la contrainte (« dans une `Row`, il FAUT un `Expanded` ; sinon le bouton est peint hors de l'écran, et en release rien ne le signale »), ③ le test garde le contrat. | 2026-08-17 |
| 38 | 🔴 **CHANTIER — L'AMITIÉ A CINQ REPRÉSENTATIONS QUI NE SE PARLENT PAS** | **Diagnostic complet : `docs/diagnostic-connexions-2026-08-17.md`**, écrit après le test de Jay sur la v0.9.112 (*« ce n'est vraiment pas pro et l'architecture ne me semble pas bonne »*). **Sept défauts prouvés** — dont le plus parlant est celui que Jay avait lui-même nommé : *« il manque une connexion entre la vérification et l'affichage »*. `PeerNetwork` calcule `isFriend`, l'émet dans `PeerIdentified`… et `markIdentified` ne le range pas dans l'entrée de présence, que le bouton lit. **Les cinq représentations** : ① `connections` (serveur, la vérité) ② `FriendKeyBook` (fichier, **trois instances avec trois caches mémoire** — la synchro écrit sur le disque, le réseau ne relit jamais) ③ `PresencePeer.isFriend` (mémoire, ce que le bouton lit) ④ `connection_requests` (serveur, ce que « Demandes & rencontres » lit — **vide, 0 ligne en base**) ⑤ `ProximityJournal` (fichier local, où vivent les demandes BLE). **Un sixième chemin, invisible au code** : `isFriend` ne veut dire « ami » que parce que la politique RLS `device_keys_friends` restreint `device_keys` aux connexions `full` — le client écrit « prends tout ». Ça marche par accident. ⚠️ **La règle proposée, à valider** : **la présence dit OÙ et À QUELLE DISTANCE, jamais QUI** — donc `PresencePeer.isFriend` disparaît, et `isFriend` se dérive au rendu d'un `FriendBook` unique. C'est ce qui rend le défaut impossible par construction au lieu d'ajouter un garde-fou à maintenir dans les trois chemins d'identification. **Quatre décisions attendues de Jay**, voir §5 du diagnostic. | 2026-08-17 |
| 37 | 🛑 **CONSIGNE DE MÉTHODE — « le coût du natif n'est JAMAIS un motif de refus »** | **Consigne explicite de Jay, 2026-08-17, en corrigeant Claude** : *« ce n'est pas grave, on code cela en natif, de toute façon j'ai l'impression que plus on avance dans le développement plus on devient natif donc on ne va pas se limiter parce qu'on devra le coder deux fois. Donc on fait du natif et on assume, on n'est pas fainéant. Je ne veux plus que tu me restreignes sur des fonctionnalités essentielles à l'UX parce qu'il faudrait le coder en natif et donc en double, la priorité c'est le confort de l'utilisateur ! »* ⚠️ **Ce que ça change dans mes réponses** : « ça n'existe que sur Android » / « il faudrait le réécrire pour iOS » / « il n'y a pas d'API cross-platform » ne sont **plus des arguments contre une fonctionnalité**. Ce sont des **chiffrages**, à donner comme tels (« voilà ce que ça coûte des deux côtés »), et la décision revient à Jay. ⚠️ **Ce que ça ne change pas** : la stratégie reste **Android d'abord, iOS additif** (`docs/strategie-multiplateforme.md`) — assumer le natif ne veut pas dire faire les deux en même temps. Et le **catalogue** `docs/parties-natives-par-os.md` devient d'autant plus important qu'il y aura plus de natif : c'est lui qui évite de découvrir le périmètre au dernier moment. 🔎 **À ressortir** dès que je m'apprête à écarter une piste pour cause de « mono-OS » — notamment Wi-Fi Direct / Wi-Fi Aware / Multipeer, et UWB (Nearby Interaction vs `androidx.core.uwb`). | 2026-08-17 |
| 36 | 🛰️ **MOTEUR SPATIAL PAR PALIERS — direction donnée par Jay le 2026-08-17** | **Méthode imposée par Jay, dans cet ordre** : ① on choisit les technologies qu'on intègre ; ② on détermine **à quoi chacune sert** (quel(s) rôle(s) on lui donne) ; ③ le système fonctionne **par paliers** — *« plus on a de techno précise disponible entre deux appareils, plus les utilisateurs auront une expérience précise et fluide, mais tout cela sans exclure ceux qui n'auraient que le BLE de base »*. **Clustering : côté SERVEUR, par GPS**, décision de Jay — et *« cela ne doit pas constituer un élément essentiel de ping »*. ⚠️ **Précision de Jay sur la promesse produit, à retenir telle quelle** : *« ce qui se passe sur NeoVibe reste sur NeoVibe »* veut dire **ce qui se PARTAGE** — anti-capture, pas d'enregistrement dans la galerie, écosystème fermé, suppression 24 h. **Ce n'est pas une promesse sur la télémétrie de position.** J'avais étendu la phrase au-delà de son intention. **Objectifs visés** : repérer les clusters (bus, école, lieu de travail) pour créer chats et événements de groupe ; indiquer la **direction** pour aller retrouver quelqu'un ; et **filtrer qui est réellement à portée** pour le chat P2P (ne pas afficher des gens hors de portée). ⚠️ **Points ouverts à trancher à l'ouverture du chantier** : (a) rétention des points GPS bruts côté serveur — un cluster est éphémère par nature, garder l'**appartenance** et pas la **trace** ; (b) les **groupes scolaires sont explicitement visés**, donc données de localisation + mineurs = la combinaison la plus encadrée qui soit (consentement, durée de conservation, minimisation) — à cadrer à la conception, jamais après ; (c) **un palier qui change en cours de session** ne doit jamais être décidé indépendamment par chaque côté (c'est exactement le motif des messages fantômes), ni baisser en silence. ⚡ **PISTE Wi-Fi — proposée par Jay le 2026-08-26, après lecture d'analyses externes sur Happn.** Pour ce chantier-là (et **seulement** celui-là), le Wi-Fi répond où le GPS échoue : « même lieu » plus fin qu'un carreau d'1 km, sur des gens **hors portée BLE**. ⚠️ **Ce qui a été vérifié le 2026-08-26, pour ne pas le redécouvrir** : ① **on utilise DÉJÀ le Wi-Fi** — `geolocator` sans `forceLocationManager` passe par le **FusedLocationProvider**, qui croise Wi-Fi + antennes + GPS ; notre `LocationAccuracy.low` est même le mode qui privilégie le plus le réseau. Dire « GPS » dans notre code est un abus de langage, et Happn ne faisait pas autre chose. ② **Lire les BSSID nous-mêmes coûterait les permissions maximales** : `getScanResults()` exige `ACCESS_FINE_LOCATION` (Android 8+), et scanner app fermée exige `ACCESS_BACKGROUND_LOCATION` — « Autoriser tout le temps », écarté le 2026-08-20 — pour un résultat bridé à **1 scan / 30 min** en arrière-plan depuis Android 9. ③ **Envoyer des BSSID au serveur donnerait notre position au mètre** (bases publiques WiGLE/Google/Apple) : on remplacerait la donnée volontairement grossière du carreau par la plus précise qui soit. Le hachage ne protège pas de NOTRE serveur (parc d'AP fini → dictionnaire précalculable) ; seul un secret de paire le ferait, et entre amis le BLE suffit déjà. ✅ **Forme à privilégier quand ce chantier s'ouvrira** : pas la liste des AP environnants, mais la question binaire « sommes-nous sur le même réseau ? » — ou mieux, une découverte **pair à pair** sur le réseau local (mDNS / `NsdManager`), qui ne demande aucune permission de localisation, ne passe par aucun serveur, et **prouve** le même réseau au lieu de le déduire. ⚠️ **Ne pas rouvrir cette piste pour la précision du ping** : le carreau fait 1 km et le voisinage 3 km, une erreur GPS de 100 m en intérieur est absorbée — la panne du 2026-08-26 n'était pas un défaut de précision mais un refus de publier. | 2026-08-17, complété le 2026-08-26 |
| 27 | 🧭 **VIRAGE STRATÉGIQUE — on arrête de pousser la caméra** | **Décision de Jay, 2026-08-14**, après être retourné sur Snapchat : *« On ne pourra pas les concurrencer sur la caméra. Ils sont trop établis et proposent trop d'options. […] On va arrêter de trop pousser la caméra. Il nous faut juste quelque chose de parfaitement fonctionnel et on va se concentrer sur le développement d'autres fonctionnalités, qui distingueront NeoVibe de Snap. »* **Conséquence de méthode, à appliquer à toute demande future touchant la capture** : la barre est désormais « **parfaitement fonctionnel** », pas « meilleur que Snap ». Une nouvelle option de prise de vue doit être **refusée par défaut** et renvoyée à cette entrée — sauf si Jay la demande explicitement en connaissance de ce virage. Le temps de développement va aux fonctionnalités **différenciantes** : #11 (quiz et mini-jeux entre amis), #10 (feed local), #1 (streaks). ⚠️ Ne PAS confondre avec un abandon : la caméra reste le geste d'entrée du produit, elle doit juste cesser d'absorber l'effort. | 2026-08-14 |
| 31 | 🎠 **CARROUSEL À DOIGT SUIVI ENTRE SECTIONS — redevenu possible, à chiffrer si Jay le veut** | Écarté le 2026-08-15 sur un motif **faux**, corrigé le 2026-08-16 après objection de Jay : j'avais écrit qu'un `PageView` ferait tourner l'écouteur BLE du Ping en permanence. **Vérifié depuis** : `ProximityService.build()` ne démarre rien, le matériel ne part que sur `enable()` (l'interrupteur de l'écran Ping) et tourne en **service de premier plan**, donc indépendamment de l'interface — app fermée comprise, ce qui est précisément la conception voulue. Monter `PingScreen` n'ajoute qu'un écouteur sur un magasin local. **Le coût réel d'un carrousel**, lui, tient en trois points : (a) `PageView` construit ses voisins → les **chargements réseau** du Cercle et du Profil partiraient au lancement (contournable avec `cacheExtent: 0`) ; (b) il **détruit** les pages quittées → l'état des onglets serait perdu, sauf à rendre les quatre écrans persistants (`AutomaticKeepAliveClientMixin`), ce qui touche quatre fichiers ; (c) la **capture n'est pas une page** — elle s'ouvre par-dessus — donc le bord gauche du carrousel demanderait un traitement à part, et Jay vient justement de faire retirer le décalage à cet endroit (2026-08-16). ⚠️ **La leçon vaut plus que le chantier** : une contrainte écrite une fois et jamais revérifiée avait fini par décider de l'architecture. C'est le deuxième commentaire faux trouvé en deux jours, après « le reste de l'app n'est pas écrit pour le bord à bord ». | 2026-08-16 |
| 33 | 🏷️ **LE TAG SE DÉDUIT DE `pubspec.yaml`, il ne se tape JAMAIS à la main** | **Défaut commis le 2026-08-16, relevé par Jay** : trois releases de suite (v0.9.96, .97, .98) ont été taguées sans que `pubspec.yaml` bouge — il est resté à `0.9.95+185`. Jay a donc installé la v0.9.98 sur une app qui **s'annonçait 0.9.95**, et n'avait aucun moyen de savoir ce qu'il testait. ⚠️ **L'instrument n'était pas en cause** : `diagnostic_bundle.dart` lit la version du **paquet Android** et porte même le commentaire « une version recopiée à la main finit toujours par mentir ». C'est le **processus** qui a divergé. **Procédure, dans cet ordre, sans exception** : ① modifier `pubspec.yaml` (`version:` **et** le numéro de build, qui doit toujours croître) ; ② commiter ; ③ **déduire le tag du fichier**, jamais le taper : `git tag "v$(grep '^version:' pubspec.yaml \| sed 's/version: //; s/+.*//')"` ; ④ construire ; ⑤ publier. *La divergence devient alors impossible par construction, au lieu d'être évitée par discipline — et la discipline a déjà échoué trois fois de suite.* | 2026-08-16 |
| 35 | ✅ **DEUX CONNEXIONS GATT SOUS UNE SEULE ADRESSE — HYPOTHÈSE FAUSSE, mesurée et close le 2026-08-17** | **`bothPathsPeak: 0` sur les DEUX appareils** (v0.9.112). `clientPaths: 1 / serverPaths: 0` côté Xiaomi, l'inverse côté tablette : topologie propre, **un seul chemin physique par pair**. 🔴 **Aucune réécriture du natif n'était justifiée** — et c'est exactement l'engagement pris en posant la mesure : *si le pic reste à 0, l'hypothèse est fausse et on n'y touche pas.* ⚠️ **La leçon vaut plus que le résultat** : le raisonnement était solide, écrit noir sur blanc dans `BleEngine.kt`, et il était **faux**. Dix lignes de compteur ont évité un gros diff non justifié et un aller-retour de test. À refaire à l'identique la prochaine fois qu'une lecture de code produit une conviction sans observation. 🔎 **En revanche, la mesure a trouvé autre chose** : une QUATRIÈME cause de message fantôme (entrée #40). *Texte d'origine ci-dessous.* | 2026-08-17 |
| 40 | ✅ **QUATRIÈME CAUSE DE MESSAGE FANTÔME — CORRIGÉE le 2026-08-17 (v0.9.114)** ⚠️ *reste à valider au test de Jay* | **Reproduite en test avant d'être corrigée**, dans ses DEUX configurations de rôle. **Cause exacte** : la session appartenait à un **rôle** — initiateur ou répondeur — déduit de *qui avait composé le numéro*. Or le rôle est un fait de **transport** que les deux côtés ne lisent pas toujours pareil : deux liens qui se croisent, ou une session reconstruite d'un seul côté, et les deux peuvent se croire initiateurs. Mêmes clés, **préfixes de nonce opposés**, AES-GCM refuse tout. Et quand la reconstruction passait, elle se faisait **à moitié** : la clé suivait, pas les compteurs — le pair repartait au compteur 0, nous étions à 5, tout était refusé. **Correctif** : la session appartient au **couple de clés éphémères** ; le sens vient de leur **ordre** ; **les deux côtés ouvrent** la poignée de main (plus personne n'attend) ; et **un `hello` reçu l'emporte toujours** — un pair ne l'envoie que s'il a perdu sa session, garder la nôtre ne peut produire que du silence. Reconstruire = **tout ou rien** (nouvelle clé éphémère, nouvelle clé de session, compteurs à zéro). ⚠️ **Un `hello` répété ne reconstruit rien** (même clé éphémère) — sinon il suffirait d'en rejouer un pour désarmer l'anti-rejeu. ⚠️ **CHANGEMENT DE PROTOCOLE** : les deux appareils doivent être à la même version, un ancien et un nouveau ne se comprendront pas. 🔎 **La leçon, plus large que le bug** : c'est la **troisième fois** sur ce chantier qu'un correctif de symptôme fabrique le défaut suivant. Une règle qui dit « ne jamais changer d'état » n'est complète que si l'on précise **ce qui fait converger les deux côtés** — sinon on a seulement déplacé le désaccord, d'un état écrasé vers un état figé, qui se voit moins. La bonne question n'était pas « faut-il remplacer le canal ? » mais **« qu'est-ce que les deux côtés peuvent calculer à l'identique ? »**. *Texte d'origine ci-dessous.* | Relevée dans les journaux de la v0.9.112, pas déduite. Côté tablette : **deux `session fermée par l'entretien — fusion d'adresses`**. Côté smartphone, 2 min après : `second lien ignoré (entrant, canal established)` puis **deux `déchiffrement refusé, compteurs 0 et 1`**. ⚠️ **Les compteurs 0 et 1 sont décisifs** : ce n'est pas un rejeu (qui porterait un compteur ancien) mais une **session NEUVE**. Le pair a reconstruit son canal ; nous avons refusé son nouveau lien parce que l'ancien canal était encore « established » ; la clé ne correspondait plus. **Deux causes, à traiter ensemble** : ① **la fusion d'adresses ferme encore un canal ÉTABLI** — `hasLiveLink` ne protège la fusion que lorsqu'une **seule** des deux adresses est vivante ; quand les deux le sont, une session vivante est sacrifiée ; ② **la règle « un canal établi ne se remplace jamais », posée le 2026-08-16, est devenue la cause du défaut suivant** : elle interdit d'adopter la session que le pair a déjà reconstruite de son côté. ⚠️ **Troisième fois sur ce chantier que le correctif d'un symptôme devient la cause du suivant.** La sortie n'est pas un troisième garde-fou : c'est que **les deux côtés doivent s'accorder sur la session courante**, au lieu que chacun applique « le premier gagne » à des événements différents. ⚠️ Jay ne l'a PAS signalé (les messages passent) — c'est une dette mesurée, pas une panne visible. | 2026-08-17 |
| 35 bis | 🔬 **Texte d'origine du 2026-08-17 — hypothèse des deux chemins GATT** | Relevé **en lisant `BleEngine.kt`**, jamais observé sur un appareil : une même adresse peut porter **deux chemins physiques simultanés** — `clientLinks` (nous sommes central) et `serverCentrals` (le pair l'est) — pour **un seul `linkId`, l'adresse**. Le repli passif de 12 s rend le cas fréquent, puisqu'il fait connecter les deux côtés. **Trois conséquences, aucune ne lève** : ① la mort d'**un** chemin est annoncée au Dart comme la mort du lien alors que l'autre vit encore ; ② `send()` **préfère toujours le chemin client**, donc il peut changer de transport en cours de conversation sans que personne ne le décide, et l'ordre entre les deux files natives n'est pas garanti ; ③ `connect()` rend un succès **immédiat** si un chemin client existe, **sans émettre d'événement de lien** — le Dart attend alors 8 s une poignée de main qui ne partira jamais, et l'état est **collant** tant que le chemin physique vit. Le ③ correspond exactement au *« poignée de main impossible »* signalé par Jay. 🔴 **RIEN N'A ÉTÉ CORRIGÉ — c'est une déduction, pas une observation** (règle 7 du projet). À la place, `BleEngine.pathStats` publie `clientPaths` / `serverPaths` / `bothPaths` et **`bothPathsPeak`**. ⚠️ **Le pic, pas l'instantané** : deux chemins peuvent coexister trois secondes puis disparaître ; relevé au moment de l'envoi du rapport, `bothPaths` vaudrait zéro et on conclurait à tort que le cas n'existe pas. **Si `bothPathsPeak` reste à 0 sur les deux appareils, l'hypothèse est fausse et il ne faut rien changer ici.** ⚠️ **Vaut aussi pour iOS** : CoreBluetooth a la même dualité central/périphérique. | 2026-08-17 |
| 34 | ✅ **MESSAGES FANTÔMES — troisième cause TROUVÉE, REPRODUITE et corrigée le 2026-08-17** ⚠️ *reste à valider au test de Jay* | **Cause** : `PresenceTracker.prune()` confondait **entendre** un pair (son annonce BLE — une diffusion non fiable qu'un téléphone en poche fait manquer 25 s de suite) et **être relié** à lui (une connexion GATT, surveillée par la pile Bluetooth). Passée la période de grâce sans annonce, il déclarait le pair parti et `tick()` détruisait le lien **et le canal** — **sans rien couper côté radio**. En face, rien n'avait changé : canal établi, **envoi réussi sans erreur**, et la trame arrivait sur un lien devenu sans canal où `_handleFrame` la jetait en silence. Émetteur satisfait, destinataire muet. ⚠️ **`hasLiveLink` existait déjà**, posé le 2026-08-16 contre le même défaut à la **fusion d'adresses** : la cause avait été traitée à **une porte de sortie sur deux**. *Leçon générale, plus large que ce bug : quand on pose une garde, chercher tout de suite les AUTRES endroits qui font la même chose — ici, les deux seuls endroits qui font sortir un pair.* **Correctif** : un pair relié n'est jamais « parti » ; c'est la mort du lien qui l'emporte. **Reproduit** par `test/peer_network_test.dart` (rouge avant, vert après) — pas déduit. 🔎 **Le double de test cachait le défaut** : `RadioSimulee` ne modélise qu'un chemin par paire, là où le natif en a deux. 102 tests verts pendant que les messages disparaissaient. *Quand un défaut résiste aux tests, regarder d'abord ce que le double ne sait pas faire.* **Instrumentation livrée avec** : `transport_trace.dart` consigne les 8 points où une trame disparaît sans un mot (dont `PeerLink.onDropped`, **qui existait et n'était branché nulle part**), avec un compteur de **livraisons** comme dénominateur, et une section dédiée dans le rapport de diagnostic. *Texte d'origine ci-dessous.* | 2026-08-16 |
| 34 bis | 🔴 **MESSAGES FANTÔMES — texte d'origine du 2026-08-16** | Jay, en clôturant le 2026-08-16 sur la v0.9.111 : *« globalement cela fonctionne mais il y a encore des messages fantômes perdus, je ne sais pas pourquoi »*. **Deux causes déjà corrigées ce jour-là**, et il en reste au moins une : ① `_onLinkUp` écrasait un canal établi quand les deux appareils se connectaient en même temps — cas rendu **fréquent par le repli passif de 12 s que j'avais moi-même ajouté** pour corriger B4 ; ② la fusion d'adresses (renouvellement de MAC) abandonnait une ligne sans fermer son lien. ⚠️ **Signature du défaut, à garder en tête** : le message part sans erreur chez l'émetteur et n'arrive jamais chez le destinataire — donc `decrypt` rend `null` quelque part, ou la trame n'atteint pas le bon canal. **Rien n'est instrumenté sur ce chemin** : ni les échecs de déchiffrement, ni les remplacements de canal, ni les trames reçues sur un lien sans canal. **C'est par là qu'il faut commencer** — le rapport ne peut pas dire ce que le code ne consigne pas, on l'a payé trois fois dans la journée. Les deux rapports de la v0.9.111 sont en base (`dev_reports`). | 2026-08-16 |
| 33 bis | 📏 **NI UWB NI WI-FI RTT sur les deux appareils de test — MESURÉ** | Relevé le 2026-08-16 par sonde `hasSystemFeature`, pas déduit d'une fiche technique : `uwb: false` et `wifiRtt: false` sur le **Xiaomi M2101K6G** (Android 13) comme sur la **LENOVO TB-X606F** (Android 10). **Conséquence** : l'architecture de négociation multi-technologies proposée par Jay est juste — c'est d'ailleurs ce qu'Android 16 a fait avec `RangingManager` — mais elle **n'a rien à négocier aujourd'hui**. La construire maintenant produirait un sélecteur à une seule branche. ⚠️ **À rouvrir dès qu'un appareil de test portera l'UWB ou le Wi-Fi RTT**, et pas avant. ⚠️ **Où transmettre les capacités le jour venu** : dans le **profil, à l'intérieur du tunnel chiffré**. Pas dans l'annonce (31 octets, on en occupe 28), pas dans le certificat (règle 5 : ne pas mélanger le contenu et le format), et **surtout pas en clair** — « cet appareil a l'UWB » est une **empreinte d'appareil**, qui trahirait tout le travail de l'identifiant rotatif. | 2026-08-16 |
| 32 | 📡 **PING ET RÉSEAU P2P — RECONSTRUITS DE BOUT EN BOUT (2026-08-16), à éprouver sur appareil** | Carte blanche de Jay après le diagnostic (13 défauts, `docs/diagnostic-proximite-2026-08-16.md`). L'ancien `proximity_service.dart` de **1040 lignes** est **supprimé**, avec `ble_link.dart` et `NativeBle.kt`. Architecture en 8 couches : `docs/architecture-proximite.md`. **Deux décisions de Jay** : ① le ping vit en **natif Kotlin dans un service dédié** (survit à la destruction de l'interface) ; ② le certificat entre amis se fait par un **échange court**. **Ce qui est corrigé** : la radio publie son **état réel** (plus un seul échec silencieux) ; l'**intention** de l'utilisateur est persistée et distincte de l'état du matériel, donc app fermée/rouverte et Bluetooth coupé/remis se rattrapent **seuls** ; la poignée de main est **signée** (l'homme du milieu était possible) et les trames portent un **compteur anti-rejeu** ; les certificats existent enfin **entre amis** ; les demandes d'amis sont **persistantes et multiples** ; le N+1 de la synchro passe de N+1 requêtes à **2**. ✅ **ÉPROUVÉ SUR DEUX APPAREILS RÉELS le 2026-08-16** : détection, reconnaissance silencieuse des amis, waves dans les deux sens, certificat de croisement co-signé vérifié par le serveur, résistance aux coupures. Huit défauts trouvés et corrigés au passage (voir le rapport de session). ⚠️ **Reste ouvert** : les messages fantômes, entrée #34. *Texte d'origine :* rien n'avait alors jamais tourné sur une vraie radio. 102 tests passent et deux piles complètes dialoguent en test, mais aucun octet n'est passé par un vrai Bluetooth. **Trois vérifications prioritaires au premier test** : (a) le service survit-il à la fermeture de l'app ; (b) couper/remettre le Bluetooth relance-t-il tout seul ; (c) deux appareils réels se découvrent-ils, et en combien de temps. ⚠️ **Pour iOS** : seule la couche 0 est à réécrire (tout le reste est du Dart sans dépendance Android), mais **le modèle ne se transpose pas** — iOS n'a pas de service de premier plan, et l'advertising en arrière-plan n'y transporte pas les données de fabricant, or c'est là que voyage notre ID rotatif. **Mode dégradé à concevoir**, pas seulement à porter. | 2026-08-16 |
| 30 bis | 📱 **LES BORDS APPARTIENNENT À ANDROID — constaté sur tablette le 2026-08-16, à appliquer à TOUT geste futur** | **Test de Jay sur tablette en navigation par gestes** : l'app se **fermait** au glissement de sortie de la caméra et au changement de section. **Cause** : notre bande de 30 px était posée **dans** la zone de gestes réservée. Le SDK Flutter l'écrit noir sur blanc (`MediaQueryData.systemGestureInsets`) : *« les glissements qui commencent dans cette zone **peuvent** ne pas être délivrés à l'app […] les apps devraient éviter d'y placer des détecteurs de gestes »*. ⚠️ **Le « peuvent » est le piège** : la livraison n'est **pas déterministe** (1 échec sur 5 chez Jay), donc ce genre de défaut **ne se reproduit pas à la demande** et ne sera jamais attrapé par un test ponctuel. **Quand les deux sont délivrés**, ils décident au **même instant** (le relâchement) : notre geste ferme l'écran, le retour système dépile ensuite l'accueil devenu sommet → fermeture de l'app. **Correctif livré (v0.9.96) et ✅ VALIDÉ AU TEST par Jay le 2026-08-16** : la bande ne s'arme que si `systemGestureInsetsOf(context).right == 0` — en mode gestes, on laisse le bord à Android, qui fait déjà le bon retour. ⚠️ **La zone d'exclusion `setSystemGestureExclusionRects` est écartée** : plafonnée par le système à ~200 dp par bord, soit **un cinquième d'une tablette** — un garde-fou qui échoue précisément sur les grands écrans, et qui demanderait un canal natif par OS. 🔎 **Relevé au passage, vaut pour toute l'app** : `targetSdk = 36` (défaut de Flutter 3.44), niveau auquel Android active le **retour prédictif par défaut** — c'est lui qu'on voit s'animer pendant le glissement. **Personne ne l'avait consigné.** | 2026-08-16 |
| 30 | 🧪 **NAVIGATION AU SWIPE ENTRE SECTIONS — essai à valider ou retirer** ⚠️ **Terme changé le 2026-08-16** : sur un appareil en **navigation par gestes**, ce geste ne se déclenchera **jamais depuis les bords** — Android les garde, définitivement (voir #30 bis). Il faut partir du milieu de l'écran. La décision « on garde ou on retire » se prend donc en sachant que le geste est **partiellement inaccessible** là où l'utilisateur pose le pouce le plus naturellement. *Texte d'origine :* | Livré le 2026-08-15 (v0.9.87) à la demande de Jay, qui l'a explicitement posé comme un test : *« ceci est un test, je veux tester si cela s'implémente bien et à l'usage si cela confère une bonne UX. Mais ce n'est pas essentiel. »* **À ressortir après son test** : soit on le garde, soit on le retire — un geste global qu'on n'assume pas est pire que pas de geste. ⚠️ **Ce qu'il faut savoir avant d'y retoucher** : aucune « zone » n'est codée en dur, et c'est volontaire. L'arène de gestes de Flutter donne déjà la priorité à l'enfant (les candidats sont ajoutés du plus interne vers le plus externe, le balayage retient le premier) — un `PageView`, un `ListView` horizontal ou un détecteur de carte gagnent tous, y compris en bout de course. Une zone en dur aurait été fausse au premier changement de mise en page, **et fausse en silence**. ⚠️ **Non fait** : le contenu ne suit pas le doigt. Un `PageView` le donnerait mais construirait les cinq onglets d'avance, ce qui ferait tomber l'`IndexedStack` paresseux — or le Ping tient un écouteur BLE vivant tant qu'il est monté. | 2026-08-15 |
| 29 | 🎨 **CYCLE DE 24 H — les quatre suites demandées par Jay** | **Notées le 2026-08-15 en fin de session, à reprendre le lendemain.** Le moteur, l'écran d'aperçu et le test des 1440 minutes sont livrés (v0.9.82) ; Jay a validé le rendu — *« c'est vraiment cool »*. Ce qui reste : ~~**① Réorganiser les plages.**~~ ✅ **FAIT le 2026-08-15 (v0.9.83)** — ancrages v3 + `HuePath` (corde/arc), chroma moyenne **vue** 0,0771 → 0,1031 (**+34 %**). **Trois choses à retenir.** (a) Le diagnostic était une **anti-corrélation quasi parfaite** entre chroma et audience : les deux plus colorées à 4 h et 6 h, le pic du soir sur les trois plus ternes. (b) **La moitié des « gradients ternes » n'étaient pas des palettes mais des TRANSITIONS** — l'interpolation OkLab linéaire est une *corde* qui coupe à travers le gris ; `HuePath.arc` fait tourner la teinte sur l'arc court, +18 % **sans toucher une seule palette** (vérifié par test : aux heures d'ancrage les deux chemins donnent la même couleur). ⚠️ **L'arc va sur le TEMPS, jamais sur la verticale du dégradé** — sur la verticale il fait sauter la couleur, ΔE 0,29 pour un seuil de 0,02. (c) ⚠️ **CONTREPARTIE ASSUMÉE, à ressortir si Jay trouve le soir fade** : il a demandé que **Desert passe de 18 h à 6 h** (« un peu sombre pour l'après-midi, ne s'insère pas bien »). Or Desert est la **seule** palette à la fois riche (0,164), chaude et mi-sombre — donc la seule qui cochait tout pour le pic du soir. Sans elle, le pic plafonne à 0,06–0,12 au lieu de 0,15. Le seul remplaçant possible dans le jeu serait **Velvet Sunset** (0,157, chaude), mais il faudrait la sortir du matin. **Décision d'arc contre décision d'audience — Jay a tranché pour l'arc, en connaissance des chiffres.** ✅ **ARC ADOPTÉ ET IMPLÉMENTÉ le 2026-08-15 (v0.9.84)** — Jay après test : *« l'arc me va nickel, tout me va »*. `HuePath` et le sélecteur de l'aperçu sont **supprimés** ; la corde ne sert plus qu'à la verticale du dégradé. Le test de comparaison a été remplacé par un **test de propriété** : *le milieu d'un segment n'est jamais plus terne que sa borne la plus terne* (0,83 avec l'arc contre 0,37 avec la corde) — il existe parce qu'un retour à la corde serait **invisible au diff comme à l'exécution**. ⚠️ **L'écran d'aperçu est CONSERVÉ** dans Développeur sur demande explicite de Jay (*« cela peut toujours servir pour plus tard »*) — il reste à retirer avec la section Développeur avant la prod (ligne 4 des points avant-prod). **② Personnalisation par l'utilisateur — ⚠️ LE POINT BLOQUANT EST RÉSOLU (2026-08-15, v0.9.87).** *Texte d'origine : inverser le cycle, modifier des plages ; « on a conçu un moteur de gradient adaptable donc ce serait bien de permettre à l'utilisateur de personnaliser » ; mais le test des 1440 minutes ne protège plus un arrangement fait par l'utilisateur — à trancher.* ✅ **La réponse est trouvée et livrée dans l'aperçu développeur** : `DayCycle.autoSchedule(ordre)` répartit les 24 h **au prorata de la distance de couleur** de chaque segment. La vitesse perçue devient **uniforme**, donc aucun segment ne peut être « le plus rapide » — et **n'importe quel ordre passe le test**, sans arbitrage à la main (mesuré : 0,008 de dérive sur 3 min pour un seuil de 0,02, soit un facteur 2,5 de marge). **Le partage est donc : l'utilisateur pose l'ORDRE, le moteur déduit les HEURES.** ⚠️ **Ne jamais lui laisser poser les heures directement** — c'est exactement ce qui rouvrirait le défaut que le test a attrapé deux fois (un segment trop rapide), et il n'y aurait personne pour le voir sur un ordre composé par l'utilisateur. **Reste à faire pour livrer la fonctionnalité aux vrais utilisateurs** : sortir le réordonnancement de l'écran développeur, et le persister (aujourd'hui il ne vit que le temps de l'écran d'aperçu). **③ Modes de vitesse.** Le défaut suit l'horloge ; ajouter des boucles plus ou moins rapides, éventuellement réglables. ⚠️ Attention : au-delà d'une certaine vitesse le mouvement **devient visible**, ce qui contredit la conception. Assumer que c'est alors un autre parti pris (un fond animé), pas le cycle. **④ 🌟 GRADIENTS VIVANTS — voir `docs/vision-produit.md`**, l'idée dépasse le thème. | 2026-08-15 |
| 28 | 🎨 **TROIS thèmes, dont un dynamique — ✅ LIVRÉ le 2026-08-15 (v0.9.85)** | ⚠️ **Le ⛔ « ne pas coder » est levé** : Jay a demandé le branchement le 2026-08-15 (*« tu n'as pas implémenté le thème sur l'app si ? là j'ai toujours le thème clair ou sombre »*). **Ce qui est livré** : `NeoThemeChoice { neovibe, light, dark }` stocké **par nom**, avec **reprise de l'ancien booléen `light_theme`** à la première lecture (l'ancienne clé n'est pas effacée — l'effacer détruirait l'information avant tout retour en arrière possible) ; `NeoTheme.neovibe()` met `scaffoldBackgroundColor` à **transparent** et le dégradé est posé **une fois** dans `MaterialApp.builder`, donc les **60 Scaffold** de l'app sont traversés sans qu'aucun soit touché. ⚠️ **`builder` et non un Stack autour de `home`** : les routes poussées sont des surfaces SŒURS de `home` — un Stack autour de `home` aurait laissé toutes les navigations sur fond noir, défaut visible seulement à la 2ᵉ page. **Les 14 écrans de contenu (caméra, visionneuses, éditeurs) posent déjà `Colors.black` explicitement** : ils sont insensibles au changement, par construction. **Ce qui reste ouvert** : ① l'**accent horaire n'est PAS branché** — les boutons gardent le dégradé de marque (consigne du 2026-08-14) ; `DayPalette.accent` existe, est testé, et attend une décision de Jay ; ② le **défaut à l'installation neuve** est `neovibe`, à confirmer ; ③ les surfaces sont des **voiles neutres opaques ou semi-opaques**, pas encore le « voile translucide » plus premium évoqué dans la doc. *Texte d'origine ci-dessous.* **Décision de Jay, 2026-08-14.** L'app proposera **trois thèmes mutuellement exclusifs** : ① **NeoVibe** — celui à construire, avec les **dégradés** et le changement **selon l'heure** (*« des couleurs vives et avec de la lumière bleue le matin, midi etc. pour éveiller l'utilisateur, et le soir des couleurs plus chaudes pour ne pas fatiguer les yeux »*) ; ② **clair** — blanc, **fixe** ; ③ **sombre** — **fixe**. ✅ **Ce découpage règle la contradiction que j'avais signalée** : l'horaire n'est pas un second axe qui se bat avec l'interrupteur clair/sombre, c'est **une propriété du seul thème NeoVibe**. Choisir clair ou sombre suffit donc à désactiver le dynamique — rien de plus à prévoir. **Reste ouvert, à trancher avec Jay** : ① le nombre de paliers et leurs heures — fixes, ou calées sur le lever/coucher du soleil local ? ; ② la **transition** entre paliers — un basculement net serait lu comme un bug, un dégradé continu oblige à recalculer le thème en cours de session ; ③ le thème **par défaut** à l'installation. ⚠️ **Contrainte technique à ne pas perdre** : les gris posés le 2026-08-14 (`NeoNeutrals`) sont **strictement** `R == G == B`, et c'est ce qui a fait disparaître le « violet terne ». Les thèmes **clair et sombre doivent garder cette contrainte** ; les dégradés et la teinte horaire appartiennent au **seul** thème NeoVibe. ⚠️ **Migration** : le réglage actuel est un **booléen** `light_theme` (`prefs.dart`). Trois thèmes = un enum, à stocker **par NOM et jamais par indice** — même raison que `StartupTab`, qui a survécu sans migration au déplacement de l'onglet Vibe le même jour. | 2026-08-14 |
| 1 | **Streaks de proximité** | Chantier dédié. La fondation est posée (certificats de croisement co-signés, 10 s de contact continu) : paliers de couleur + dégradation progressive restent à faire. ⚠️ **Complété le 2026-08-02** : `public.encounters` est désormais **purgé à 24 h** (consigne Jay, migration `20260802120000_encounters_ttl_24h.sql`). Un streak ne pourra donc **PAS** se calculer en relisant l'historique de cette table — la ligne d'une paire disparaît 24 h après le dernier croisement. Le chantier streaks devra ajouter **son propre compteur persistant** (dernier jour compté + longueur courante), mis à jour à chaque appel de `report_encounter`, et non purgé. À ressortir à l'ouverture du chantier. | 2026-07-13 |
| 2 | **Stories — socle livré et VALIDÉ (v0.9.38-v0.9.41), refonte de la présentation DÉCIDÉE le 2026-08-02** | Décisions de Jay : une story **EST une Card publiée en story** (pas de pipeline média séparé), **24 h consultable sans limite**, et en ping **croisements des dernières 24 h** uniquement. **Livré et validé au test** : table `stories` + règles d'accès + purge, réglage `stories_public` (défaut FAUX : sans lui, seuls les amis voient), bandeau en haut du Cercle et du Ping, visionneuse (corrigée en v0.9.41). ⚠️ **CHANTIER OUVERT — Jay, au vu du résultat : « le format card tel qu'il est actuellement n'est pas adapté pour les stories ».** Il veut un **dérivé du format Card, sans recto/verso ni retournement**, présenté en **deck / éventail de cartes**. Spécification donnée, à figer : voir la ligne « Stories en deck » des **Décisions en attente**. **Reste à faire ensuite** : indicateur « déjà vue » ; stories en Wi-Fi Direct (voir chantier #3). *Texte d'origine : Format à part ; les cards pourront être mises en story. Emplacement déjà réservé en haut du hub Cercle.* | 2026-07-12 |
| 3 | **Wi-Fi Direct pour les cards en conversation ping** | Le chat ping est en BLE (texte et petits paquets). L'envoi de cards/médias dans les conversations éphémères passera par Wi-Fi Direct. | 2026-07-13 |
| 4 | **Vignettes des grilles** — bibliothèque **FAIT** (v0.9.27), enregistrements **reste à faire** | Les grilles de bibliothèque (la mienne et celle des autres) lisent désormais le **cache local d'abord** (`thumbSourceProvider`, `mini_card.dart`) et décodent en taille réduite ; les photos importées sont copiées en local à l'upload. **Restent sur le réseau** : l'écran « Enregistrements » (`saved_items_screen.dart`, widget `FaceThumb`) et les contenus des AUTRES (jamais mis en cache par une simple consultation de grille — décision volontaire, on ne télécharge pas la bibliothèque d'autrui pour l'afficher). **Vignettes vidéo : FAIT en v0.9.28** (canal natif `neovibe/media`, extraction à la demande + cache) — mais **seulement pour les vidéos LOCALES** : une face filmée d'un autre utilisateur reste une icône grise en grille (il faudrait télécharger la vidéo entière pour en extraire une image). | 2026-07-13 |
| 6 | ~~**Double flux Oneshot — reste la vidéo double**~~ **TERMINÉ (v0.9.23, 2026-07-25)** | ⚠️ *Entrée périmée, corrigée le 2026-08-01 : elle a induit en erreur à l'ouverture de session.* **La vidéo double simultanée est FAITE et validée** (v0.9.13 vidéo, v0.9.14 audio partagé, v0.9.21 dans le vrai Oneshot) — deux encodeurs `MediaCodec` alimentés par une 2e surface EGL, un `.mp4` par caméra, une piste audio unique muxée dans les deux. Le double flux n'est plus opt-in : il s'ouvre par défaut en Oneshot. Le rendu logiciel décrit ci-dessous a été **supprimé** (étape 5d). État réel : `rapports-de-sessions/REPRISE-chantier-gpu-camera.md`. *Texte d'origine (2026-07-14, avant le chantier GPU) :* Le double live FONCTIONNE sur le Redmi Note 10 Pro : 1 flux par caméra, rendu logiciel, capture simultanée. Reste **opt-in développeur** (le bouton « Double live » n'apparaît que si l'option est active). **La vidéo double simultanée reste à faire** (deux encodeurs). Ne JAMAIS revenir à : 2 flux par caméra (frontale affamée), reconfiguration de session pendant que l'autre caméra tourne, sonde de configurations à l'usage (casse le service caméra). | 2026-07-14 |
| 9 | ~~**CHANTIER MAJEUR — rendu caméra en GPU/OpenGL**~~ **TERMINÉ ET VALIDÉ (v0.9.23, 2026-07-25)** | ⚠️ *Statut mis à jour le 2026-08-01.* Les 5 étapes sont livrées : double aperçu 2×30 i/s constants, photo double instantanée (83-119 ms), vidéo double + audio partagé, branchement dans le vrai Oneshot, repli séquentiel, suppression du moteur logiciel. **Il ne reste aucun code** au titre de ce chantier — seulement le test multi-appareils (ligne 8 ci-dessus). Bilan et leçons : `rapports-de-sessions/REPRISE-chantier-gpu-camera.md`. *Texte d'origine :* Direction validée par Jay (2026-07-24). Le rendu logiciel actuel (`lockCanvas`) plafonne à ~20-27 i/s avec freezes ; une app tierce (GoNext « Dual Vlog Camera ») fait **~45 i/s constant + photo + vidéo double** sur le même Redmi → prouve que le GPU/OpenGL est la clé (1 flux/caméra en **SurfaceTexture matérielle**, composité GPU pour aperçu + photo readback + encodeur vidéo). Objectif : fluide + instantané + vidéo double sur appareils capables, **repli séquentiel instantané** (technique « dernière image ») sur les autres = universel. Feuille de route en 5 étapes testables (voir rapport 2026-07-24). Garder le moteur dual logiciel actuel comme filet jusqu'à ce que le GPU le remplace. | 2026-07-24 |
| 8 | **Journal caméra à retirer avant la prod** | `CamLog.kt` écrit une trace détaillée sur le disque de l'appareil + écran « Journal caméra » dans Réglages → Développeur. Outil de diagnostic : à retirer avec la section Développeur (voir ligne 4). | 2026-07-14 |
| 5 | **`contentType` d'upload** | Les fichiers sont des PNG mais uploadés en `image/jpeg`. Sans effet visible, à nettoyer. | 2026-07-12 |
| 10 | **CHANTIER — feed local** | Décidé par Jay le 2026-07-26 (voir `docs/vision-produit.md`). Contenu publié par les gens de sa ville / région / pays, plus des comptes créateurs à visibilité internationale illimitée. Jay assume : « un mal nécessaire », on ne change pas d'un coup le système dans lequel tout le monde est pris. **Implique du contenu DURABLE**, en plus de l'éphémère TTL 24 h — accepté en connaissance de cause. **À ressortir à l'ouverture du chantier** : (a) borne dure sur la part de contenu créateur, décidée à la conception et pas en modération a posteriori, sinon le feed local devient un feed d'attention ; (b) les deux régimes de contenu à faire cohabiter (stockage, quota, purge) ; (c) modération / signalement / blocage, à faire en heuristiques simples. **Jay : « on en reparlera en détail au moment de l'implémenter. »** | 2026-07-26 |
| 11 | **CHANTIER — quiz et mini-jeux entre amis** | Décidé par Jay le 2026-07-26. But : créer de l'interaction et de l'activité **entre amis uniquement** — des amis qui passent un moment ensemble en ligne. Avec compatibilité, classements et FOMO (« ton ami a joué à… »). **Deux règles de conception à tenir** : (1) entre amis uniquement, pas d'inconnus ni de classement mondial — le cercle reste la frontière ; (2) **le jeu est un tremplin vers le réel** : chaque mécanique doit se demander comment elle redirige vers une sortie ou une rencontre physique. | 2026-07-26 |
| 19 | ~~**DÉCISION EN ATTENTE — la limite de vues doit-elle survivre à une mise en story ?**~~ **SANS OBJET depuis le 2026-08-11** — la refonte « 1 contenu = 1 format » (volet 1 du chantier du 2026-08-11) supprime la question : une vibe ne peut plus être **à la fois** envoyée et publiée en story. La story devient un objet distinct, sans limite de vues ; la vibe envoyée garde les siennes. Aucun arbitrage n'est plus nécessaire. *Texte d'origine ci-dessous, conservé pour la trace.* | Question posée à Jay le 2026-08-10 en fin de session, **il réfléchit, réponse attendue à la reprise**. Aujourd'hui, publier une Vibe en story **annule** la limite de vues fixée pour un destinataire en DM : `has_unlimited_card_access` est testée AVANT la livraison dans `open_card_media`. Ce n'est pas un trou de sécurité — c'est la règle affichée dans l'app (« en bibliothèque, la lecture est illimitée ») — mais l'émetteur ne réalise probablement pas qu'en publiant une story il vide de son sens le « 2 vues » qu'il a choisi. **Correctif gratuit** (quelques lignes dans `open_card_media`, zéro octet dupliqué) mais il change la sémantique. **Recommandation donnée** : que la limite tienne, c'est la promesse faite à l'émetteur. **Revers signalé** : le destinataire ayant épuisé ses vues ne verrait plus une story que tout le monde voit. | 2026-08-10 |
| 18 | ~~**BUG — « Enregistrer » ne garde pas**~~ **CORRIGE le 2026-08-11 (v0.9.52)** — `saved_cards` est SUPPRIMEE. Une sauvegarde est desormais une copie locale en clair sur l'appareil : elle ne depend plus de son auteur, seule la moderation peut la retirer (revocation cooperative, faille acceptee). Effet de bord majeur : c'etait la derniere branche « acces illimite » de `can_view_card_file`, qui passe de SIX chemins (2026-08-10) a DEUX. *Texte d'origine ci-dessous.* **BUG — « Enregistrer » ne garde pas : `saved_cards` est en ON DELETE CASCADE** — ⚠️ **Correctif décidé le 2026-08-11, à livrer à l'étape 5 du chantier de refonte** : le volet 3 de Jay règle le problème autrement que par une copie serveur — une sauvegarde devient une **copie locale sur l'appareil**, sans espace serveur dédié, donc indépendante de la ligne de l'auteur. Révocation **coopérative** (le serveur demande, l'app supprime) ; faille du client modifié **connue et acceptée par Jay**. | Découvert le 2026-08-10. Si l'auteur supprime sa Vibe, **tous ceux qui l'ont enregistrée la perdent, sans avertissement**. Or « Enregistrer » promet de garder. **Correctif retenu et recommandé** : faire une VRAIE copie dans l'espace de celui qui sauvegarde, au lieu d'un accès perpétuel au fichier d'origine. C'est le seul cas où la duplication de fichier est le BUT et non un coût — et elle est bornée (16 sauvegardes sur 64 Vibes en base de dev). **Non fait**, à ouvrir avec Jay. | 2026-08-10 |
| 17 | ~~**ABANDONNÉ**~~ → **ROUVERT ET DÉCIDÉ le 2026-08-11 — séparer stories et bibliothèque publique par bucket** | ⚠️ **Le motif de l'abandon est tombé.** L'objection de Jay portait sur la **duplication de fichiers** ; sa refonte du 2026-08-11 (« 1 contenu = 1 format », choix **exclusif** au partage : story OU publication OU cercle OU bibliothèque de conversation) la supprime — un média n'existe plus que dans un seul contexte de diffusion, et le repartage est un **chemin** vers la source, jamais une copie. C'est le « nouvel élément » qu'exigeait cette entrée. **Chantier ouvert, séquencé en 6 étapes** (voir le rapport `2026-08-11_17-59.md`). *Texte d'origine ci-dessous.* Plan proposé le 2026-08-10, **retiré le jour même** après objection de Jay sur la duplication. Deux motifs : (1) ces objets sont **la même Vibe dans plusieurs états de publication**, pas des objets distincts — les séparer copierait des fichiers identiques dans plusieurs buckets, sur le poste le plus lourd de l'app (faces vidéo) ; (2) depuis le chiffrement de la v0.9.46, **les octets sont inertes partout** : ce qui décide est qui obtient la CLÉ, et cela se joue dans une seule fonction. Le contrôle n'est plus une question de stockage mais de politique de clé. **Ne pas rouvrir ce chantier sans nouvel élément.** Analyse complète : `docs/controle-de-la-distribution.md`. | 2026-08-10 |
| 16 | **PROD — `avatars` est un bucket PUBLIC** | Relevé le 2026-08-10 en cartographiant le stockage. Les photos de profil sont lisibles par **n'importe qui connaissant l'URL, sans compte**. C'est l'usage courant pour des avatars, mais NeoVibe est une app de cercle restreint : à réexaminer avant la prod. Détail dans `docs/stockage-et-acces.md`. | 2026-08-10 |
| 15 | **SÉCURITÉ — `can_view_card_file` ouvre un fichier par SIX chemins en OU** | Relevé le 2026-08-10. Le plus permissif gagne, toujours et en silence — c'est le motif exact qui rendait les bibliothèques éphémères vulnérables avant leur séparation. **Deux effets vérifiés, non corrigés** : (a) la branche « livraison » ne teste QUE `destroyed_at is null`, jamais le compteur de vues — celui-ci est appliqué par `mark_card_viewed`, une RPC que le client *choisit* d'appeler, donc un client modifié peut retélécharger une face après épuisement ; (b) une Vibe publiée en **story** échappe aux limites de sa livraison, `is_story_card` n'en tenant aucun compte. Cohérent avec « coûteux et visible, pas impossible », mais ce sont des effets de bord, pas des décisions. **Si les limites de vues doivent devenir une garantie réelle** : servir le fichier par une URL signée courte délivrée par une RPC qui décompte, comme la clé des bibliothèques. Cartographie complète et candidats à séparation dans `docs/stockage-et-acces.md`. | 2026-08-10 |
| 14 | **UX — le double flux Oneshot ne s'ouvre pas du premier coup** | Relevé par Jay au test de la v0.9.43, **à corriger plus tard, décision de Jay : on ne touche pas maintenant**. Le double live finit par s'ouvrir, mais il faut **swiper plusieurs fois entre Vibe et Oneshot** ; ce n'est pas instantané. Cause probable, non confirmée : `_onTypeChanged` n'applique le type qu'après un **délai de repos de 350 ms** (`_typeSettle`), et `_applyTypeToCamera` sort immédiatement si `type == _cameraType` — un aller-retour rapide sur le sélecteur peut donc laisser la caméra sur l'ancien type sans nouvelle tentative. Voir aussi le verrou **statique** `NativeCameraController.dualFailedThisSession`, qui interdit tout réessai jusqu'au redémarrage après un seul échec (y compris un timeout de 12 s). **Ne rien changer sans lire le journal caméra** d'un cas raté : ce sous-système est fragile, et « un réessai par session » est une décision motivée (un essai raté peut laisser le service caméra d'Android hors service). | 2026-08-10 |
| 13 | **CHANTIER — bibliothèques éphémères de conversation** | Ouvert par Jay le 2026-08-10. Prendre une vibe et l'ajouter à une **bibliothèque partagée** (groupe ou DM) au lieu de l'envoyer ; tout reste masqué pour **tous, auteur compris**, jusqu'au **reveal de 18h30**. Bibliothèque **souvenir** par défaut. **Spécification complète et arrêtée : `docs/bibliotheques-ephemeres.md`** — la lire avant de coder. Décisions déjà prises par Jay : renommage **Card → Vibe en interface seulement** (code et base gardent `card`) ; masquage par **placeholder réduit à 16-24 px**, préchargement **chiffré à 18h25**, clé à 18h30, animation de défloutage sur la vraie image ; **un seul instant de reveal** pour toute la conversation ; journée de collecte de 18h30 à 18h30 ; **tableau noir ET import galerie désactivés** dans ce mode ; annonce **nommée** dans le fil ; la bibliothèque **échappe à la règle One of One**. **5 points restent à trancher** — voir la fin de la spec. | 2026-08-10 |
| 12 | **IDÉE — émojis uniques de mention sur les Cards** | Donnée par Jay le 2026-08-10, **à ne pas implémenter maintenant** : un émoji unique servirait à signaler le **thème** d'une Card. Rien d'autre n'a été précisé (qui le choisit, s'il est imposé ou libre, où il s'affiche, s'il sert au filtrage). **À faire à l'ouverture du chantier** : redemander la spécification à Jay avant de coder, et la passer à la grille de décision de `CLAUDE.md` — un thème de Card n'ajoute de valeur que s'il sert la relation, pas s'il devient un système de tags qui pousse à publier. | 2026-08-10 |
| 7 | ~~**FPS double live — levier n°2 (rendu)**~~ **SANS OBJET (2026-08-01)** | ⚠️ Cette entrée visait à alléger le **rendu logiciel**, qui a été **supprimé** à l'étape 5d (v0.9.23). Le moteur GPU tient 30 i/s constants sur les deux caméras : le problème qu'elle décrit n'existe plus. Conservée pour la trace. *Texte d'origine :* Le levier n°1 (forçage de la plage capteur `CONTROL_AE_TARGET_FPS_RANGE`) est fait en v0.9.x. **Si insuffisant après mesure de Jay** : alléger le rendu logiciel — n'afficher qu'UNE face (saut du dessin de la face cachée côté natif, `renderEnabled` par caméra), la face cachée restant vivante pour la capture simultanée ; éventuellement supprimer l'aller-retour JPEG du pipeline de dessin. Ne pas égaler le natif (rendu GPU inaccessible en flux unique) est assumé. | 2026-07-24 |

## Décisions en attente

| # | Sujet | Détail | Depuis |
|---|-------|--------|--------|
| 27 | 🎨 **REFONTE UI GLOBALE — annoncée le 2026-08-13, vues attendues de Jay dans `docs/`** | « On va refaire l'UI globale de l'app […] il faut proposer à nos utilisateurs un **design premium**. Je vais te fournir des vues dans `docs/` à reproduire. » **Rien n'est commencé — ne rien entreprendre avant ses vues.** ⚠️ **Cadrage franc de Jay, à retenir** : « tu n'es pas encore capable de créer des rendus visuels de qualité designer comme j'ai besoin pour NeoVibe ». Le partage est donc : **l'authoring visuel lui revient, l'intégration et le système de mouvement me reviennent.** 💡 **Proposition faite et NON TRANCHÉE** : poser d'abord un **système de mouvement** — un jeu unique de 3-4 durées et 2-3 courbes appliqué à tous les écrans — avant de traiter les écrans un par un. C'est ce qui porte l'essentiel de l'effet « premium » (cohérence et retenue, pas décoration), c'est gratuit, et l'app en a déjà la preuve : la dissipation du flou au reveal, le retournement et l'inclinaison sont écrits à la main et sont bons. | 2026-08-13 |
| 26 | 🔧 **RIVE — ADOPTÉ par Jay (abonnement Cadet pris le 2026-08-13). Ce qu'il faut savoir AVANT d'y toucher** | **Décision** : Jay veut des animations **de designer ET interactives** ; Lottie a été écarté par lui pour ça (pur Dart et JSON éditable, mais aucune interactivité). Rive est le seul des trois à faire de l'interactif. **MCP et outils Rive à brancher** — Jay redémarre Claude Code pour ça. **① `.riv` ≠ éditable.** C'est un binaire de sortie : Rive y supprime tout ce qui ne sert pas au runtime. Le format éditable est le **`.rev`**. ⚠️ **SANS LE `.rev`, UNE ANIMATION EST FIGÉE POUR TOUJOURS** — le rappeler à Jay avant toute commande à un designer. **② Ce qui reste pilotable depuis le Dart** : entrées de state machine, **Data Binding / ViewModels** (couleurs, textes, nombres), text runs. Une couleur **liée à un ViewModel** suit la palette de l'app ; une couleur codée en dur est figée. C'est à exiger à l'authoring, pas rattrapable après. **③ Ce qu'il faut demander au designer** : garder les `.rev` · lier couleurs et textes à des ViewModels · une state machine par composant, entrées nommées **en anglais** · la **liste** des entrées (nom, type, sens) · éviter images matricielles et polices embarquées. **④ Taille — mesurée le 2026-08-13, le premier chiffre trompe** : APK gras 60,9 → 82,4 Mo (+35 %), mais c'est **trois** copies du moteur natif (arm64 7,3 · armv7 6,4 · x86_64 7,7 Mo, cette dernière pour émulateurs). **Un vrai téléphone paie +7,3 Mo.** En livrant par ABI : **29,3 Mo, Rive inclus** — moitié moins qu'aujourd'hui (voir #25). **⑤ Zéro réseau à l'exécution** : le natif est compilé dans l'APK, le `.riv` est un asset local. La dépendance réseau est **au build seulement**. **⑥ Reste à mesurer** : le **coût de rendu** sur une app caméra-first — seule inconnue, à chiffrer sur un pilote avant de généraliser. **⑦ Dette** : `rive_native` applique l'ancien plugin Kotlin, Flutter avertit que les futures versions refuseront de compiler. **⑧ CARTOGRAPHIE DES DRAPEAUX D'UNE TRANSITION (relevée le 2026-08-14, documentée nulle part)** : le MCP n'expose ni `enableExitTime` ni `isDisabled` comme propriétés — ce sont des **bits de la propriété `flags` (clé 152)**. Relevés un par un en écrivant puis en relisant le décodage de l'éditeur : **4 = enableExitTime · 8 = exitTimeIsPercentage · 16 = pauseOnExit · 32 = enableEarlyExit** (1 et 2 non testés, ce sont `isDisabled` et `durationIsPercentage` dans un ordre indéterminé). ⚠️ **Ne JAMAIS deviner un bit** : `isDisabled` est dans le même octet, et l'écrire par erreur désactive la transition **sans aucune erreur** — le composant continue de s'afficher, il ne réagit simplement plus. Toujours écrire puis relire `queryStateMachineLayer`, qui est le seul décodeur. Les durées (`duration` clé 158, `exitTime` clé 160) sont en **millisecondes**. | 2026-08-13 |
| 25 | 🛠️ **PIÈGE DE BUILD — `rive_native` exige `dart` DANS LE PATH, et notre toolchain est hors PATH** | Le build échoue sur un message qui ne dit rien : `Execution failed for task ':rive_native:runRiveNativeSetup' > Process 'command 'cmd'' finished with non-zero exit value 1`. **Cause réelle** (obtenue en rejouant la tâche Gradle seule avec `--stacktrace`) : `'dart' n'est pas reconnu en tant que commande interne ou externe`. La tâche Gradle appelle `dart` en supposant qu'il est dans le PATH ; le projet l'a **délibérément hors PATH** (`C:\Charles\dev\flutter`, RSUNA §1.2). **Correctif vérifié** : `PATH="/c/Charles/dev/flutter/bin:$PATH" flutter build apk --release`. ⚠️ **Retombera à l'identique sur la seconde machine.** 💡 **Trouvaille indépendante de Rive, à proposer à Jay** : livrer l'APK **par architecture** (`--split-per-abi`, prendre `app-arm64-v8a-release.apk` — le Xiaomi M2101K6G est arm64) ferait passer l'APK de test de **60,9 à ~22 Mo** (29,3 Mo avec Rive). Moitié moins à télécharger à chaque version. ✅ **TRANCHÉ par Jay le 2026-08-14** — « pour le format des builds, ne sers que l'APK dont j'ai besoin, cela me convient. » Les builds de test sont donc livrés en **arm64 seul**. ⚠️ Ne vaut que pour les APK de test : en production on inclut toutes les architectures (voir #17). 🔴 **PIÈGE MESURÉ le 2026-08-14 : `--target-platform android-arm64` ne suffit PAS.** Il restreint le **moteur Flutter**, mais les bibliothèques natives des **plugins** (`rive_native` en tête) restent empaquetées pour les trois architectures → **46 Mo au lieu de 31**. La seule commande correcte est **`flutter build apk --release --split-per-abi`**, puis livrer `app-arm64-v8a-release.apk`. | 2026-08-13 |
| 24 ter | ✅ **DÉCISION DE JAY (2026-08-13) — les bibliothèques ne seront PAS streamées : elles sont amenées d'avance** | « On télécharge les contenus de la bibliothèque 5 minutes avant le reveal […] **comme si les cards étaient toujours là mais qu'elles étaient juste bloquées en local.** » **Ne pas reproposer la lecture par intervalles sur ce chemin** : une vibe de bibliothèque est **connue d'avance** (annoncée dans le fil, heure de reveal écrite en base), donc il n'y a rien à deviner — contrairement à un feed. Quand on peut tout amener d'avance, streamer n'apporte rien. **Livré en v0.9.70** : `LibraryVaultCache` (scellés persistants sur disque, plafond 80 Mo LRU, balayage au démarrage, section dans Réglages → Stockage) ; `cacheFace()`/`prefetch()` préchargent **les deux faces** (le verso ne l'était pas du tout) ; `openRevealed()` lit le cache et demande la clé **en parallèle** ; le paramètre `sealedBytes` disparaît — il n'existait que pour compenser l'absence de cache. ⚠️ **La barrière des 5 min est SERVEUR** (politique du coffre : `now() >= reveal_at - interval '5 min'`), le cache n'en décide rien. ✅ **Sécurité inchangée, et mieux énoncée** : le scellé sur disque est inerte sans la clé, que le serveur retient jusqu'au reveal — le contenu est *présent mais verrouillé* au lieu d'être *absent parce qu'on refuse de le livrer*. | 2026-08-13 |
| 24 bis | ✅ **BIBLIOTHÈQUES DE CONVERSATIONS — alignées le 2026-08-13 (v0.9.69), sauf le streaming** | Ce qui est **fait** : scellement au format par blocs (`NVC1`) via `FaceDelivery` (donc `fastStart` compris), `openRevealed()` rend un `OpenedMedia` via `MediaOpen`, les deux écrans passent par `VibePhotoFace`/`VibeVideoFace`. **Plus aucun clair n'est écrit sur le disque** — la promesse « ce qui se passe sur NeoVibe reste sur NeoVibe » est tenue sur ce chemin. 🐛 **Bug corrigé au passage** : les deux écrans faisaient `Image.file` sur un `.mp4`, donc **une vibe vidéo en bibliothèque de conversation ne s'affichait pas** — jamais relevé faute d'instrumentation sur ce chemin. ⚠️ **Ce qui RESTE à faire** : (a) le scellé est encore **téléchargé en entier** — la lecture par intervalles réclame une URL signée sur `library_vault` et un cache dédié ; (b) ce chemin n'est **toujours pas instrumenté** (invisible dans les mesures). ⚠️ **Ce qui ne doit PAS changer** : `get_library_vibe_key` porte sa propre règle (le reveal) — le transport se mutualise, la politique de clé reste distincte. ⚠️ **Migration** : aucune n'a été nécessaire, la remise à zéro venait de vider la table — **si le format avait changé sans elle, toutes les anciennes vibes seraient devenues illisibles** (leur AES-GCM d'un seul bloc était un TROISIÈME format, que `MediaOpen` n'a jamais su lire). | 2026-08-13 |
| 24 | 🔧 **CHANTIER OUVERT — les bibliothèques de conversations sont un TROISIÈME chemin média, non aligné** | Angle mort relevé par Jay le 2026-08-13 (« on a oublié de parler des bibliothèques de conversations »), **confirmé dans le code et en base**. `library_vibes` / coffre `library_vault` / clé `get_library_vibe_key` : indépendant des deux autres chemins. Trois points, par ordre de gravité : **(a) `library_vibes_repository.openRevealed()` écrit le média déchiffré EN CLAIR dans un fichier temporaire** (`vibe_<id>.<ext>`, ligne 236) — précisément ce que le format par blocs et le lecteur natif ont été construits pour supprimer ; **(b)** il télécharge le **fichier entier** et déchiffre **en Dart**, donc aucun bénéfice du streaming par blocs, et il n'est **pas instrumenté** (invisible dans toutes les mesures) ; **(c)** il porte le nombre de fonctions rendant une clé à **QUATRE** (`open_card_media` décompte une vue · `open_content_media` ne fait plus rien · `library_media_keys` rien · `get_library_vibe_key` rien — les deux dernières **vérifiées en base** le 2026-08-13). ✅ **Bonne nouvelle vérifiée** : ouvrir une bibliothèque de conversation ne brûle **aucune** vue. ⚠️ **Découverte du nettoyage du 2026-08-13** : `library_vibes.card_id` est **nul** sur les lignes réelles — la table est une **racine de contenu autonome**, pas une vue sur `cards`. Un `delete from cards` ne l'emporte donc pas. ⚠️ **Ne PAS fusionner naïvement** (même mise en garde que la ligne 23) : le transport se mutualise, la **politique de clé doit rester distincte**. | 2026-08-13 |
| 23 | 🐛 **CORRIGÉ le 2026-08-13 (v0.9.68) — le lecteur vidéo était construit DEUX FOIS** | Les deux visionneuses choisissaient leur structure d'après l'arrivée du verso : `TiltableCard` tant qu'il chargeait, `FlippableCard` ensuite. À la même position de l'arbre, le widget changeait de **type** — Flutter démonte alors tout le sous-arbre : **le lecteur du recto était détruit et recréé au milieu de la lecture**. Conséquences : la vidéo redémarrait, un second décodeur était instancié, et la trace de mesure — encore ouverte — se voyait écraser son `· attente avant natif` par le **temps de téléchargement du verso** (2 873 ms, 3 316 ms, et 550 545 ms sur une session où le téléphone avait été posé). **Configuration déclenchante, banale** : recto **vidéo** (ne télécharge rien) + verso **photo** (téléchargé en entier) — le recto gagne toujours la course. **Correctif** : la structure ne dépend plus que de `hasBack`, constant ; le verso qui charge occupe sa place (`VibeFaceLoading`). ⚠️ **Règle à retenir** : *dans un `build`, ne jamais faire dépendre le TYPE d'un widget d'un état qui change en cours de vie* — c'est indiscernable d'un simple rafraîchissement au diff, et ça détruit l'état de tout le sous-arbre. | 2026-08-13 |
| 22 | 🐛 **CORRIGÉ le 2026-08-13 (v0.9.68) — la purge des stories supprimait le mauvais bout du graphe** | `stories.id` référence `contents.id` en `on delete cascade` : supprimer le **contenu** emporte la story, **l'inverse est faux**. Le cron faisait `delete from public.stories`, laissant derrière lui le `contents`, sa clé, ses autorisations et ses vues. Relevé en base le 2026-08-13 : **14 `contents` orphelins, 14 clés, 6 autorisations, 26 objets de coffre — pour zéro story vivante.** ⚠️ **Ce n'était pas une faille** (vérifié : `private.can_view_story_file` interroge `stories`, donc la ligne partie, aucune lecture n'est autorisée) **mais c'est la « sécurité qui s'énonce par une négation »** de la règle 4 : rien n'était lisible *parce que plus rien ne l'indexait*, et la **clé survivait au contenu**. Migration `story_purge_deletes_the_content_not_the_story` ; orphelins rattrapés. ⚠️ **Les octets, eux, restent dans le coffre** : `storage.objects` n'a aucune clé étrangère vers le contenu et la suppression directe y est refusée par un trigger Supabase. **À traiter avant la prod** — c'est une fuite de stockage sans borne (voir avant-prod). | 2026-08-13 |
| 21 | ✅ **PRÉCHARGEMENT — TRANCHÉ ET LIVRÉ le 2026-08-13 (v0.9.64), options 1 + 2b ; l'option 3 reste à faire** | **Décision de Jay**, après lecture de `docs/cours-livraison-des-medias.html` : (1) **on ne précharge PAS les Vibes en messagerie** — « cela ne sert à rien de toute façon » ; une ouverture y déclenche toujours une vue, et un léger délai y est accepté ; (2) **options 3 et 2b** pour le socle, avec cette précision de Jay : « les vues comptées séparément de l'envoi du fichier côté client — quand le client regarde disons 3 s le contenu, alors il envoie au serveur qu'il l'a vu ». **Livré en v0.9.64** : migration `content_view_recorded_on_display` (`open_content_media` n'enregistre plus, nouvelle RPC `record_content_view`), `ContentPreloader` (URL + clé + premier bloc, socle uniquement), `ContentViewReporter` (seuil 3 s), méthode native `prime` sur `neovibe/player`, branchement dans les visionneuses de stories et de publications. ⚠️ **L'option 3 — préparer un lecteur d'avance (−149 ms) — N'EST PAS FAITE** : c'est le dernier morceau, et le plus utile au feed. ⚠️ **Effet de bord positif** : il n'existe plus qu'**UN SEUL** point d'enregistrement de vue, là où `open_content_media` comptait et `library_media_keys` non — le journal de propagation **sous-comptait déjà** avant cette migration. ⚠️ **Contrepartie assumée** : l'enregistrement dépend désormais du client ; accepté car un contenu du socle **n'a aucun budget de vues**, donc aucune promesse d'auteur n'en dépend. | 2026-08-13 |
| 20 | 🛑 **PRÉCHARGER LA CLÉ CONSOMMERAIT UNE VUE — contrainte majeure de l'étape 4, découverte le 2026-08-13** | ⚠️ **À lire avant d'écrire la moindre ligne de préchargement.** Relevé **en base**, pas déduit : `open_card_media` **vérifie, décompte et rend la clé dans une seule transaction** (`update card_deliveries set view_count = view_count + 1 … return media_key`). C'est la garantie voulue par Jay le 2026-08-10 — « la limite de vues est une garantie, pas une convention » — et il n'existe **aucun autre chemin** vers la clé. **Conséquence** : précharger la clé d'une Vibe en DM **brûlerait une vue sur un contenu que l'utilisateur n'a pas regardé**. Le plan de la ligne 18 (« précharger la clé ») ne vaut donc **PAS** pour les Vibes en DM. **Et ce n'est pas gratuit non plus pour les stories/publications** : `open_content_media` appelle `private.record_view`, donc précharger y **inscrirait des vues fantômes** dans le journal de propagation — la donnée la plus sensible de l'app (ligne 7 des décisions en attente). **Ce qui reste préchargeable sans effet de bord** : le **premier bloc** (256 Ko) et l'**URL signée** — aucun des deux ne touche à un compteur. **Deux pistes si l'on veut aussi la clé** : (a) une RPC de préchargement qui rend la clé **sans décompter**, le décompte partant d'un second appel à l'affichage réel — mais cela **rouvre la faille** que la fusion des deux avait fermée (un client modifié appellerait la première et jamais la seconde) ; (b) ne précharger la clé **que** pour les contenus sans budget de vues (stories, publications) et en acceptant le point sur `record_view`. **Décision de Jay requise** — la piste (a) touche une décision verrouillée. | 2026-08-13 |
| 19 | **INSPECTEUR DE RÈGLES — outil de test livré le 2026-08-13 (v0.9.63)** | Demande de Jay : « des outils développeurs spécifiques permettant de voir pour une card ses règles et de comparer avec mes actions et ce que je vois ». Livré : **Développeur → Journaux et mesures → Règles des Vibes**. Pour chaque ouverture, trois colonnes — **ce que le serveur dit** (max_views, durée/face, saveable, scrubbable, chiffrée), **ce que l'écran a décidé** (limites applicables, phase, restant), **ce qui s'est passé** (temps réel par face, déroulé horodaté) — et un **verdict** : le décompte serveur relu avant/après doit valoir exactement **+1 par ouverture**. `0` = la limite n'est pas une garantie ; `2` = elle se consomme deux fois trop vite. Inclus dans « Tout copier ». Fichiers : `lib/core/diagnostics/card_rules_trace.dart`, `lib/features/settings/rules_inspector_screen.dart`. **À retirer avec la section Développeur** (ligne 4). | 2026-08-13 |
| 18 | 🎯 **CE QUE L'ÉTAPE 4 DOIT PRÉCHARGER — chiffré le 2026-08-13 (v0.9.61), instrument enfin fiable** | Relevé sur cache **complet** (7 mesures) : médiane **412 ms** pour une cible de 300. Décomposition : `média local` **174 ms**, `· décodage entête` **149 ms**, `· attente avant natif` 52 ms, `· construction lecteur` 6 ms, **`· ouverture média` 0 ms**. **Ce zéro est la preuve qui manquait** : sur un cache complet, aucun octet ne part sur le réseau — et pourtant on paie encore 174 ms d'URL signée (165 ms) ‖ clé (110 ms). **L'URL signée n'est jamais utilisée dans ce cas** et c'est la plus lente des deux. **Donc, à précharger, dans cet ordre** : (1) **la clé** — inévitable par sécurité pour le contenu d'autrui, mais préchargeable ; le lot `library_media_keys` le fait déjà en un appel pour une bibliothèque ; (2) **ne pas demander d'URL signée quand le cache est complet** (~55 ms, le delta URL−clé) ; (3) **le premier bloc** (256 Ko) du contenu suivant — c'est `· ouverture média`, 468 à 622 ms à froid. **Gain attendu** : cache complet 412 → ~238 ms, **sous la cible**. Reste ensuite `· décodage entête` (~149 ms), qui est du travail réel d'ExoPlayer. ⚠️ **Deux contenus atypiques à surveiller** : `551106d5…` décode son en-tête en **794 ms** (deux relevés concordants) ; un aller-retour réseau isolé à 744/872 ms. | 2026-08-13 |
| 17 | ⚠️ **LA MESURE COMPTAIT LE TEMPS DE RÉFLEXION DE L'UTILISATEUR — corrigé en v0.9.61, à connaître AVANT le préchargement** | Relevé du 2026-08-13 (v0.9.60) : deux faces **arrière** affichaient **11 339 ms** et **2 758 ms** d'ouverture, dont 10 712 et 2 154 ms d'« attente avant natif ». **Ce n'était pas de la latence** : `publication_viewer_screen.dart` et `story_viewer_screen.dart` demandent le **verso dès l'ouverture de l'écran**, alors qu'il ne s'affiche qu'au retournement. Le chronomètre tournait donc pendant que Jay regardait le recto. **L'app faisait exactement ce qu'il fallait** — elle préchargeait. **Effet sur les chiffres** : la médiane à froid tombe de **1 002 ms à ~853 ms** une fois les deux artefacts retirés, c'est-à-dire **sous la cible de 1 s**. ⚠️ **C'ÉTAIT BLOQUANT POUR L'ÉTAPE 4** : une fois le préchargement en place, *tout* contenu est préchargé — l'instrument aurait annoncé que le préchargement avait tout ralenti, alors qu'il aurait tout accéléré. **Correctif (v0.9.61)** : `VideoOpenTrace.prefetched` + `displayedAt`, et les résumés lisent `perceived` (l'attente ne court qu'à partir de l'affichage). Les écrans qui préchargent le **déclarent** — eux seuls savent ce qu'ils montrent. **Toute nouvelle source de préchargement devra appeler `VideoOpenTrace.markPrefetched`**, sinon elle réintroduira l'artefact. | 2026-08-13 |
| 16 | **CHIFFRÉ le 2026-08-13 : 757 ms des 1 000 ms à froid sont du RÉSEAU — et « préchauffer le lecteur » est ABANDONNÉ** | Relevé v0.9.59, seau **froid** (n=4) : médiane **1 000 ms**, dont `média local` 219 (URL signée 200 ‖ clé 106, en parallèle), `lecteur prêt` 780 — décomposé en **`· ouverture média` 538 ms**, `· construction lecteur` **7 ms**, `· décodage entête` 149 ms. **Conclusion 1 — hypothèse tuée, mais À MOITIÉ SEULEMENT** : créer un ExoPlayer neuf coûte **2 à 7 ms**. ⚠️ **Corrigé le 2026-08-13 après question de Jay** : ces 2-7 ms ne couvrent que la **construction de l'objet**, jusqu'au retour de `prepare()` — qui est **asynchrone**. Le vrai travail se fait après, et porte un autre nom dans la mesure : **`· décodage entête` = ~149 ms**, stable sur tous les relevés (analyse du conteneur MP4 + initialisation du décodeur). Donc : **construire un lecteur est gratuit, mais le PRÉPARER sur un média coûte ~149 ms — et ça, ça se prépare d'avance.** C'est ce que font les feeds type TikTok (deux ou trois lecteurs déjà préparés sur les contenus suivants). Après préchargement des octets, `· décodage entête` deviendrait le poste **dominant** (~149 ms sur ~207). **Le préchargement du feed porte donc sur TROIS choses : le premier bloc, la clé *(voir la contrainte de la ligne 20)*, et le lecteur préparé.** **Conclusion 2 — la cible est le réseau** : `· ouverture média` (538 ms) est la requête `Range` d'amorçage (en-tête + premier bloc de 256 Ko depuis le Storage Supabase). Avec les 219 ms d'URL+clé, **757 ms sur 1 000 sont du réseau**, c'est-à-dire exactement ce que le préchargement supprime (ligne 15). Le seul coût de traitement réel est `· décodage entête`, ~147 ms, stable en froid comme en partiel. ⚠️ **86 ms restaient inexpliquées** dans `lecteur prêt` (Riverpod → widget → contrôleur) : instrumenté en v0.9.60 sous `· attente avant natif`. | 2026-08-13 |
| 15 | **PRÉCHARGEMENT (étape 4) — CONFIRMÉ ESSENTIEL par Jay le 2026-08-13** | ⚠️ **Consigne de Jay, donnée en corrigeant Claude** : « je ne suis pas d'accord quand tu dis que l'étape 4 devient inutile […] le préchargement des contenus suivants sera très utile pour réduire encore plus le temps d'attente **surtout dans le cadre du feed** qu'on intégrera à l'avenir. Donc cela reste essentiel. » **Ne pas déprioriser cette étape.** L'argument qui l'avait fait déclarer inutile était **faux** : il s'appuyait sur le seau « complet », c'est-à-dire le seul qui, par construction, ne contient que du contenu **déjà téléchargé** — il ne pouvait pas montrer l'intérêt de précharger (biais de sélection). Dans un feed, le contenu suivant est **froid par définition**. **Deux choses sont à précharger, pas une** : les **octets** (le premier bloc suffit à afficher la première image) **et la clé** — la clé d'un contenu d'autrui exige un appel serveur à chaque ouverture, c'est une propriété de sécurité voulue, et c'est elle qui plafonne le gain sinon. Le lot `library_media_keys` existe déjà et fait ça en un appel pour une bibliothèque. | 2026-08-13 |
| 14 | **MESURE D'OUVERTURE VIDÉO — chiffres reçus le 2026-08-13, et pourquoi ils ne concluaient pas** | Relevé de Jay : **en cache** médiane 761 ms sur 6 (cible 300), meilleure 348, **pire 16 108 ms** ; **à froid** médiane 1 637 ms sur 5 (cible 1 000), meilleure 701, pire 3 411. ⚠️ **Le premier chiffre était illisible** : le seau « déjà sur l'appareil » était rempli par `hasOthers()`, qui répondait vrai dès que le fichier de cache existait — or `RemoteChunkStore` donne au fichier sa **taille définitive dès le premier bloc** et l'index est posé **à l'ouverture**. Une vidéo vue deux secondes comptait donc comme étant en cache, alors que ses blocs restaient à télécharger : les 16 s en sont la trace, aucune lecture réellement locale ne pouvant les atteindre. **Corrigé en v0.9.58** : le classement vient du **natif** (`SealedChunkStore.availability`), en **trois** états — complet / partiel / froid — et l'écran affiche les **médianes par étape**. **Le relevé est donc à refaire**, et c'est lui qui décidera du préchargement (étape 4). ⚠️ **Ne pas comparer les nouveaux chiffres aux anciens** : ils ne portent pas sur les mêmes populations. **Deuxième relevé (v0.9.58), seau « complet » seul** : médiane **422 ms** sur 9 (cible 300), meilleure 306, pire 1745 ; média local 139, clé 0, **lecteur prêt 335**, première image 1. **Le coût est à 80 % dans le LECTEUR** — ni réseau ni déchiffrement, ce seau n'a que des octets déjà présents. Ouvert en trois sous-mesures en v0.9.59 (`· ouverture média` / `· construction lecteur` / `· décodage entête`) pour savoir si c'est l'instanciation du **décodeur matériel** — auquel cas le levier est de **réutiliser ou préparer un lecteur d'avance**, et non de précharger des octets. ⚠️ **Le `clé : 0 ms` de ce relevé n'est PAS une mesure** : les deux jalons ayant été rendus simultanés, 139 ms est le `max(URL, clé)` et 0 en est la conséquence mécanique. Corrigé en v0.9.59 (chronométrage séparé). **Pour obtenir les seaux froid et partiel : Réglages → Stockage → « Vider le stockage des stories et publications »** — le cache est sur l'appareil, inutile de toucher à la base. | 2026-08-13 |
| 13 | ~~🔔 **À RÉCLAMER DÈS LA REPRISE — les chiffres de l'écran de mesure vidéo**~~ **RÉCLAMÉS ET REÇUS le 2026-08-13** — voir la ligne 14 ci-dessus. *Texte d'origine ci-dessous.* | **Jay a demandé qu'on le lui rappelle** en clôturant la session du 2026-08-12. Il doit ouvrir **Réglages → Développeur → « Lecture vidéo — temps d'ouverture »** et donner **deux nombres** : la médiane **en cache** et la médiane **à froid** (icône nuage). ⚠️ **Ne pas engager les étapes 4 (préchargement) et 5 (rendu allégé) avant de les avoir** : sa validation du 2026-08-12 est **visuelle**, et portait peut-être sur du contenu déjà en cache — auquel cas elle ne dit rien du démarrage à froid. Ces deux nombres tranchent entre « on s'arrête là » et « il reste du travail ». Cible : **300 ms en cache, 1 s à froid**. La reprise se fait **sur les tests** (règles des cards et des Vibes, système de vues), pas sur du nouveau code. | 2026-08-12 |
| 12 | ~~**LECTURE VIDÉO — quel levier prendre**~~ **TRANCHÉ le 2026-08-12 : option 3, LECTURE NATIVE DIRECTE** | **Décision de Jay** : « même si cela contraint de coder une version pour chaque OS, la priorité c'est la sécurité et le confort des utilisateurs ». Livré côté Android le jour même : `SealedChunkReader.kt` (format `NVC1` en accès aléatoire, AES-GCM matériel), `SealedDataSource.kt` (source de données media3) et `NativePlayer.kt` (ExoPlayer sur une texture Flutter), pilotés par `SealedVideoController` côté Dart. **Trois pièces supprimées** : le serveur HTTP local, son jeton, et l'exception `cleartext` du manifeste. ⚠️ **Ce que la décision engage pour la suite** : (a) le format a désormais **deux implémentations** (Dart pour sceller, Kotlin pour ouvrir) et en aura **trois** avec iOS — seuls les **vecteurs de test croisés** (`docs/format-media-scelle.md` §6) les empêchent de diverger, une divergence étant invisible au diff comme à la compilation ; (b) **le portage iOS devra écrire `AVAssetResourceLoaderDelegate` + CryptoKit** et rejouer ces mêmes vecteurs — c'est inscrit au catalogue natif §7 ; (c) le format hérité n'est **pas** porté en natif, il garde son chemin Dart. **Non encore validé au test de Jay.** *Texte d'origine ci-dessous.* | Jay au test de la v0.9.55 (2026-08-12) : « la vidéo fonctionne mais c'est tellement saccadé et très mauvais côté UX/UI que ce n'est pas suffisant pour la prod ». **Cause mesurée** (banc `test/chunked_seal_bench_test.dart`) : l'AES-GCM de `package:cryptography` est en **Dart pur** — **2,7 Mo/s sur un PC de bureau**, donc moins sur un téléphone — et il s'exécute sur **l'isolate qui dessine l'écran**, pendant que le lecteur réclame presque tout le fichier d'un coup (il prend jusqu'à 50 s d'avance). Le format et le serveur local sont **hors de cause** (22 tests). **Cours, schéma, simulateur et comparaison des solutions : `docs/simulateur-video.html`** — à rouvrir avant de coder. **Quatre options, aucune engagée** : (1) AES natif via `cryptography_flutter` **2.3.4** — vérifié compatible, gain ×50 à ×100, couvre iOS ; (2) déchiffrer sur un **isolate de fond** — traite le saccadement précisément, ne change pas le débit ; (3) **lecture native directe** (source de données Kotlin + `AVAssetResourceLoader` iOS) — supprime le serveur HTTP local, le jeton ET l'exception cleartext, mais ouvre du natif sur deux OS ; (4) passer en **AES-CTR** — **déconseillé**, on perdrait l'authentification (détection d'un octet modifié). **Recommandation donnée : 1 + 2 ensemble d'abord, 3 en chantier séparé seulement si la mesure sur l'appareil ne suffit pas.** ⚠️ **La vitesse AES du téléphone n'a jamais été mesurée sur l'appareil** — à faire avant de trancher entre 1+2 et 3. | 2026-08-12 |
| 10 | ~~**TÉLÉCHARGEMENT PROGRESSIF**~~ **LIVRÉ le 2026-08-12 pour les stories, publications et le futur feed — PAS ENCORE pour les Vibes en DM** | Une vidéo d'autrui n'est plus téléchargée avant d'être montrée : le lecteur natif ne réclame que les blocs qu'il traverse (`SealedChunkStore.kt`), et le cache est **partiel**. Une fois tous ses blocs arrivés, ce cache **est** le fichier scellé d'origine — un seul fichier, un seul format, pas deux caches aux règles différentes. Vérifié par `PartialStreamingTest.kt`, qui mesure **les octets ayant réellement voyagé** (démarrage = 1 requête ; un saut = 1 bloc). ⚠️ **Trois points restent ouverts** : (a) le chemin des **Vibes en DM** (`card_media_cache.dart`) télécharge toujours en entier — c'est le prochain morceau, et c'est le second exemplaire du chemin de téléchargement, à unifier avec le premier ; (b) **l'URL signée expire à une heure** et n'est pas renouvelée en cours de lecture ; (c) le **préchargement** des contenus suivants (étape 4 du plan) n'est pas fait — c'est lui qui transformera « rapide » en « instantané ». *Texte d'origine ci-dessous.* | Le format par blocs est livré (v0.9.54) : une vidéo **déjà en cache** démarre instantanément, mémoire bornée à 256 Ko, plus aucun clair sur le disque. **Mais une vidéo vue pour la PREMIÈRE fois attend encore son téléchargement complet.** Volontairement sorti du lot pour ne pas empiler deux refontes. Ce qu'il reste à faire : télécharger par intervalles (`Range` sur l'URL signée, que Supabase Storage sait servir) et garder un cache **partiel**. ⚠️ **Réécrit le 2026-08-12 après le passage à la lecture native** : il n'y a plus de serveur local à faire patienter — c'est **`SealedChunkReader`, côté Kotlin, qui devra attendre l'arrivée des blocs manquants**, et son équivalent iOS ensuite. Le travail change de langue et de fichier, et il faudra le faire **deux fois** ; il reste la seule suite technique faisable sans décision de Jay, mais il n'est plus « court ». | 2026-08-12 |
| 11 | **DEUX FORMATS DE MÉDIA COEXISTENT — sans migration, volontairement** | Depuis la v0.9.54, les médias sont scellés **par blocs**. Les contenus créés avant restent au format **bloc unique** et sont reconnus à la magie du fichier par `MediaOpen` — **le seul endroit de l'app qui sait qu'il y en a deux**. Aucune migration : les stories héritées meurent à 24 h, les publications se recréent. ⚠️ **Le format hérité garde son défaut** : une vidéo ancienne écrit encore son clair dans le répertoire temporaire pendant le visionnage. Le jour où plus aucun contenu ancien ne circule, `MediaSeal` et la branche héritée de `MediaOpen` peuvent disparaître — **à vérifier avant la prod**. ⚠️ **Renforcé le 2026-08-12** : le lecteur natif **n'implémente délibérément pas** le format hérité (décision consignée dans `docs/format-media-scelle.md` §4). Une vidéo ancienne passe donc par un chemin qui n'a plus aucun autre usage — c'est maintenant la **seule** raison d'existence du clair temporaire sur le disque. Sa suppression n'a jamais été aussi propre à faire. | 2026-08-12 |
| 9 | **PORTÉE DU BLOCAGE — à étendre, décision de Jay attendue** | Le blocage livré le 2026-08-12 (v0.9.53) coupe **la visibilité des contenus du socle** (stories, publications), **les relais** (dans les deux sens, y compris ceux reçus avant le blocage) et **les partages contournants** (on ne peut pas relayer vers quelqu'un que l'auteur a bloqué). **Ce qu'il ne coupe PAS encore, faute d'arbitrage** : la connexion elle-même, les conversations existantes, les Vibes envoyées en DM, la visibilité du profil, et les croisements BLE. Un blocage qui laisse la conversation ouverte est probablement incomplet du point de vue de l'utilisateur — **à trancher avec Jay** avant la prod. | 2026-08-12 |
| 8 | ~~**DURÉE DE VIE D'UNE PUBLICATION**~~ **TRANCHÉ le 2026-08-11 : PERMANENTE** | Réponse de Jay : « la publication en bibliothèque est permanente, c'est ce qui a toujours été décidé ». La table des publications ne portera donc **pas** de colonne d'expiration (étape 2 de la refonte). ⚠️ **Conséquence à ressortir avant la prod** : combinée à la viralité **sans limite de sauts** validée le même jour, une publication permanente retire toute borne au stockage — c'est exactement le déclencheur économique décrit à la ligne 11 des points de prod, et il est désormais **actif**, sans attendre le feed local. | 2026-08-11 |
| 7 | **DURÉE DE CONSERVATION DU JOURNAL DE PROPAGATION — non tranchée** | `contents`, `content_grants` et `content_views` (créées le 2026-08-11) **survivent volontairement au contenu** : une story meurt à 24 h, son graphe reste. C'est la décision de Jay (« cela contredit l'éphémère mais garantit une sécurité »). **Mais aucune durée de conservation n'a été fixée** — en l'état ces lignes s'accumulent indéfiniment. Deux raisons de trancher : le volume (une ligne par spectateur et par partage, sur du contenu viral sans limite de sauts), et le fait qu'un enregistrement permanent de qui a vu quoi est la donnée la plus sensible de l'app. À décider avec Jay, et à ressortir avant la prod. | 2026-08-11 |
| 6 | **STORIES EN DECK — spécification de Jay, 3 questions ouvertes avant de coder** | **Décidé par Jay** (2026-08-02, après le test de la v0.9.41) : le recto/verso ne convient pas aux stories. Nouveau format = **dérivé de la Card, sans retournement**, photos et vidéos présentées en **deck / éventail**. **Mécanique donnée** : swipe gauche = passer · swipe droite = liker · input en bas pour **réagir en DM lié à la story** (amis uniquement) · **clic sur le 1er quart gauche** = story précédente · clic à droite = story suivante, puis auteur suivant si c'était la dernière. **Trois questions posées à Jay, SANS RÉPONSE à ce jour — ne pas coder avant** : (1) **le verso des Cards existantes** : deux cartes dans le deck, recto seul, ou choix de l'auteur ? (2) **deck ou éventail** : une pile séquentielle ou des cartes étalées où l'on pioche ? (défaut proposé si pas d'arbitrage : pile avec 2-3 cartes qui dépassent) ; (3) **le like** : gardé tel quel, remplacé par la réaction, ou les deux ? **Deux réserves signalées à Jay, à trancher par lui** : (a) **le like ne passe pas la grille de décision de `CLAUDE.md`** — ni présence physique, ni valeur ajoutée à une relation ; c'est l'échange « trop facile » que le produit combat. Alternative proposée : le swipe droite ouvre la **réaction** au lieu d'envoyer un like ; si un signal léger est gardé, le rendre **nominatif et privé**, jamais un compteur public. (b) **conflit de gestes** : swipe gauche (passer) et clic gauche (précédent) sont deux actions OPPOSÉES du même côté, séparées par la seule distance du doigt ; et swipe gauche fait doublon avec clic droite, ce qui ne laisse aucun geste pour « auteur suivant ». Contre-proposition consignée dans le rapport `2026-08-02_14-36.md`. **Idée additionnelle non tranchée** : en ping, la réaction d'un croisé partirait en **wave** au lieu d'un DM — la story devient alors la porte d'entrée du croisement physique vers le cercle d'amis (coche les deux axes de la grille). | 2026-08-02 |
| 2 | **Flash en Oneshot** | Le Oneshot n'a **aucun** outil (consigne Jay 2026-07-26 : « la caméra pure ») — le flash y aura ses propres règles, **Jay détaillera**. ~~Le flash en frontale~~ **TRANCHÉ le 2026-07-26** : c'est un **flash écran** (lueur blanche à beige sur le contour, deux curseurs chaleur/intensité, manuel uniquement), livré en v0.9.33. | 2026-07-26 |
| 4 | **Outils de prise en BeReal** | La grille, le retardateur et le HD sont **exclus du BeReal pour le moment** (consigne Jay 2026-07-26) : « j'ai d'autres projets pour le BeReal et je ne sais pas si cela sera compatible avec ces fonctionnalités. Je te l'expliquerai plus tard. » **À ressortir quand Jay ouvrira le chantier BeReal.** **Complété le 2026-07-31** — principe donné par Jay : « le BeReal est **obligatoirement une photo prise avec les caméras du téléphone et non modifiée** ». Le bouton couleur en a été retiré (v0.9.35). Mais Jay a explicitement **limité le changement à ce seul bouton** : la **vidéo 15 s** reste possible en BeReal, et le bouton « retoucher » du récap y reste actif — alors que les deux contredisent la lettre du principe. **À trancher au chantier BeReal**, pas avant. | 2026-07-26 |
| 3 | ~~**Bouton couleur en BeReal**~~ **TRANCHÉ le 2026-07-26, FAIT en v0.9.35** | Jay : « pour le BeReal il n'y aura **pas** de bouton couleur, cela va de soi. » Retiré du code le 2026-07-31 (`card_capture_screen.dart`). Motif consigné : le BeReal est obligatoirement une photo prise avec les caméras du téléphone. | 2026-07-26 |
| 5 | **Flash frontal — profil du bandeau, en attente du screen de Snap** | Jay (2026-07-31) : « sur Snap le bandeau lumineux n'est pas de la même épaisseur sur tout l'écran ; en bas il commence épais sur environ 1/8 de l'écran, constant, et l'épaississement commence à partir de là. Si tu veux je peux t'envoyer un screen. » Implémenté en v0.9.35 d'après cette description seule : plancher du bandeau bas à 1/8 de la hauteur ÉCLAIRÉE (mesuré à 12,3 % hors Flutter), les trois autres bords inchangés. **Le screen reste à fournir** pour caler l'épaisseur exacte et vérifier si le bas doit rester proportionnellement plus épais à fort réglage (aujourd'hui il rejoint les autres bords à mi-course). **Deuxième réglage à valider** : la correction d'exposition du capteur est à la **moitié** du maximum de l'appareil (~+1 EV) ; à monter ou descendre selon le rendu. | 2026-07-31 |
| 1 | **Reprise caméra après éviction par le système** | Vu dans le journal du 2026-07-25 (`erreur caméra 3` = `ERROR_CAMERA_DISABLED`, les deux caméras d'un coup, après ~60 s de double flux) : Android retire la caméra à l'app (veille, arrière-plan, autre app). Jay pense être passé sur autre chose entre-temps. L'app s'en remet, mais **le comportement au retour au premier plan n'est pas décidé** : rouvrir le double flux automatiquement, ou revenir au flux simple et laisser relancer ? À trancher avec Jay avant la prod — et à traiter prudemment (une reprise caméra mal faite casse le service caméra d'Android). | 2026-07-25 |

## Bugs connus, à corriger

| # | Sujet | Détail | Depuis |
|---|-------|--------|--------|
| 3 | ~~**Lecture vidéo saccadée**~~ **CORRIGÉ et VALIDÉ AU TEST le 2026-08-12 (v0.9.57)** — Jay : « ça fonctionne, la vidéo arrive instantanément, c'est très correct ». Validation **visuelle** : les chiffres de Réglages → Développeur → Lecture vidéo n'ont pas encore été relevés, et c'est eux qui diront si la cible (300 ms en cache, 1 s à froid) est réellement tenue. *Texte d'origine ci-dessous.* | Constaté par Jay au test de la v0.9.55 : la vidéo joue de bout en bout, mais l'interface saccade fortement. Cause : le déchiffrement AES-GCM tournait en Dart sur l'isolate qui dessine l'écran. **Correctif livré** : lecture native directe (décision de Jay, décisions en attente #12) — le déchiffrement est passé en Kotlin, sur les fils du lecteur. ⚠️ **Reste un bug ouvert tant que Jay ne l'a pas constaté sur son appareil** : la vitesse AES du téléphone n'a **jamais** été mesurée, et le banc de mesure ne tourne que sur PC. | 2026-08-12 |
| 2 | ~~**Aperçu à l'envers pendant la bascule de caméra**~~ **CORRIGÉ en v0.9.35, VALIDÉ au test le 2026-08-01** | En Mono et en Oneshot séquentiel, la bascule avant/arrière montrait brièvement l'image **renversée**, puis un sursaut de correction. Entre l'arrière (capteur 90°) et l'avant (270°) il y a 180° d'écart. **Cause confirmée** (l'hypothèse notée le 2026-07-25 était la bonne) : `previewInfo` arrive à la CONFIGURATION de session, donc AVANT les premières images ; la nouvelle rotation était appliquée aux **dernières images de la caméra précédente**, encore dans la texture. C'est pourquoi la tentative de la v0.9.24 (attendre `previewInfo`) n'avait rien changé. **Correctif** : un vrai signal de première image, `previewReady`, produit par un `CaptureCallback` greffé sur la session via `Camera2Interop` (3 images comptées) ; le voile de bascule ne se lève qu'à ce moment. Le délai de confort de 160 ms devient inutile (ramené à 40 ms de garde-fou). **Réponse à la question de Jay (« c'est 0 degré normalement »)** : non — le capteur livre des images en paysage, une rotation est physiquement nécessaire ; la seule question est QUI l'applique. Le moteur GPU la fait dans le shader et annonce bien `rotation = 0` : **c'est le chantier GPU qui supprimera le problème par construction**, ce correctif ne fait que le rendre invisible d'ici là. | 2026-07-25 |
| 1 | ~~**Changement de mode pendant la prise**~~ **CORRIGÉ en v0.9.1** | Jay avait pu créer une **Mono à deux faces** en changeant de mode pendant le traitement d'une capture Oneshot. Corrigé par trois barrières : type figé au déclenchement (`_lockedType`), sélecteur inerte et grisé pendant la prise, et vérification de cohérence faces/type avant le récap. Capture accélérée dans la foulée (écriture des deux faces en parallèle + un seul encodage ; recadrage/mise au format en natif au lieu d'un encodage PNG en Dart). **À revérifier au test** : durées affichées dans le journal caméra. | 2026-07-14 |

## Décisions verrouillées à ne PAS reproposer

- **Pression du doigt** comme déclencheur vidéo : **abandonnée** (2026-07-13).
  L'appui maintenu suffit.
- **Streaming zéro-écriture** pour l'éphémère : évalué et rejeté (MVP).
