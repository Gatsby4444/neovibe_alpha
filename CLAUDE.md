# neovibe_alpha

Nouveau départ du projet neovibe. Ce dépôt vient d'être initialisé — il n'y a pas encore de code.

## État actuel (2026-07-11)

- Dépôt git initialisé localement, pas encore de repo GitHub.
- Backend : projet Supabase **neovibe_alpha** (`dvixmhvqqjvbrpsckmyi`, région eu-west-1), connecté via MCP (`.mcp.json`).
- Stack applicative (frontend/framework/langage) : pas encore décidée.

## Supabase

- Le serveur MCP Supabase est configuré dans `.mcp.json` (type http, `https://mcp.supabase.com/mcp`).
- Utiliser les outils `mcp__supabase__*` pour lister les tables, appliquer des migrations, consulter les logs/advisors avant toute modification de schéma.
- Toujours privilégier le développement/test local avant d'appliquer des changements sur le projet distant.

## À compléter au fur et à mesure

- Stack technique choisie et structure des dossiers.
- Commandes de build/dev/test une fois le projet démarré.
- Conventions de code spécifiques au projet.
