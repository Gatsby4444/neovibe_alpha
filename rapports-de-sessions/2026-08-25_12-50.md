# Session du 2026-08-25 (12h50) — Rangement du dépôt et build à tester

## Contexte

Jay demande trois choses :
1. ranger le dépôt et sortir **un build à tester** ;
2. confirmation sur les **versions d'Android mises de côté** ;
3. confirmation sur les **changements de base de données** (migrations) qui
   devaient être faits.

C'est le point 0 de `RSUNA.md` §2.4 — le seul point bloquant : la refonte de la
proximité du 2026-08-20 n'a **jamais tourné sur un téléphone**.

## Modifications apportées

*(complété au fil de la session)*

## Réponses aux deux questions de Jay — relevées, pas supposées

### ① Versions d'Android mises de côté — OUI, et c'est une décision

`android/app/build.gradle.kts:42` → **`minSdk = 31`** (Android 12).
Décision de Jay du 2026-08-20, consignée en `RAPPELS.md` **#57**.

**Android 10 et 11 sont donc hors périmètre.** Motif : sur ces versions, un scan
BLE exige `ACCESS_FINE_LOCATION`, et le système ne considère l'app « au premier
plan » pour la localisation que si son service de premier plan a le type
`location` — le nôtre est `connectedDevice`. Interface fermée, `onScanResult`
n'aurait rien remonté, **sans erreur ni trace**. L'alternative revenait à
demander « Autoriser la localisation tout le temps », l'invite la plus dissuasive
d'Android, sur une app dont la thèse est la confiance. **On a supprimé la cause
plutôt que de la garder.**

⚠️ **Conséquence directe sur le test à deux appareils** : le second téléphone
doit tourner sous **Android 12 minimum**, sinon l'APK ne s'installera pas. À
vérifier avant le test.

### ② Changements de base de données — DÉJÀ FAITS le 2026-08-20, et vérifiés ce jour

Vérification faite **sur les objets réels en base**, pas sur les fichiers de
migration (mémoire `feedback-verify-db-not-migration-files`), projet de dev
`dvixmhvqqjvbrpsckmyi`, via le PAT :

| Objet attendu | Relevé en base |
|---|---|
| table `sightings` | **présente** |
| table `encounters` | présente |
| fonction `report_sightings` | **présente** |
| fonction `purge_sightings` | **présente** |
| job cron `neovibe_purge_sightings` | **présent** (à côté de `neovibe_purge` et `neovibe_purge_library`) |
| colonne `device_keys.x25519_pub` | **présente** |
| colonnes `broadcast_key`, `broadcast_key_prev`, `rotated_at` | **absentes** (bien supprimées) |

Les deux migrations `20260820120000_pairwise_secret` et
`20260820180000_mutual_sightings` sont donc **appliquées**. **Rien à rejouer.**

⚠️ **Piège relevé au passage** : `supabase_migrations.schema_migrations`
s'arrête à `20260820012709` et **ne contient ni l'une ni l'autre**. La table
d'historique n'est donc **pas** une preuve d'application ici — les migrations
passées par l'API Management n'y sont pas inscrites. Seul l'inventaire des
objets répond. *Ne pas conclure « migration manquante » sur la foi de cette
table.*

## Difficultés rencontrées ou erreurs commises

*(complété au fil de la session)*

## Notes et consignes de Jay

- « Range le dépôt et sors-moi un build à tester. »

---

## Résultat de la session

### Dépôt rangé

| Commit | Contenu |
|---|---|
| `b0c4ea5` | `RAPPELS.md` #58 (accès Supabase par le PAT) + rapport de vérification machine du 12h10 |
| `8044b3a` | `pubspec.yaml` → `0.9.122+212` + ce rapport |

`ba8b1ca` (« Bascule de machine terminée »), qui restait local, est poussé dans
la foulée. **`master` est aligné sur `origin/master`, working tree propre.**

### Build livré

**v0.9.122** — https://github.com/Gatsby4444/neovibe_alpha/releases/tag/v0.9.122

- `app-release.apk`, **89 880 515 octets** (85,7 Mo), état `uploaded` — vérifié
  par `gh release view`, pas supposé depuis le code de sortie de `gh`.
- **Aucun changement de code** depuis la v0.9.121 : même socle, republié pour le
  premier test à deux appareils. Le seul diff est le numéro de version.

### Contrôles avant livraison

| Contrôle | Résultat |
|---|---|
| `dart format` | 178 fichiers, **0 modifié** |
| `flutter analyze` | **No issues found** (10,4 s) |
| `flutter test` | **188 tests, tous passent** |
| `flutter build apk --release` | **succès**, 85,7 Mo, 49,6 s |

*Tests Kotlin non rejoués : aucun fichier `.kt` touché depuis la passe du
12h10 (26 tests, 0 échec, avec `--rerun-tasks` après suppression de
`test-results`).*

## Difficultés rencontrées ou erreurs commises

- **`supabase_migrations.schema_migrations` n'est pas une preuve d'application.**
  Elle s'arrête à `20260820012709` et ne contient aucune des deux migrations du
  2026-08-20 — pourtant tous leurs objets sont bien en base. Les migrations
  passées par l'API Management ne s'y inscrivent pas. *Leçon : conclure
  « migration manquante » sur la foi de cette table aurait mené à les rejouer,
  donc à un `drop column` sur des colonnes déjà supprimées et à une table
  `sightings` recréée. Seul l'inventaire des objets répond.* Application directe
  de `feedback-verify-db-not-migration-files`.

## Ce qui reste ouvert (rappelé à Jay)

1. **Le test à deux appareils** — le seul point bloquant. Les deux téléphones
   **ensemble** (protocole v3), et le second doit être en **Android 12 minimum**.
2. **Le checkup dissociation acquisition / usage** (`RAPPELS.md` #52) — commandé
   par Jay, jamais fait.
3. **Le relevé de mesures vidéo** — en attente depuis la v0.9.67, cinq versions.
4. **`--split-per-abi` non encore proposé** : ~22 Mo au lieu de 85,7 pour un
   appareil arm64. Mesuré le 2026-08-13. La forme de la livraison appartient à
   Jay — proposé, pas appliqué.

---

# Deuxième partie — retour d'Android 10 et 11 (`minSdk` 31 → 29)

## La question de Jay

> « Pourrait-on coder un système qui détecte la version d'Android et, si c'est
> inférieur à Android 12, alors on demande cette permission ? Sinon on n'embête
> pas l'utilisateur avec. Possible ? Est-ce que le nouveau système BLE peut
> fonctionner sur des versions antérieures à Android 12, même si on demande des
> permissions supplémentaires ? »

## Ce que la question a fait apparaître — `RAPPELS.md` #57 était faux sur un point

#57 affirmait que les alternatives à `minSdk = 31` revenaient à demander
**« Autoriser la localisation tout le temps »**, l'invite la plus dissuasive
d'Android. **C'est faux.**

#57 confondait deux choses distinctes :

| | Ce qu'il faut | Coût pour l'utilisateur |
|---|---|---|
| **La permission** | `ACCESS_FINE_LOCATION` en « pendant l'utilisation » | une invite ordinaire |
| **Le type du service** | `location` sur le service de premier plan | **aucun** — c'est du manifeste |

Le vrai manquant du 2026-08-20 était le **second**, pas le premier. Un service
de premier plan de type `location` fait compter l'app comme « au premier plan »
pour la localisation : la permission « pendant l'utilisation » suffit alors, y
compris interface fermée et écran éteint.

`ACCESS_BACKGROUND_LOCATION` ne sert qu'à **démarrer** un scan sans interface.
**Relevé, pas supposé** : sur le manifeste fusionné, le seul receveur
`BOOT_COMPLETED` est celui du plugin `flutter_foreground_task`, et il est
**désarmé par `autoRunOnBoot: false`** (`lib/main.dart:120`) ; `ProximityService`
n'est lancé que depuis l'écran Ping.

## L'appareil de test existait déjà, et la mémoire du projet le savait

Jay ne se souvenait plus de la version de la tablette de Mimi. Les rapports
répondent, et par une **mesure** :

- `rapports-de-sessions/2026-08-16_12-23.md:425` — *« [le rapport serveur] a livré
  "Android 10" et donc la cause du défaut 6 »*.
- Même rapport, ligne 384, colonne « preuve » : **« tablette passée de 0 à
  1 395 annonces NeoVibe »** une fois la localisation allumée.

Donc : le chemin pré-12 **a déjà tourné** sur cet appareil, et le défaut 6 de
cette session disait déjà *« Android ≤ 11 exige le SERVICE de localisation
allumé, pas seulement la permission »*.

## Décision de Jay

- **Plancher à 29** (Android 10+), pas 26. Android 8/9 ont un troisième modèle
  de permission (COARSE suffit ; les types de service de premier plan n'existent
  pas avant l'API 29) — une branche de plus que la tablette Android 10 ne
  pourrait **pas** exercer. *On ne code pas un chemin qu'on ne peut pas
  éprouver : c'est exactement ce qui a produit le défaut du 2026-08-20.*

## Modifications apportées

| Fichier | Quoi |
|---|---|
| `android/app/build.gradle.kts` | `minSdk` 31 → **29**, avec l'historique des trois valeurs et le motif de chacune |
| `android/app/src/main/AndroidManifest.xml` | `BLUETOOTH`, `BLUETOOTH_ADMIN`, `ACCESS_FINE_LOCATION` rétablies en **`maxSdkVersion="30"`** ; `FOREGROUND_SERVICE_LOCATION` ajoutée (non bornée) ; `ProximityService` en **`connectedDevice|location`** |
| `ble/RadioStatus.kt` | `LocationOff` rétabli + sérialisé ; `BlePermissions.required()` à deux branches ; contrôle de localisation dans `evaluateRadio` ; `isLocationEnabled` rendue `internal` (**un seul endroit répond**, deux appelants) |
| `ble/ProximityService.kt` | `foregroundTypeUsed` — le type `location` **seulement** sous API 31 ; texte de notification pour `LocationOff` ; **trois sondes** ajoutées à `stats()` |
| `ble/ProximityBridge.kt` | canal `openLocationSettings` rétabli |
| `net/radio_status.dart` | `RadioLocationOff` + cas `locationOff` |
| `net/ble_radio.dart` | `openLocationSettings()` |
| `ping_screen.dart` | bandeau + action ; **la demande de permission suit ce que le NATIF déclare manquant** |
| `proximity_diagnostic_screen.dart` | ligne `locationOff` |
| `docs/parties-natives-par-os.md` | section proximité réécrite (règle du catalogue natif) |
| `RAPPELS.md` | #57 **renversé** (texte d'origine conservé en #57 bis), **#59** ajoutée |

### Deux décisions d'implémentation qui méritent d'être dites

**① La demande de permission côté Dart ne teste PAS la version.** Le natif
renvoie déjà `RadioPermissionsMissing(missing)` avec la liste exacte calculée
par `BlePermissions.required()` ; le Dart ne fait que **traduire** ces noms en
permissions `permission_handler`. Écrire un second test de version côté Dart
aurait été décider une deuxième fois de ce qu'Android exige, à un endroit qui ne
le sait pas — et le jour où les deux ne diraient plus la même chose, **rien ne le
signalerait** : la demande porterait sur une permission, le blocage sur une
autre. C'est le défaut A2 du diagnostic, déjà corrigé en 2026-07.

**② Les trois prérequis pré-12 sont exposés au diagnostic** — `needsLocation`,
`fgsLocationType`, `locationEnabled` dans `stats()`. Aucun des trois ne lève
d'erreur quand il manque : `startScan` réussit et ne rend jamais rien, donc un
`neoScans` à zéro est indiscernable de « personne autour ». Ces trois lignes
disent **laquelle** des trois manque.

## Difficultés rencontrées ou erreurs commises

- **J'ai d'abord écrit « nous n'avons aucun receveur `BOOT_COMPLETED` »** dans
  deux commentaires, sur la foi de notre seul manifeste. Le manifeste **fusionné**
  en contient un, apporté par `flutter_foreground_task`. La conclusion tenait
  (il est désarmé par `autoRunOnBoot: false`), **mais la justification était
  fausse** — et c'est précisément le piège que #57 documentait : *« un plugin
  peut réintroduire une permission à la fusion »*. Les deux commentaires ont été
  corrigés pour dire **ce qui a été constaté, où, et à quelle date**.
- **`gradlew -q` a masqué un échec de compilation.** La sortie ne montrait que
  des avertissements de plugins ; aucun XML de résultats n'a été produit. C'est
  l'absence de résultats — pas la sortie — qui l'a trahi. *Ne pas conclure d'un
  `gradlew -q` silencieux : vérifier que les fichiers de résultats existent.*
- **Le compilateur Kotlin a trouvé le nœud que j'allais oublier** : le `when` du
  texte de notification n'était plus exhaustif sans `LocationOff`. Un `when`
  exhaustif sur une `sealed class` est un contrôle réel, pas une formalité.
- **Les XML de tests Kotlin ne sont pas sous `android/app/build/`** mais sous
  `build/app/` (Flutter redirige le dossier de build). Chercher au mauvais
  endroit donne « aucun test » — indiscernable de « les tests n'ont pas tourné ».

## Contrôles

| Contrôle | Résultat |
|---|---|
| `dart format` | 0 modifié |
| `flutter analyze` | **No issues found** |
| `flutter test` | **188 tests, tous passent** |
| `gradlew :app:testDebugUnitTest --rerun-tasks` | **26 tests, 0 échec** (4+8+3+11), 228 tâches exécutées |
| `flutter build apk --release` | **succès**, 85,8 Mo |

### Le manifeste fusionné *release* — le seul contrôle qui prouve quelque chose

| Élément | Relevé |
|---|---|
| `minSdkVersion` | **29** |
| `BLUETOOTH_SCAN` | `usesPermissionFlags="neverForLocation"` |
| `BLUETOOTH_ADVERTISE`, `BLUETOOTH_CONNECT` | présentes |
| `BLUETOOTH`, `BLUETOOTH_ADMIN` | `maxSdkVersion="30"` |
| `ACCESS_FINE_LOCATION` | `maxSdkVersion="30"` |
| `ACCESS_BACKGROUND_LOCATION` | **ABSENTE** |
| `FOREGROUND_SERVICE_LOCATION` | présente (non bornée — voir `RAPPELS.md` #59) |
| `ProximityService` | `connectedDevice|location` |

Les trois permissions pré-12 sont bornées à l'API 30, donc **inexistantes sur
Android 12+** : la propriété obtenue le 2026-08-20 est intacte.

🔴 **Rien de tout ceci n'est validé sur appareil.** À éprouver sur la tablette
Android 10.
