# Session du 2026-08-29, 20:48 — reprise après extinction brutale du PC

## Pourquoi cette session

Le PC s'est éteint en pleine session précédente (rapport
`2026-08-29_11-33.md`), sans clôture. Première tâche : **constater l'état réel**
du dépôt et de la release, à la source — pas dans le rapport de la session
d'avant.

## État constaté (vérifié, pas déduit)

| Point vérifié | Comment | Résultat |
|---|---|---|
| arbre de travail | `git status` | **propre**, aucun fichier en cours, aucun stash |
| commits en attente | `git log origin/master..HEAD` | **aucun** — tout est poussé |
| dernier commit | `git log` | `9a1b707` — v0.9.154, **20:34:28** locale |
| tags v0.9.153 / v0.9.154 | `git ls-remote --tags origin` | **présents sur GitHub** (absents en local, jamais fetchés) |
| releases | `gh release list` | **v0.9.154 publiée à 18:35 UTC = 20:35 locale**, marquée `Latest` |
| APK | `build/app/outputs/.../app-release.apk` | daté du **29/08 20:34**, cohérent |
| version déclarée | `pubspec.yaml` | `0.9.154+3028` |
| chaîne d'outils | `flutter analyze` | **0 problème** en 14,2 s — Flutter/JDK toujours en place |

**Conclusion : rien n'a été perdu.** L'extinction est survenue ~14 minutes après
la publication de la release. Le dernier temps de la session précédente
(v0.9.154, la course sur les rappels d'annonce) était **terminé, committé, poussé
et publié**. Il n'y a aucun travail à reprendre ni à refaire.

⚠️ Seule anomalie cosmétique : les tags `v0.9.153` et `v0.9.154` ont été créés
**par GitHub** (via `gh release`), donc le dépôt local ne les connaît pas tant
qu'on n'a pas fait `git fetch --tags`. Ce n'est pas une perte.

## Ce qui reste ouvert (repris du rapport de 11:33)

1. **Le test de nuit de la v0.9.154 n'a pas encore eu lieu.** C'est le test qui
   doit lire ensemble les deux lignes neuves du diagnostic :
   - `advertDataRefus` > 0 → la pile Bluetooth a **refusé** l'écriture ;
   - `advertDataRefus` = 0 **et** `advertSlotDrift` > 0 → l'appareil a **dormi**
     (`uptimeMillis` ne compte pas en sommeil profond) — cause **non corrigée**,
     arbitrage batterie qui appartient à Jay, consignée en `RAPPELS.md` #94 ;
   - `advertRappelsPerimes` > 0 → la course corrigée en v0.9.154 existait bien.
2. **Le chemin « natif sans Dart » n'a toujours jamais tourné en vrai** :
   `resumedFromDisk` valait `false` des deux côtés à chaque relevé. Android n'a
   encore jamais tué le processus pendant un test.
3. **Le test d'éloignement** (celui qui doit constater la fin d'une présence)
   reste à faire.

## Modifications apportées

Aucune modification de code. Session de reprise et de constat.

## Notes et consignes de Jay

- 20:48 — « l'ordinateur s'est éteint en pleine session, qu'on n'a pas clôturée
  ni rien, tu peux reprendre proprement et retrouver où on en était ? »

## Deuxième temps — « y a-t-il autre chose d'impératif ? » (relevé, pas de code)

Question de Jay : peut-on quitter le ping et attaquer autre chose ?

### Relevé fait dans `RAPPELS.md` et dans le code

- **Rien n'est impératif MAINTENANT** hors des trois tests que Jay va mener
  lui-même (nuit → le presque → distance en inconnus). Aucun d'eux ne demande de
  code.
- **Impératif AVANT LA PROD seulement** : #6 (anti-capture à réactiver), #15
  (onglet Jeux vide interdit en prod), #14 (bots de test), #19 (outils de test +
  une permission), #18 (SMTP), #17 (AAB), #59 (`FOREGROUND_SERVICE_LOCATION` à
  déclarer chez Google), #58 (le PAT Supabase expire vers le **2026-09-17**).
- 🔴 **Un seul point ouvert qui ne touche pas au ping et qui vaut mieux qu'une
  dette de prod** : `RAPPELS.md` #70 — la politique `requests_insert_sender`
  autorise encore l'insertion **directe** dans `connection_requests`. La
  garantie « on ne demande en ami que quelqu'un qu'on a rencontré » est devenue
  une règle serveur ; cette porte-là la contourne. Aucun code de l'app ne s'en
  sert, mais c'est la **barrière fondatrice du produit**.
- 🟡 #88 — l'audit du 2026-08-28 **n'a couvert que le ping**. Caméra, Vibes,
  stories, chat, bibliothèque, livraison, réglages : non relus.

### Mode événement / soirée — constat

⚠️ **Ce chantier n'existait dans AUCUN document** (`docs/`, `CLAUDE.md`,
`RAPPELS.md` — vérifié par recherche le 2026-08-29). Il vivait uniquement dans
les échanges. **Consigné en `RAPPELS.md` #99**, avec les mots de Jay.

Point technique vérifié pendant ce relevé, dans
`lib/features/proximity/net/proximity_sync.dart` : **le ping ne demande jamais
« qui sont mes amis »**. Il lit `device_keys` et prend tout ce que le serveur lui
rend ; c'est la politique RLS `device_keys_friends` qui restreint aux
`connections` en `status = 'full'`. **Le joint pour brancher une soirée est donc
côté serveur** — élargir la règle, sans toucher au ping.

🔴 Conséquence à ne pas rater le jour venu : tout ce qui entre au carnet est
aujourd'hui **présenté comme un ami**. Élargir la source sans introduire le
libellé du lien ferait passer des inconnus pour des amis.

## Modifications apportées

- `RAPPELS.md` : entrée **#99** (mode événement / soirée + états de relation).
- Aucun changement de code.

## Troisième temps — la « porte ouverte » #70, REPRODUITE en base

Question de Jay : *« est-ce que c'est une faille de sécurité ? Je ne vois pas où
est le problème puisque le serveur vérifie que chaque appareil a détecté l'autre
avant de certifier la connexion. »*

**Sa prémisse est vraie — mais seulement pour la porte d'entrée officielle.**
Vérifié en base, définitions vivantes, le 2026-08-29 :

| Ce qui a été relevé | Résultat |
|---|---|
| `accept_connection_request` (définition vivante) | ne teste que : la demande existe · je suis le destinataire · elle est `pending` et non expirée. **Aucune vérification de proximité.** |
| `requests_insert_sender` (politique vivante) | `sender_id = auth.uid()` **et** pas déjà connectés. **Rien d'autre.** |
| `connections` — politiques d'écriture | **aucune** `insert`, **aucune** `update` → la table d'amitié ne s'écrit que par la fonction |
| `private.can_view_profile` | accorde la vue du profil **dès qu'une demande existe** entre les deux |

**Reproduction** (règle 7 — pas de conclusion sur déduction) : sous l'identité de
Malik, rôle `authenticated`, `request.jwt.claims` posé, vers Sofia — **0
connexion, 0 croisement jamais enregistré**. L'insertion directe dans
`connection_requests` **réussit**. Essai annulé par `rollback`, vérifié : aucune
ligne laissée en base.

**Conclusion calibrée** : ce n'est pas une fuite de données ni une prise de
compte. C'est **la règle fondatrice du produit qui se contourne** — la preuve de
rencontre est portée par la seule fonction `request_connection_from_proximity`,
et qui ne l'emprunte pas ne la subit pas. Le destinataire doit toujours accepter.

`RAPPELS.md` #70 mis à jour avec ces relevés.

## Quatrième temps — BUG CORRIGÉ : l'onglet de démarrage ne s'appliquait pas

### Le défaut, décrit par Jay (reproduit sur téléphone ET tablette)

Réglage « Démarrage » mis sur **Profil**. Au lancement : l'écran montre le
**Cercle**, mais la barre de navigation est sur **Profil**. Un glissement vers la
gauche mène à **Jeux** (et non Ping) ; un glissement vers la droite ne fait
**rien**, comme si on était déjà au bout.

### La cause, trouvée dans le code

`HomeShell` porte **deux** valeurs, et c'est normal : la bascule s'anime, donc
la barre désigne déjà la destination pendant que l'écran montre encore l'origine.

| Valeur | Ce qu'elle pilote |
|---|---|
| `_index` | `NavigationBar.selectedIndex` **et** le point de départ des glissements |
| `_shownIndex` | `IndexedStack.index`, donc l'écran réellement peint |

🔴 **`_shownIndex` n'était mis à jour QUE par le tic de l'animation**
(`_onSwitchTick`). Or `initState` lisait la préférence de façon **asynchrone** et
posait `_index` seul, par `setState`, **sans lancer d'animation**. Rien ne
rattrapait donc l'affichage.

Les trois symptômes en découlent, exactement : la barre montrait `_index` (4,
Profil), l'écran `_shownIndex` (2, Cercle) ; le glissement partait de 4 → 3
(Jeux) ; et 4 étant le dernier onglet, la droite était bloquée.

### Le correctif — la cause supprimée, pas le symptôme rattrapé

**Deux niveaux, parce qu'un seul aurait laissé le piège en place :**

1. **`SectionCursor`** *(nouveau, pur — `lib/features/home/section_cursor.dart`)*.
   Il n'y a plus deux champs libres qu'on peut écrire séparément, mais **trois
   gestes nommés** : `poser` (sans animation, les deux bougent), `viser` (la
   barre part, l'écran suivra), `relayer` (l'écran rattrape). `_shownIndex`
   devient un **getter** : plus aucun code ne peut le désaccorder.
2. **La préférence est lue AVANT le premier rendu.** `main()` attend désormais
   `SharedPreferences.getInstance()` et pose la valeur dans
   `startupTabAtLaunchProvider` (nouveau, sans valeur par défaut : un oubli
   d'override **lève** au lieu d'ouvrir silencieusement le Cercle). `initState`
   n'a plus aucune lecture asynchrone — l'app ne saute plus d'onglet après coup.

⚠️ **Deux providers voisins, deux questions différentes**, et il ne faut pas les
confondre : `startupTabProvider` dit *« quel onglet est réglé, à l'instant »*
(l'écran des Réglages doit le voir changer) ; `startupTabAtLaunchProvider` dit
*« où l'app devait s'ouvrir »* — figé pour la session. S'abonner au premier
ferait sauter l'app d'onglet **pendant que Jay règle son démarrage**.

**Gain de côté** : `_visited` part maintenant sur l'onglet de démarrage. Le
Cercle n'est plus monté au lancement quand on démarre ailleurs — ses chargements
réseau ne partent plus pour rien.

### Vérifications

- `flutter analyze` : 0 problème · `dart format` appliqué
- **252 tests Dart** au vert (246 avant, **+6**)
- **Contre-test** : ligne `_affiche = section` retirée de `poser` → **2 tests
  tombent**, dont celui nommé d'après le bug. La ligne est vivante.

## Cinquième temps — direction visuelle (aucun code)

Jay : *« on ne ressent pas assez la vibe, les interfaces sont fades, l'appli n'a
pas d'identité visuelle propre. »*

**Constat vérifié dans le code** : aucune police n'est déclarée (ni dans
`pubspec.yaml`, ni dans `theme.dart`) — l'app tourne sur Roboto, la police par
défaut d'Android.

Cinq directions typographiques proposées, chacune posée sur les mêmes trois
fragments d'interface (en-tête du Cercle, tuiles Ping, stories en cartes) et sur
les couleurs existantes de NeoVibe :
https://claude.ai/code/artifact/fa3df4ac-3908-46c5-879d-e5e1bf57a0de

**Choix de Jay : direction C, « le rond »** — Fredoka (titres) + Figtree
(textes), *« il faut aérer pour ne pas faire trop enfantin »*.

Toutes les décisions de cet échange sont consignées en **`RAPPELS.md` #100**
(fond dégradé retiré, PP rondes / stories cartes, grille Ping 2 colonnes,
mention spéciale, grille Cercle façon Apple Watch, ordre paliers-d'abord, et la
vérification qui écarte le conflit de glissement).

**En attente** : Jay envoie des images d'inspiration avant qu'on code quoi que
ce soit.

## Cinquième temps — LA DIRECTION ARTISTIQUE (v0.9.155)

### Réponses de Jay aux quatre questions bloquantes

| Question | Réponse |
|---|---|
| Montée des paliers | **automatique** — l'app constate |
| Combien / réversible | **2 paliers au-dessus d'ami, et ça redescend** |
| Ce qu'ils débloquent | stories réservées · droit d'envoyer un Rush · présence en direct |
| Reveal vs BeReal | **deux choses distinctes** |

Et sa consigne cadre : *« ne discute pas ce point, ton travail n'est pas de me
ralentir dans l'ajout de fonctionnalités mais de les intégrer de la meilleure
des manières pour l'UX parfaite. »* Rush est ajouté tel quel ; les profils
croisés (Suivre / Score / Booster) sont écartés, le suivi sans interaction est
réservé aux **comptes créateurs**.

### Le constat de départ, vérifié dans le code

⚠️ **L'app ne déclarait AUCUNE police.** Ni `pubspec.yaml`, ni `theme.dart` :
elle tournait sur Roboto, la police par défaut d'Android. C'est la première
cause du « fade », et aucun fond coloré ne pouvait la compenser.

### Ce qui a été livré

**A. Les couleurs deviennent des DONNÉES** — `lib/core/palette.dart` (nouveau).

`theme.dart` portait à la fois les couleurs et leur usage : hexadécimaux en dur
et `if (isDark)` répété vingt fois. Il n'y a désormais **qu'un seul thème**,
construit depuis une `NeoPalette`. Ajouter une identité = ajouter une palette ;
`theme.dart` ne bouge plus.

**Cinq identités** (`NeoIdentity`) : `sombre`, `clair`, **`aurore`** (nouveau
défaut — magenta/cyan/jaune sur blanc froid), **`sable`** (beige, blanc cassé,
bruns), et `cycle` (l'ancien fond dégradé horaire, **conservé en option** comme
demandé). Aurore et Sable ont chacune une palette **jour et nuit**.

⚠️ La règle « gris strictement neutres » du 2026-08-14 ne vaut plus que pour
`sombre` et `clair` — Jay l'a rouverte pour les deux nouvelles identités.

**B. La typographie** — `lib/core/typography.dart` (nouveau) + polices
**embarquées** dans `assets/fonts/` (Fredoka, Figtree ; licences OFL au dépôt).

Frontière tenue : **Fredoka aux titres de section uniquement**, Figtree partout
ailleurs — y compris les `title*` de Material, qui servent aux LIGNES de liste.
C'est cette frontière qui tient le risque « enfantin », pas le choix de police.
Plus `NeoSpace` (échelle d'espacement) et `NeoRadius` (dosage des arrondis).

**C. Le dégradé de marque appartient à la palette**, plus à l'app. Un dégradé
jaune-orange-rose-violet posé sur Sable aurait juré. Les 12 appels à
`NeoGradients` ont été repris ; la classe est supprimée.

**D. Plus aucun éclair de thème au lancement.** L'identité était lue en
asynchrone après le premier rendu — même mécanisme que le bug de l'onglet de
démarrage corrigé ce matin. Elle est désormais résolue dans `main()`.

**E. Les surfaces toujours sombres** (caméra, capture, visionneuses) prennent
`context.darkPalette` : elles ont besoin d'un accent lisible sur du noir **même
quand l'app est en clair**.

### 🔴 Le test de contraste a trouvé QUATRE défauts, dont deux antérieurs

`test/palette_test.dart` mesure chaque palette (WCAG). Il a fait tomber :

| Palette | Défaut | Correction |
|---|---|---|
| aurore (jour) | bordure de champ sous 3:1 sur le remplissage | #97A2B4 → #6E7A8D |
| sable (jour) | idem | #9C8B76 → #7E6C57 |
| **clair** | 🔴 **antérieur** : bordure à 2,7:1 sur le remplissage. Le commentaire affirmait « 3,4:1 sur blanc » — vrai, et **mesuré contre la surface la plus favorable des deux** | #8A8A8A → #757575 |
| **sombre** | 🔴 **antérieur** : blanc sur le rose d'action à 3,9:1, sous le seuil du texte | #E1306C → #D62976 (le rose central du dégradé, déjà utilisé en clair) |

⚠️ **Aucun seuil n'a été abaissé.** Un test qu'on assouplit pour qu'il passe ne
mesure plus rien.

### Vérifications

- `flutter analyze` : 0 problème · `dart format` appliqué
- **298 tests Dart** au vert (252 avant, **+46**)
- **Contre-test** : `extensions: [NeoPaletteTheme…]` retiré du thème → **10
  tests tombent**. Sans lui, `context.palette` retombe **en silence** sur la
  palette sombre et une identité claire rendrait des couleurs sombres.

## Sixième temps — LES PALIERS D'AMITIÉ (v0.9.156)

### L'architecture, en trois étages qui ne se mélangent pas

| Étage | Ce qui y vit | Ce qu'il ne fait JAMAIS |
|---|---|---|
| **Acquisition** | `meeting_days` — un jour où deux amis se sont croisés | décider de quoi que ce soit |
| **Dérivation** | `days_met` → `tier_for_days`, rangé sur `connections.tier` | connaître un écran |
| **Usage** | politiques RLS, écrans | recompter, ou connaître un seuil |

⚠️ **Pourquoi RANGER le résultat plutôt que le recalculer** : le compte porte
sur des JOURS, il ne peut donc changer qu'à un changement de jour ou à une
nouvelle rencontre. La tâche quotidienne n'est pas un cache qui approxime,
c'est l'évaluation **exacte** à chaque pas de la fenêtre.

### 🔴 Une décision de `RAPPELS.md` a été REJOUÉE, pas déroulée

#86 prévoyait de loger les paliers dans le type `connection_status`. **Sa
prémisse a changé** : on ignorait le 2026-08-28 qu'ils seraient automatiques et
réversibles. `status` dit « le lien existe » et ne bouge qu'à l'acceptation ;
`tier` dit « à quel point » et bouge tout seul. Deux règles, deux durées de vie,
donc **deux colonnes** — sinon `has_any_connection`, qui ne teste que
l'existence d'une ligne, donnerait les droits du palier le plus haut à tout le
monde, en silence.

### ⚠️ `meeting_days` n'est pas `encounters`, et ne doit jamais le remplacer

`encounters` dit « croisés **récemment** » et vit 24 h — c'est lui qui ouvre le
profil d'un inconnu croisé. `meeting_days` dit « croisés **le jour J** » et vit
indéfiniment, et **n'accorde aucun droit**. Y brancher une lecture
transformerait une fenêtre de 24 h en fenêtre éternelle, sans qu'aucune erreur
ne le dise.

### La série : le second usage de la MÊME acquisition

`tier_days` est borné par la fenêtre de 30 jours (donc réversible : c'est le
palier). La **série** est la longueur de la suite en cours, tolérance 2 jours,
et peut atteindre 100. Les confondre aurait plafonné la série à 30.

Elle est **entièrement recalculable depuis les faits** — donc elle ne peut pas
dériver si une tâche planifiée saute un tour, contrairement à un compteur qu'on
incrémenterait.

### Ce qui est branché

- **Stories réservées** : `stories.min_tier`, appliqué dans `story_audience` —
  **sur tous les chemins d'accès**, y compris le relais par `content_grants`.
  Poser le palier sur la seule audience ordinaire aurait laissé la porte du
  relais ouverte (règle 3 : compter les chemins).
- **Le délai du presque suit le palier** : inséparable = tout de suite,
  proche = 15 min, ami = 45 min. L'interrupteur global l'emporte quand il est
  allumé.
- 🟠 **Le droit d'envoyer un Rush attend que le Rush existe.**

### 🔴 Vérifié EN BASE, pas déduit

Quatre cas rejoués sous identité, transaction annulée :

| Cas | Résultat |
|---|---|
| story `close`, vue par un ami simple | **false** |
| même story, après montée au palier `close` | **true** |
| même story, vue par un inconnu | **false** |
| story sans réserve, ami simple | **true** |

Et `my_friendships()` appelée sous l'identité de Charles rend bien ses 5 amis,
avec 1 jour de croisement pour mimi et « encore 4 jours » avant `Proche`.

### Vérifications

- `flutter analyze` : 0 problème · `dart format` appliqué
- **314 tests Dart** au vert (298 avant, **+16**)
- Cron `neovibe_tiers` créé, **actif**, `11 3 * * *` — son propre job, pas greffé
  sur `neovibe_purge`
- **Contre-test** : le repli de `FriendshipTier.fromKey` mis sur `inner` au lieu
  de `friend` → **2 tests tombent**. Le sens de ce repli est une règle de
  sécurité, pas une commodité.

### Consignes et notes

- Jay, 2026-08-30 : *« garde tous les thèmes, cela fera aussi partie de l'esprit
  NeoVibe de pouvoir choisir son thème »* — le choix de thème devient un trait
  du produit, pas un réglage.
- Jay sur Aurore : *« très joli, épuré et en même temps coloré, une vibe sobre
  et cool qui annonce quelque chose de magique »*.
- ⚠️ **Deux points à faire valider par Jay**, consignés en `RAPPELS.md` #101
  et #102 : les libellés « Ami / Proche / Inséparable » (que j'ai posés seul),
  et l'interprétation de « la présence en direct » après la correction de ma
  propre question.

## Septième temps — LA CONSTELLATION + les règles écrites (v0.9.157)

### Les règles de palier, écrites pour Jay

Demande de Jay le 2026-08-30 : *« ajoute les règles de palier dans le guide
utilisateur et aussi ailleurs, de sorte que je puisse les relire précisément »*.

- **`docs/paliers-d-amitie.md`** *(nouveau)* — la référence précise : les quatre
  chiffres, où chacun se change, quand le calcul se fait, ce que ça débloque, ce
  qu'un palier n'est pas, et ce qui reste à décider par Jay.
- **`docs/guide-utilisateur.md`** — nouvelle section « 5 bis. Les paliers
  d'amitié », en langage utilisateur. La section « Les presque » est corrigée
  (le délai dépend désormais du palier) et le tableau des durées de vie
  distingue le **croisement** (24 h) du **jour de croisement** (gardé).

### Ce que Jay a dit sur les noms

*« Oui il faudra les changer mais je n'ai pas encore d'idée, peut-être un badge
d'amitié à la place de mots, et peut-être rajouter deux paliers intermédiaires ;
les règles je te demanderai de les modifier. »* → consigné en `RAPPELS.md` #101,
**avec le piège technique** : l'ordre de déclaration du type `friendship_tier`
EST l'ordre de comparaison en base, donc un palier intermédiaire s'insère par
`add value … before …`, jamais en fin de liste. Côté app, le compilateur
protège : les couleurs d'anneau sont un `switch` exhaustif.

### La constellation

`lib/features/circle/constellation_screen.dart` — tous les amis en nid
d'abeille, façon écran d'accueil d'Apple Watch. Accessible depuis le Cercle.

- **Écran poussé**, donc aucun conflit avec le glissement de navigation.
- Les pastilles **rétrécissent vers les bords** — l'effet signature de la ruche.
- ⚠️ **Elles rétrécissent pour de vrai, pas par `Transform.scale`.** Une mise à
  l'échelle laisse la boîte de clic à sa taille d'origine : on aurait eu deux
  boîtes, et un appui à côté d'une petite pastille aurait ouvert le profil du
  voisin. Ici la TAILLE est calculée, donc les deux boîtes n'en font qu'une.
- Le pseudo **s'efface avant de devenir illisible**, pas quand il l'est déjà.
- Filtres par palier, et anneau de palier sur chaque pastille.

### 🔴 Le glissement diagonal ne peut PAS exister ici

Jay proposait un glissement diagonal pour fermer, avec une brisure d'écran.
**Il n'est pas là, et ce n'est pas un oubli** : le conflit de gestes qu'on
venait d'éviter en poussant l'écran **se reforme à l'intérieur**. Une grille
qu'on traîne au doigt prend TOUS les glissements, diagonale comprise — c'est sa
raison d'être. Un second lecteur du même geste laisserait l'arène des gestes
décider à notre place.

Il redeviendrait possible sur une grille **fixe**, ou en le confinant à un bord
de l'écran. Les deux sont des décisions de Jay.

### Vérifications

- `flutter analyze` : 0 problème · `dart format` appliqué
- **319 tests Dart** au vert (314 avant, **+5**)
- La géométrie du nid d'abeille est **sortie du widget** et testée : un
  chevauchement ne lève aucune erreur, il fait juste disparaître un ami sous un
  autre — invisible à l'œil au-delà de quelques amis.
- **Contre-test** : pavage cassé d'un demi-pas → **3 tests tombent**.
