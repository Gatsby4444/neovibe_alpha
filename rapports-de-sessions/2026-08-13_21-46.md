# Rapport de session — 2026-08-13 21:46

## Contexte à la reprise

Relu en ouverture : `CLAUDE.md`, `RSUNA.md` (parties 2.3 à 2.5), le rapport
`2026-08-13_14-10.md` (clôture de la session précédente), `RAPPELS.md`.

**Pas de bascule de machine** : même poste (`C:\charles\neovibe_alpha`, profil
`Utilisateur`, mémoire présente). La procédure RSUNA partie 1 ne s'applique pas.

### Vérifications d'environnement

| Point | État |
|---|---|
| `pubspec.yaml` | **0.9.67+157** |
| Arbre de travail | propre |
| Branche | `master`, alignée sur `origin/master` |
| Tags locaux | jusqu'à `v0.9.67` |
| `lib/core/config/env.dart` | présent |
| Paquet de transfert | `C:\charles\neovibe-transfert\` présent (zip + mémoire) |

---

# Relevé de la v0.9.67 — diagnostic

Relevé fourni par Jay le 2026-08-13 à 21:48, Xiaomi M2101K6G, Android 13,
app 0.9.67+157. **Partiellement en 4G** (précision de Jay).

Constat de Jay : *« il manque toujours des données […] les résultats ne sont pas
concluants, il a dû y avoir un bug »*. Les deux sont exacts, et le diagnostic
ci-dessous ne repose pas sur une déduction : chaque cause a été relevée dans le
code ou en base.

## Les chiffres bruts

| Seau | n | Médiane |
|---|---|---|
| Complet sur l'appareil | **0** | — |
| Amorcé par le préchargement | **0** | — |
| Partiel | 1 | 1 007 ms |
| À froid | 6 | **3 459 ms** (pire **551 297 ms**) |

## Cause n°1 — le lecteur est construit DEUX FOIS, et la mesure compte le second

**C'est la cause principale, et ce n'est pas qu'un défaut de mesure : c'est un
bug visible par l'utilisateur.**

`story_viewer_screen.dart:187-210` et `publication_viewer_screen.dart:145-162`
portent le **même** code :

```dart
final backFile = back?.value;
if (backFile == null) {
  return Center(child: TiltableCard(child: frontFace));   // ← verso pas encore là
}
return Center(child: FlippableCard(front: frontFace, back: …));  // ← verso arrivé
```

À la même position de l'arbre, le widget passe de `TiltableCard` à
`FlippableCard`. **Flutter n'apparie pas deux widgets de types différents** : il
démonte tout le sous-arbre et en inflate un neuf. Le `_VibeVideoFaceState` est
détruit, son `SealedVideoController` avec, et un **second lecteur natif** est
créé pour la même face.

Conséquences, dans l'ordre de gravité :

1. **La vidéo du recto redémarre** quand le verso finit d'arriver.
2. Un second `create` natif, donc un second décodeur instancié.
3. La trace, elle, est toujours ouverte (elle ne se ferme qu'à `première
   image`) : le second `_create` **écrase** `· attente avant natif` avec le
   temps écoulé depuis le début. Ce chiffre n'est donc pas une latence
   d'interface — **c'est le temps de téléchargement du verso**.

### Pourquoi ça frappe les stories et pas les publications

| | Recto | Verso | Qui gagne la course |
|---|---|---|---|
| Story « oneshot » seedée | **vidéo** — URL + clé seulement, pas de téléchargement | **photo** — téléchargée **en entier** | le recto, largement → **échange, et donc bug** |
| Publication seedée | vidéo | vidéo | les deux ensemble → `FlippableCard` bâti directement, **pas d'échange** |

Ce que le relevé confirme, mesure par mesure :

| Face | `· attente avant natif` | Lecture |
|---|---|---|
| `551106d5…_f` | **11 ms** | pas d'échange |
| `9a5d7a32…_f` | **18 ms** | pas d'échange |
| `36d3f49f…_f` | **2 873 ms** | téléchargement du verso photo (4G) |
| `23a50d43…_f` | **3 316 ms** | idem |
| `5af6f66d…_f` | **550 545 ms** | idem + téléphone posé (voir cause n°2) |

**Le « bug » que Jay soupçonnait existe, il est là.** Et la médiane à froid de
3 459 ms ne mesure pas l'ouverture d'une vidéo : elle mesure l'arrivée d'une
photo de verso.

## Cause n°2 — la mesure continue de courir quand l'app ne tourne pas

`5af6f66d…_f` : **551 297 ms**, soit 9 min 11 s.

Journal de l'app : `StoryViewerScreen` ouvert à 21:29:00.967, fermé à
21:37:42.348 — **8 min 41 s sans une seule ligne de journal**. Téléphone posé,
écran éteint. Flutter ne produit plus de frame, le second lecteur n'est
construit qu'au retour.

`VideoOpenTrace` chronomètre en `DateTime.now()`, donc en **temps mural**. Une
trace qui traverse une mise en arrière-plan n'est pas une mesure lente : ce
n'est pas une mesure. Aujourd'hui rien ne les distingue.

C'est la **quatrième** occurrence du motif « l'instrument compte le temps de
l'utilisateur » (après le verso préchargé du matin, corrigé par `markPrefetched`).

## Cause n°3 — le préchargement a fonctionné, et la mesure l'a rangé au mauvais endroit

`ca6987a3…_f`, classé **[partiel]** :

```
URL signée : 5 ms      ← vient de la mémoire du préchargeur
clé : 5 ms             ← idem
· ouverture média : 0 ms  ← les octets étaient déjà sur l'appareil
```

Les trois signatures du préchargement réussi sont là. **Cette mesure aurait dû
atterrir dans « Amorcé ».** Le seau n'est pas vide parce que le préchargement ne
marche pas — il est vide parce que le classement rate.

**La cause, relevée dans le code** — `content_face.dart:81` :

```dart
trace?.primedByApp = preloader.wasPrimed(spec.contentId, front: spec.front);
```

C'est un **instantané pris trop tôt**. Dans `content_preloader.dart:124-138`,
la clé et l'URL sont mémorisées **avant** que `prime` (le premier bloc) ne
réponde :

```dart
_remember(spec.contentId, key);   // clé disponible tout de suite
_rememberUrl(id, url);            // URL disponible tout de suite
…
final ok = await _channel.invokeMethod<bool>('prime', …);
if (ok == true) _primed.add(id);  // ← seulement ICI
```

Si l'utilisateur ouvre la face pendant que `prime` est en vol, il profite déjà
de l'URL et de la clé (5 ms chacune, c'est ce qu'on lit) mais `wasPrimed` répond
**faux**. Les octets, eux, arrivent avant que le lecteur ne s'ouvre
(878 ms plus tard ici) — d'où `· ouverture média : 0 ms`.

**La donnée était bonne, l'étiquette était fausse.**

## Pourquoi « Complet sur l'appareil » est vide — et pourquoi c'est normal

Depuis la lecture par intervalles, un fichier n'est « complet » que si la vidéo
a été **regardée jusqu'au bout**. Jay a passé ~1 s par face. Aucun contenu ne
pouvait devenir complet.

⚠️ **Ce n'est donc plus le seau de référence.** Le seau qui porte la cible des
300 ms est désormais **« Amorcé »** — celui qui est vide pour la cause n°3.

## L'hypothèse 4G

Elle ne porte pas sur ce qu'on croyait. `· ouverture média` (la vraie requête
réseau du lecteur) reste à **248-362 ms** sur toutes les mesures froides : c'est
stable, et c'est bon. La 4G se paie ailleurs — sur le **téléchargement complet
du verso photo**, qui est justement ce que la cause n°1 fait entrer dans le
chiffre.

## Règles des Vibes — un ❌ qui est un faux positif

`b53bc632` : `max_views=2`, `vues 2 → 2`, `phase=exhausted`,
`restant avant=0` → **❌ DÉCOMPTE ANORMAL : 0 vue(s)**.

**Le code a raison, c'est le verdict qui a tort.** La Vibe était déjà épuisée :
l'écran a refusé de l'ouvrir, aucune clé n'a été demandée, aucune vue n'a été
consommée. C'est exactement le comportement voulu — `0` sur un contenu épuisé
est la **preuve que la limite tient**.

`card_rules_trace.dart:101-104` :

```dart
bool? get countingOk {
  if (limitsApply != true || consumed == null) return null;
  return consumed == 1;      // ← ne connaît pas le cas « épuisé »
}
```

Quatrième instrument de la série qui répond juste à la mauvaise question.

**Les 20 autres verdicts sont verts**, sur Oneshot, Vibe, BeReal, y compris avec
`max_views=2` (`b2dcad6e` : 1 → 2, restant 1). Le décompte, lui, va bien.

---

# Les deux remarques de Jay

## 1. Les bibliothèques de conversations — non, ce n'était pas pris en compte

**Remarque juste.** `ConversationLibraryScreen` apparaît bien dans le journal
(21:42:16) et n'est traité par aucun des chantiers de la journée. C'est un
**troisième chemin média**, indépendant des deux autres. Relevé dans le code et
en base :

| | Stories / publications | Vibes en DM | **Bibliothèque de conversation** |
|---|---|---|---|
| Table | `contents` + `stories`/`library_items` | `cards` | **`library_vibes`** |
| Coffre | `stories` / `library` | `cards` | **`library_vault`** |
| Clé | `open_content_media` · `library_media_keys` | `open_card_media` | **`get_library_vibe_key`** |
| Effet de bord de la clé | journal (déplacé à 3 s) | **décompte une vue** | **aucun** (vérifié en base) |
| Transport | par blocs, cache partiel, lecteur natif | fichier entier | **fichier entier** |
| Déchiffrement | **natif**, jamais en clair sur disque | Dart | **Dart** |
| Clair sur le disque | non | non | **OUI** — `library_vibes_repository.dart:236` |
| Instrumenté | oui | non | **non** |

Deux points sérieux :

- **`openRevealed()` écrit le média déchiffré en clair dans un fichier
  temporaire** (`vibe_<id>.<ext>`). C'est précisément ce que le format par blocs
  et le lecteur natif ont été construits pour supprimer. Ce chemin l'a conservé.
- **Il y a quatre fonctions qui rendent une clé**, chacune avec sa propre
  politique. C'est la règle 3 de `CLAUDE.md` (« compter les chemins qui mènent à
  un contenu ») : quatre chemins, quatre règles.

Bonne nouvelle mesurée en base : `get_library_vibe_key` **ne décompte rien et
ne journalise rien**. Aucune vue n'est brûlée en ouvrant une bibliothèque de
conversation.

## 2. Le Oneshot mixte — confirmé, c'est le seed, et c'est en base

**Remarque juste, et vérifiée aux deux bouts.**

*Ce que la capture impose* — `card_capture_screen.dart:596-607` : un Oneshot
filme les **deux caméras en même temps** ; si l'appareil n'en est pas capable,
il **reste photo sur les deux faces**. Il n'existe aucun chemin produisant un
Oneshot mixte.

*Ce que le seed produit* — `tool/seed_bot_media.dart:236-241` :

```dart
await _content(story: true, frontVideo: true, backVideo: false, type: 'oneshot');
```

*Ce qu'il y a en base* :

| Table | Type | n | **Mixte** |
|---|---|---|---|
| `stories` | oneshot | 6 | **6** |
| `stories` | standard | 6 | 0 |
| `library_items` | standard | 13 | 0 |
| `cards` | standard | 17 | 4 *(légal : une standard peut être mixte)* |

**Les 6 stories Oneshot de la base sont impossibles.** Et ce sont exactement
celles qui ont déclenché la cause n°1 — un recto vidéo instantané avec un verso
photo lent est la configuration qui provoque l'échange de widget.

⚠️ À noter : **le bug n'est pas réservé aux objets impossibles.** Une Vibe
*standard* recto vidéo / verso photo est parfaitement légale (4 en base), et
elle le déclenche pareil.

---

---

# Modifications apportées — v0.9.68+158

Feu vert de Jay sur les cinq correctifs, avec deux consignes : **« pour la base
on repart de 0 pour tout »** et **« pour les bibliothèques de conversations fais
comme tu veux »**.

## 1. La structure du widget devient stable (la cause n°1)

| Fichier | Changement |
|---|---|
| `vibe_face.dart` | nouveau `VibeFaceLoading` — un verso qui **occupe sa place** au lieu de la créer en arrivant |
| `story_viewer_screen.dart` | la structure ne dépend plus que de `hasBack`, constant |
| `publication_viewer_screen.dart` | idem |

Le lecteur du recto ne peut plus être détruit par l'arrivée du verso : la cause
est supprimée, pas gardée.

**Vérifié par inventaire** (règle : lister les occurrences du motif, pas lire le
diff). Cinq sites emploient `TiltableCard`/`FlippableCard` :

| Site | Condition | Verdict |
|---|---|---|
| `story_viewer_screen.dart` | `backFile == null` | ❌ **corrigé** |
| `publication_viewer_screen.dart` | `backFile == null` | ❌ **corrigé** |
| `saved_items_screen.dart:230` | `item.hasBack` | ✅ constant |
| `vibe_faces_screen.dart:134` | `vibe.hasBack` | ✅ constant |
| `card_viewer_screen.dart:576` | `_shownBack == null` | ✅ **sain** — les deux faces sont chargées (ligne 210) **avant** le passage en `_Phase.viewing` (240/258) : la condition ne change pas pendant l'affichage |

## 2. Une trace qui traverse un arrière-plan est jetée (la cause n°2)

`video_open_trace.dart` : un `AppLifecycleListener` installé à la première
mesure ; tout état ≠ `resumed` invalide **toutes** les ouvertures en cours, et
une trace invalidée n'entre jamais dans l'historique.

Énoncé **positivement** (règle 4) : *une mesure n'est retenue que si l'app est
restée au premier plan d'un bout à l'autre.* On ne retranche pas le temps
suspendu — rien ne dirait quelle part du travail aurait eu lieu sans la
suspension.

## 3. `primedByApp` devient une question, pas un instantané (la cause n°3)

`bool primedByApp` → `bool Function()? isPrimed`, évaluée dans
`sealed_video_controller._create()`, au moment où le natif rend son verdict.

La course disparaît **par construction** : le préchargeur mémorise la clé et
l'URL avant que `prime` ne réponde, et il n'y a plus de fenêtre où la question
soit posée trop tôt.

## 4. Le verdict de l'inspecteur connaît le cas « épuisé »

`card_rules_trace.dart` : `countingOk` attend **0** quand `phase == 'exhausted'`,
**1** sinon. Nouveau libellé : `✅ budget épuisé : ouverture refusée, 0 vue
consommée`. Le message d'échec indique désormais la valeur attendue.

## 5. Le seed ne peut plus fabriquer un Oneshot impossible

`tool/seed_bot_media.dart` : `_refuseImpossibleOneshot()` **lève** si un
`oneshot` reçoit deux types de face différents. Appelée par `_vibe` et
`_content` — donc la contrainte tient quel que soit l'appel, au lieu de dépendre
de la vigilance à chaque site (règle 1 : supprimer la cause).

Catalogue de contenu revu :

| | Avant | Après |
|---|---|---|
| Stories / bot | 2 (1 photo + 1 oneshot **mixte**) | **4** — standard photo, standard **mixte légal**, oneshot vidéo/vidéo, BeReal |
| Publications / bot | 2 | **3** (ajout du cas mixte) |

**L'anneau de 4 est un choix de mesure** : le préchargement ne s'amorce qu'en
avançant *à l'intérieur* d'un anneau. Avec 2 stories, il n'avait qu'une occasion
par anneau — d'où « une ouverture sur sept » au relevé du 2026-08-13. Avec 4, il
en a trois, dont deux sur une face vidéo.

## 6. 🛑 Trouvé pendant le nettoyage : la purge des stories supprimait le mauvais bout du graphe

**Relevé en base, pas déduit.** `stories` affichait **0 ligne** pendant que
`contents` en portait **14 de contexte `story`**.

`stories.id` référence `contents.id` en `on delete cascade` : supprimer le
**contenu** emporte la story, l'inverse est faux. Or le cron faisait
`delete from public.stories where expires_at < now()`.

Ce qui survivait à chaque story expirée :

| Objet | Comptés le 2026-08-13 |
|---|---|
| `contents` orphelins | **14** |
| `content_media_keys` | **14** |
| `content_grants` | **6** |
| Objets dans le coffre `stories` | **26** |

⚠️ **Ce n'était pas une faille de confidentialité** — vérifié avant de
l'annoncer : `private.can_view_story_file` interroge la table `stories`, donc la
disparition de cette ligne suffisait à interdire toute lecture. Mais c'est
exactement la « sécurité qui s'énonce par une négation » que la règle 4 proscrit :
rien n'était lisible *parce que plus rien ne l'indexait*, pas parce que ça
n'existait plus. Et la **clé survivait au contenu**.

Migration `story_purge_deletes_the_content_not_the_story` : le cron supprime
désormais le `contents`, et le cascade fait le reste. Les orphelins déjà
accumulés ont été rattrapés.

⚠️ `cron.schedule` **remplace** un job de même nom : la commande a été recopiée
**entièrement**. Vérifié après application — un seul job `neovibe_purge`, et la
nouvelle clause y est.

## 7. Remise à zéro complète

Consigne : « on repart de 0 pour tout ». **Périmètre : tout le contenu**, y
compris celui de Jay. **Non touchés** : comptes, profils, amitiés, conversations,
bots.

| Étape | Résultat |
|---|---|
| `delete from contents` | cascade sur stories, library_items, grants, keys, views, reports |
| `delete from cards` | cascade sur card_deliveries, card_media_keys |
| `delete from messages` | les annonces pointant vers rien (FK en `set null`) |
| `delete from library_vibes` | **explicite — voir ci-dessous** |
| Coffres `stories` · `library` · `cards` · `library_vault` | **293 objets** supprimés |

**Deux surprises, toutes deux instructives :**

- **`library_vibes` a survécu au `delete from cards`.** Sa clé étrangère
  `card_id` est bien en cascade, mais elle vaut **`null`** sur les lignes
  réelles : la table porte désormais son propre média. C'est donc une **racine
  de contenu à part entière**, et c'est la confirmation en base du diagnostic
  sur les bibliothèques de conversations — un quatrième objet, pas une vue sur
  les Vibes.
- **La suppression directe dans `storage.objects` est refusée** par le trigger
  `storage.protect_delete` (contrainte déjà notée dans
  `purge_expired_library_vibes`). Passage par l'API Storage avec la clé
  `service_role`. ⚠️ **`library_vault` range sur TROIS niveaux**
  (`owner/vibe/fichier`) là où les autres coffres n'en ont que deux : un listing
  récursif écrit pour deux niveaux a répondu « déjà vide » sur 14 objets bien
  présents. Corrigé, puis **vérifié en base** — c'est le compte SQL qui a
  attrapé l'erreur, pas la sortie du script.

## 8. Le contenu regénéré, vérifié en base

| Table | Type | n | Mixte |
|---|---|---|---|
| `stories` | standard | 10 | 5 *(légal)* |
| `stories` | **oneshot** | 5 | **0** ✅ |
| `stories` | bereal | 5 | 0 |
| `library_items` | standard | 15 | 5 *(légal)* |
| `cards` | standard | 12 | 3 *(légal)* |
| `cards` | **oneshot** | 6 | **0** ✅ |
| `cards` | bereal | 3 | 0 |

**Zéro Oneshot mixte.** Les mixtes restants sont tous des `standard` — un format
où le cas est légal, et qu'il faut justement pouvoir surveiller.

## Difficultés rencontrées ou erreurs commises

### Un script vérifié par sa propre sortie, pas par la mesure

Le premier script de purge des coffres a annoncé **« library_vault : déjà
vide »** alors que 14 objets s'y trouvaient : son listing récursif ne descendait
que de deux niveaux, et `library_vault` en a trois. Sa sortie était rassurante et
fausse.

Ce qui l'a attrapé : le **contrôle en base après coup**
(`select count(*) from storage.objects`), pas la lecture du journal du script.
Même famille que les leçons du 2026-08-13 — *l'instrument affirmait un zéro que
sa propre forme produisait*.

### Ce qui n'a PAS été fait, et pourquoi

Les **bibliothèques de conversations** ne sont pas alignées dans cette
livraison, bien que Jay ait laissé le choix. Motif : le transport se mutualise,
mais `get_library_vibe_key` porte une politique distincte, et les mélanger au
même diff que la mesure rendrait les deux illisibles. Chantier suivant, décrit
en `RAPPELS.md`.

## Vérifications de fin de livraison — v0.9.68+158

- `dart format` + `flutter analyze` : **propres, 0 problème**.
- **56 tests Dart** passent.
- **Aucun fichier natif touché** (`git diff --name-only | grep .kt` : vide) —
  `docs/parties-natives-par-os.md` reste exact, rien à corriger.
- APK release construit (60,7 Mo), **v0.9.68+158**. Version montée **avant** le
  build.
- Migration appliquée en base **et** déposée dans `supabase/migrations/`
  (`20260813215500_story_purge_deletes_the_content_not_the_story.sql`).
  État vérifié après application : un seul job `neovibe_purge`, clause corrigée
  présente, zéro orphelin.
- Aucun secret dans le diff (seule correspondance : le mot `service_role` dans
  le texte de `RAPPELS.md` #16, qui décrit la contrainte sans citer de clé).
- Commit `4f06484`, tag `v0.9.68`, **release GitHub publiée** avec l'APK et le
  protocole de relevé détaillé.
- `RAPPELS.md` : **#22, #23, #24 nouveaux** (décisions en attente) et
  **avant-prod #16 nouveau** (fuite de stockage).

## Ce qui reste à faire

1. **Le relevé de la v0.9.68** — c'est lui qui dira si le seau « Amorcé » se
   remplit enfin, et si la cible des 300 ms est tenue sur du contenu froid.
2. **Les bibliothèques de conversations** (`RAPPELS` décisions #24) — chantier
   suivant, décidé pour cette session mais volontairement séparé du diff de
   mesure.
3. **L'option 3 — préparer un lecteur d'avance** (~149 ms), à n'engager
   qu'après le relevé.
4. **La fuite d'octets dans les coffres** (`RAPPELS` avant-prod #16).

---

# Modifications apportées — v0.9.69+159

Trois demandes de Jay après validation de la v0.9.68.

## 1. Nouvelle voie de la vision — « l'Apple des réseaux sociaux »

> « Une voie en plus, c'est qu'on veut construire l'Apple des réseaux sociaux.
> Cela implique une interface claire et épurée mais qui reste cool, un contrôle
> total de l'écosystème de l'app : "ce qui se passe sur NeoVibe reste sur
> NeoVibe". C'est pour cela qu'on veut sécuriser et chiffrer les contenus de
> cette manière ! »

Cette voie n'ajoute pas une fonctionnalité : elle **donne leur raison d'être**
à des choix techniques déjà pris, et un critère pour ceux qui restent.

Consigné dans `docs/vision-produit.md` §1 (avec les trois exigences et leurs
conséquences techniques), résumé dans `CLAUDE.md`, et en mémoire longue
(`project-neovibe-apple-of-social`).

**Ce que ça change concrètement** : un média déchiffré écrit en clair sur le
disque cesse d'être une commodité d'implémentation pour devenir un **manquement
à la promesse**. C'est ce qui a rendu le chantier n°3 ci-dessous prioritaire
plutôt que cosmétique.

⚠️ **Tension signalée à Jay pour mémoire** : « contrôle total de l'écosystème »
et « interface épurée » se contredisent si chaque garantie réclame son réglage.
**La bonne réponse est le défaut juste, pas l'option supplémentaire.**

## 2. L'enchaînement des anneaux de stories

Demande : *« si on est à la dernière story de l'utilisateur A et qu'on clique à
droite, au lieu de fermer on passe à la première story de l'utilisateur B. On ne
ferme qu'au bout ou avec la croix. »*

`StoryViewerScreen` reçoit désormais **tous les anneaux du bandeau** et l'index
de celui par lequel on entre, au lieu d'un anneau isolé.

| Élément | Rôle |
|---|---|
| `_nextPosition` | la position suivante, **anneaux compris** ; nulle au tout dernier contenu |
| `_previousPosition` | son symétrique |
| `_next()` | ferme **uniquement** si `_nextPosition` est nul |

**Un seul calcul pour deux usages** : `_preloadNext()` suit `_nextPosition`,
donc le préchargement **franchit lui aussi la frontière entre deux auteurs**.
C'est nécessaire, pas décoratif — depuis que le clic enchaîne, la première story
d'un auteur n'est plus une entrée à froid, et s'arrêter au bord de l'anneau
laisserait sans préparation exactement le contenu que l'utilisateur va voir.
Les écrire séparément aurait garanti qu'un jour l'un précharge ce que l'autre
n'affiche pas.

**Aucun cas particulier pour le partage en conversation** : `chat_screen.dart`
passe une liste d'**un** anneau. Il n'y a alors pas de suivant, et l'écran se
ferme au bout — le comportement voulu tombe tout seul.

⚠️ **Décision prise seule, à confirmer par Jay** : le clic à **gauche** a reçu
la symétrie (au premier contenu d'un anneau, on revient au dernier de l'auteur
précédent). Jay n'a demandé que la droite. Sans cette symétrie, revenir sur ses
pas après un enchaînement serait impossible — on ne pourrait plus que continuer
ou fermer. **Facile à retirer** si ce n'est pas voulu.

## 3. Les bibliothèques de conversations rejoignent le format commun

C'était le chantier annoncé, et la voie « Apple » lui a donné son ordre de
priorité.

### Le défaut, énoncé simplement

`library_vibes_repository` chiffrait en **AES-GCM d'un seul bloc**
(`box.concatenation()`) — un format que le lecteur natif ne sait pas lire.
Conséquence en chaîne : il fallait tout déchiffrer en Dart, puis **écrire le
clair dans un fichier temporaire** (`vibe_<id>.<ext>`). Soit exactement ce que
le format par blocs et le lecteur natif ont été construits pour supprimer.

### Ce qui change

| Fichier | Changement |
|---|---|
| `library_vibes_repository.dart` | `_seal`/`_unseal` (AES-GCM d'un bloc) **supprimés** ; scellement par `FaceDelivery.seal` — donc format `NVC1` **et** `fastStart` sur les vidéos |
| idem | `openRevealed()` rend un **`OpenedMedia`** via `MediaOpen.open`, plus un `File` en clair |
| `vibe_faces_screen.dart` | `VibePhotoFace` / `VibeVideoFace` au lieu de `Image.file` |
| `revealed_vibe_screen.dart` | idem, sous l'animation de reveal |

**Passer par `FaceDelivery` plutôt que d'appeler `ChunkedSeal` directement**
n'est pas un détail : c'est le passage obligé dont l'existence sert précisément
à ce qu'une préparation ne soit pas oubliée à un endroit sur quatre.

### 🐛 Un bug trouvé au passage, et corrigé gratuitement

Les deux écrans faisaient **`Image.file` sur un `.mp4`**. **Une vibe vidéo en
bibliothèque de conversation ne pouvait pas s'afficher** — et personne ne
l'avait relevé, faute de toute mesure sur ce chemin. C'est l'illustration
directe du diagnostic du matin : un chemin non instrumenté est un chemin où les
pannes sont muettes.

### Ce qui n'a PAS changé, et pourquoi

- **Le fichier scellé est toujours téléchargé en entier.** La lecture par
  intervalles réclamerait une URL signée sur `library_vault` et un cache dédié :
  chantier distinct, inscrit en `RAPPELS.md`.
- **La politique de clé reste distincte.** `get_library_vibe_key` porte sa
  propre règle — le reveal — et n'a pas bougé. **Le transport se mutualise, les
  règles non** (règle 2 de `CLAUDE.md`). C'est la mise en garde de `RSUNA` §2.4
  point 3, appliquée.

### Migration : aucune, et c'est un coup de chance à noter

Le changement de format aurait dû casser toutes les vibes existantes. Il n'y en
avait **aucune** : la remise à zéro de la v0.9.68 venait de les supprimer.
Vérifié en base avant de livrer (`select count(*) from library_vibes` → **0**).

⚠️ **Si le format avait changé sans la remise à zéro**, les anciennes vibes
seraient devenues illisibles — `MediaOpen` connaît deux formats, et l'AES-GCM
d'un seul bloc de ce chemin était un **troisième**, qu'il n'a jamais su lire.

### Vérification par inventaire

`_seal(`, `_unseal(`, `AesGcm` dans `lib/features/library_vibes/` : **aucune
occurrence**. `Image.file` : plus aucune (seules deux mentions en commentaire,
qui documentent la correction). Quatre appelants de `openRevealed`, tous
migrés.

---

# Modifications apportées — v0.9.70+160

## « Comme si les cards étaient toujours là, mais bloquées en local »

Formulation de Jay, et c'est **une conception, pas une optimisation** :

> « On télécharge les contenus de la bibliothèque 5 minutes avant le reveal,
> c'est une excellente idée car l'utilisateur ouvre les cards instantanément et
> ne voit même pas le téléchargement… comme si les cards étaient toujours là
> mais qu'elles étaient juste bloquées en local. »

### Pourquoi elle bat la lecture par intervalles, ici

J'avais proposé de streamer ce chemin comme le socle. **La proposition de Jay
est meilleure sur ce cas précis**, et la raison mérite d'être écrite : une vibe
de bibliothèque est **connue d'avance** — elle est annoncée dans le fil, et son
heure de reveal est écrite en base. Il n'y a rien à deviner, contrairement à un
feed où l'on ignore ce que l'utilisateur regardera ensuite.

**Quand on peut tout amener d'avance, streamer n'apporte rien.** Le streaming
sert à ne pas payer ce qu'on ne regardera pas ; ici, on sait qu'on regardera.

### Ce qui manquait pour que la formule soit vraie

Le préchargement à 5 minutes **existait déjà**. Mais les octets vivaient dans
l'état d'un widget (`_VibeTileState._sealed`) : ils **mouraient avec l'écran**.

| Situation | Avant |
|---|---|
| Reveal, en restant sur la bibliothèque | ✅ instantané |
| Sortir puis revenir | ❌ retéléchargement complet |
| Rouvrir une vibe révélée le lendemain | ❌ retéléchargement complet |
| **Retourner la carte** (verso) | ❌ **jamais préchargé du tout** |

L'illusion ne tenait donc que pendant la minute qui suivait le reveal. Pour
qu'elle soit vraie, les octets doivent **vivre sur l'appareil**.

### Ce qui a été fait

**Nouveau `lib/features/library_vibes/library_vault_cache.dart`** — un cache
persistant dédié au coffre, sur le modèle de celui du socle.

| Élément | Rôle |
|---|---|
| `tryFace` / `store` | scellés sur disque, nommés `<vibe>_<face>.seal` |
| écriture en `.part` puis renommage | un téléchargement interrompu ne peut pas laisser un **fichier tronqué** que `tryFace` rendrait ensuite comme valide — la vibe deviendrait illisible sans qu'aucune erreur ne dise pourquoi |
| `sweep()` + plafond 80 Mo, LRU | branché au démarrage dans `main.dart`, à côté des deux autres caches |
| `usageBytes()` / `clear()` | nouvelle section dans Réglages → Stockage |

**Dépôt** : `prefetchSealed()` (qui rendait des octets) devient `cacheFace()`
(qui rend un **fichier**, et ne retélécharge pas s'il est déjà là), plus
`prefetch()` qui prépare **les deux faces** sans jamais lever.

**`openRevealed()`** lit le cache. La clé et le scellé partent **ensemble**
(`.wait`) : ils sont indépendants, et les enchaîner ajouterait un aller-retour à
chaque ouverture — leçon du chantier livraison, appliquée ici.

### Une simplification que le cache rend possible

Le paramètre `sealedBytes` **disparaît**. Il existait pour porter les octets de
la tuile jusqu'à l'écran de reveal en évitant un second téléchargement — un
média transporté à travers trois widgets pour économiser un appel. **C'était une
conséquence de l'absence de cache.** La cause a disparu, le paramètre avec
(règle 1).

### Pourquoi poser des scellés sur le disque ne coûte rien en sécurité

Ces octets sont **chiffrés**, et la clé n'est pas sur l'appareil : le serveur la
retient jusqu'au reveal (`get_library_vibe_key`). Un scellé sur le disque n'est
qu'un bloc d'octets inertes.

**La barrière est même mieux énoncée qu'avant** : le contenu est *présent mais
verrouillé*, au lieu d'être *absent parce qu'on refuse de le livrer*. C'est
exactement la forme positive que demande la règle 4 de `CLAUDE.md`.

⚠️ **Vérifié en base avant d'y toucher** : la fenêtre de 5 minutes est tenue
**côté serveur**, dans la politique du coffre —
`now() >= v.reveal_at - interval '00:05:00'`. Ce n'est pas une convention
client, et le cache n'en décide rien : il ne conserve que ce que le serveur a
accepté de livrer.

### Ce que ça ferme

`RAPPELS.md` décisions #24 : le point (a) — « le scellé est téléchargé en
entier » — n'est plus un défaut mais **la conception voulue**. Reste le point
(b) : ce chemin n'est toujours **pas instrumenté**.

---

# Modifications apportées — v0.9.71+161 : les photos de profil

Chantier confié pendant que Jay prépare les propositions d'UI. **Le refonte de
l'UI globale n'est PAS commencée** — ses vues sont attendues dans `docs/`.

## Ce que le diagnostic a montré : la fonctionnalité était à MOITIÉ faite

Relevé avant d'écrire quoi que ce soit, plutôt que de repartir de zéro :

| Pièce | État trouvé |
|---|---|
| Colonne `profiles.avatar_url` | ✅ existe |
| Coffre `avatars` privé + 4 politiques | ✅ en place et correctes |
| Widgets `Avatar` / `AvatarFill` | ✅ point de passage unique, bien fait |
| Choix d'image + dépôt | ✅ présent — **mais en double**, inscription et édition |
| `image_picker` + permissions | ✅ |
| **Avatars en base** | ❌ **aucun**, sur les 7 profils |

Il ne manquait donc pas la plomberie. Il manquait **ce qui fait qu'on s'en
sert**, et il y avait deux défauts qui ne se voient pas au diff.

## 🐛 Défaut n°1 — changer sa photo n'avait aucun effet visible

Le fichier s'appelait toujours `<uid>/avatar.jpg`. Changer sa photo écrasait
donc l'ancienne **au même chemin**, et l'ancienne image restait affichée — à
**trois** niveaux de cache qui n'en savaient rien :

1. `avatarUrlProvider`, famille Riverpod **sans `autoDispose`**, indexée sur la
   valeur stockée — inchangée, donc jamais recalculée ;
2. l'URL signée elle-même, valable une heure ;
3. le cache d'images de Flutter, indexé sur l'URL.

**Correctif : le chemin porte une version** (`avatar_<horodatage>.png`).

On aurait pu ajouter un paramètre anti-cache, invalider le provider et vider
`imageCache` : trois garde-fous à maintenir, pour une cause qui tient en une
phrase — **deux images différentes portaient le même nom**. Un chemin versionné
supprime la cause, y compris pour les caches qu'on ajoutera plus tard sans y
penser (règle 1).

L'ancien fichier est supprimé dans la foulée : sans ça, le coffre grossissait
d'une photo à chaque changement.

## 🐛 Défaut n°2 — retirer sa photo était impossible

Les deux écrans écrivaient `'avatar_url': ?avatarUrl`, l'entrée conditionnelle
de Dart, qui **omet la colonne** quand la valeur est nulle. On pouvait donc
changer sa photo, **jamais l'enlever** — et l'écran enregistrait sans erreur,
donc rien ne le disait.

Bouton « Retirer la photo » ajouté, avec une mise à jour explicite à `null`.

## Ce qui a été ajouté

### Un recadrage carré, écrit en Flutter

`lib/features/profile/avatar_cropper_screen.dart` — déplacement et zoom sur un
aperçu **rond** (celui de l'avatar réel), rendu à 512 px.

**Écrit plutôt que pris dans un paquet, et c'est la voie « Apple » qui tranche** :
un recadreur tiers **voit la photo**, souvent en la faisant transiter par une
activité native et un fichier temporaire qui ne nous appartient pas. « Une
dépendance externe qui verrait passer un contenu en clair est disqualifiée » —
et une photo de visage est exactement le cas. Bénéfice secondaire : rien à
reconfigurer côté natif pour le portage iOS.

Sans recadrage, une photo 3:4 est rognée par le **centre géométrique**, qui
n'est presque jamais le visage. C'est la différence entre « ça marche » et
« c'est soigné ».

### Le choix appareil photo **ou** galerie

L'appareil photo **en premier** dans la feuille de choix. Ce n'est pas un détail
d'ordre : NeoVibe est caméra-first, et reléguer la prise de vue au second rang
dirait le contraire de tout le reste du produit. Auparavant, seule la galerie
était proposée.

### Un cache disque pour les avatars

`AvatarFileCache` dans `avatar.dart`. Un avatar s'affiche sur une douzaine
d'écrans dès l'ouverture : sans cache, chacun coûtait **un aller-retour pour
signer l'URL, puis un téléchargement**, à chaque démarrage. C'est ce que la
consigne « l'affichage de MES contenus ne doit jamais attendre le réseau »
proscrit — étendue ici aux avatars des autres, qui sont dans le même cas.

⚠️ **Un cache d'avatar est d'ordinaire un piège** (l'image change, le nom ne
change pas, l'ancienne reste). Il est sûr ici **parce que le chemin est
versionné** : deux images différentes ne portent jamais le même nom, donc une
entrée ne peut pas devenir fausse — seulement inutile. **Le correctif du défaut
n°1 est ce qui rend le cache possible.**

Écriture en `.part` puis renommage, plafond 200 entrées en LRU, balayage au
démarrage à côté des trois autres caches.

### Un seul endroit pour « ma photo de profil »

`lib/features/profile/avatar_service.dart` — choisir, recadrer, déposer,
retirer. La séquence était **recopiée** dans l'inscription et l'édition. Sans
conséquence tant qu'elle se limitait à « choisir un fichier et le téléverser » ;
elle en a une dès qu'il faut recadrer, versionner et faire le ménage : **une
étape oubliée d'un côté ne se verrait ni au diff, ni à `flutter analyze`**.

⚠️ À l'inscription, la **ligne de profil est insérée AVANT** le dépôt de la
photo : `upload()` fait un `update`, et sans la ligne la photo atterrirait dans
le coffre sans que rien ne la désigne.

### Suppression de `avatarUrlProvider` — les deux sens relevés (règle 8)

*Entrant* : aucun appelant réel (deux mentions en commentaire, mises à jour).
*Sortant* : il appelait `avatarPath` et `supabaseProvider`, tous deux encore
utilisés par trois autres endroits — aucun orphelin créé.

### Les bots ont une photo

`tool/seed_bot_media.dart` : chaque bot reçoit un avatar à sa teinte, au **même
format de chemin versionné** que l'app. Sans ça, tous les écrans retombaient sur
l'initiale et **rien ne permettait de juger le rendu réel** — ce qui compte
d'autant plus à la veille d'une refonte d'UI.

**Vérifié en base** : 6 objets dans le coffre `avatars`, 6 profils sur 7
pourvus.

## Vérification par inventaire

- Dépôt dans le coffre `avatars` : **un seul chemin** (le service).
- `ImagePicker` : trois sites, dont **deux sans rapport** — l'import d'une photo
  en bibliothèque (`profile_screen`) et l'import en capture. Vérifiés un par un.
- `avatar.jpg` en dur : plus aucune occurrence de code, seulement deux mentions
  documentaires.

## Une observation de terrain

Le relevé en base après le seed montre que **Jay a défini sa photo pendant la
session**, à l'ancien chemin fixe (`avatar.jpg`). C'est toléré par `avatarPath`,
qui accepte les deux formes ; son prochain changement basculera sur un chemin
versionné et supprimera l'ancien fichier. **Aucune migration à écrire.**

## Notes et consignes de Jay

- **Test mené en partie en 4G** — précision donnée avec le relevé.
- **Demande** : refaire les publications et les stories avec les bots, et
  **nettoyer la base des anciens** contenus.
- **Remarque 1** : les bibliothèques de conversations n'ont pas été prises en
  compte dans le chantier livraison — exact.
- **Remarque 2** : le seed fabrique une Vibe Oneshot avec une face vidéo et une
  face photo, impossible en conditions réelles — exact, 6 stories concernées.
