# Session du 2026-09-15 — 00:34

> Document vivant : mis à jour au fil de la session.

## 0. Point de départ

Reprise après la session du 2026-09-14 (rapport `2026-09-14_12-20.md`,
dernière livraison v0.9.184, `versionCode 5058`). Branche `master`, arbre
propre, dernier commit `352382a`.

Vérifié à la reprise (pas déduit) :

- le jeton Supabase de `docdev/` (fichier du 2026-08-25) **répond encore**
  (`GET /v1/projects/…` → 200) ;
- **aucun nouveau diagnostic** dans `dev_reports` depuis celui du 2026-09-14
  à 12:03 (« nuit ») — le test de nuit ×2 (secteur / batterie) n'a pas
  encore été fait, ou pas encore envoyé.

Suite décidée la veille : chantier **B « Publication »**
(`docs/plan-refonte-partage.md` §6), puis le feed local.

## 1. Modifications apportées — chantier B2 « les albums » (v0.9.185)

Plan écrit puis construit en une passe : `docs/plan-publications.md` (§9 =
ce qui a été construit). Relevé à la source avant d'écrire : les six lecteurs
serveur de `library_items` (`profile_stats`, `library_media_keys`,
`can_view_publication_file`, `publish_to_library`, `publication_audience`, le
déclencheur des octets) et les deux lecteurs Dart hors `library/`
(`shared_content.dart`, `fomo_listener.dart`).

### 1.1 Serveur — `supabase/migrations/20260915120000_les_albums.sql`

- `library_kind` (`card` / `album`), `library_items.kind`, `aspect_w` /
  `aspect_h` (contrainte : nuls pour une Card, un des trois ratios pour un
  album).
- **`library_media`** (`item_id, owner_id, slot 0..10, path, is_video,
  duration_ms ≤ 60 000, poster_path, width, height`) **pour les deux
  formats** ; les 43 publications existantes reprises (83 lignes : 43 rectos +
  40 versos) ; les colonnes `front_path` / `back_path` / `front_is_video` /
  `back_is_video` **supprimées** avec leur contrainte et leur déclencheur.
- RLS : lecture par `publication_audience(item_id)` (le même juge que
  l'en-tête), écriture révoquée (la RPC seule).
- `can_view_publication_file` sur `library_media` (chemin ou poster) ;
  déclencheur `library_media_octets_a_supprimer` ; `publish_to_library`
  réécrite (`p_kind`, `p_media jsonb`, `p_aspect_w/h`, contrôles : 1–2 faces
  ou 1–11 médias, chemins sous `<me>/`, vidéo ≤ 60 s).
- Rejouée à blanc puis **sous RLS** (propriétaire Testeur, ami Malik, inconnu
  Yanis) : **17 contrôles verts**, puis appliquée sur la base de dev.

### 1.2 Socle Dart

- `content_face.dart` : `ContentFace.slot: int` remplace `front: bool` ;
  `ContentSlot` (`front` = 0, `back` = 1, `poster(n)` = 100 + n,
  `cacheId`). Les places 0/1 gardent les suffixes `f`/`b` et les noms de
  fichier `front`/`back` : les caches déjà posés restent valables.
- `content_media_cache.dart` : « complet » **par place** dans l'index (un
  ancien `true` = la place 0) ; `content_preloader.dart` en `slot`.
- Appelants adaptés : `story_viewer_screen`, `stories_repository`,
  `chat_screen`, `publication_viewer_screen`, `mini_card`.

### 1.3 Modèle, dépôt, grille, visionneur

- `library_item.dart` : `LibraryKind`, `AlbumAspect`, `LibraryMedia`,
  `LibraryItem.media` (triés par place), `LibraryItem.select` (la jointure),
  `frontPath` / `backPath` / `hasBack` dérivés ; égalité de valeur étendue.
- `library_repository.dart` : `publish` (Card) et **`publishAlbum`** sur un
  seul `_publish` (une clé pour tous les médias, couvertures en cache `own/`,
  progression par fichier) ; `AlbumUpload`, `AlbumMediaUpload`.
- `mini_card.dart` : album = couverture (poster si vidéo) dans **le même
  cadre**, pastille ▣ N ou ▶, tap → `album_viewer_screen.dart` (nouveau :
  `PageView` au ratio, points, légende, enregistrer / repartager / retirer /
  signaler, `PullDownToClose`, média suivant demandé d'avance).
- `shared_content.dart` : la jointure `library_media(*)`.

### 1.4 Profil et « Publier »

- `library_deck_screen.dart` **supprimé** (deux appelants relevés et retirés,
  un commentaire de `home_shell.dart` mis à jour) ; `profile_screen.dart` :
  bouton « Publier » à la place du deck, **bouton flottant d'import direct
  retiré**, `AlbumPublishBanner` en tête ; `user_library_screen.dart` sans deck.
- `publish_choice_sheet.dart` : « Une Vibe » → `CardCaptureScreen(publicationOnly:
  true)` ; « Photos ou vidéos » → `AlbumFlow.start`.
- **B1** : `ShareContext.libraryOnly` (défaut faux, `VibeShareContext` le
  porte), `card_capture_screen.dart` (`publicationOnly` → `_ShareStep.libraryOnly`,
  retour au profil après l'envoi), `recipient_picker_screen.dart` : bibliothèque
  cochée d'office et non décochable (`_PublishChip.onTap` nullable), ni story,
  ni recherche, ni événement, ni « Groupes et amis ».

### 1.5 L'éditeur — `lib/features/library/album_editor/`

| Fichier | Rôle |
|---|---|
| `color_grade.dart` | `ColorGrade` (7 réglages) → **une matrice 4×5** ; `over` (retouche par-dessus un filtre) ; `AlbumFilter` (19 presets) ; `Vignette` (la courbe de référence) |
| `album_draft.dart` | `CropSpec` (zoom + centre, relatifs à la source ; `sourceRect`, `normalizedRect`, `panned`, `zoomed`), `VideoTrim` (`normalized` ≤ 60 s), `AlbumDraftMedia`, `AlbumDraft` (11 max, `reorder`, `withAspect` remet les cadrages, `splitPlan` de la découpe) |
| `album_picker.dart` | galerie multi / photo / vidéo (`image_picker` 1.2, photos ≤ 2160 px), sonde native, découpe proposée d'une vidéo > 60 s |
| `album_editor_screen.dart` | aperçu au ratio (glisser / pincer), bande réordonnable + « + » + retirer, outils Cadrer (ratio) · Filtres (puces = ce média à travers chaque filtre) · Réglages (7 curseurs) · Rogner (vidéo : `RangeSlider` ≤ 60 s, couverture) ; aperçu vidéo par `video_player` en boucle sur le morceau |
| `album_caption_screen.dart` | légende (≤ 2200), selon mes règles / publique (`RadioGroup`), partageable, sauvegardable, « Publier » |
| `album_export.dart` | `outputSize` (1080 de large — seule définition), `vignettePaint` (huit arrêts, partagé aperçu / export), photo : décodage borné + `Canvas` + `ColorFilter.matrix` + JPEG natif ; vidéo : transcodeur natif + couverture tirée du fichier produit |
| `album_publish_queue.dart` + `album_publish_banner.dart` | rendu puis envoi en arrière-plan, un seul à la fois, bandeau « Préparation… n/N (x %) » / « Publication… n/N » / « Publié » / échec |
| `album_flow.dart` | choisir → éditer → publier |

### 1.6 Natif

- **`MediaTranscoder.kt`** (nouveau) : `MediaExtractor` → `MediaCodec` sur
  `SurfaceTexture` OES → shader GL ES 2 (recadrage par coordonnées de texture,
  rotation défaite, matrice 4×5, vignette — la même définition que Dart) →
  encodeur H.264 (3,5 Mbit/s) → `MediaMuxer` ; audio AAC recopié et
  entrelacé ; attentes bornées (3 s / 4 s).
- `NativeMedia.kt` : `videoThumbnail(atMs)`, `probe`, `encodeJpeg`,
  `transcode` (+ `transcodeProgress` vers Dart, au plus une fois par %).
- `native_media.dart` : les quatre méthodes, `MediaProbe`, `TranscodeResult`.
- `docs/parties-natives-par-os.md` § 4 bis et vue d'ensemble à jour.

### 1.7 Tests, docs, version

- `test/album_draft_test.dart` (24), `test/library_item_media_test.dart` (5) ;
  `flutter test` **489** ; `flutter analyze` 0 ; `compileDebugKotlin` OK.
- `docs/plan-publications.md` (nouveau, §9 = construit), `docs/arbre-fonctionnalites.md`,
  `RAPPELS.md` **#135**.
- `pubspec.yaml` `0.9.185+3059`.

### 1.8 Livraison v0.9.185

`tool/build-release.sh` (deux signataires, `versionCode 5059`) puis
`tool/publish-release.sh` (5059 > 5058). Vérifié **sur l'artefact publié**
(téléchargé depuis GitHub) : `versionCode='5059' versionName='0.9.185'`.
https://github.com/Gatsby4444/neovibe_alpha/releases/tag/v0.9.185

## 1.9 Retours de Jay sur la v0.9.185 → v0.9.186

Six captures. *« Ok c'est un début correct mais il y a encore beaucoup à
améliorer. »* Trois points :

| # | Vu | Cause | Fait |
|---|---|---|---|
| 1 | « Le format n'est pas bon, on avait dit vertical comme sur Instagram » | trois ratios offerts, Jay avait cadré en 1:1 | question posée (4:5 / 3:4 / 9:16 / vertical par défaut) → **3:4 uniquement**. `AlbumAspect.tall`, sélecteur retiré, `withAspect` supprimé, migration `20260915180000_les_albums_en_3_4.sql` (3:4 ajouté aux ratios admis, appliquée : 43 Cards + 1 album 1:1 de test) |
| 2 | réglages de story dans la feuille ⚙︎ de la caméra restreinte | la feuille ignorait le mode | `PublicationSettingsSheet.libraryOnly` |
| 3 | la vidéo mal orientée (couchée, comme stockée) | `KEY_ROTATION` lu sur le format de piste de `MediaExtractor` — absent sur le Xiaomi → 0 ; la vignette venait de `MediaMetadataRetriever`, qui la lit → droite | rotation passée par Dart depuis la sonde (`transcode(rotation:)`), `KEY_ROTATION` forcé à 0 sur le décodeur. Le sens reste à confirmer au test |

Contrôles : `flutter analyze` 0, `flutter test` 489. **Livraison v0.9.186**
(`versionCode 5060`), vérifiée sur l'artefact publié :
https://github.com/Gatsby4444/neovibe_alpha/releases/tag/v0.9.186

## 1.10 « Tout au max, comme Instagram » — v0.9.187

Jay, seize captures (six de NeoVibe, dix d'Instagram) : *« Ok c'est encore
trop moyen. Je t'envoie des screenshots de l'App mais aussi de Instagram pour
t'inspirer. On en discute avant que tu code. »* Tableau comparatif livré
(galerie intégrée, barre d'outils, filtres à intensité, réglages en ronds,
texte, superposition, ce qui est hors chantier), ordre proposé, cinq
questions. Réponse : *« tu pousses tout au max, tu as carte blanche mais tu
dois faire un rendu pro et ergonomique, épuré comme le veut notre DA. »*
Construit en une passe — détail dans `docs/plan-publications.md` §9.2.

**Le principe** : chaque effet défini une fois (pur Dart), trois moteurs qui
le consomment — l'aperçu, l'export photo (`shaders/album_grade.frag`, premier
`FragmentProgram` de l'app), le transcodeur vidéo (la même formule en GL).
Contrats : `ColorGrade.toUniforms()` (24 nombres), `CropGeometry.corners()`
(les coins du cadre dans la source), `OverlayPainter` (le seul dessin des
calques, rendu en PNG pour la vidéo).

Fichiers : `NativeGallery.kt` (+ manifeste, `MainActivity`), `MediaTranscoder.kt`
(coins, uniformes, calque), `gallery/` (4 fichiers), `crop_geometry.dart`,
`overlay_model.dart`, `overlay_painter.dart`, `grade_shader.dart`,
`editor_theme.dart`, `editor_images.dart`, `media_preview.dart`,
`text_editor_screen.dart`, `sticker_picker.dart`, `album_editor_screen.dart`
(réécrit), `album_export.dart` (réécrit), `album_flow.dart`,
`album_caption_screen.dart` (thème sombre), `color_grade.dart`,
`album_draft.dart` ; `album_picker.dart` supprimé ; `vector_math` en
dépendance directe. Tests : `crop_geometry_test.dart` (10), `album_draft_test.dart`
(+8 : intensité, Lux, contrat, calques) — **504** au total.

## 2. Difficultés rencontrées ou erreurs commises

- **`out` est un mot réservé de GLSL** : le premier jet du shader Flutter
  nommait ainsi la coordonnée de sortie. Renommé `p` avant compilation.
- **`Matrix4` et `CustomPainter` ne sont pas dans `package:flutter/painting.dart`**
  (`rendering.dart` pour `CustomPainter`, `vector_math` pour `Matrix4`) ;
  `vector_math` n'était qu'une dépendance transitive — ajoutée au `pubspec`.
- **`LIMIT` dans l'ordre de tri du `MediaStore`** est refusé depuis
  Android 11 : pagination par `Bundle` (`QUERY_ARG_LIMIT` / `OFFSET`), écrite
  avant le premier test plutôt qu'après.
- **Deux lecteurs de la même étiquette qui ne disent pas la même chose.**
  `MediaMetadataRetriever` (la sonde, les vignettes) lisait bien la rotation
  du fichier ; `MediaExtractor.getTrackFormat` (le transcodeur) ne la
  portait pas sur le Xiaomi. Deux chemins pour une donnée = un désaccord que
  rien ne signale (règle « un chemin, une donnée »). Corrigé en faisant
  passer la valeur de la sonde jusqu'au transcodeur.

- **Le déclencheur des octets à supprimer ne trouvait plus le propriétaire.**
  Au rejeu sous RLS, la suppression d'un album donnait 0 pierre tombale : la
  cascade `contents → library_items → library_media` supprime l'en-tête
  **avant** de descendre, et le `before delete` du média cherchait le
  propriétaire dans une ligne déjà partie. Réparé en mettant `owner_id` sur la
  ligne de média (comme `old.owner_id` dans l'ancien déclencheur). Sans le
  rejeu, ce défaut n'aurait levé aucune erreur — juste des fichiers orphelins.
- **L'index « complet » du cache était par contenu.** Juste pour deux faces
  téléchargées d'un bloc ; faux pour onze médias (la place 0 arrivée aurait
  déclaré les autres complètes). Passé par place, avec reprise de l'ancien
  booléen.
- **Les scripts Python en heredoc bash cassent au-delà d'une certaine
  taille** (« unexpected EOF while looking for matching `''` »), même avec
  un délimiteur cité. Réflexe : écrire le script dans le scratchpad avec
  l'outil d'écriture, puis l'exécuter.
- `num.clamp` rend un `num` : les bornes en `int` ont besoin d'un `.toInt()`.

## 3. Notes et consignes de Jay

- **Retours sur la v0.9.185** (captures) : *« un début correct mais il y a
  encore beaucoup à améliorer »* — ① *« Le format n'est pas bon, on avait dit
  vertical comme sur Instagram »* → tranché : **3:4 uniquement** ; ② pas de
  réglages de story dans la caméra restreinte ; ③ la vidéo mal orientée.
- **Reprise** : Jay a demandé « le tableau avec les prochains points à voir,
  tout » — livré dans le chat (A chantiers / B tests / C décisions / D dettes /
  E avant-prod), tiré de `RAPPELS.md`, du plan de partage et de la vision §12.
- **Choix du chantier** : B « Publication », **option B2** (la publication
  classique + éditeur) — *« tu fais tout à fond et comme Instagram, la seule
  différence sera l'affichage du format »*. Carte blanche.
- **Spécification de Jay (2026-09-15)** :
  - une publication contient **jusqu'à 11 contenus** (photos et vidéos),
    feuilletés à l'horizontale ; **légende** ; format et images **comme
    Instagram** ; source **galerie ou caméra** ;
  - **pas de 4K** ; **vidéo de 1 min max par contenu** ; une vidéo trop longue
    → proposer de la **découper et répartir automatiquement** sur plusieurs
    contenus du carrousel (parmi les 11), « comme fait Snap » ;
  - **un éditeur digne d'Instagram**, sans les musiques, « pour le reste tu
    pousses tout au max » ;
  - apparaît **dans la grille des publications du profil** (la bibliothèque)
    et **dans le feed si rendue publique** ; **pas** pour le partage de Vibe ni
    de story ; le partage reste possible par « ajouter au feed d'un ami » ;
  - 🔴 **l'affichage de la bibliothèque reste identique** : même conteneur,
    même format style Card pour toutes les vignettes ; **seul le visionneur
    change** à l'ouverture ;
  - **supprimer le second moyen de consulter la bibliothèque (le visionneur
    carrousel)** ; à la place de son bouton, **le bouton pour ajouter une
    publication** (même bouton ou un autre, carte blanche) ;
  - point 6 (le « + » ouvre le choix Vibe / photos) : **oui** ; point 7 (la
    grille de décision) : *« je sais et c'est assumé »*.
  - *« On construit un système hybride en promouvant le plus notre format
    Card mais en gardant la possibilité de publier du contenu vertical
    puisque notre format Card est limité dans la création de contenus
    classiques. »*
