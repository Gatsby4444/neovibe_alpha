# Rapport de session — 2026-07-25 13:30

## Contexte à la reprise

Relu : `CLAUDE.md`, `RAPPELS.md`, `REPRISE-chantier-gpu-camera.md`, les deux
derniers rapports (2026-07-24 et 2026-07-25 00:19).

État à l'ouverture — dernière release **v0.9.14** (versionCode 104) :
- Chantier GPU/OpenGL caméra, **étapes 1 à 4 VALIDÉES par Jay** : aperçu double
  GPU (2×30 i/s constant), photo double instantanée (83-119 ms), vidéo double
  (2 encodeurs H264), audio partagé muxé dans les deux `.mp4`.
- Moteur GPU **complet et validé mais isolé** dans l'écran de test dev
  (`gl_preview_test_screen.dart`, Réglages → Développeur → « Aperçu GPU »).
  Le vrai Oneshot n'est PAS encore touché.

## Objectif de la session — Étape 5a

**Décisions de Jay en ouverture :**
1. On attaque **5a maintenant** : aperçu + photo GPU dans le vrai Oneshot,
   remplacement à comportement identique (photo double simultanée).
2. **Vidéo double dans le vrai Oneshot (5b) = chantier dédié PLUS TARD** (pas
   enchaîné après 5a). Raison : 2 vidéos/card à stocker/lire, upload (limite
   Supabase 50 Mo/fichier, RAPPELS #7), lecteur double dans le viewer.

## Modifications apportées

(en cours)

## Difficultés / erreurs

(en cours)

## Notes et consignes de Jay

- 5a maintenant ; 5b (vidéo Oneshot) plus tard, chantier dédié.
