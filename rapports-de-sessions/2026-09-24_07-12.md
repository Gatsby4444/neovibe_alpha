# Session du 2026-09-24 — 07:12

## 0. Reprise

- Relus : rapport `2026-09-23_12-01.md` (§9-10, « pour la prochaine
  session »), mémoire « next focus ».
- Vérifié en base : aucun nouveau diagnostic dans `dev_reports` depuis
  `1e2ec5af` (2026-09-23 09:58 UTC) — le test à deux téléphones n'a pas
  encore eu lieu.

## 1. Modifications apportées — v0.9.252 : l'annonce BLE passe en non connectable

Jay : *« Ok pour le 1. Mais revérifie que cela ne sert à rien d'autre ! »*

**Vérification avant de couper (les deux sens, règle 8)** :
- sortant : plus aucun code GATT dans le dépôt (`connectGatt`,
  `openGattServer`, `BluetoothGatt` : zéro hors docs/rapports) ; permission
  `BLUETOOTH_CONNECT` déjà absente du manifeste (retirée le 2026-08-27) — sans
  elle, Android 12+ refuse toute connexion de toute façon ;
- entrant : aucune réponse de scan fournie (`null` à `startAdvertisingSet`) ;
  le récepteur ne lit que `getManufacturerSpecificData(0xFFFF)` — ni
  `isConnectable`, ni `advertiseFlags`, ni position fixe d'octet (grep :
  zéro) ; aucun plugin BLE Dart ; la version du protocole ne change pas
  (5), donc un téléphone en v0.9.251 et un en v0.9.252 se voient.
- effets du mode connectable qui restaient : 3 octets de drapeaux, un
  téléphone auquel n'importe quel appareil pouvait se connecter, des réponses
  de scan vides.

**Fichiers** :
- `BleEngine.kt` : `neoAdvertParams()` — **un seul endroit** pour les
  paramètres d'un jeu d'annonces (legacy, non connectable, non scannable,
  250 ms, puissance moyenne) ; mode cycle `.setConnectable(false)` ;
  commentaire « le chat passe par le GATT » retiré ; octets 27-29 → 24-26.
- `AdvertCapacityProbe.kt` : lit `neoAdvertParams()`. **Défaut trouvé et
  réparé au passage** : la sonde avait sa propre copie des paramètres, restée
  à `INTERVAL_LOW` (100 ms) quand la production est passée à 250 ms le
  2026-09-22 — alors que son commentaire affirmait « les mêmes paramètres que
  la production ».
- `AdvertCapacityStore.kt` : la signature d'une mesure inclut les paramètres
  d'émission (lus à leur source) — les plafonds appris en connectable sont
  oubliés d'eux-mêmes et réappris.
- `docs/annonce-ble-octet-par-octet.md` : 27 octets sur 31 (4 libres), tableau
  renuméroté ; le schéma du 2026-09-23 plaçait la puissance avant le
  fabricant, contraire au tableau vérifié — corrigé.
- `docs/donnees-du-ping.md` §D, `pubspec.yaml` (commentaire « GATT,
  NativeBle.kt » périmé) ; version 0.9.252+3126.

⚠️ **Non mesuré sur les ondes** : à confirmer au test à deux téléphones
(`neoScans` comparable ; nRF Connect : annonce non connectable, sans drapeaux).

## 1 bis. v0.9.253 — l'arrivée en soirée, interface de test (Développeur › Outils)

Jay : *« Fais un test activable depuis les paramètres développeur pour
l'initialisation en soirée de l'app. Propose-moi qqc dans l'esprit et la DA
NeoVibe. Carte blanche, c'est à part. »*

- `lib/features/arrival/` (nouveau) :
  - `arrival_flow.dart` — l'état et l'enchaînement (7 étapes : accroche,
    prénom, selfie, compte, autorisations, radar, dedans). **N'écrit rien** :
    ni compte, ni profil, ni présence. Lit les vraies soirées autour
    (`nearbyEventsProvider`, la plus proche à portée) ; sinon la démo,
    étiquetée. Le selfie (fichier temporaire de la caméra) est effacé à la
    reprise et à la sortie.
  - `arrival_permissions.dart` — la cuisine des autorisations, **par les
    chemins existants** : `LivePosition.requestPrecise`, `BleRadio.probe` +
    `requestRadioPermissions`, `permission_handler` (notifications, caméra).
  - `arrival_widgets.dart` — le halo (rond de lumière à anneau tournant aux
    couleurs de la signature), l'ambiance (deux lumières qui dérivent), le
    sonar, le bouton en dégradé, la progression. Aucune couleur écrite : tout
    vient de `NeoPalette`.
  - `arrival_screen.dart` — les écrans, toujours en identité sombre (« la
    soirée, c'est la nuit »). Selfie à la caméra frontale native
    (`NativeCameraController`), dans le rond, obligatoire.
- `lib/features/proximity/net/radio_permissions.dart` (nouveau) : la
  traduction « permission Android → permission_handler » sortie de
  `ping_screen.dart`, pour qu'elle n'existe qu'une fois.
- `developer_tools_screen.dart` : entrée « Arrivée en soirée (test) ».
- `test/arrival_flow_test.dart` : 4 tests (recul borné, prénom, soirée la plus
  proche à portée, démo sans position/soirée). Suite complète : 574 verts.

## 1 ter. v0.9.254 — inscription sans mail, bornée par le téléphone

Jay : *« ok fais le point 1 »* (plafond proposé : 3 comptes / téléphone /
30 jours, comptes de test à part).

- Natif : `NativeDeviceIdentity.kt` (canal `neovibe/device`, `androidId`),
  branché dans `MainActivity` — séparé de `NativeDiagnostics` (autre usage).
- Dart : `lib/core/device_identity.dart` (empreinte SHA-256 salée),
  `lib/features/auth/auth_repository.dart` (connexion + inscription avec
  l'empreinte). **Défaut réparé au passage** : `auth_screen.dart` appelait
  Supabase lui-même ; il passe maintenant par le dépôt.
- Base : migration `20260924100000_un_telephone_un_plafond_de_comptes.sql`,
  appliquée — `private.signup_rules` (1 ligne : 3 / 30 jours),
  `private.device_signups` (sans FK : supprimer un compte ne rend pas la
  place), `private.signup_device_exempt`, `public.hook_before_user_created`
  (exécutable par `supabase_auth_admin` seul), trigger
  `record_device_signup` sur `auth.users`.
- Config auth (API de gestion) : hook activé **et** `mailer_autoconfirm`
  activé, dans le même appel.
- **Vérifié sur le vrai serveur** par de vraies inscriptions : sans empreinte
  → 400 ; 3 comptes acceptés avec session immédiate ; 4ᵉ → 429 ; empreinte
  exemptée → acceptée ; après suppression des comptes, le registre les compte
  toujours. Comptes et lignes de test supprimés (vérifié : 0 et 0).
- Tests : `test/device_identity_test.dart` (vecteur calculé hors Dart).
- Docs : `docs/inscription-et-appareil.md` (nouveau),
  `docs/parties-natives-par-os.md` (ligne `neovibe/device`, équivalent iOS
  plus faible), RAPPELS #166.

## 1 quater. v0.9.255 — le thème Vice6

Jay : *« appliquer le thème du test exactement de la manière dont tu l'as
codé, avec des effets d'ombres lumineuses autour des éléments, les couleurs
des boutons et le fond dégradé […] à un nouveau thème pour toute l'app, qui
s'appellera vice6 […] carte blanche pour tout »*.

- `palette.dart` : identité `vice6` (clé `vice6`), **palette = `sombre`
  exactement** (celle du test) ; deux effets nommés : `lumieresDerivantes`
  et `lueurs` ; `fondVivant` = cycle **ou** Vice6 (un seul critère pour
  rendre les Scaffold transparents).
- `core/widgets/ambience.dart` (nouveau, `NeoAmbience`) : les lumières du
  test, sorties de `arrival_widgets.dart` pour servir aux deux — **15 images/s
  par minuterie, pas 60**, arrêt hors du premier plan, `RepaintBoundary`.
  Posée sous toute l'app par `MaterialApp.builder` (`app.dart`), comme le
  fond du cycle. Coût **calculé, pas mesuré**.
- `theme.dart` : `neoGlow()` (le halo de `GlowButton`), extension
  `context.lueurs` ; sous Vice6 : boutons pleins et contour **en pilule avec
  halo**, cartes translucides qui rayonnent (ombre couleur d'action, bord
  lumineux), dialogues et feuilles du bas cerclés de lumière, indicateur de
  navigation plus vif.
- `gradient.dart` : sous Vice6, l'anneau des photos, les pastilles, le bouton
  flottant, les icônes et textes en dégradé (dont l'onglet sélectionné)
  rayonnent.
- Réglages › Apparence : Vice6 apparaît seul dans la liste (elle lit
  `NeoIdentity.values`) ; sa pastille d'aperçu rayonne.
- `test/vice6_theme_test.dart` (3 tests). Suite complète verte.

## 1 quinquies. v0.9.256 — Vice6 plus vif

Jay : *« mets la version plus vive dans toute l'app »*. Vérifié avant : le
fond de Vice6 était identique à celui du test (mêmes couleurs, forces 30 % /
26 %, mêmes trajets), sauf l'écran « Tu es dedans » à force ×1,4. Constante
`NeoAmbience.soiree = 1.4`, utilisée par l'app (Vice6) et par cet écran.

## 1 sexies. v0.9.257 — l'arrivée en soirée et l'écran de soirée, pour de vrai

Jay : *« l'UI de test est validée, on l'implémente ! »* ; choix : **tout
d'un coup** (soirée + inscription), **un vrai titre** pour les Vibes, **la
scène de nuit toujours** en soirée.

**① Le titre d'une Vibe de Drop**
- Migration `20260924140000_le_titre_d_une_vibe_de_drop.sql`, appliquée en
  une transaction : `library_vibes.title` (1-60, rogné) ; `p_title` ajouté
  aux deux fonctions (supprimées puis recréées : relevé avant — aucun
  appelant SQL, droits d'origine recopiés et **vérifiés identiques** après).
- `LibraryVibe.title` / `challengeId`, `addVibe(title:)`, champ « Un titre
  (facultatif) » sur l'écran d'ajout (Drop d'événement seulement).
- **Défaut réparé** : l'écran d'ajout faisait `popUntil(isFirst)` (le
  commentaire disait « retour au chat », le code ramenait à l'accueil — et
  depuis la soirée, il en faisait sortir). Remplacé par `pop()`.

**② L'écran de soirée et l'entrée**
- `core/widgets/stage.dart` (déplacé de `arrival_widgets.dart`, classes
  renommées `StageHalo`, `StageTitle`, `StageChip`, `StageProgress`) +
  `NightStage` (thème sombre forcé + lumières).
- `event_finder_screen.dart` (nouveau) : le radar sur les vraies soirées,
  `VenueFoundView` (« Tu es au … » + « Rejoindre la soirée », via
  `rejoindreEvenement`), `MyStageHalo` ; entrée « Trouver ma soirée » en tête
  de l'écran Événements.
- `event_screen.dart` **réécrit** : « Tu es dedans. », points chauds, cadre
  des présents → `event_people_sheet.dart` (nouveau, la liste et les rôles
  de l'ancien écran, inchangés), Drop en grille (case « Ta Vibe ici » +
  `LibraryVibeTile` avec titre / auteur / heure / défi), défis, bouton
  superposé ; chat et carte en haut, réglages / inviter / quitter / fermer
  dans « ⋯ », récap une fois fermé. Tableau ancien → nouveau en tête du
  fichier.
- `LibraryVibeTile` : la tuile de Drop rendue publique (un seul chemin pour
  précharger et ouvrir), avec légende facultative.

**③ La vraie inscription**
- `arrival_flow.dart` : **deux modes, deux mémoires**
  (`arrivalFlowProvider(mode)`, famille sans `autoDispose`) ; mode réel :
  `begin`, `keepSelfie`, `createAccount` (compte → profil → photo),
  `finish` ; `arrivalWantsFinderProvider` (le radar s'ouvre une fois).
- `AvatarService.squareFromPhoto` : le selfie en carré 512 PNG.
- `arrival_screen.dart` : le mode transmis par un `InheritedWidget` (lu sans
  abonnement, pour les `initState`) ; accroche avec « J'ai déjà un compte » ;
  erreurs du serveur affichées au-dessus du geste.
- `app.dart` (`RootGate`) : pas de compte ou pas de profil → le parcours ;
  **une inscription en cours garde la main** ; à la fin, le radar.
- `auth_screen.dart` : **connexion seule**, se referme une fois connecté.
- `onboarding_screen.dart` **supprimé** (seul appelant : `RootGate`).
- 🔴 **Relevé en base** : le nom affiché est **unique**
  (`profiles_username_unique`). Géré (retour au prénom avec un message,
  compte et selfie gardés) ; décision produit → RAPPELS #167.
- Tests : `arrival_flow_test.dart` passe à 9 (règles du mode réel). Suite
  complète : 586 verts.
- Docs : `docs/arrivee-en-soiree.md` (nouveau), `docs/evenements.md` §8 et
  §11.

## 1 septies. v0.9.258 — username unique, pseudo libre

Jay : username unique obligatoire à l'inscription, pseudo facultatif non
unique, pseudo affiché s'il existe sinon username, réglage « pseudo ou
username » pour les groupes et les autres, **username toujours sur les
publications**, limites de caractères. Choix : format Instagram, username
3-20, pseudo 1-30.

- **Relevé en base avant d'écrire** : les deux colonnes existaient déjà
  (`display_name` unique = username ; `tag_name` = pseudo, 1-30) ; manquaient
  le format, le réglage et son application.
- Migration `20260924180000_username_unique_et_pseudo_libre.sql` : usernames
  convertis au format, contrainte `^[a-z0-9._]{3,20}$`, `show_pseudo`,
  colonne calculée `pseudo_shown` ; les 5 fonctions qui donnent un nom aux
  autres lisent `pseudo_shown` (même signature). `admin_users` garde le brut.
- 🔴 **Erreur commise, réparée** : voir §2.
- Vérifié sous l'identité de Charles (rôle `authenticated`) : les fonctions
  répondent ; un pseudo caché donne `pseudo_shown = null`.
- App : `core/username.dart` (règles + conversion de frappe),
  `Profile.showPseudo` / `pseudoShown`, `chatName` les respecte ; carnet
  d'amis `tag_name:pseudo_shown` ; `ProfileRepository.create(tagName:)` et
  `setShowPseudo` ; réglage « Montrer mon pseudo » (Sécurité et
  confidentialité) ; édition du profil (@username formaté, pseudo) ; stories
  (visionneuse, barre) et Drop de soirée passent au **username** ;
  inscription : étape « Choisis ton username » + pseudo facultatif.
- Tests : `test/username_test.dart` (5), `arrival_flow_test.dart` mis à jour.
  Suite : 591 verts.

## 1 octies. v0.9.259 — la tuile « Autour de moi » écrasée

Capture de Jay : le titre de la soirée s'affichait **une lettre par ligne**, et
un énorme « Je suis là » au milieu. Cause : un `FilledButton` en `trailing`
d'un `ListTile` — le thème lui donne une largeur minimale infinie
(`Size.fromHeight`), il écrase le titre. Défaut antérieur à Vice6. **Cause
retirée** : plus de bouton dans la tuile ; la toucher ouvre « Tu es au … »
(`VenueFoundView`), le chemin unique pour entrer. Inventaire : c'était le seul
bouton plein en `trailing` de toute l'app.

Idées de Jay pour après l'UI (mode restreint par la position, démarrage à
froid sans BLE) → RAPPELS #168.

## 1 nonies. v0.9.260 — Drop en direct, générique de fin, finitions

Consignes de Jay : *« fais dès maintenant le 2 »* (le direct) ; 5 : oui au
username vérifié et à la photo temporaire, **non au bouton « Créer une
soirée »** — proposer de réessayer ; 6 : *« fais-le, fais-moi rêver, carte
blanche »* ; 7 : *« lorsque je le prends je me vois comme dans un miroir, mais
enregistré je me vois dans l'autre sens […] il faut garder la vue miroir »*.

- **Selfie en miroir** : `_SelfieImage` (affichage retourné) et
  `AvatarService.squareFromPhoto(mirror: true)` (photo de profil retournée).
  Le réglage `selfieMirrorProvider` (caméra des Vibes, aperçu seulement) n'est
  pas concerné.
- **Username vérifié pendant la frappe** : `public.username_available`
  (migration `20260924200000_username_libre.sql`, ouverte à `anon`, rend un
  booléen) — essayée sans compte : « charles » pris, « charles.nv » libre ;
  `ProfileRepository.usernameAvailable`, `ArrivalState.usernameCheck`
  (400 ms après la dernière touche, réponse périmée ignorée), ligne « @x est
  libre » / « Déjà pris ».
- **Photo temporaire** : `ArrivalAvatarPref` (le chemin déposé à
  l'inscription) ; sur mon profil, « Ta photo, c'est ton selfie d'arrivée »
  (Changer / Je la garde) tant que la photo EST ce chemin.
- **Radar sans soirée** : plus de « Créer une soirée » ; le bouton est
  « Chercher encore ».
- **Le Drop en direct** : `conversationLibraryLiveProvider` (écoute des
  messages `library_add` de la conversation — déjà diffusés, déjà filtrés par
  la politique des membres) ; **les défis en direct** :
  `eventChallengesLiveProvider` + migration
  `20260924210000_les_defis_en_direct.sql` (`event_challenges` ajoutée à la
  diffusion). Les présents étaient déjà en direct (`event_presences`).
  ⚠️ **Non éprouvé à deux téléphones.**
- **Le générique de fin** : `event_recap_screen.dart` (story en 5 images :
  « C'était … », chiffres qui comptent, Drop qui zoome, constellation des
  présents, « Ta soirée est gardée »). Ouvert à la fermeture sous mes yeux
  (`ref.listen` dans l'écran de soirée), par la notification « c'est fini »
  (payload `recap:<id>`, `main.dart`), et par « Revivre la soirée ».
- Tests : `event_recap_test.dart` (1), `arrival_flow_test.dart` (+3 : libre
  puis pris, réponse tardive ignorée, format invalide). Suite : 595 verts.

## 2. Difficultés / erreurs

- 🔴 **Usernames réduits à 3 lettres (2026-09-24, migration username)** :
  `rpad(v, 3, '0')`, écrit pour compléter les noms trop COURTS, **coupe aussi
  les noms trop longs** (« charles » → « cha »). Appliqué directement sur la
  base de dev, sans essai à blanc. Vu en relisant la base juste après.
  **Réparé** par `20260924181000_usernames_repares.sql` : noms d'origine
  reconstruits exactement (mails des 8 comptes réels relevés juste avant ;
  formule de `tool/figurants.sql` pour les figurants), passage par un nom
  provisoire, même conversion corrigée. Un premier essai de réparation a
  échoué (noms provisoires identiques : les identifiants des figurants
  commencent tous pareil) — **sans dégât**, la transaction a tout annulé.
  ➡️ **Leçon** : une migration qui RÉÉCRIT des données s'essaie d'abord dans
  une transaction terminée par `rollback`, en relisant le résultat, AVANT le
  `commit`. Et une fonction de chaîne s'essaie sur un cas long ET un cas
  court.

- Un script Python passé en heredoc Bash a échoué (« unexpected EOF ») : les
  gros patchs passent par un fichier écrit avec Write (déjà noté en mémoire :
  le Bash de cette machine altère certains caractères).
- Test Vice6 d'abord rouge : changer de thème s'anime (200 ms) et le test
  lisait le bouton en plein fondu. `pumpAndSettle` avant la lecture.

*(rien pour l'instant)*

## 3. Consignes de Jay

- Point 1 validé, avec vérification exigée que le mode connectable ne sert à
  rien d'autre.
- Point 2 : *« propose-moi un onboarding flow digne de ce nom, tu as carte
  blanche pour cette partie »*.
- Point 3 (carte) : plus tard, pas l'essentiel aujourd'hui.
- Réponses sur l'arrivée : ① confirmation du mail retirée **mais** avec une
  sécurité sur l'appareil lui-même (l'identifier, l'empêcher de créer des
  comptes en rafale) ; ② le QR code : plus tard, il renverra simplement vers
  le Play Store — *« arrête de parler du QR code, ce n'est pas la
  priorité »* ; ③ **le selfie est obligatoire** et devient la photo de profil
  temporaire.
- Sur l'interface de test de l'arrivée : *« j'adore la DA proposée pour le
  test, c'est vraiment parfait »* — référence visuelle pour la suite du mode
  événement (halo, scène de nuit, une idée par écran).
