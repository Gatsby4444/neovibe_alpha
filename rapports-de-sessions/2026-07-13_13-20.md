# Rapport de session — 2026-07-13 13:20

## Contexte

Retour de test de Jay sur la v0.6.1 : la plupart des problèmes réglés, mais
un bug résiduel sur l'appui/relâchement vidéo, plus une série de modifs
UI/UX du profil, une demande d'explication (sécurité BLE token) et une
demande d'analyse (utiliser du natif Android pour les fonctionnalités que
Flutter ne couvre pas).

## Modifications apportées

### 1. Bug vidéo « verrou fantôme » (card_capture_screen.dart)

Symptôme rapporté : appui 180 ms → la vidéo démarre ; relâcher (sans avoir
glissé) → la vidéo ne s'arrête PAS et continue sans aucune UI de verrou ;
pour l'arrêter il faut un nouvel appui long 180 ms (un simple tap ne fait
rien).

**Cause identifiée** : le `setState` de `_startVideo` (déclenché DANS
`onLongPressStart`) masque plusieurs enfants conditionnels du `Stack`
(sélecteur de type, boutons galerie/tableau/bascule) et en ajoute un
(chrono). Aucun enfant du Stack n'avait de clé → l'algorithme de diff de
Flutter apparie les enfants PAR POSITION : l'élément du bouton déclencheur
était recréé au rebuild, et l'ancien `LongPressGestureRecognizer` — celui
qui suivait le doigt — était détruit en plein geste. Son `onLongPressEnd`
n'était jamais délivré → `_pressHeld` restait bloqué à true, la vidéo
continuait. Le 2e appui long passait par le recognizer tout neuf, dont le
`onLongPressEnd` (au relâchement) stoppait la vidéo — d'où l'impression
qu'il faut « réappuyer 180 ms ».

**Leçon : ne jamais laisser un `setState` déclenché pendant un geste
modifier la LISTE des enfants d'un Stack sans clés — poser des `ValueKey`
sur chaque enfant dès qu'un geste continu (long press, drag) coexiste avec
des enfants conditionnels.**

**Correctifs** :
- `ValueKey` sur chaque enfant du Stack de capture (l'élément du
  déclencheur survit au rebuild, le recognizer reste vivant).
- Filet de sécurité dans `_onShutterTap` : un simple tap arrête aussi un
  enregistrement « orphelin » (`_recording && !_pressHeld` sans verrou).

### 2. Refonte layout profil (consignes Jay du jour)

- `profile_header.dart` réécrit : PP à gauche, stats (amis/posts/cards 7 j)
  à droite de la PP sur la même ligne ; en dessous, alignés à gauche :
  username (inchangé, titleLarge gras), tag name SANS guillemets ni mention
  « en conversation », typo distincte (italique, blanc 60 %, letterSpacing
  0.6 — à ajuster selon retour de Jay), puis bio.
- Bio repliable : au-delà de 130 caractères, coupe + « voir plus » (même
  typo que la bio, blanc plus lumineux avec léger halo, cliquable) ;
  déplié : bio complète + « voir moins » à la fin. Implémenté en
  `Text.rich` + `TapGestureRecognizer` (widget `_ExpandableBio`).
- Limite bio 300 → **500 caractères** : `maxLength` dans
  `profile_edit_screen.dart` + migration `profiles_bio_500` (contrainte
  check en base, appliquée sur le projet dev + versionnée dans
  `supabase/migrations/`). Vérifié en SQL : contrainte unique à 500.
- Guillemets autour du tag name aussi retirés dans `friends_list_screen.dart`
  et `request_popup.dart` (cohérence avec la consigne).

### Vérifications

`dart format` 0 changement, `flutter analyze` 0 issue.

## Release

- **v0.6.2** (relâchement vidéo fiable + nouveau layout profil) : APK
  58,2 Mo, signé debug —
  https://github.com/Gatsby4444/neovibe_alpha/releases/tag/v0.6.2
- Build : `JAVA_HOME` doit être posé explicitement dans la session
  PowerShell (`C:\Charles\dev\jdk17`), sinon Gradle échoue immédiatement.

## Difficultés rencontrées

- Le nom auto-généré de la contrainte bio (`profiles_bio_check`) a été
  vérifié en SQL après la migration (un `drop if exists` sur un mauvais nom
  aurait laissé coexister les contraintes 300 et 500).

## Notes et consignes de Jay

- Retour v0.6.1 : tout est réglé sauf le bug vidéo ci-dessus (décrit très
  précisément par Jay : pas le mode verrouillage, aucune UI de verrou,
  arrêt seulement par nouvel appui long).
- Profil : PP à gauche / stats à droite de la PP / username puis tag name
  puis bio à gauche sous la PP ; pas de guillemets ni « en conversation » ;
  typo du tag name modifiée ; bio 500 caractères max, repli à 130 avec
  « voir plus » / « voir moins » (voir plus = même typo que la bio mais
  plus blanc et lumineux).
- Jay demande une explication pédagogique du point de sécurité BLE token
  (fournie dans le chat, résumé : le token BLE est lisible par les simples
  croisés → risque de suivi passif ; correctif envisagé : rotation du
  token et/ou colonne masquée, volet anti-stalking).
- Jay demande d'étudier l'usage de code natif Android (platform channels)
  pour les mécaniques que Flutter/plugins ne couvrent pas (double caméra
  Oneshot, bascule caméra en cours de vidéo, etc.) — analyse fournie dans
  le chat, décision d'implémentation à venir.
- **Décisions de fin de session** : l'amélioration natif Android ET la
  sécurisation du BLE token (masquage + rotation) sont reportées à une
  prochaine étape — à reprendre en priorité quand Jay le demandera. La
  piste « pression du doigt » est **abandonnée définitivement** (pas
  nécessaire, ne plus la proposer).

## Suite de session — lancement des gros chantiers (v0.6.2 validée)

Test v0.6.2 concluant. Jay lance la discussion sur 3 chantiers, avec
demande explicite de questions de clarification PUIS d'un plan à approuver :

1. **Sécurité BLE + chat hors ligne** : double sécurité validée (cacher le
   token + rotation ~15 min « comme Google »). ET nouvel objectif produit :
   **la messagerie de proximité doit fonctionner hors ligne, sans
   internet** — textes et petits paquets directement en BLE ; à l'avenir,
   Wi-Fi Direct pour l'envoi de cards dans les conversations éphémères.
2. **Chantier caméra native** : passer à notre propre techno native de
   contrôle de l'appareil (double flux Oneshot, bascule en cours de vidéo).
3. **Optimisation ouverture des cards** (constat : chargements lents à
   l'ouverture et à chaque retournement de face) : (a) cards du créateur
   stockées en local, zéro requête serveur inutile ; (b) cards reçues :
   les deux faces préchargées en cache pour un retournement fluide ;
   éviction par durée ou taille max ; (c) cards « à restrictions » dans un
   chat : purge immédiate du cache/mémoire à la fermeture pour empêcher la
   récupération par manipulation de la mémoire du téléphone.

Questions de clarification posées dans le chat (réponses de Jay à
consigner ici), plan à établir ensuite pour approbation.

### Réponses de Jay — première série

**A. Chat hors ligne / BLE**
- **Vision recadrée** : la partie découverte/proximité fonctionne
  UNIQUEMENT en BLE — confidentialité (argument de vente), simplicité
  (« ça marche partout »), et **zéro coût serveur** pour une
  fonctionnalité essentielle. Tout est géré localement.
- Jay demande des idées pour que les appareils se **reconnaissent entre
  eux AVANT tout partage de profil**, sans possibilité de stalking par un
  programme tiers (réponse donnée dans le chat : IDs éphémères rotatifs +
  poignée de main chiffrée + clés de reconnaissance entre amis).
- **A1 (b)** : les inconnus sont inclus — c'est le cœur de NeoVibe.
  Modèle mixte BLE/serveur pour les demandes d'amis : demande envoyée en
  LOCAL via BLE, état accepté/refusé stocké sur les DEUX appareils, envoyé
  au serveur quand internet revient ; le serveur vérifie que les deux
  appareils rapportent le MÊME état, sinon la connexion est rejetée
  (anti-bypass). La messagerie internet (amis) n'est débloquée qu'après
  validation du lien par le serveur.
- **A2 (a)** : les messages hors ligne rejoignent la conversation normale
  (à re-préciser : tension avec le zéro coût serveur — question posée).
- **A3 (a)** : la messagerie de proximité passe TOUJOURS par le BLE
  direct — « fonctionnalité principale et indiscutable de l'app ».
- **A4** : anti-spam 3 messages appliqué côté RÉCEPTEUR : l'app du
  receveur bloque l'affichage des messages excédentaires ; une app émettrice
  modifiée peut envoyer plus, mais les messages ne seront jamais reçus.

**B. Caméra native**
- **B1** : remplacement complet confirmé.
- **B2** : ordre de priorité = 1) Oneshot double flux, 2) bascule caméra
  en cours de vidéo (Mono), 3) FLAG_SECURE anti-capture — avec, pour le
  3, une **option développeur dans les paramètres pour le désactiver**
  (Jay doit pouvoir prendre des screenshots pendant le dev).
- **B3** : Jay est sûr que son téléphone supporte la double caméra (son
  app photo native le fait) — modèle non précisé, mini-test natif prévu
  en amont de toute façon.

**C. Cache des cards**
- **C1** : cache plus long pour les cards où il RESTE des vues, SAUF les
  Hot (purge immédiate).
- **C2** : les cards du créateur = tout garder en local, avec un espace
  alloué PARAMÉTRABLE par l'utilisateur, la possibilité de voir le
  dossier de stockage, et repli cloud pour les plus anciennes si l'espace
  alloué devient trop petit.
- **C3** : plafonds cache contenu des autres = 200 Mo / **48 minutes**
  (au lieu des 48 h proposées).

Deuxième série de questions d'affinage posée dans le chat.

### Réponses de Jay — deuxième série

- **A5 — SÉPARATION TOTALE des deux messageries** : après réflexion de
  Jay, la messagerie ping (BLE, 100 % locale) et la messagerie d'amis
  (internet) sont **complètement séparées — aucune migration d'historique,
  jamais**. Argument : simplicité technique + « sécurité et
  confidentialité à 100 % des conversations » comme argument de vente.
  ⚠️ Ceci REMPLACE la décision du 2026-07-12 (trigger
  `connections_promote_prox` qui migrait la conversation prox vers Cercle
  avec historique) — le trigger et les conversations prox côté serveur
  deviendront obsolètes avec le passage du ping en BLE pur.
- **A6 (a)** : révélation automatique mutuelle des mini-profils quand les
  deux sont en mode visible (comportement actuel conservé).
- **A7** : (a) — remonter les croisements au serveur quand internet
  revient — mais Jay hésite avec (c) — preuve signée de croisement,
  présentée au serveur seulement à l'usage — et demande une réexplication
  de (c) avant de trancher (fournie dans le chat, avec recommandation (c)).
- **C4 (a)** : écran de gestion du stockage DANS l'app (emplacement,
  jauge, espace alloué réglable, bouton vider), stockage réel en espace
  privé de l'app — « la sécurité d'abord ».
- **C5** : validé — cache des cards avec vues restantes jusqu'au TTL 24 h
  du message dans la limite des 200 Mo ; Hot purgée immédiatement ; purge
  dès vues épuisées.

Troisième (dernière) série de questions posée : détection BLE en
arrière-plan (app fermée ?) et TTL local des messages ping.

### Réponses de Jay — troisième série

- **A7 : option (a) conservée** (croisements remontés au serveur) — les
  fonctionnalités prévues par Jay (notifications « vous avez croisé … il y
  a … », suggestions de profils croisés souvent, évolutions futures)
  nécessitent que le serveur connaisse les croisements. MAIS l'idée du
  certificat est retenue sous une autre forme : **certificat de
  croisement co-signé entre les deux appareils, ~10 s de proximité
  continue pour se créer** — double rôle : anti-fraude (impossible de
  prétendre avoir croisé quelqu'un sans la signature de son appareil) et
  anti-faux-positifs (passer à 80 km/h en voiture à côté de quelqu'un ne
  crée pas de croisement). Le serveur ne devra accepter QUE des
  croisements certifiés.
- Rappel de cadrage de Jay : ne pas perdre de vue sa vision produit — les
  fonctionnalités de rétention (notifications de croisement, suggestions)
  font partie du cœur de l'évolution prévue de l'app.
- **Q1 : option (b)** — détection BLE en arrière-plan (service +
  notification permanente). ⚠️ **Jay demande qu'on lui RAPPELLE plus tard
  la question de la consommation batterie** (consigné en mémoire
  prod-reminders).
- **Q2 : TTL 12 h** pour les conversations ping (au lieu des 24 h
  proposées).

### Décisions finales et feu vert

- **Q3 validé** : croisement = 10 s de contact BLE continu (certificat
  co-signé) ; en dessous = « presque » (FOMO). Seuil ajustable après
  tests terrain.
- **Q4 : option (a)** — ce chantier pose la fondation (certificats), les
  **streaks arrivent dans un chantier dédié ultérieur — NE PAS OUBLIER**
  (insistance explicite de Jay).
- **Feu vert de Jay pour coder la totalité** : refonte caméra native,
  gestion BLE/ping hors ligne, cache des cards — tout ce qui a été
  discuté et validé ci-dessus.
- Ordre d'exécution choisi : **C (cache) → B (caméra native) → A
  (BLE/ping)** — du plus petit gain immédiat au plus gros chantier, avec
  releases testables par chantier.

## Implémentation (en cours)
