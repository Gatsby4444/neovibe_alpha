# Session du 2026-09-23 — 12:01

Reprise : *« on en est où, je t'ai envoyé un diagnostic, analyse-le »*.

## 1. Lecture du diagnostic `1e2ec5af` (lu en base, `dev_reports`)

2026-09-23 11:58, **app 0.9.249+5123**, Xiaomi M2101K6G, pas de note.
`finesse : precise` ✅ (le rapport est donc lisible, #162).

### 🔴 A. La nuit : l'app entière est morte sans prévenir, deux fois

| Constat (journal du service) | Ce que ça dit |
|---|---|
| `cree` + `demarre par l'app` à 23:26:45 **sans `detruit` avant** | le processus était déjà mort entre 23:15 (dernière alarme) et 23:26 |
| aucune ligne entre 23:26:45 et 10:14:04, puis `cree` + `demarrage de l'app` | mort une seconde fois, ~11 h sans ping |
| `up=517354s` → `up=556193s` : +38 839 s = 10 h 47 | **le téléphone n'a PAS redémarré** : c'est l'app qui a été tuée |
| `detruit 0` · `relance par Android 0` · `tache retiree 0` | aucun adieu, aucune relance, pas de balayage des apps récentes détecté |
| aucune `alarme` après 23:26 (elles tombent ~toutes les 15 min avant) | les réveils programmés sont morts avec l'app |
| `chargeur=oui` à 23:26 | **être en charge n'a pas protégé** |
| `exempt=non` partout · `memoire basse` ×46 · 1188 Mo libres sur 5570 | pas d'exemption batterie, téléphone sous pression mémoire |

**Hypothèse, NON constatée** : un arrêt de type « arrêt forcé » par la surcouche
MIUI — c'est la seule sorte de mort connue qui annule aussi les alarmes. À
confirmer auprès de Jay (a-t-il balayé l'app ? « nettoyage » MIUI ?).

### 🔴 B. Ce matin : l'écoute Bluetooth s'est arrêtée à 10:53 et n'est jamais repartie

- `10:53:27 radio : failed — scan — Android a refusé d'enregistrer la détection`
  (`SCAN_FAILED_APPLICATION_REGISTRATION_FAILED`).
- Au relevé de 11:58 : `scanMode : aucun scan` → **1 h 05 sourd**.
- Chronologie : écran éteint 10:52:25 → une minute plus tard (10:53:25) la
  règle « écran éteint = écoute au quart du temps » (v0.9.24x, 2026-09-22)
  arrête puis relance le scan → refus 2 s après. **Lien probable, non prouvé.**
- **Constaté dans le code** (`BleEngine.onScanFailed`) : ce code d'erreur n'a
  **aucune reprise** — seuls `ALREADY_STARTED` (une fois par session) et
  `SCANNING_TOO_FREQUENTLY` repartent seuls. Rien ne relance jamais le scan
  ensuite, sauf un redémarrage du service ou du Bluetooth.

### 🔴 C. Deux instruments qui mentent sur B

1. `11:34:26 radio : running` : le journal ne note que le **type** d'état
   (`ProximityService.onStatus`). `Running(advertising=true, scanning=false)`
   s'écrit « running » — la ligne dit « tout va bien » alors que l'écoute est
   morte.
2. La LECTURE en tête du diagnostic (`proximity_diagnostic_screen.dart:226`)
   conclut **« Ton écoute fonctionne »** à partir de `rawScans` (22 969), un
   compteur cumulé depuis 10:14 — sans regarder `scanMode : aucun scan`.

### Le reste

- Position : `precise`, `± 64 m`, 2 relevés en 10 s — correct, probablement en intérieur.
- `neoScans : 0` : normal, pas de second téléphone.
- Carte : à 10:28, ~30 tuiles OpenStreetMap coupées d'un coup
  (`Connection reset by peer`) — une seule rafale, cause non établie (réseau
  ou limitation OSM) ; relève de #157.
- L'erreur `ref` après démontage du journal caméra date d'une v0.9.198 :
  ancienne, hors sujet.

## 2. Modifications apportées — v0.9.250 (Jay : *« ok pour 2 et 3 »*)

**B — la reprise de l'écoute** :
- `ble/ScanRecovery.kt` (nouveau) : décision pure — `REGISTRATION_FAILED`,
  `INTERNAL_ERROR`, `OUT_OF_HARDWARE_RESOURCES` se réessaient à 5 s, 30 s,
  2 min, puis toutes les 5 min tant que le ping est voulu. Jamais d'abandon.
- `BleEngine.kt` : `onScanFailed` libère l'enregistrement et programme
  `repriseScan` ; annulée par `teardown()` et `start()` ; la panne se clôt à la
  première annonce reçue. Trois instruments : `scanRefus`,
  `scanPanneDepuisMillis`, `ecouteVoulue`.
- ⚠️ **Non reproduit** : le refus n'a pas été rejoué sur appareil (un seul
  téléphone, cause Android). Le correctif ne dépend pas de la cause — il ferme
  un chemin sans issue constaté dans le code — mais son efficacité reste à
  voir au prochain diagnostic (`scanRefus` > 0 suivi d'un `scanMode` actif).

**C — les instruments** :
- `ProximityService.onStatus` : le carnet écrit `running (ecoute coupee)`
  quand le service tourne sans écouter (l'émission reste hors du libellé,
  elle bascule à chaque rotation).
- `lib/core/diagnostics/radio_reading.dart` (nouveau) : **une seule** lecture
  pour l'écran et le rapport (deux copies existaient, avec le même défaut) ;
  l'état présent (`scanMode`) passe avant le cumul (`rawScans`).
- `diagnostic_bundle.dart` : les trois nouveaux compteurs rangés à côté de `scanMode`.

**Vérifications** : `flutter analyze` 0 problème ; **566 tests Dart verts**
(+6, `test/radio_reading_test.dart`) ; `ScanRecoveryTest` 4/4 ;
contre-épreuve faite (branche `aucun scan` cassée → 3 tests rouges, restaurée
→ verts). APK construit par `tool/build-release.sh` : versionCode 5124,
V3.1 CN=NeoVibe. `docs/parties-natives-par-os.md` : entrée `ScanRecovery.kt`.

## 3. Difficultés / erreurs

- Requête sur `profiles.username` refusée (400) : colonne inexistante ou autre
  nom — sans importance, requête refaite sans jointure.
- Édition de `diagnostic_bundle.dart` par script Python en heredoc : échouée,
  **le Bash a mangé les antislashs** (piège déjà consigné). Refait par Edit.
- En retirant l'ancienne lecture du rapport, j'ai supprimé la variable `neo`
  dont un bloc plus bas (`selfScans`) dépendait — attrapé par `flutter analyze`.
  Règle 8 : relever les deux sens aussi pour une variable locale.

## 4. Consignes de Jay

- *« ok pour 2 et 3 »* — reprise de l'écoute + instruments.
- Point A (mort de nuit) : *« je ne me rappelle pas mais c'est possible que ce
  soit moi. Pas sûr… »* — cause non établie. Pour la prochaine nuit : ne pas
  balayer l'app des récentes, et noter si on le fait.

## 5. La carte — questions de Jay (explications, aucun code)

- Position (gratuite, le téléphone) ≠ fond de carte (un fournisseur qui
  dessine et sert les rues). OpenStreetMap interdit l'usage d'une vraie app.
- Snap loue son fond à **Mapbox** (style sur mesure dérivé de « Outdoors » +
  satellite, géocodage), sources : blog Mapbox. Proposé : comparer Mapbox,
  Google, MapTiler sur leurs pages officielles. Pas encore tranché.

## 6. Document de l'annonce BLE, octet par octet

Jay : *« écris le doc complet et supprime l'ancienne version obsolète »*.

- `docs/annonce-ble-octet-par-octet.md` (nouveau) : les 31 octets.
  **Vérifié à la source** : notre code (`advertDataFor`, `BleConstants`) et le
  code source d'Android 13 (`AdvertiseHelper.advertiseDataToBytes` : fabricant
  puis puissance ; `btm_ble_multi_adv.cc` l. 739-749 : drapeaux insérés en tête
  si l'annonce est connectable). **30 octets occupés sur 31.** Pas encore lu
  sur les ondes (nRF Connect, week-end).
- Anciennes versions retirées : section D de `donnees-du-ping.md` remplacée
  par un renvoi (le reste du fichier — secrets, jetons, temps, disque — reste
  valable, donc pas supprimé entier) ; « on en occupe 28 » corrigé en 30 dans
  `moteur-spatial-et-transports.md` ; `cours-le-ping.html` précisé.
- 🔴 **Erreur de ma part, corrigée dans le doc** : dans le chat j'avais placé
  la boîte « puissance » avant la boîte fabricant ; le code d'Android montre
  l'inverse. C'est la lecture de la source qui l'a révélé.
- 🔴 **Défaut probable trouvé, NON mesuré** : le récepteur lit
  `ScanResult.txPower` (en-tête des annonces étendues, 127 pour une annonce
  classique) au lieu de `scanRecord.txPowerLevel` (notre boîte). Si confirmé,
  la distance tourne toujours sur la valeur supposée et la boîte « puissance »
  ne sert à rien. Instrument à poser avant correctif (règle 7) — proposé à Jay.
- Reste signalé : le mode « connectable » ne sert plus depuis la suppression
  du GATT (2026-08-27) ; commentaire périmé dans `BleEngine.kt`. Pour le week-end.

## 7. v0.9.251 — l'instrument de la puissance d'émission

Jay : *« ok pose le compteur de puissance pour le week-end et après on passe à
autre chose »*. Explications données avant : pourquoi la v6 (tri par la puce,
identifiant 16 bits du SIG) et comment elle tient en 31 octets (NV et la boîte
puissance disparaissent ; même forme que l'annonce Covid).

- `BleEngine.onScanResult` : par annonce NeoVibe d'un autre appareil à la
  bonne version, compte `txPower` présent (en-tête) et `scanRecord.txPowerLevel`
  présent (boîte), avec les dernières valeurs. **Ne change pas `tx`** — pas de
  correctif avant la mesure. Remis à zéro dans `start()` avec les autres.
- `ProximityService.stats()` + ordre du diagnostic ; `lirePuissance` dans
  `radio_reading.dart` écrit la conclusion (confirmé / calibré / aucun).
- 570 tests Dart verts (+4), analyse propre, Kotlin compile.
- **À lire au test du week-end** (deux téléphones obligatoires).

## 8. Le plan « démarrage par la soirée » — avis externe de ChatGPT, transmis par Jay

Jay : les fonctionnalités de rétention sont là, il manque le **cold start** ;
plan : on rejoint la soirée avec le strict nécessaire, on voit les présents
(constellation) et la galerie, on publie une Vibe dans l'événement, 3 jours
pour retrouver les croisés, identifier des gens sur ses Vibes, puis NeoVibe
propose des activités (laser game) qui refont se croiser. Discussion
ChatGPT jointe (tronquée à la fin, « Event Engine »). Aucune décision.

Relevé dans le code pour l'analyse :
- **Inscription** : `auth_screen.dart` exige la **confirmation par email**
  avant la première connexion — sortie de l'app vers la boîte mail.
- **Identifier quelqu'un sur une Vibe** : n'existe pas.
- **Entrée par QR / billet** : rien dans le code.
- **Événement d'établissement** : pas de plateforme commerçant, création par
  SQL seulement (`tool/ouvrir_une_soiree.sql`).
- Existe : rejoindre, présents avec leur relation, Drop, défis, récap, carte.

## 9. Décisions de Jay sur le plan (fin de session)

- **Croisement d'inconnus** : un contact BLE **durable** et mutuel ; 184
  croisés si 184 contacts durables. Idée : s'inspirer de TousAntiCovid
  (algorithmes publics : ROBERT, GAEN) → RAPPELS #165. Amis : règles à part.
- **Présents visibles avec leur photo** : pas un problème, la photo de profil
  est publique.
- **Identification sur les Vibes** : écartée pour le moment.
- **Priorité** : les premières secondes, le mode événement mis en avant, le
  côté « Instagram » en retrait. Manquent pour le lendemain : **la mise en
  avant du Drop** et **un algorithme de suggestions** → RAPPELS #164.
- *« Pour aujourd'hui enregistre, fin de session. »*

## 10. Clôture

- Releases : **v0.9.250** (reprise de l'écoute + instruments), **v0.9.251**
  (compteur de puissance). Doc `annonce-ble-octet-par-octet.md`.
- `dart format` + `flutter analyze` propres ; **570 tests Dart verts** ;
  `ScanRecoveryTest` 4/4 ; Kotlin compile.
- `docs/parties-natives-par-os.md` : `ScanRecovery.kt` ajouté ; le compteur
  de puissance n'ajoute ni fichier ni méthode de canal (clés de `stats()`).
- Aucun secret dans les diffs.
- RAPPELS : #164, #165.

## Pour la prochaine session — dans cet ordre

1. **Week-end : le test à deux téléphones** (v0.9.251 sur les deux, position
   précise rouverte sur chacun). À lire : `scanRefus`/`scanMode`, la ligne
   « PUISSANCE », `neoScans`. Et nRF Connect pour lire l'annonce brute.
2. Retirer le mode « connectable » (3 octets, reste du GATT) — à éprouver le
   même week-end.
3. Les 30 premières secondes (#164) : inscription sans sortie vers les mails,
   entrée dans une soirée par lien/QR, écran d'arrivée de la soirée.
4. Le croisement durable (#165), après les mesures de RSSI/puissance.
5. Le fournisseur de carte (#157) : comparaison chiffrée Mapbox / Google /
   MapTiler, proposée, pas encore demandée.
6. Point A de la nuit (mort silencieuse) : prochaine nuit sans balayer l'app.
