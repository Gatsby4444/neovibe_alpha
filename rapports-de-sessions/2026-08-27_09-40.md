# Session du 2026-08-27, 09 h 40

## Point de départ

Relecture faite : `RAPPELS.md`, rapports du 2026-08-26 et 2026-08-25, mémoire
`project_neovibe_next_focus`. Vérifié à la source : `git tag` (dernier
**v0.9.134**), `pubspec.yaml` (`0.9.134+3008`), arbre de travail propre sur
`master`.

## Tâche de la session

Consigne de Jay : retirer **l'avatar du nouveau chemin** et **le bloc BLE mort**,
« en vérifiant en profondeur que rien d'autre n'est connecté à cela », en allant
« jusqu'au bout de chaque chaîne de chaque fonction ».

Arbitrages de Jay pendant la session :

- le défaut **B** du test du 2026-08-26 (bouton chat qui échoue entre amis)
  n'en est pas un : Jay avait déjà une conversation ouverte par l'ancien système
  BLE, l'erreur était donc attendue. **À revérifier au prochain test propre.**
- le défaut **C** (session refermée par l'entretien) est écarté.
- **Croisements** : « on ne garde que le certificat de croisement côté serveur ».
  Le certificat co-signé en BLE part avec le bloc.

## Vérifications à la source (règle : jamais dans un document)

### 1. L'avatar du nouveau chemin était DÉJÀ corrigé

Le rapport du 2026-08-26 disait « aucun [des trois défauts] n'est corrigé à ce
stade » — **faux au moment où je l'ai lu**. `ping_screen.dart:909` passe déjà par
le widget partagé :

```dart
leading: Avatar(stored: personne.avatarUrl, fallback: Text(initiale)),
```

`git log -S` situe le correctif dans **e18be74** (v0.9.132), écrit après la
section du rapport. Inventaire complet : plus aucun `NetworkImage` dans `lib/`,
et les 12 rendus d'avatar passent tous par `Avatar(stored:)`. **Rien à faire.**

C'est exactement le piège de la règle : le rapport ne se contredisait pas, il
décrivait ce qui était vrai à l'heure où il a été écrit.

### 2. Le certificat de croisement n'a JAMAIS abouti

Relevé en base, pas déduit :

| Fait | Source |
|---|---|
| `encounters.proof` a pour défaut `'certificate'` | `information_schema.columns` |
| La seule ligne existante porte `proof = 'mutual_sighting'` | `select * from encounters` |
| `report_sightings` écrit lui-même dans `encounters` | corps de la fonction |
| **Rien ne lit `proof`** — `can_view_profile` et `can_view_stories` ne testent que l'EXISTENCE de la ligne | corps des deux fonctions |

Donc le certificat BLE est **redondant** avec les constats mutuels, et il n'a
jamais produit une seule ligne serveur en deux mois. `RAPPELS.md` #72 le disait
« probablement supprimable » ; c'est désormais **prouvé**.

### 3. Ce que la suppression rend orphelin côté serveur

À reporter, pas à supprimer dans ce chantier :

- `report_encounter` et `submit_ble_connection` perdent leur dernier appelant ;
- `device_keys.ed_pub` perd ses deux seuls lecteurs (ces deux fonctions) — la
  colonne `x25519_pub`, elle, reste **essentielle** (jeton d'ami par paire) ;
- `ProximityIdentity.sign` / `.verify` perdent tous leurs appelants ;
- la branche `stories_public` de `can_view_stories` devient inatteignable pour
  un non-ami — mais **elle l'était déjà** : `report_sightings` exige
  `connections.status = 'full'`, donc une ligne `encounters` ne peut exister
  qu'entre amis, et `are_connected` couvre déjà ce cas.

## Modifications apportées

(en cours)

## Difficultés rencontrées ou erreurs commises

(en cours)

---

# Le retrait du bloc BLE mort

**~3 870 lignes supprimées, 684 ajoutées** (dont l'essentiel est du commentaire
expliquant ce qui est parti et pourquoi). 25 fichiers touchés.

## Fichiers supprimés en entier

| Fichier | Lignes | Ce qu'il portait |
|---|---|---|
| `lib/features/proximity/net/peer_link.dart` | 185 | découpage/réassemblage des trames sur un lien |
| `lib/features/proximity/net/secure_channel.dart` | 360 | X25519 → HKDF → AES-GCM, anti-rejeu |
| `lib/features/proximity/net/proximity_protocol.dart` | 553 | le format du fil, et les 8 messages applicatifs |
| `lib/features/proximity/net/transport_trace.dart` | 120 | le journal des pertes du transport |
| `test/peer_link_test.dart` | 132 | |
| `test/secure_channel_test.dart` | 509 | |

## Fichiers amputés

- **`peer_network.dart`** (833 → 290) — il ne reste que : scan → registre →
  événements de présence. Plus de `RadioCommands`, donc **le réseau ne donne
  plus aucun ordre à la radio**, il ne fait qu'écouter.
- **`peer_session.dart`** — `link`, `channel`, `connecting`, `profileSent`,
  `certified`, `hasChannel`, `release()`, `touch()`, `noteTraffic()` retirés.
  `PresenceStage.identifying` retiré : il ne peut plus être atteint.
  `identify()` ne rend plus un couple `(session, merged)` — il n'y a plus rien à
  refermer.
- **`proximity_controller.dart`** (1014 → 390) — devient `AsyncNotifier<void>`.
  Retirés : `_onMessage` et ses 8 cas, `_sweepCertificates`, `_counterSign`,
  `_storeEncounter`, `requestFriendship`, `respondToRequest`, `_onFriendRequest`,
  `_onFriendAccept`, `dismissOutgoing`, `sendMessage`, `wasRejectedBy`,
  `FriendRequestRefused`, `ProximityView`, `_RadioAdapter`.
- **`ping_store.dart`** — conversations locales (`PingConversation`,
  `PingMessage`, règle anti-spam, TTL 12 h) et croisements locaux
  (`LocalEncounter`). Il ne reste que la **file d'envoi**.
- **`proximity_journal.dart`** (11 433 → 3 700 caractères) — les deux modèles de
  demande d'ami et leurs 8 méthodes. Il ne reste que le **cooldown des waves**.
- **`ping_screen.dart`**, **`heart_screen.dart`** — les cartes de demande BLE, la
  section « Croisés récemment », les trois états du bouton « demander ».
- **`proximity_sync.dart`** — les cas d'envoi `encounter` et `connection`.
- **`diagnostic_bundle.dart`** — la section « CE QUE LE TRANSPORT A PERDU » et
  les quatre compteurs `clientPaths` / `serverPaths` / `bothPaths` /
  `bothPathsPeak`.
- **`ble_radio.dart`**, **`radio_status.dart`** — `connect`/`disconnect`/`send`
  et les événements `link`/`frame`.

## Côté natif

- **`BleEngine.kt`** (51 601 → 37 300 caractères) — serveur GATT, client GATT,
  files de notification, `connect`/`disconnect`/`send`/`mtuOf`, `pathStats`,
  `bothPathsPeak`, et **9 imports Bluetooth**.
- **`ProximityService.kt`** — les quatre relais vers le moteur, `onLink`,
  `onFrame`, la fusion de `pathStats` dans `stats()`.
- **`ProximityBridge.kt`** — les trois ordres et les deux événements.
- **`RadioStatus.kt`** — `SERVICE_UUID`, `RX_UUID`, `TX_UUID`, `CCCD_UUID`,
  `DEFAULT_MTU`, et **`BLUETOOTH_CONNECT` retirée de `required()`**.
- **`AndroidManifest.xml`** — `BLUETOOTH_CONNECT` retirée.

## Les décisions prises, et pourquoi

### Le côté receveur part aussi

`RAPPELS.md` #72 proposait de le garder « le temps que le parc se mette à jour ».
Il n'y a **que deux appareils de développement, tous deux à jour**, et aucune
production. Un serveur GATT ouvert qui accepte des écritures que plus rien ne lit
n'est pas une compatibilité — c'est une surface d'attaque sans lecteur, et
exactement la « règle la plus permissive qui gagne en silence » de `CLAUDE.md`.

### Le certificat de croisement part — arbitrage de Jay

> « on ne garde que le certificat de croisement côté serveur »

Ce que ça emporte : `_sweepCertificates`, `_counterSign`, `_storeEncounter`,
`LocalEncounter`, et **la section « Croisés récemment »** de l'écran Ping —
laquelle était **vide en permanence**, faute de certificat jamais abouti. La
rebrancher sur la table `encounters` est consigné dans `RAPPELS.md` #74.

### Ce qui est conservé délibérément, et signalé comme tel

`ProximityIdentity.sign` et `.verify` n'ont plus **aucun** appelant. Ils sont
gardés avec un commentaire daté, parce que les supprimer emporterait la clé
Ed25519, sa publication au serveur, `report_encounter`, `submit_ble_connection`
et `private.verify_ed25519` — et que deux chantiers écrits en dépendent
(`RAPPELS.md` #2). **Question posée à Jay dans `RAPPELS.md` #75.**

## Ce qui est prouvé, et ce qui ne l'est pas

| Vérification | Résultat |
|---|---|
| `flutter analyze` | **No issues found**, `lib/` et `test/` |
| `flutter test` | **178 tests au vert** |
| `./gradlew :app:compileDebugKotlin` | compile |
| `./gradlew :app:testDebugUnitTest` | au vert |
| Inventaire des 31 motifs supprimés dans `lib/`, `test/`, `kotlin/` | ne restent que les commentaires qui documentent le retrait |

🔴 **Le retrait de `BLUETOOTH_CONNECT` ne peut se confirmer que sur l'appareil.**
C'est le seul changement de ce chantier dans ce cas, et **il ne lèvera aucune
erreur** : la radio se tairait, simplement. Au prochain test, lire `rawScans` et
`advertMode` au diagnostic. Consigné dans `RAPPELS.md` #73, avec la marche à
suivre pour l'annuler seul.

## Ce que la suppression fait perdre en couverture de test

`test/peer_network_test.dart` faisait tourner **deux piles complètes** branchées
l'une sur l'autre par une radio simulée — poignée de main signée, révélation de
profil, chat, reconstruction de session, message fantôme. C'était le montage le
plus utile du chantier : il avait remplacé « deux téléphones dans les mains de
Jay » par quelques millisecondes de `flutter test`.

⚠️ **Ces tests ne pouvaient pas être adaptés : ils éprouvaient exactement ce qui
n'existe plus.** Et ce qu'ils protégeaient est passé au serveur — donc **prouvé
en base, sous identité utilisateur, plus jamais en test Dart**. C'est une
différence de nature à ne pas perdre de vue : la boucle de vérification du chemin
de proximité est passée de « automatique à chaque commit » à « manuelle, en
SQL ».

## Difficultés rencontrées

- 🔴 **J'ai annoncé « analyze propre » alors qu'il ne l'était pas.** Je filtrais
  la sortie de `flutter analyze` avec un `grep -E "^\s+(error|warning|info) "`
  pour écarter la liste des paquets périmés — et ce filtre **ne rendait rien**
  alors que cinq avertissements existaient (imports et déclarations devenus
  orphelins par le retrait). Je m'en suis aperçu en analysant **un seul fichier**,
  ce qui affiche la sortie brute.

  ⚠️ **La leçon est celle du projet** : *ce seau peut-il contenir la preuve du
  contraire ?* Un filtre qui rend zéro et un projet sans défaut sont
  indiscernables — c'est exactement l'instrument qui impose son zéro. **Lire la
  ligne de conclusion de l'outil** (`N issues found` / `No issues found!`), jamais
  un décompte que j'ai fabriqué moi-même.

  Corrigé : 6 avertissements au total (5 puis 1 en cascade), tous des orphelins
  du retrait — `unused_import` × 4, `unused_local_variable`, `unused_element`.
  Ils confirment au passage l'intérêt de la règle : chacun était un nœud dont
  j'avais coupé le dernier appelant sans m'en rendre compte.

- **Un `heredoc` bash avec du texte français long échoue par intermittence**
  (`unexpected EOF while looking for matching '`). Contourné en écrivant les
  scripts Python dans un fichier du scratchpad, puis en les exécutant. À faire
  d'emblée pour toute retouche de plus de quelques lignes.
- **Un motif de remplacement laissé en place par erreur** (`onLinkPLACEHOLDER`,
  reste d'un brouillon) a été écrit dans `ProximityBridge.kt` avant d'être
  annulé. Cause : le script y appliquait un renommage sans utilité. Parade
  adoptée pour la suite : chaque script **échoue** si un motif est introuvable
  (`raise SystemExit`) et n'écrit le fichier qu'après avoir appliqué **tous** ses
  remplacements — ce qui a évité les fichiers à moitié modifiés.

## Notes et consignes de Jay

- Le défaut B du test du 2026-08-26 (bouton chat qui échoue entre amis) **n'en
  est pas un** : une conversation avait déjà été ouverte par l'ancien système
  BLE. **À revérifier au prochain test propre.**
- Le défaut C (session refermée par l'entretien) est **écarté**.
- « on ne garde que le certificat de croisement côté serveur ».
- Jay ne peut pas tester à deux appareils avant quelques heures.

---

# Seconde passe de vérification — demandée par Jay

> « revérifie que tout est bien propre, respecte la règle de dissociation et que
> tu n'as pas débranché ou cassé d'autres nœuds et fonctions. »

Elle a trouvé **quatre nœuds orphelins**, dont **un vrai défaut fonctionnel que
j'avais introduit**. Tous corrigés.

## 🔴 ① Le « tirer pour rafraîchir » de l'écran Ping ne faisait PLUS RIEN

En retirant la lecture des fichiers locaux, j'avais remplacé `_reload()` par :

```dart
ref.invalidate(pingNearbyProvider);
```

**`pingNearbyProvider` est l'USAGE, pas l'acquisition.** C'est une vue dérivée
qui filtre `pingNearbySourceProvider` sur le délai de grâce. L'invalider ne fait
que **recalculer le même filtre sur les mêmes données** : le geste de
l'utilisateur ne produisait rien, et rien ne le signalait — ni erreur, ni test,
ni `analyze`.

C'est exactement la règle de dissociation prise à l'envers : le client allait en
cuisine, et se servait dans l'assiette du serveur.

✅ Corrigé : `ref.read(pingNearbySourceProvider.notifier).refresh()` — on
s'adresse à **ce qui acquiert**.

## ② `PingStore` était encore un `ChangeNotifier` que plus personne n'écoutait

Son unique observateur était `PingScreen`, qui rechargeait conversations et
croisements à chaque écriture. Les deux sections ont disparu ; `addListener` et
`removeListener` n'avaient plus **aucun appelant**, et le `notifyListeners()`
restant ne réveillait personne.

✅ Corrigé : `PingStore` est une classe ordinaire, `ChangeNotifierBase` supprimée.

## ③ Douze motifs de diagnostic sans producteur

Relevé par comptage, et **comparé à `HEAD~1`** pour distinguer ce que j'avais
orphelinisé de ce qui l'était déjà :

| | Motifs | État avant mon chantier |
|---|---|---|
| 4 compteurs | `demandes émises / reçues / acceptées / refusées` | 1 producteur chacun |
| 6 motifs | `requestToFriend`, `requestFromFriend`, `badSignature`, `acceptNotOurs`, `notForUs`, `declineWithoutRequest` | 1 producteur chacun |
| 2 motifs | `acceptUndeliverable`, `bookChanged` | **déjà orphelins** |

Les laisser aurait affiché **quatre zéros permanents** dans le rapport de
diagnostic de Jay — une invitation à lire « aucune demande » là où il n'y a plus
rien à compter.

⚠️ **`acceptNotOurs` était le plus important de la liste** : il guettait
quelqu'un qui tente de s'inscrire lui-même comme ami accepté — la barrière
fondatrice. Ce qu'il surveillait est désormais tenu par
`request_connection_from_proximity`. **La surveillance n'a pas disparu, elle a
changé d'endroit — et ce nouvel endroit n'est pas dans le rapport de diagnostic.**

## ④ Une notification devenue inatteignable dans la file d'envoi

`_drainOutbox` gardait, dans son `catch`, un cas spécial :

```dart
if (item['type'] == 'connection') { … 'Connexion non enregistrée' … }
```

Ce type n'a plus de producteur, et il tomberait de toute façon dans `default`,
qui `continue` **avant** de pouvoir lever. Un `if` que rien ne peut satisfaire est
un garde-fou qui rassure sans protéger.

✅ Corrigé, avec la raison : accepter une demande est maintenant un appel serveur
direct, qui réussit ou lève **devant l'utilisateur**. Plus de file, donc plus
d'échec différé et silencieux à rattraper.

## Ce que la vérification a confirmé de bon

| Chaîne | Vérification |
|---|---|
| **Le démarrage de la proximité** | `PingScreen` observe toujours `proximityControllerProvider` — c'est ce qui lance le réseau, la synchro des clés et le balayage. Le retirer aurait tout éteint en silence. |
| **La file d'envoi** | producteurs (`sightings`, `wave`) et consommateurs correspondent **exactement**. Et le `default` **absorbe proprement** les reliquats `encounter` / `connection` restés sur les appareils de Jay : abandonnés et comptés, jamais retentés à l'infini. |
| **Le pont natif** | 9 ordres exposés côté Kotlin, 9 appelés côté Dart ; 2 événements émis, 2 décodés. **Aucun orphelin dans un sens ni dans l'autre.** |
| **Le catalogue natif** | sa liste d'ordres correspond, méthode par méthode, à ce que `ProximityBridge.kt` expose réellement. |
| **Tous les providers touchés** | `presenceProvider`, `presenceKeysProvider`, `peerViewProvider`, `isNearbyProvider`, `isFriendProvider`, `pingNearbyProvider`, `pingBeaconProvider` ont tous encore des consommateurs. |
| **La dissociation** | `PeerNetwork._publish()` publie toujours **sans condition** : l'acquisition ne décide pas de l'affichage. `presence_feed` compare, comme avant. |

## Deux observations, antérieures à ce chantier — signalées, pas touchées

- **`ping_screen.dart:347` construit un `BleRadio()`** pour ouvrir les réglages
  de localisation : un écran qui parle au natif. **Présent à l'identique sur
  `HEAD~1`.** Ce n'est pas une acquisition de donnée (c'est une intention
  système), mais ça reste un chemin direct écran → natif.
- **`proximity_diagnostic_screen.dart:39`** fait de même — légitime, c'est un
  outil de lecture du natif, et il part avec la section Développeur
  (`RAPPELS.md` #4).
- **`PresenceKeys.pending`** n'a plus d'afficheur (le compteur « N appareils
  détectés » a été retiré le 2026-08-27, avant cette session). Il participe
  encore à l'égalité de `PresenceKeys`, donc à la décision de redessin —
  conservé pour cette raison.

## État final vérifié

| | |
|---|---|
| `flutter analyze` | **No issues found** |
| `flutter test` | **178 tests au vert** |
| `:app:compileDebugKotlin` + `:app:testDebugUnitTest` | exit 0 |
| APK release | construit |
| `aapt2 dump permissions` sur l'APK | `BLUETOOTH_CONNECT` **absente**, aucun plugin ne la réinjecte |

---

# Troisième temps — rebrancher l'état de la demande, et finir le ménage

## ✅ « Les presque » sont intacts — vérifié, pas supposé

Question de Jay. Réponse : **oui, rien n'y a touché.** La chaîne complète, relevée
dans le code et en base :

| Étape | Où | État |
|---|---|---|
| Déclenchement | `_onPeerEvent` → `PeerIdentified` → `_maybeWave` si ami | intact |
| Anti-répétition | `ProximityJournal.mayWave` (2 h, persisté) | intact |
| Notification | « Le presque… — X est passé tout près de toi » | intact |
| Remontée serveur | file d'envoi `{'type': 'wave'}` → table `waves` | intact |
| Affichage | onglet **Waves** de l'écran cœur (`wavesProvider`) | intact |

📏 **Preuve** : `git diff HEAD~2 -- lib/` ne montre, sur tout ce qui touche aux
waves, que des **commentaires**. La seule ligne de code retirée qui contenait
`NotifChannel.waves` était la notification « Connexion non enregistrée » — un
autre usage du même canal de notification, pas un presque. En base : table
`waves`, **10 lignes**, 3 politiques.

⚠️ **À ne pas confondre**, et c'est ce qui a motivé la question : « Croisés
récemment » et « les presque » sont deux choses différentes. Le premier listait
les gens croisés **avec certificat BLE** (donc jamais rien) ; le second notifie
qu'un **ami** est passé tout près. Seul le premier est parti.

## ✅ L'état de la demande d'ami, rebranché au serveur

**Aucune requête nouvelle.** `outgoingRequestsProvider` — un flux temps réel sur
`connection_requests` — existait déjà, et n'avait **aucun observateur** : seul un
`ref.read` du battement de cœur le touchait. Il en a un maintenant.

Ajouté dans `proximity_repository.dart`, à côté des demandes qu'il possède déjà :

```dart
enum EtatDemande { aucune, envoyee, declinee }
final etatDemandeProvider = Provider.family<EtatDemande, String>(...);
```

**Trois décisions, et chacune vient d'une règle du projet :**

1. **Un enum, pas la ligne.** Le battement de cœur réécrit `expires_at` toutes
   les 30 s : rendre le `ConnectionRequest` aurait reconstruit la tuile à chaque
   fois, pour un bouton identique. *Ce qu'on compare doit être ce que l'œil
   voit* — la règle de `PeerView`.
2. **Une famille par personne.** Un bouton n'est réveillé que quand **sa**
   demande change, pas quand n'importe laquelle bouge.
3. **Aucune invalidation après l'envoi.** La ligne arrive par le temps réel. Une
   invalidation écrite dans l'écran serait un second chemin vers un état dont le
   serveur est déjà la source.

**Un seul bouton pour les deux tuiles.** `_BoutonDemande` remplace deux
`IconButton` et **deux fonctions d'envoi quasi identiques** — exactement le genre
de doublon qui avait produit deux boutons « Ajouter » faisant deux choses
différentes. Le test de la règle : la prochaine tuile qui en aura besoin n'aura
rien à réécrire.

**Vérifié en base avant de coder** :

- la politique `requests_select_own` laisse l'émetteur relire ses demandes, **y
  compris refusées** — donc les deux états se dérivent du **même** flux ;
- `decline_connection_request` passe la ligne à `declined`, il ne la supprime pas ;
- `request_connection_from_proximity` ne cherche qu'une demande **`pending`** :
  redemander après un refus est **explicitement autorisé**. D'où « appuie pour
  redemander » et non « appuie pour oublier » — il n'y a plus rien à oublier.

**Le test compte, il ne décrit pas** (`test/etat_demande_test.dart`, 6 cas). Le
dernier est le seul qui aurait attrapé la mauvaise conception : cinq réécritures
de `expires_at` → **zéro reconstruction**.

## ✅ Les deux points restants du ménage

- **`ping_screen` ne parle plus au natif.** Il construisait son propre
  `BleRadio()` pour ouvrir les réglages de localisation. `openLocationSettings`
  vit désormais sur le **superviseur**, qui possède la radio. L'import de
  `ble_radio.dart` a disparu de l'écran.
- **`PresenceKeys.pending` retiré.** Ce compteur n'avait plus d'afficheur depuis
  le retrait du bandeau « N appareils détectés » — mais il **entrait dans
  l'égalité** de l'objet, donc l'apparition d'un inconnu que rien n'affiche
  reconstruisait la liste. Le test qui vérifiait sa valeur **compte maintenant
  les reconstructions** : zéro.
- **`proximity_diagnostic_screen` garde son `BleRadio()`** — c'est un instrument
  de lecture du natif, c'est sa raison d'être, et il part avec la section
  Développeur (`RAPPELS.md` #4). Signalé, pas touché.

## 🔴 Ce que le rebranchement a mis au jour — `RAPPELS.md` #76

**Une demande de proximité vit 7 jours côté serveur et 90 secondes côté client.**

`request_connection_from_proximity` crée la ligne à `now() + 7 days`. Mais
`ProximityRepository._refresh()`, un minuteur de 30 s, **réécrit**
`expires_at = now() + 90 s` dès que le destinataire est à portée. Les deux se
séparent → la demande meurt en ≤ 90 s, et disparaît des **deux** écrans, alors
que la base dit encore `pending`.

⚠️ **C'est une prémisse morte, pas un bug.** La règle (« la demande expire à la
sortie de portée », spec 4.2) était juste quand il fallait **être à portée pour
répondre** — la réponse voyageait dans le canal BLE. Répondre est maintenant un
appel serveur : **on accepte de n'importe où**. Le commentaire dit d'ailleurs
« on prolonge `expires_at` » alors que le code le **raccourcit**.

🟡 **Et ce minuteur n'est même pas toujours vivant** : `ProximityRepository` n'est
construit qu'au premier accept/decline de la session. Le comportement dépend donc
de ce que l'utilisateur a fait plus tôt — invisible depuis l'écran.

**Rien n'a été changé** : c'est une décision produit. Ma recommandation est dans
`RAPPELS.md` #76 — garder les 7 jours du serveur et retirer le battement, la
barrière étant déjà vérifiée **à l'émission**.

⚠️ **Ça ne gêne pas le test « deux inconnus »** : tant que les deux appareils sont
côte à côte, la demande reste `pending` et le bouton affiche bien « envoyée ».

## État final

| | |
|---|---|
| `flutter analyze` | **No issues found** |
| `flutter test` | **184 tests au vert** (178 + 6 neufs) |
| Kotlin | compile, tests au vert |
| APK release | construit |

---

# Quatrième temps — la durée des demandes, et le guide utilisateur

## ⚠️ Correction d'une conclusion fausse que j'ai écrite plus haut

J'ai écrit, dans la section « Ce que la suppression rend orphelin côté serveur » :

> la branche `stories_public` de `can_view_stories` devient inatteignable pour un
> non-ami — mais **elle l'était déjà** : `report_sightings` exige
> `connections.status = 'full'`, donc une ligne `encounters` ne peut exister
> qu'entre amis.

**C'est faux.** Le raisonnement portait sur la *création* de la ligne et oubliait
sa **survie** : une ligne `encounters` créée entre amis reste après que l'amitié
a été retirée. La branche est donc parfaitement atteignable — et elle l'était au
moment même où je l'écrivais. Ce qui suit en est la conséquence.

## 🔴 Retirer un ami ne coupe pas son accès — `RAPPELS.md` #77

Trouvé en vérifiant les chiffres du guide, **reproduit en exécutant les fonctions
en base**, pas en les lisant :

```
private.can_view_profile(charles, mimi)  → true
private.can_view_stories(mimi, charles)  → true
```

**13 heures après** que Jay ait retiré l'amitié avec le bouton de la v0.9.134. Il
n'y a **aucune ligne** dans `connections` pour cette paire, et l'accès tient
quand même.

**La cause** : retirer un ami efface la ligne `connections` — mais pas la ligne
`encounters`, qui lui survit jusqu'à sa purge (24 h). Or `can_view_profile` teste
l'existence d'un croisement **sans aucune limite de temps**.

C'est la règle 8 de `CLAUDE.md` — *une suppression est une opération sur un
réseau* — appliquée au bouton « Retirer de mes amis » livré cette nuit : le nœud
`connections` a été coupé sans relever ce qui **dépendait** de lui.

🟡 Ce n'est pas absurde en soi (« je t'ai croisé, je vois ton profil public » est
la sémantique voulue d'un croisement), mais **ce n'est pas ce qu'un utilisateur
attend** de ce bouton. **Question posée à Jay, rien changé.**

## ✅ Une demande d'ami vit sept jours — appliqué

Décision de Jay, suivant la recommandation de `RAPPELS.md` #76.

**Le chiffre était écrit à trois endroits, avec deux valeurs :**

| Où | Valeur | Effet |
|---|---|---|
| `request_connection_from_proximity` | `now() + 7 days` | ce qui était posé |
| défaut de la colonne `expires_at` | **`now() + 90 secondes`** | ce qu'aurait reçu tout autre chemin |
| `ProximityRepository._refresh()` | réécrit à **90 s** | **écrasait les 7 jours en moins de 30 s** |

🔴 **Et ça ne masquait pas, ça détruisait.** Le cron `neovibe_purge` passe toutes
les cinq minutes et bascule en `expired` toute demande `pending` échue. Une
demande était donc **perdue définitivement** 90 secondes après la séparation —
pas cachée, perdue.

**Livré :**

- le minuteur supprimé, et `liveOutgoingRequestsProvider` avec lui — il en était
  l'unique lecteur, et encore par un `ref.read`, donc sans même s'y abonner ;
- le défaut de colonne porté à 7 jours — migration `20260827150000`, **appliquée
  et vérifiée sur la colonne**, pas sur le fichier ;
- ⚠️ **vérifié avant de toucher au défaut** : `request_connection_from_proximity`
  est le **seul** à insérer dans cette table, côté serveur comme côté Dart. Le
  défaut était donc inatteignable — corrigé quand même, parce que la politique
  `requests_insert_sender` autorise encore une insertion directe (#70).

⚠️ **La barrière de présence physique n'est pas affaiblie** : elle est vérifiée
**à l'émission** (paire mutuelle de moins de 10 minutes) et elle le reste. Ce qui
change n'est pas qui peut demander, c'est le temps laissé pour répondre.

## ✅ `docs/guide-utilisateur.md`

Premier jet, **chaque chiffre relevé à la source**. Il couvre : les deux chemins
d'amitié, la demande à 7 jours, le ping (les deux étapes GPS/Bluetooth et leurs
rayons), les messages, les presque, les croisements, le tableau des durées de
vie, la vie privée, et **ce qui n'existe pas encore** — section séparée, pour ne
rien promettre qui ne soit construit.

Il porte deux marqueurs internes à retirer avant publication :

1. **ne rien écrire sur le retrait d'ami** tant que #77 n'est pas tranché ;
2. le **plafond de recommandations** (10/mois dans `CLAUDE.md`) est introuvable
   dans le code — à vérifier avant de l'annoncer.
