# Rapport de session — 2026-07-11 16:52

## Contexte

Première session de développement de NeoVibe V1. Jay a fourni la spécification complète V1
(comptes, Ping BLE, messagerie de proximité, recommandations A→B→C, messagerie directe/groupe
éphémère 24h, Cards + 4 types spéciaux, notifications FOMO, profil/bibliothèque, Waves).
Le repo était vide (scaffolding seul). Le projet Supabase `neovibe_alpha`
(ref `dvixmhvqqjvbrpsckmyi`, eu-west-1) existe et est sain — c'est l'infra annoncée comme prête.

## État de l'environnement au démarrage

- **Flutter : non installé** sur la machine (aucune trace dans PATH ni emplacements usuels)
- **Java/JDK : non installé**
- **Android SDK : non installé**
- Git présent. MCP Supabase opérationnel.

→ Installation de la chaîne complète nécessaire avant tout build. Choix : installation
utilisateur sans droits admin dans `C:\Charles\dev\` (Flutter stable, JDK 17 Temurin,
Android cmdline-tools + platform-tools + build-tools).

## Modifications apportées

(mis à jour au fil de la session)

- Création de ce rapport.
- **Backend Supabase complet** (projet `dvixmhvqqjvbrpsckmyi`), 5 migrations dans
  `supabase/migrations/` (versionnées dans le repo et appliquées en base) :
  1. `schema_initial` — enums extensibles (dont `card_type` à 5 valeurs), tables :
     profiles (avec `ble_token` distinct de l'id de compte), connections (paire ordonnée,
     statut partial/full), connection_requests (BLE, expiration par heartbeat),
     recommendations (A→B→C), conversations/members (direct/group/proximity, `pair_key`
     unique), messages (TTL 24h), message_reads, cards, card_deliveries, library_items,
     library_access, waves.
  2. `functions_triggers` — helpers RLS, RPC (accept/decline demandes BLE,
     forward/accept/decline recommandations avec plafond 10/mois, get_or_create
     conversations, confirm_partial_connection, mark_card_viewed, destroy_oneshot,
     resolve_ble_tokens), triggers (règle 3 messages proximité, création de connexion
     partielle au premier échange bilatéral, unicité One of One, interdiction
     oneshot/1-1 en bibliothèque).
  3. `rls_policies` — RLS sur toutes les tables, aucun annuaire global (profil lisible
     uniquement via relation légitime).
  4. `storage_realtime_cron` — buckets avatars (public) / media / cards / library
     (privés, policies liées aux tables), publication Realtime, purge pg_cron toutes
     les 5 min (messages expirés, demandes/recommandations/liens partiels expirés,
     oneshots détruits).
  5. `security_hardening` — helpers RLS déplacés dans le schéma `private` (non exposé
     par l'API REST), révocation EXECUTE anon/public sur RPC et fonctions trigger,
     search_path immuable.

### Choix d'implémentation signalés (ambiguïtés de spec tranchées, cf. section 8 de la spec)

- **Recommandation A→B→C — désignation de C** : la spec ne dit pas comment B désigne C
  sans annuaire ni accès à la liste des connexions de A. Choix : B décrit C en texte libre
  (`target_hint`) dans sa demande à A ; A choisit le vrai C parmi SES connexions au moment
  de transmettre. Le plus conforme au principe "pas de découverte par annuaire".
- **Refus de recommandation** : enregistré `declined` en base, mais jamais affiché à B
  (le client de B traite tout ce qui n'est pas accepté comme une expiration silencieuse).
- **Réinitialisation des 3 messages de proximité** : les messages expirant à 24h, le
  compteur repose sur les messages non expirés → réinitialisation naturelle, cohérente
  avec "nouvelle rencontre BLE réinitialise".
- **Expiration des demandes BLE** : `expires_at` à 90 s rafraîchi par le client émetteur
  tant que le pair est détecté ; sortie de portée → plus de refresh → expiration.
- **Oneshot / One of One en bibliothèque** : interdits (contradiction avec vue unique /
  exclusivité). Standard/Hot/BeReal publiables.
- **Hot — mise en avant** : implémenté comme `hot_boosted` privé chez le destinataire si
  la Card est ouverte < 2 min après réception (badge/tri dans SON interface, rien de
  public). **À valider par Jay** (la spec demandait validation si comportement flou).
- **Purge Storage** : la purge cron supprime les lignes DB ; les fichiers média orphelins
  deviennent inaccessibles (policies liées aux lignes) mais ne sont pas encore effacés du
  bucket — nettoyage physique à ajouter plus tard (edge function).

### Application Flutter complète (V1)

- **Toolchain installée dans `C:\Charles\dev\`** : Flutter 3.44.6 stable, JDK 17 Temurin,
  Android SDK (platforms 35+36, build-tools 35/36, platform-tools). `flutter doctor` OK.
  ⚠️ Ces outils ne sont PAS sur le PATH système — les commandes de build doivent préfixer :
  `$env:JAVA_HOME="C:\Charles\dev\jdk17"; $env:Path="C:\Charles\dev\flutter\bin;..."`.
- **Projet Flutter** créé à la racine du repo (package `neovibe`, `com.neovibe.neovibe`,
  Android uniquement, minSdk 26). Analyse statique : **0 issue**. Tests unitaires : verts.
- **Clé Supabase** : la clé publishable (publique par conception, protégée par RLS) est dans
  `lib/core/config/env.dart`, **gitignoré** ; `env.example.dart` committé comme modèle.
  Conforme à la consigne « pas de clé dans le repo ».
- **Structure** : feature-first (`lib/features/{auth,home,connections,recommendations,
  conversations,cards,library,proximity,waves,notifications,settings}`), modèles partagés
  dans `lib/core/models/`, Riverpod 3 (Notifier/StreamProvider/FutureProvider), navigation
  Navigator classique (go_router retiré — inutile pour cette taille d'app).
- **Fonctionnalités implémentées** (toutes branchées sur le vrai backend) :
  - Auth email/mot de passe + onboarding profil (nom + avatar).
  - Ping BLE : advertising manufacturerData (0xFFFF, préfixe "NV" + ble_token 16 octets),
    scan continu flutter_blue_plus, résolution des tokens via RPC, liste temps réel avec
    « Très proche / Proche » (RSSI grossier, jamais de distance), demandes de connexion
    avec heartbeat d'expiration (90 s, rafraîchi tant que le pair est en portée).
  - Messagerie de proximité : canal texte, règle des 3 messages (trigger serveur),
    bannière lien partiel avec confirmation mutuelle sous 3 jours, canal masqué hors
    portée sauf lien établi.
  - Recommandations A→B→C : demande (hint texte libre), transmission par A vers un C de
    son cercle (plafond 10/mois serveur), acceptation par C, échecs silencieux pour B.
  - Messagerie directe + groupes : texte/photo/vidéo éphémères 24h (compte à rebours
    affiché sur chaque bulle), typing indicator (broadcast Realtime), accusés de lecture
    (message_reads), création/renommage/ajout/retrait/quitter groupe.
  - Cards : flux capture recto (arrière) → verso (avant) → choix du type → envoi et/ou
    publication. 5 types avec tag + couleur signature (standard indigo, Oneshot rouge,
    1/1 or, Hot orange, BeReal gris). Oneshot : durée 1-10 s paramétrable, minuteur
    visible, destruction (y compris fermeture anticipée). 1/1 : destinataire unique.
    Hot : estompage après 8 s + boost privé si ouverte < 2 min (icône flamme dans la
    liste de conversations du destinataire). BeReal : fenêtre de capture 30 s.
  - Bibliothèque : grille persistante (photos + Cards publiées), visibilité
    connexions/restreinte + liste d'accès individuelle, consultation des bibliothèques
    des connexions (RLS applique les droits).
  - Notifications FOMO locales (canaux Android séparés) : « X t'a envoyé une Card [Type] »,
    « X a publié » — via Realtime. Typing = indicateur in-app uniquement (sobriété).
  - Waves : détection des connexions croisées pendant le scan BLE (anti-spam 2 h),
    notification différée ~45 min par défaut, temps réel opt-in dans les réglages,
    historique avec horodatage volontairement flou, service de premier plan pour la
    détection en arrière-plan.

## Difficultés rencontrées / erreurs

- Machine de dev sans toolchain mobile : Flutter, JDK et Android SDK absents.
  Solution : installation locale scriptée dans `C:\Charles\dev\` (pas de droits admin requis).
- `sdkmanager --licenses` ignore le pipe PowerShell (`"y" | cmd`) : utiliser une
  redirection cmd (`cmd /c "sdkmanager.bat --licenses < yes.txt"`).
- Flutter 3.44 exige Android SDK **36** (le doctor le dit après coup) : installer
  `platforms;android-36` + `build-tools;36.0.0` d'emblée.
- **Riverpod 3** : `AsyncValue.valueOrNull` n'existe plus → `.value` (qui ne throw plus).
- **flutter_blue_plus** : `advertisementData.msd` est une liste ; la map fabricant
  s'appelle `manufacturerData`.
- Flutter 3.44 déprécie `RadioListTile.groupValue/onChanged` → wrapper `RadioGroup`.
- Le RPC `resolve_ble_tokens` ne renvoyait pas le token scanné → impossible d'associer
  profil ↔ trame BLE côté client. Corrigé par migration (le token est renvoyé : pas de
  fuite, l'appelant le possède déjà).

## Limitations connues / dette assumée (V1)

- **Pas de FCM** : les notifications fonctionnent tant que le process app est vivant
  (service de premier plan quand la visibilité BLE est active). Notifications app tuée =
  nécessite un projet Firebase → à décider avec Jay.
- **Confirmation d'email Supabase** : si activée (défaut), l'inscription demande une
  confirmation par mail. Pour fluidifier les tests, désactiver « Confirm email » dans
  Auth > Providers du dashboard Supabase (je n'ai pas accès à ce réglage via MCP).
- Purge des fichiers Storage orphelins non implémentée (les fichiers deviennent
  inaccessibles mais restent dans le bucket).
- Détection de capture d'écran (Oneshot) non implémentée (FLAG_SECURE et détection
  seraient l'étape suivante du volet anti-capture).
- APK release signé avec la clé debug (pas encore de keystore de release).

## Build

- `flutter build apk --release` : **OK** → `build/app/outputs/flutter-apk/app-release.apk`
  (56,5 Mo). `dart format` appliqué, `flutter analyze` : 0 issue, tests unitaires verts.
- Accros de build résolus (à connaître pour les prochains builds) :
  - NDK corrompu par un build interrompu → réinstaller via
    `sdkmanager "ndk;28.2.13676358"` plutôt que laisser Gradle le retélécharger.
  - `flutter_local_notifications` exige le core library desugaring →
    `isCoreLibraryDesugaringEnabled = true` + dépendance `desugar_jdk_libs:2.1.4`
    (fait dans `android/app/build.gradle.kts`).
  - Premier build Gradle ~13 min (téléchargements) ; les suivants ~2 min.

## Correctif post-test (retour de Jay)

- **Symptôme** : impossible d'ajouter du contenu non textuel (avatar, Card, photo) —
  erreurs Postgres. Le texte fonctionnait.
- **Cause** (logs Postgres) : `infinite recursion detected in policy for relation
  "cards"/"card_deliveries"`. Les policies des deux tables se référençaient
  mutuellement. Effet aggravant : la policy SELECT du bucket Storage `cards` joignait
  ces tables → l'INSERT Storage (qui évalue les policies SELECT au RETURNING) échouait
  pour TOUS les buckets, d'où l'échec de tout upload.
- **Fix** : migration `fix_cards_policy_recursion` — helpers `security definer`
  (`private.owns_card`, `private.has_card_delivery`, `private.can_view_card_file`)
  à la place des sous-requêtes croisées. Vérifié en SQL avec le rôle `authenticated` :
  insert+returning, selects et storage OK.
- **Leçon** : quand deux tables se référencent dans leurs policies RLS, toujours passer
  par une fonction security definer d'un côté. Et tester les INSERT `.select()` (RETURNING)
  qui évaluent les policies SELECT.
- Correctif 100 % côté serveur : **l'APK v0.1.0 existant reste valable**.
- Reste à tester par Jay (2 appareils requis) : Ping BLE, messagerie proximité,
  recommandations, groupes, Waves.

## Cards V2 (deuxième vague de consignes de Jay, même session)

Nouvelles mécaniques par type (migration `cards_v2_mechanics` + client) :
- **Classique/1of1/Oneshot** : vues paramétrables 1-5 (défaut 2) et durée de lecture
  1-20 s ou illimitée (défaut 10 s) — défauts modifiables dans Réglages > Cards,
  ajustables card par card à l'envoi. Trace 24 h dans le chat ; **replay uniquement
  sur accord de l'émetteur** (bouton « accorder » sur sa card dans le chat —
  conforme à la décision verrouillée du CLAUDE.md).
- **Hot** : UNE vue, temps limité, destruction immédiate et disparition du message
  du chat (aucune trace) — reprend l'ancien comportement Oneshot. Boost privé < 2 min
  conservé.
- **Oneshot (nouveau sens)** : les deux faces en un seul déclenché (arrière puis
  avant enchaînées au plus vite — la capture strictement simultanée des deux caméras
  n'est pas permise par Android sur la plupart des appareils ; signalé comme
  simplification). Sinon comportement classique, publiable en bibliothèque.
- **BeReal** : plus de déclenchement manuel. La notification (fenêtre de 5 min)
  arrivera par un déclenchement aléatoire serveur PLUS TARD (décision Jay) ; en
  attendant : **mode test développeur** dans Réglages (déclencher maintenant /
  programmer à la seconde près, notification exacte).
- **Bibliothèque : lecture illimitée** (les limites ne valent qu'en chat).
- **Jauge de lecture** chez le destinataire : barre qui s'écoule de gauche à droite
  proportionnellement à la durée.
- **Couleurs V2** : standard blanc éclatant, Hot rouge Torino (#C8102E), 1/1 or
  (inchangé), Oneshot dégradé bleu→magenta électrique, BeReal dégradé vert
  nature→turquoise.
- **Éditeur de face façon Snapchat** (« les deux » au choix de Jay) : annoter la
  photo OU créer une face vierge — dessin au doigt, textes déplaçables (appui long =
  supprimer), palette, fonds ; export PNG qui remplace la face.
- **Bug d'affichage corrigé** : états distincts chargement/erreur avec bouton
  « Réessayer » (plus de point bloqué), placeholders et erreurs inline sur les
  images (`loadingBuilder`/`errorBuilder`).
- Popup de première utilisation expliquant les règles + rappel dans les réglages.

## Retour de test n°2 de Jay (flux de capture)

- **Caméra frontale écran noir** : cause identifiée — deux contrôleurs caméra ouverts
  simultanément lors de la bascule (l'ancien n'était libéré qu'après l'initialisation
  du nouveau). Corrigé : dispose de l'ancien AVANT d'ouvrir le nouveau + indicateur
  pendant la bascule. Vaut aussi pour le bug Oneshot (écran noir + interface bloquée :
  en plus, l'exception du 2e cliché n'était pas attrapée → UI figée ; try/catch ajouté
  + délai de stabilisation 250 ms + overlay de progression).
- **Sélection du type AVANT la prise** (consigne) : sélecteur horizontal de pastilles
  au-dessus du déclencheur (le type teinte le déclencheur). Le Oneshot devient un mode
  de prise (un déclenché = deux faces) — plus de choix de type au récap (logique :
  on ne peut pas décréter Oneshot a posteriori). Réponses de Jay : sélecteur
  horizontal ; pas de nouvel onglet Card (juste le retour dans le flux).
- **Face tableau** : bouton par face pendant la prise → fond noir généré à la place
  de la photo, à dessiner au récap. L'éditeur gagne « tout effacer / revenir à
  l'image initiale », et le récap un bouton « restaurer » par face modifiée.
- **Navigation du flux Card** : l'écran d'envoi est maintenant poussé (plus de
  pushReplacement) → le retour ramène au récap, puis à la prise, puis à l'onglet
  actif précédent.
- Jay précisera le comportement exact du mode Oneshot une fois ces bugs validés.
- **Validation Jay (v0.2.1)** : caméra frontale OK, sélection du type avant la prise
  conforme, Oneshot fonctionnel — « on part sur une bonne base ». En attente : sa
  précision sur le mode Oneshot.

## Releases

- `master` poussé sur `Gatsby4444/neovibe_alpha`. Trois releases GitHub avec APK :
  - **v0.1.0** — V1 complète initiale.
  - **v0.1.1** — fix récursion RLS (uploads) + retournement physique des Cards au swipe.
  - **v0.1.2** — réglage du sens de retournement (Réglages > Cards).
- Note : `gh release create` avec un libellé d'asset `fichier#label` échoue sous
  PowerShell 5.1 (« no matches found ») — passer le chemin nu + `--notes-file`.

## État en fin de session

- Backend et app V1 complets, déployés et releasés. `flutter analyze` : 0 issue,
  tests verts, aucun secret dans le repo.
- Validé sur appareil par Jay : compte, profil, bibliothèque, création/affichage de
  Card, retournement au swipe.
- **Non testé (nécessite 2 appareils)** : Ping BLE, messagerie de proximité,
  recommandations A→B→C, groupes, Waves, notifications FOMO entre comptes.
- En attente de décision Jay : comportement Hot (boost privé < 2 min), intégration
  FCM, désactivation « Confirm email » côté dashboard Supabase.
- Realtime « X a publié » : abonnement global filtré par RLS — à surveiller en charge.

## Notes et consignes de Jay

- **UI d'affichage d'une Card (consigne du 2026-07-11, après premier test)** : le
  retournement doit se faire au **swipe tactile avec une vraie physique de carte** —
  le doigt entraîne et incline légèrement la carte dans l'espace (mix 2D/3D,
  perspective) ; un swipe ou un angle de retournement suffisant suivi d'un
  relâchement termine le retournement, sinon la carte revient à plat.
  → Implémenté dans `lib/features/cards/flippable_card.dart` (rotation Y continue
  au doigt + inclinaison X subtile, projection de la vélocité au relâchement,
  snap sur la face la plus proche, tap = retournement complet). Ce widget est LA
  référence d'interaction des Cards — le réutiliser partout où une Card s'affiche.
- **Retour de test de Jay sur le retournement** : animation validée (« parfaite »),
  mais le sens du swipe peut être perçu comme inversé → ajout d'un réglage
  « Inverser le sens de retournement » (Réglages > Cards, préférence locale via
  shared_preferences, paramètre `invertDrag` du widget). Sens par défaut inchangé.

- La spec V1 fait foi ; V1 = base fonctionnelle, pas le MVP final. Android uniquement.
- Hors scope V1 strict : marketplace, EVNT, défis/mini-jeux, feed local, streaks, scores publics.
- En cas d'ambiguïté : trancher selon les principes directeurs (section 3 de la spec) et
  signaler explicitement le choix fait.
- Point que la spec demande de valider avec Jay si flou : comportement exact de la
  "mise en avant" des Cards **Hot** (à traiter au moment de l'implémentation des Cards).
