# Rapport de session — 2026-08-11 17:59

## Contexte à la reprise

Relu en ouverture : `CLAUDE.md`, `RAPPELS.md`, le rapport `2026-08-10_14-45.md`,
`docs/stockage-et-acces.md`, `docs/controle-de-la-distribution.md`, la mémoire
longue.

État à l'ouverture :
- dernier commit `21a8c28`, working tree propre (hors `docdev/` non suivi)
- dernière release **v0.9.46** ; **dernière version validée au test : v0.9.41**
  — cinq releases attendent le retour de Jay
- questions en attente : limite de vues vs story (RAPPELS #19), « Delivered »,
  bouton bibliothèque vs wave, 3 questions du chantier « stories en deck »

## Chantier de la session

Jay ouvre un **gros chantier de refonte** en quatre volets, présentés sous
forme de plan écrit :

1. **Un contenu = un format.** Au partage, choix EXCLUSIF entre story,
   publication en bibliothèque, ou partage dans le cercle. Trois buckets
   séparés. Le repartage devient une **référence** à l'objet existant, pas une
   copie.
2. **Affichage local-first** pour mes propres contenus (avec réglage de
   désactivation, purge locale à 72 h, retéléchargement au retour), streaming
   vidéo + cache ~1 h pour les contenus d'autrui.
3. **Sauvegardes en local uniquement** — plus d'espace serveur dédié.
4. **Content ID persistant**, journal d'événements, graphe de propagation,
   révocation forcée y compris des copies locales.

**Rien n'a été implémenté à ce stade.** Analyse rendue à Jay et questions
posées avant écriture de code (méthode habituelle).

---

## Relevés faits en base avant de répondre

Projet de dev `dvixmhvqqjvbrpsckmyi`.

**Chevauchement réel des formats sur les 64 vibes existantes** :

| Envoyée | Publiée | Story | Nombre | dont sauvegardées |
|---|---|---|---|---|
| non | oui | non | 35 | 6 |
| oui | non | non | 11 | 3 |
| non | non | non | 9 | 2 |
| **oui** | **oui** | non | **9** | 5 |

→ **9 vibes sur 64 sont en double format** (envoyée + publiée). Aucune story
vivante au moment du relevé (TTL 24 h). C'est le volume réel de la migration.

**Buckets existants** : `avatars` (privé depuis le 2026-08-10), `cards`,
`library`, `media`, `library_vault`.

**Tables concernées** : `cards`, `card_deliveries`, `card_media_keys`,
`stories` (référence `card_id`, non autonome), `library_items` (porte déjà
`media_path` ET `card_id`), `saved_cards` (en `ON DELETE CASCADE`),
`library_vibes` (déjà autonome depuis le 2026-08-10).

## Ce que le plan de Jay referme, sans qu'il l'ait demandé

Trois entrées ouvertes de `RAPPELS.md` disparaissent d'elles-mêmes :

- **#19 (limite de vues vs story)** — sans objet : une story devient un objet
  distinct sans limite de vues, la vibe envoyée garde les siennes. La question
  n'a plus lieu d'être posée.
- **#18 (`saved_cards` en CASCADE)** — réglé par le volet 3 : une sauvegarde
  est une copie locale, elle ne dépend plus de la ligne de l'auteur.
- **#17 (séparation par bucket, ABANDONNÉ le 2026-08-10)** — **légitimement
  rouvert**. Le motif de l'abandon était la duplication de fichiers ; le choix
  exclusif au partage la supprime. C'est le « nouvel élément » qu'exigeait
  l'entrée.

## Points signalés à Jay avant de coder

Voir la réponse en session. En résumé, les tensions relevées :

1. Le repartage par référence déplace la cascade au lieu de la supprimer
   (supprimer la source casse toutes les références) — à décider explicitement.
2. Contradiction entre le volet 3 (sauvegarde = fichier local en clair) et le
   volet 4 (révocation forcée des copies locales).
3. Le retéléchargement après 72 h ne peut concerner que les publications : une
   story expirée à 24 h n'existe plus sur le serveur.
4. Le « téléchargement local au partage » est en réalité une **rétention** :
   le fichier est déjà sur l'appareil, il a été capturé là.
5. Le One of One automatique devient mécanique pour 100 % des envois à un seul
   destinataire.
6. Le journal de propagation est un enregistrement durable de qui a vu quoi —
   durée de conservation et lisibilité à trancher.

## Décisions de Jay sur les 6 points — 2026-08-11

| # | Question | Réponse de Jay |
|---|---|---|
| 1 | Repartage et suppression de la source | **La référence meurt avec la source.** Précision de Jay : la référence **n'est pas un fichier**, c'est un **chemin, un raccourci** vers la source. Un seul média, jamais deux. |
| 2 | Sauvegardes locales vs révocation | **Révocation coopérative assumée.** Le serveur demande, l'app obéit. Un client modifié peut passer outre : **faille connue et acceptée** (« statistiquement très peu d'utilisateurs ont un client modifié »). **Pas de mécanisme de clé** pour les sauvegardes, contrairement aux bibliothèques de conversation. |
| 3 | Streaming vidéo | **Sorti de ce chantier**, reporté au suivant. À la reprise : réexpliquer à Jay le fonctionnement actuel, et les avantages/inconvénients des deux systèmes (fichier entier vs chiffrement par blocs). |
| 4 | Rétention locale (pas téléchargement) | **Accepté en entier.** On garde le fichier déjà présent, on ne retélécharge pas. |
| 5 | Nombre d'options au partage | **4 options.** Plus un **bucket supplémentaire pour le BeReal** — « c'est encore un format différent ». |
| 6 | Journal de propagation | **Gardé**, malgré la contradiction avec l'éphémère : « cela garantit une sécurité pour les utilisateurs ». Illisible par le client. **MAIS** : prévoir que certaines informations alimentent des **statistiques offertes à l'utilisateur** — « ils ont le droit de savoir qui a vu leur contenu, c'est logique et légitime ». |

**Correction importante de Jay sur le P2P** : le « P2P » du volet 4 n'est **pas**
celui du système ping (BLE / Wi-Fi Direct). Il désigne la propagation
**d'ami en ami à l'intérieur des cercles, par internet**. Conséquence directe :
tout passe par le serveur, **le graphe de propagation n'aura donc aucun trou**.
La réserve que j'avais émise sur les événements hors ligne est **sans objet**.

## Deuxième tour de décisions — propagation, BeReal, statistiques

| Sujet | Décision de Jay |
|---|---|
| **Propagation du contenu** | Le contenu doit pouvoir **voyager de cercle en cercle**, « comme un réseau lightning » : chaque destinataire peut repartager à ses amis, et l'utilisateur final n'a pas besoin de connaître la source. **Mais ce n'est pas obligatoire** — il faut un interrupteur : soit une option **« partageable »** à la création de la story, soit un **réglage global de compte** (public / privé, comme Instagram). **À DISCUTER AVANT DE CODER** — consigne explicite de Jay. |
| **BeReal** | **Mis de côté**, ce n'est pas la priorité. À ne pas oublier. Place réservée dans l'architecture (contexte de diffusion de premier rang), bucket **non créé**. |
| **Statistiques** | L'auteur voit **tous ceux qui ont vu** sa story, nominativement. Il ne voit **pas** qui a partagé à qui — **la chaîne de propagation est réservée à l'admin**. |

**Relevé fait en base et en code à cette occasion** : il n'existe **aucun
système de signalement ni de blocage** dans l'app — ni table, ni fonction, ni
écran (`reports`, `blocks` : absents ; les occurrences de `report` en code sont
`report_encounter`, qui concerne le BLE). Point signalé à Jay comme **bloquant**
avant toute ouverture de la propagation vers des inconnus.

## Consigne architecturale ajoutée à `CLAUDE.md`

Demandée explicitement par Jay (« c'est impératif ») :

> « La fiabilité vient de l'architecture et de la structure, pas du rebouchage
> des failles. Et on sépare clairement et distinctement tout ce que l'on peut. »

Écrite en section impérative de `CLAUDE.md`, avec les 6 règles qui en découlent
(supprimer la cause avant de garder le symptôme ; deux règles = deux objets ;
compter les chemins ; énoncer la sécurité positivement ; ne pas mélanger
contenu et format ; rejouer les décisions après un changement d'architecture).

## Étape 1 LIVRÉE EN BASE — la story devient un objet autonome

Cinq migrations écrites, **appliquées et vérifiées en base de dev**. Aucune
donnée détruite : `stories` contenait **0 ligne** au moment de la migration
(TTL 24 h), la purge destructive des 64 vibes n'était donc **pas nécessaire**
ici — elle reste pour l'étape 2.

| Migration | Contenu |
|---|---|
| `20260811183000_content_context_enum` | Énumération `content_context` (`story`, `publication`, `direct`, `conversation_library`), **isolée** — PostgreSQL interdit d'utiliser une valeur d'enum dans la transaction qui la crée. `bereal` volontairement absent. |
| `20260811183100_contents_spine` | `contents` (Content ID + révocation), `content_grants` (graphe), `content_views` (statistiques), helpers `is_revoked` / `record_view`. |
| `20260811183200_stories_autonomous` | `stories` reconstruite avec ses propres fichiers, `story_media_keys`, bucket `stories`, politiques, `story_audience`. **`is_story_card` supprimée.** |
| `20260811183300_stories_rpc` | `publish_story`, `open_story_media`, `share_story`, `story_viewers`, `story_viewer_count`. |
| `20260811183400_story_viewer_count_rename` | Renommage : la fonction compte des **personnes**, pas des ouvertures. |

### Décisions d'architecture prises dans cette étape

1. **Le Content ID est introduit MAINTENANT**, pas au volet 4. Sans lui, les
   tables de format auraient été réécrites une deuxième fois. Un contenu = un
   format = **une identité** : `stories.id` **est** le Content ID.
2. **Les trois tables du socle survivent au contenu.** Une story meurt à 24 h ;
   son identité, son graphe et ses vues restent. C'est ce qui rend la
   révocation et la traçabilité possibles *après* la disparition du média —
   sans quoi il n'y aurait plus rien à retracer une fois le mal fait.
   **Vérifié** : la commande exacte du cron `neovibe_purge` supprime la story
   et laisse identité, graphe et vues intacts.
3. **Le cron n'a pas été touché.** `delete from public.stories where
   expires_at < now()` reste valable sur la nouvelle table — pas de nouveauté
   greffée sur un job partagé (leçon du 2026-08-10).
4. **`can_view_card_file` passe de SIX chemins à CINQ**, et
   `has_unlimited_card_access` perd sa branche story. Ce n'est pas un effet de
   bord : c'est le bénéfice principal de la séparation.
5. **Le nominatif est toujours enregistré** dans `content_views`, même quand
   l'interface n'affiche qu'un agrégat — l'option premium prévue par Jay
   n'exigera aucune migration de données.

### Vérification fonctionnelle — 10 contrôles, tous passés

Test joué en base avec les comptes bots (Lea autrice, Charles ami, Sofia hors
cercle), puis **données de test supprimées** (`contents`, `content_grants`,
`content_views` et `stories` revenus à 0).

| Contrôle | Résultat |
|---|---|
| Un ami voit la story | ✅ |
| Un non-ami ne la voit pas | ✅ |
| Repartage d'une story **non** partageable | ✅ refusé |
| Un hors-cercle obtient l'accès par repartage | ✅ |
| …sans obtenir l'accès à l'autre story | ✅ |
| Clé rendue à l'ouverture | ✅ |
| 3 ouvertures, **aucune limite** (`view_count` = 3) | ✅ |
| Ouverture après **révocation** | ✅ refusée |
| Total des spectateurs vu par l'autrice | ✅ 1 |
| Spectateurs **nommés** (hors cercle → agrégé seul) | ✅ 0 |

`get_advisors` (sécurité) repassé : les seules alertes nouvelles sont les
`rls_enabled_no_policy` **voulues** (`content_grants`, `content_views`,
`story_media_keys` — aucune politique = table illisible, comme
`card_media_keys`). Les `authenticated_security_definer_function_executable`
sur les nouvelles RPC sont attendues : ce sont précisément des fonctions
destinées aux clients, au même titre que `open_card_media`.

## Étape 1 — couche Dart LIVRÉE

`dart format` + `flutter analyze` **propres**, **APK release construit**
(60,2 Mo).

| Fichier | Changement |
|---|---|
| `core/models/story.dart` | `Story` devient autonome (chemins, type, drapeaux vidéo, `shareable`, `encrypted`). Ajout de `StoryViewer`. Plus aucune référence à `CardModel`. |
| `features/stories/story_media_cache.dart` | **Nouveau.** Cache séparé de celui des Cards : une story a **une seule** règle de rétention, son expiration — là où le cache des Cards raisonne en budgets de vues et en TTL de message. |
| `features/stories/stories_repository.dart` | `publish()` dépose les faces chiffrées dans le bucket `stories` et appelle `publish_story` ; `openMedia`, `shareToConversation`, providers de statistiques. Plus de jointure `cards(*)`. |
| `features/stories/story_viewer_screen.dart` | Ne passe plus par `CardViewerScreen` : chemin média propre (scellé → clé → clair → affichage), partage dans une conversation, feuille de statistiques. |
| `features/cards/card_send_screen.dart` | **Le choix exclusif de destination** — 4 options mutuellement exclusives, et un aiguillage par destination à l'envoi. |
| `core/utils/ids.dart` | **Nouveau.** `newUuid()` extrait de `library_vibes_repository` (deuxième appelant). |
| `core/widgets/card_type_badge.dart` | **Nouveau.** `CardTypeBadge` sorti de `card_viewer_screen.dart`. |
| `main.dart`, `settings/storage_screen.dart` | Balayage au démarrage et section « Stories » de l'écran de stockage. |

### Deux défauts nettoyés au passage (consigne de Jay)

1. **`_uuid()` dupliqué en germe.** La fonction vivait en privé dans
   `library_vibes_repository`. Le deuxième appelant l'aurait copiée — extraite
   dans `core/utils/ids.dart` avant que ça n'arrive.
2. **`CardTypeBadge` déclarée dans un écran.** Son propre commentaire disait
   qu'elle avait été rendue publique « parce que la visionneuse de stories en
   avait besoin » : les stories devaient donc importer tout l'écran de lecture
   des Cards pour afficher une étiquette. Déplacée dans `core/widgets`.

### Trois décisions d'implémentation

1. **La visionneuse de stories n'emprunte plus `CardViewerScreen`.** Celui-ci
   est bâti sur les livraisons, les budgets de vues et les durées de lecture —
   une story n'a rien de tout cela. Le faire transiter par là aurait imposé de
   neutraliser chaque mécanisme au cas par cas, c'est-à-dire de refaire le
   mélange qu'on supprime. Le chemin média des stories est propre et court.
2. **Le bouton « Enregistrer » disparaît des stories.** `saved_cards`
   référence `cards` ; une story n'en est plus une, l'enregistrement
   provoquerait une violation de clé étrangère. La sauvegarde revient à
   l'étape 5 sous la forme décidée par Jay — une **copie locale**.
3. **La bibliothèque de conversation est refusée à une capture importée de la
   galerie et au BeReal.** Ce sont deux règles déjà arrêtées par Jay le
   2026-08-10 ; l'option est donc grisée avec son motif, plutôt qu'offerte puis
   échouée à l'envoi.

### ⚠️ Limite à connaître, à traiter à l'étape 5

**« Local d'abord » n'est tenu qu'à moitié.** Les octets de mes propres
contenus sont bien sur l'appareil, mais **la clé vient du serveur** : rouvrir
ma propre story demande encore le réseau. Ce n'est pas une régression — c'est
le comportement des Cards depuis la v0.9.46 — mais le volet 2 de Jay
(« la source n'est pas le serveur mais le téléphone ») ne sera tenu qu'une fois
la clé de mes propres contenus conservée localement.

### ~~Reste à faire pour terminer l'étape 1~~ — FAIT

**La couche Dart n'est pas encore adaptée.** `stories_repository.dart` demande
toujours `select('*, cards(*)')` sur une table qui n'a plus de `card_id` :
**les stories sont cassées dans l'app** tant que cette couche n'est pas
réécrite. `flutter analyze` ne le verra pas — c'est une requête à l'exécution.
Fichiers concernés : `stories_repository.dart`, `core/models/story.dart`,
`stories_bar.dart`, `story_viewer_screen.dart`, plus le chemin de capture qui
devra appeler `publish_story`.

**Aucun APK ne doit être construit avant.**

## Livraison — v0.9.47

Version `0.9.47+137`, commit `88b08f6`, tag `v0.9.47`, APK release (60,2 Mo)
attaché : https://github.com/Gatsby4444/neovibe_alpha/releases/tag/v0.9.47

Diff contrôlé avant commit : aucune clé ni token en clair (la seule occurrence
de `secretKey` est du code préexistant, sans valeur littérale). `docdev/`
volontairement non commité.

## Point de décision AVANT l'étape 2 — la purge des données de dev

L'étape 2 (publication autonome) déplace des fichiers existants : elle exige
donc la purge décidée par Jay le 2026-08-10 (« on purge » plutôt qu'un outil de
migration), jamais exécutée depuis.

⚠️ **Mais cette purge détruirait précisément ce qui sert à tester la v0.9.47** :
les 64 Vibes, 20 livraisons et 44 entrées de bibliothèque antérieures au
chiffrement, dont les notes de release demandent explicitement de vérifier
qu'elles s'affichent encore. **Question posée à Jay** : purger maintenant, ou
après son test de la v0.9.47 ?

## Étape 2 LIVRÉE — la publication devient autonome

**Purge exécutée** (décision de Jay confirmée le 2026-08-11) : 64 Vibes,
20 livraisons, 44 publications, 16 sauvegardes. Portée volontairement limitée à
ce que l'étape exigeait — `library_vibes` n'a **pas** été touchée.
⚠️ 159 fichiers restent orphelins dans le bucket `cards` (SQL ne peut pas
supprimer d'objets de stockage) — `RAPPELS.md` ligne 5.

### Le socle est devenu générique, plutôt que dupliqué

L'étape 2 allait recopier pour les publications ce qui venait d'être écrit pour
les stories : table de clés, RPC d'ouverture, RPC de partage, deux fonctions de
statistiques. **Ces quatre choses ne dépendent pas du format** — elles
interrogent `contents`. Les dupliquer aurait installé la même règle à deux
endroits. Fusionnées :

| Avant (spécifique story) | Après (socle) |
|---|---|
| `story_media_keys`, `library_media_keys` | **`content_media_keys`** |
| `open_story_media` | **`open_content_media`** |
| `share_story` | **`share_content`** |
| `story_viewers`, `story_viewer_count` | **`content_viewers`**, **`content_viewer_count`** |
| `stories.shareable`, `library_items.shareable` | **`contents.shareable`** |

Ne reste spécifique au format que ce qui l'est vraiment : l'**audience**
(`story_audience`, `publication_audience`, aiguillées par `content_audience`)
et la **publication** (les colonnes diffèrent).

### `can_view_card_file` : de SIX chemins à TROIS

Propriétaire, livraison, sauvegarde. Les branches bibliothèque et bibliothèque
publique sont tombées avec l'étape 2 ; la sauvegarde tombera à l'étape 5.

### Vérification — 11 contrôles, tous passés

Un ami voit la publication · un non-ami ne la voit pas · repartage d'une
publication non partageable refusé · une story partageable atteint le hors
cercle · clé rendue par `open_content_media` · ouverture refusée hors audience ·
`library_media_keys` renvoie 1 clé au propriétaire et **0** à un tiers ·
`content_viewer_count` = 1 · **0 spectateur nommé** hors cercle · ouverture
refusée après révocation. Données de test supprimées ensuite.

### Deux défauts trouvés PAR INVENTAIRE, invisibles au compilateur

1. **`profile_stats` était cassée** : elle joignait `library_items.card_id`,
   colonne supprimée par l'étape 2. Elle levait à chaque appel. Réparée, et
   améliorée au passage — le compte de la semaine inclut désormais les stories,
   que rien ne comptait auparavant.
2. **`contents` était illisible par l'audience.** `shareable` ayant déménagé
   sur `contents`, le client ne pouvait plus savoir s'il a le droit de relayer :
   **le bouton de partage n'apparaissait jamais**, y compris pour l'auteur d'un
   contenu qu'il venait de marquer partageable. Politique de lecture ajoutée
   pour l'audience ; la **chaîne** de partage reste dans `content_grants`, qui
   n'a toujours aucune politique.

C'est exactement la leçon du 2026-08-10 : ni le diff ni `flutter analyze` ne
voient ces deux-là. Seul l'inventaire des références au motif d'origine les
remonte.

### Côté app

| Fichier | Changement |
|---|---|
| `core/content/content_media_cache.dart` | Le cache des stories **devient celui du socle** : expiration optionnelle (nulle = publication permanente). Déplacé dans `core`, il ne sert plus une seule fonctionnalité. |
| `core/models/library_item.dart` | `LibraryItem` autonome : faces, type, drapeaux vidéo, `shareable` (via la jointure `contents`). Plus de `kind`, plus de `card_id`, plus de `media_path`. |
| `features/library/library_repository.dart` | `publish()` chiffrant vers le bucket `library`, `libraryKeysProvider` (**toutes les clés d'une bibliothèque en un aller-retour**), `publicationFaceProvider`, partage, suppression. |
| `features/library/publication_viewer_screen.dart` | **Nouveau.** Lecture plein écran d'une publication, sans la machinerie des livraisons. |
| `features/library/mini_card.dart` | Vignette de publication sur le chemin chiffré, **image de couverture des vidéos** (possible maintenant que la face est déchiffrée localement — elle ne l'était pas pour le contenu d'autrui, cf. `RAPPELS.md` #4). |
| `features/cards/card_send_screen.dart` | La publication passe par le nouveau chemin ; « Partageable » vaut pour story **et** publication ; « Enregistrer pour moi » retiré de la publication (publier dans MA bibliothèque, c'est déjà la garder). |

### Trois morceaux de code mort supprimés

- `Thumb` / `ThumbSpec` / `ThumbSource` / `thumbSourceProvider` — plus aucun
  appelant depuis que la publication a son propre chemin. **Leurs deux qualités
  ont été reprises** dans la nouvelle vignette (décodage en taille réduite,
  image de couverture des vidéos) plutôt que jetées avec le reste.
- `photo_viewer_screen.dart` — écran devenu sans appelant.
- `CardsRepository.publishToLibrary` — morte **et** cassée : elle insérait un
  `card_id` dans une table qui n'en a plus.

## Retour de test de la v0.9.48 — panne, diagnostic, correctif (v0.9.49)

**Symptômes rapportés par Jay** : seul l'envoi d'une Vibe en DM fonctionne.
Story et publication « chargent indéfiniment », bibliothèque de conversation
« rien ne s'affiche ».

### Diagnostic — relevé en base AVANT toute hypothèse

L'écriture était **entièrement saine** : 1 story, 2 publications, 2 vibes de
bibliothèque, 1 card ; les fichiers présents dans les bons buckets ; les clés
présentes ; les chemins enregistrés correspondant exactement aux objets
déposés ; 3 annonces `library_add` postées. **Le défaut était donc côté
lecture, pas côté écriture** — ce qui a écarté d'emblée la moitié des pistes.

### 1. La bibliothèque de conversation n'était PAS un bug

Les deux vibes ont été ajoutées à **20h21**, donc après le reveal de 18h30 :
leur révélation est fixée au lendemain 18h30. Elles sont masquées pour tout le
monde, Jay compris. **C'est la mécanique qu'il avait spécifiée**, elle a
fonctionné exactement comme prévu.

### 2. Cause certaine — les clés étrangères de propriétaire

`stories.owner_id` et `library_items.owner_id` référençaient **`auth.users`**
alors que toute la base suit la convention **`public.profiles(id)`** (`cards`,
`saved_cards`, `library_vibes`, et l'ancienne table `stories`).

PostgREST résout les jointures **par le nom de la contrainte**. L'app demande
`profiles!stories_owner_id_fkey(*)` : la contrainte existait toujours, mais ne
menait plus à `profiles`. **La requête des stories échouait entièrement.**

**Leçon** : une clé étrangère n'est pas seulement une contrainte d'intégrité,
c'est le **chemin que le client emprunte pour joindre**. En changer la cible
est un changement d'API — invisible en SQL, invisible à `flutter analyze`,
visible seulement à l'exécution. En reconstruisant une table, **relever les
contraintes de l'ancienne** au lieu de les réécrire de mémoire.

### 3. Cause probable — `ref.watch` après `await`

`publicationFaceProvider` allait chercher le lot de clés (`ref.watch`) **au
milieu** de la fonction, après deux `await`. C'est précisément la règle écrite
dans `stories_repository.dart` après la panne du 2026-08-02 : *« un `ref.watch`
placé après une suspension n'enregistre pas sa dépendance de façon fiable »*.
J'ai enfreint une règle que j'avais moi-même consignée dans le fichier d'à
côté. Corrigé : tous les `ref.watch` avant le premier `await`.

### 4. Faute de conception qui a rendu la panne illisible

La vignette de publication affichait un **rectangle gris muet** au chargement
**et** rien de distinct en cas d'échec. Impossible pour Jay de dire si ça
chargeait ou si c'était cassé — d'où « charge indéfiniment ».

C'est exactement le défaut que le bandeau des stories documente depuis le
2026-08-02 : *« une erreur ne doit PAS se traduire par un bandeau vide, les
trois états doivent se distinguer au premier coup d'œil »*. Corrigé :
chargement = indicateur animé, échec = icône d'erreur avec le message en
info-bulle.

**Et la cause racine du « indéfiniment »** : `_download` n'avait **aucun délai
maximal**. Une requête bloquée attendait pour toujours. Ajouté : 10 s de
connexion, 25 s au total. Une erreur visible vaut mieux qu'une attente
silencieuse.

## LA CAUSE RÉELLE — trouvée au 2e retour de test (correctif serveur seul)

**Jay après la v0.9.49 : « j'ai toujours les mêmes problèmes. Je crains qu'en
plus tu n'aies cassé quelque chose en modifiant des choses qui ne corrigent pas
le problème. »** Il avait raison sur la méthode : j'avais livré un correctif
fondé sur une déduction, sans avoir reproduit la panne.

### Comment je l'ai enfin trouvée

En rejouant la lecture **sous l'identité de Jay, avec la sécurité active** :

```sql
set local role authenticated;
select set_config('request.jwt.claims', '{"sub":"…","role":"authenticated"}', true);
select count(*) from stories;
→ ERROR 42501: permission denied for function story_audience
```

Une ligne, et tout s'explique.

### La cause

**Une politique RLS s'évalue avec les droits de celui qui interroge.** Si elle
appelle une fonction, l'utilisateur doit pouvoir l'EXÉCUTER. J'avais révoqué ce
droit sur les cinq fonctions créées aux étapes 1 et 2 :

| Fonction | Droit d'exécution | Résultat |
|---|---|---|
| `can_view_card_file`, `can_view_library`, `can_view_profile`, `can_view_stories` (anciennes) | **accordé** | le DM marchait |
| `story_audience`, `publication_audience`, `content_audience`, `can_view_story_file`, `can_view_publication_file` (les miennes) | **révoqué** | tout le reste tombait |

La frontière entre « ce qui marche » et « ce qui ne marche pas » suivait
exactement cette colonne.

### Pourquoi je me suis trompé — la leçon du 2026-08-10 mal appliquée

Elle disait : « toute fonction `SECURITY DEFINER` non destinée aux clients doit
être révoquée ». Elle est juste, mais elle visait le schéma **`public`** — le
seul exposé par PostgREST, où une fonction reste joignable sur `/rest/v1/rpc/`.

**Le schéma `private` n'est pas exposé.** Y révoquer l'exécution ne protège de
rien et casse toutes les politiques qui s'en servent. La preuve la plus
simple : `get_advisors` ne signale **jamais** une fonction de `private` — il
n'en liste que du schéma `public`. J'avais cette preuve sous les yeux à chaque
audit sans la lire.

Les deux pièges (celui-ci et celui des clés étrangères) sont désormais inscrits
dans `CLAUDE.md`, avec une 7e règle : **ne jamais livrer un correctif fondé sur
une déduction — reproduire la panne d'abord.**

### Le correctif de la v0.9.49 n'était pas inutile, mais il était insuffisant

La clé étrangère vers `auth.users` était un **vrai** second défaut :
`profiles!stories_owner_id_fkey(*)` n'aurait pas pu résoudre. Simplement, la
requête échouait plus tôt, sur les permissions — donc corriger la seconde cause
seule ne pouvait rien changer de visible. Deux défauts empilés, un seul
visible.

### Vérifié après correctif, sous l'identité de Jay

| Contrôle | Résultat |
|---|---|
| stories visibles | 1 |
| publications visibles | 3 |
| jointure story → profil auteur | 1 |
| jointure story → contents | 1 |
| fichiers lisibles dans `stories` / `library` | 1 / 7 |
| `open_content_media` sur la story et sur une publication | clé rendue |
| lot `library_media_keys` | 3 clés |
| **non-régression** : cards, livraisons, conversations, messages, vibes de conversation, 131 fichiers `cards` | tous visibles |
| `profile_stats` | 3 publications (elle levait avant) |

`get_advisors` : aucune alerte nouvelle.

**Correctif 100 % serveur : aucun APK à réinstaller, la v0.9.49 fonctionne
telle quelle.**

## Remarque de Jay après validation : « les cards ne s'affichent plus comme avant »

Exacte, et c'est une régression que j'ai introduite — pas un choix.

### Le diagnostic

`CardViewerScreen` contenait **deux choses** que j'ai confondues :

1. Les **règles** d'une Vibe envoyée — livraisons, budgets de vues, durées,
   replay, faces épuisées. Une story et une publication n'en ont aucune.
2. La **présentation** d'une Vibe — `_FaceFrame` : liseré à la couleur du type
   (dégradé pour Oneshot et BeReal, or épais de 4 px pour la One of One), coins
   arrondis 20/17, fond noir, halo coloré.

J'ai eu raison d'écarter (1) en écrivant les nouvelles visionneuses. J'ai eu
tort d'abandonner (2) avec : mes écrans affichaient un `Image.file` nu, sans
cadre, sans couleur, sans halo.

### C'est exactement l'erreur que j'avais évitée juste avant

Deux heures plus tôt, en supprimant la machinerie `Thumb` devenue morte, j'avais
**repris ses deux qualités** (décodage en taille réduite, image de couverture
des vidéos) au lieu de les jeter avec le reste. Puis j'ai commis la même faute
un cran plus haut, sans la voir.

**Le réflexe à installer** : quand on remplace un composant, séparer ce qu'il
*contraint* de ce qu'il *apporte*. On jette les contraintes qui ne s'appliquent
plus ; on garde ce qu'il apportait. Un remplacement n'est jamais une pure
soustraction.

### Le correctif

`core/widgets/vibe_face.dart` — `VibeFaceFrame`, `VibePhotoFace`,
`VibeVideoFace`. Les **trois** visionneuses y passent désormais, y compris
`CardViewerScreen` dont la copie privée a été supprimée.

**La frontière est maintenant à la bonne place** : l'apparence appartient au
CONTENU (une Vibe ressemble à une Vibe, quel que soit son contexte de
diffusion), les règles appartiennent au FORMAT. Le chat garde sa propre face
vidéo — elle doit décompter, synchroniser les deux faces d'un Oneshot filmé et
verrouiller la barre selon le choix de l'émetteur — mais **seul le cadre est
commun**, et c'est la bonne frontière.

Vérifié par inventaire : plus aucune classe de cadre privée dans le code, et
les 7 sites d'affichage passent par les widgets partagés.

## Difficultés rencontrées ou erreurs commises

### Deux jointures écrites sur des tables non relues

`profiles.username` (la colonne est `display_name` + `tag_name`) et
`conversations.kind` (la colonne est `conversation_type`). Les deux ont échoué
à l'application de la migration, donc sans dégât — mais c'est **exactement**
l'erreur déjà commise le 2026-08-10 sur `connections.user_a` / `user_low`, et
déjà consignée : *ne jamais écrire une jointure sur une table qu'on n'a pas
relue*.

**Comment ne pas le reproduire** : relever les colonnes de CHAQUE table citée
dans une nouvelle fonction **avant** de l'écrire, pas après le premier échec.
Le coût est d'une requête ; l'oubli a coûté deux allers-retours.

### Défaut préexistant repéré au passage, non corrigé

La politique `cards_select_library` (table `cards`) contient
`li.card_id = li.id` — une comparaison entre `library_items.card_id` et
`library_items.id`, qui n'est vraie qu'en cas de collision d'UUID. **Cette
politique ne s'applique donc jamais.** Sans effet visible aujourd'hui
(`cards_select_public_library` et les autres branches couvrent les cas), mais
c'est une règle morte. À traiter à l'étape 2, quand `library_items` deviendra
autonome et que ces politiques seront réécrites.

## Notes et consignes de Jay

- Consigne architecturale impérative — voir ci-dessus, inscrite dans `CLAUDE.md`.
- Le streaming vidéo fera l'objet du **chantier suivant**, avec une explication
  préalable demandée par Jay.
