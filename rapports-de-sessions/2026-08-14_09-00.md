# Rapport de session — 2026-08-14 09:00

## Contexte à la reprise

Relu en ouverture : `CLAUDE.md`, le rapport `2026-08-13_21-46.md` (clôture de la
session précédente).

Point n°2 de la liste « ce qui attend » de la session précédente : **brancher le
MCP Rive**. C'est la première demande de Jay à la reprise.

---

## Vérification du MCP Rive — ✅ CONNECTÉ ET FONCTIONNEL

Vérifié par **appel réel**, pas par la présence des outils dans le catalogue :
un serveur MCP peut être déclaré et répondre en erreur.

| Contrôle | Résultat |
|---|---|
| `list_artboards` | ✅ répond — 1 artboard, `0-2` « Main Board », actif |
| `get_artboard_hierarchy` | ✅ arbre complet rendu (Layout, Root Group, 8 enfants) |
| `open_file_editor / getSelectedArtboard` | ✅ 220 × 110, x=0 y=0 |

L'éditeur Rive est donc **joignable en écriture comme en lecture** depuis cette
session : les 40 outils `mcp__rive__*` sont disponibles (édition de composants,
animations, scripts, ViewModels, upload d'assets et de `.rev`).

### ⚠️ Le fichier ouvert n'est pas un fichier NeoVibe

Contenu relevé dans l'artboard actif :

```
Main Board (220 × 110)
└── Layout
    └── Root Group
        ├── BTN Outline, BTN BG, BTN BG HoverArea  (Shape)
        ├── Text, PlayBTN                          (Text, Node)
        ├── BlurredBtn, BlurredCircle              (Image, Shape)
        └── Orbs                                   (Node)
```

C'est un **bouton de démo** (fond `#ff202020`), pas un élément d'interface
NeoVibe. Rien à en conclure de négatif — le MCP est bien branché — mais toute
action d'édition porterait sur ce fichier tant que Jay n'en ouvre pas un autre.

---

---

# Chantier — refonte de l'envoi d'une Vibe

Quatre demandes de Jay. Les trois premières sont livrées, la quatrième est
bloquée sur un export que seul lui peut faire.

## Ce que le relevé a montré AVANT d'écrire

Trois faits qui ont changé le plan, relevés en base et dans le code :

| Croyance de départ | Réalité relevée |
|---|---|
| « 1 vue = 1 ouverture » est à construire | **Déjà vrai côté serveur.** `open_card_media` rend **une clé pour toute la card** et le viewer l'appelle **une fois par ouverture** (`card_viewer_screen.dart:200`). Le décompte n'a jamais été en cause. |
| « visionnage illimité » demande une migration | **Aucune.** Contrainte relevée en base : `max_views is null or between 1 and 5`, et `open_card_media` fait `coalesce(max_views, 2147483647)`. `null` = illimité, déjà accepté. |
| « format » = type de Vibe | **Non — destination.** Le type (standard/oneshot/BeReal) se choisit déjà à la capture (`card_capture_screen.dart:172`). Confirmé par la formulation de Jay « le paramétrage du cercle ». |

**La règle n'était donc pas fausse en base : elle était trahie dans
l'affichage.** `_onSideSettled` appelait `_markFaceDone(left)` — retourner la
carte tuait la face quittée, et deux retournements fermaient une Vibe que
l'utilisateur n'avait ouverte qu'une fois.

## 1. L'envoi passe en deux étapes

`card_send_screen.dart` (850 lignes, quatre destinations dans un seul `build`)
**supprimé**, remplacé par `lib/features/cards/send/` :

| Fichier | Rôle |
|---|---|
| `vibe_draft.dart` | ce qui sort de la capture — **immuable** |
| `send_format_screen.dart` | **étape 1** : quel format, et l'explication de chacun |
| `circle_settings_screen.dart` | **étape 2** — cercle |
| `story_settings_screen.dart` | **étape 2** — story |
| `publication_settings_screen.dart` | **étape 2** — bibliothèque |
| `conversation_library_settings_screen.dart` | **étape 2** — bibliothèque de conv. |
| `send_common.dart` | ce qui obéit aux **mêmes** règles partout |

Ce n'était pas qu'un problème de longueur : **les réglages survivaient au
changement de destination**. Un `shareable` activé pour une story repartait
avec une publication sans que rien ne l'annonce. Maintenant un paramétrage naît
avec son écran et meurt avec lui.

### 🐛 Deux défauts que le découpage a fait tomber tout seuls

- **Un interrupteur mort.** « Barre de lecture contrôlable » s'affichait pour la
  bibliothèque et la bibliothèque de conversation. Or `scrubbable` n'existe que
  sur `cards` — vérifié par inventaire : ni `LibraryRepository.publish` ni
  `addVibe` n'ont ce paramètre. L'interrupteur se laissait basculer et n'allait
  **nulle part**.
- **Une règle produit cachée dans un `if`.** « Enregistrer pour moi » était
  cochable pour la bibliothèque de conversation mais ne faisait rien
  (`if (_saveForMe && id != null)`, et `_addToConversationLibrary` rend `null`).
  C'est pourtant une vraie règle : l'auteur ne revoit pas son ajout avant le
  reveal. Elle se **lit** désormais — le bouton n'existe pas sur cet écran.

**Envoi direct depuis un chat** : seul chemin qui saute l'étape 1, il entre
directement dans le paramétrage du cercle.

## 2. « Enregistrer pour moi » devient un bouton qui agit tout de suite

`SaveForMeButton` + `lib/core/widgets/top_banner.dart` (bandeau haut, glissé,
auto-effacé) : « Vibe sauvegardée » / « Vibe déjà sauvegardée ».

**Pas un `SnackBar`** (il arrive par le bas, là où est le bouton d'envoi) ni un
`MaterialBanner` (il pousse la mise en page, donc décale la liste des
destinataires sous le doigt). Le bandeau vit dans l'`Overlay` et ne déplace
rien.

**Ce que le changement corrige au passage** : la case cochée n'agissait
qu'après un envoi **réussi**. Abandonner l'écran ou échouer à l'envoi faisait
perdre la prise alors que la case était cochée — l'utilisateur croyait sa copie
faite.

### ⚠️ Le piège que ça ouvrait, et qui a été fermé

Sauvegarder **avant** l'envoi veut dire sauvegarder avant qu'un Content ID
n'existe. D'où un identifiant local `local-<uuid>` (via le `newUuid()` déjà au
projet, pas un second générateur).

Deux conséquences traitées :

1. **`SavedStore.purgeRevoked` envoie toutes les clés de l'index au serveur.**
   Une clé `local-…` aurait fait échouer le cast en `uuid[]` — et comme l'échec
   est avalé (`catch (_) => 0`, pour le hors-ligne), **une seule** clé locale
   aurait suspendu la purge de TOUTES les autres, en silence. Les clés locales
   sont désormais écartées de la question. Le préfixe est là pour ça : un UUID
   nu aurait été indistinguable d'un vrai Content ID.
2. **`SavedStore.rekey`** : après un envoi réussi, la copie prend son vrai
   Content ID. Sans ce recollage, une sauvegarde anticipée serait devenue
   **injoignable pour la révocation de modération** — une garantie perdue en
   remplaçant un composant.

**Un troisième défaut trouvé en me relisant** : le récap reconstruit un
`VibeDraft` à chaque appui sur « Continuer » (les faces ont pu être retouchées).
Il en tirait donc un identifiant neuf à chaque fois — partir de l'étape 1 puis
revenir aurait donné deux identités à la même prise, et deux copies. L'identifiant
est maintenant tiré une fois, dans l'état du récap.

## 3. Les règles de visionnage passent derrière un bouton, et changent

- **Bouton `tune` en haut à droite** du paramétrage du cercle → feuille de
  réglages. L'écran ne montre plus que ce qui se décide vraiment ici : **à qui**.
- **Défauts** : durée de lecture **illimitée** (était 10 s), **2 ouvertures**.
- **Ouvertures illimitées** possibles — cran « ∞ » au bout du curseur (6),
  envoyé en base comme `max_views = null`. Même cran ajouté dans
  Réglages → Vibes.
- Un rappel cliquable des règles en vigueur reste sur l'écran : les cacher sans
  les annoncer aurait remplacé un défaut par un autre.

### La vraie modification : une ouverture est une ouverture

`card_viewer_screen.dart` — `_onSideSettled` n'épuise plus la face quittée.

| | Avant | Après |
|---|---|---|
| Retourner la carte | tue la face quittée | **met son budget en pause** |
| Revenir sur la première face | face morte | **encore visionnable** |
| Fin de session | 2 retournements suffisaient | quand **toutes** les faces ont épuisé **leur propre** temps |

Décision de Jay sur le cas non tranché (durée limitée + retournement libre) :
**budget par face, en pause**. Le temps ne coule que sur la face regardée, et
reprend là où il s'était arrêté — une remise à zéro aurait rendu la limite
contournable en retournant la carte.

Le consommé vit dans `_frontElapsedMs` / `_backElapsedMs` ; le timer n'est
qu'un robinet. Recalculer une date de fin à chaque retournement aurait ajouté
un état dérivé de plus, et un écart possible avec ce que la jauge affiche.

## 4bis. Le composant Rive modifié — clic au lieu de survol

Feu vert explicite de Jay : « j'aimerai que tu modifies l'élément pour que
l'animation se déclenche au clic et pas au hover ».

### 🛑 Trouvé en premier : une condition morte qui bloquait la transition

La transition `Default State → HoverState` (`0-676`) portait **deux**
conditions :

| Condition | État |
|---|---|
| `inputCondition` sur `Hover BG?` | valide |
| `inputTriggerFired` sur l'input **`0-0`** | ⚠️ **`0-0` n'existe pas** |

Vérifié plutôt que déduit : `query_objects(["0-0"])` ne rend rien, et la liste
des inputs de la state machine n'en compte que deux (`0-920`, `0-675`). C'était
un **déclencheur fantôme**, reste d'une version précédente, sur la seule
transition qui fait entrer dans l'état animé. Supprimé — sinon le passage au
clic n'aurait rien changé non plus.

### Le remappage

| Listener | Cible | Avant | Après |
|---|---|---|---|
| `BTN Outline Down` (`0-889`) | `BTN Outline` | `enter` | **`down`** |
| `BTN Outline Up` (`0-921`) | `BTN Outline` | `exit` | **`up`** |
| `BTN BG Down` (`0-917`) | `BTN BG HoverArea` | `enter` | **`down`** |
| `BTN BG Up` (`0-901`) | `BTN BG HoverArea` | `exit` | **`up`** |

Les actions, les inputs, les transitions et les deux animations
(`Default State`, `HoverState`, `ColorFloat`) sont **intacts** : seule la source
du déclenchement change. C'est la discipline « un remplacement n'est pas une
soustraction » — ce que le composant *contraint* (le survol) change, ce qu'il
*apporte* (les deux états, les courbes, les orbes) ne bouge pas.

### Pourquoi `up` et pas `exit` — et le cas limite que ça laisse

J'ai d'abord voulu garder `exit` **en plus** de `up` comme filet : `up` ne se
déclenche que si le doigt se relève **sur** la cible, donc glisser hors du
bouton avant de relâcher laisserait l'état pressé collé.

**Impossible** : `duplicate_objects` refuse les listeners (« is not a
Component »), et `create_listeners` ne sait produire que des actions
`viewModelChange` — or ces deux entrées sont des **inputs legacy**, pas des
propriétés de ViewModel. Il n'y a donc qu'un seul listener « faux » par booléen.

Entre les deux, `up` est le bon choix, et pour la raison même du chantier :
**`enter`/`exit` sont des notions de souris**. Sur un écran tactile il n'y a pas
de survol, donc `exit` pourrait ne jamais se déclencher — et l'état resterait
pressé **pour toujours**. Avec `up`, le seul cas dégradé est « relâcher hors du
bouton », qui se rattrape au geste suivant.

👉 **Le vrai correctif est de passer ces deux booléens en propriétés de
ViewModel** — ce qui résoudrait aussi le libellé figé. À proposer à Jay.

### 4ter. Passage en ViewModel — feu vert de Jay, et le filet revient

Les deux entrées legacy (`Hover BTN?`, `Hover BG?`) **n'existent plus**.

| Créé | Détail |
|---|---|
| ViewModel `SendButton` (`0-1027`) | `pressedBg` (bool), `pressedBtn` (bool), `label` (string) |
| Instance `Default` (`0-1035`) | `label = "ENVOYER"`, les deux bools à `false` |
| Liaison | instance liée à l'artboard `Main Board` |
| Data binding | le `TextValueRun` (`0-128`) suit `label` — **le libellé n'est plus figé dans le binaire** |
| 6 listeners | `down` → true, `up` → false, **`exit` → false**, sur chacune des deux zones |
| 6 conditions | `viewModelComparison` sur les 4 transitions, **les deux layers** |

**Le filet `exit` est revenu, et c'est le ViewModel qui l'a rendu possible** :
`create_listeners` ne sait produire que des actions `viewModelChange`, jamais de
`boolChange` sur une entrée legacy. Tant que les booléens étaient des inputs, on
ne pouvait ni dupliquer un listener ni en créer un troisième. Relâcher hors du
bouton ne laisse donc plus l'état pressé collé.

**Suppression menée dans les deux sens** (règle 8) : les 2 inputs étaient cités
par **6 conditions réparties sur les deux layers** (`BTN Shape` *et*
`Floating Colors` — le second aurait été oublié en ne lisant que la transition
visible) et par les 4 actions de listeners. Tout remplacé **avant** de supprimer.

**Vérifié par relevé de l'état, pas par la sortie des commandes** :
`queryStateMachine` rend `"inputs": []`, six listeners `viewModelChange`, et les
quatre transitions portent des `viewModelComparison` aux bonnes valeurs. Les
valeurs constantes des six actions ont été relues une par une
(`true/false/false` × 2). Animations, états, interpolateurs et orbes :
**intacts**.

### Ce qui reste à surveiller au test

Sur un appui bref, l'état pressé ne dure que le temps de l'appui (transitions à
200 ms). Si Jay veut que l'animation se **joue en entier** au tap, c'est un
`exitTime` sur la transition `HoverState → Default State` — modification plus
profonde de sa state machine, pas engagée sans son accord.

## 4. Bouton Rive — intégré (v0.9.72+162)

**Le MCP Rive n'exporte pas.** Vérifié sur le catalogue complet : `upload_rev`
et `upload_asset` envoient **vers** Rive, `assets_tool` ne fait que lister et
instancier. Aucun outil ne sort un binaire de l'éditeur — Jay a donc exporté
lui-même.

| Fichier | Emplacement | Taille |
|---|---|---|
| `watch_reel_button.riv` | `assets/rive/` | 137 Ko |
| `watch_reel_button.rev` | `design/rive/` (hors assets, pas dans l'APK) | 2,2 Mo |

**Export vérifié avant d'écrire une ligne de Dart** : `SendButton`,
`pressedBg`, `pressedBtn`, `label`, `Main Board`, `State Machine 1` et
`ENVOYER` sont tous présents dans le binaire. Sans ce contrôle, une erreur de
liaison à l'exécution aurait été impossible à distinguer d'une erreur de code.

### Ce qui a été écrit

| Fichier | Changement |
|---|---|
| `pubspec.yaml` | dépendance `rive 0.14.11` (+ `rive_native 0.1.11`), section `assets:` |
| `main.dart` | `await rive.RiveNative.init()` — **enveloppé** : un moteur natif absent ne doit pas empêcher l'app de démarrer |
| `core/widgets/rive_send_button.dart` | **nouveau** — chargement, data binding, libération des ressources natives |
| `send/send_common.dart` | `SendActionBar` monte le bouton, **un seul point pour les quatre écrans** |

**API lue dans le paquet, pas écrite de mémoire** (`example/lib/examples/
databinding.dart` et `rive_widget.dart` du cache pub) : `File.asset(…,
riveFactory: Factory.rive)` → `RiveWidgetController` → `dataBind(DataBind.auto())`
→ `viewModelInstance.string('label')`.

### Trois décisions qui méritent d'être écrites

1. **`RiveHitTestBehavior.transparent`**, et pas `translucent`. Relevé dans
   `rive_native/lib/src/rive_hit_test.dart` : `translucent` laisse Rive
   **consommer** le tap dès qu'une zone d'écoute est touchée — c'est-à-dire
   exactement sur le bouton, donc le `onPressed` Flutter n'aurait jamais tiré.
   `transparent` est le seul mode qui donne les deux : l'animation joue **et**
   le geste traverse.
2. **Le repli est un vrai `FilledButton`.** Tant que le graphique n'est pas
   chargé, et définitivement s'il échoue, c'est lui qui s'affiche. Un bouton
   d'envoi absent rend l'app inutilisable : le rendu Rive est un habillage,
   jamais une dépendance.
3. **L'état « envoi en cours » est posé PAR-DESSUS**, en Flutter. La state
   machine de Jay n'a pas cet état, et lui en inventer un aurait voulu dire
   toucher à son authoring pour un cas que Flutter sait déjà montrer.

Les deux booléens ne sont **jamais écrits depuis le Dart** : ce sont les
listeners `down`/`up`/`exit` qui les portent, donc l'animation répond au doigt
sans aller-retour par Flutter.

### ⭐ L'APK par architecture, mesuré

Proposé la session dernière, appliqué ici parce que Rive alourdissait l'APK gras
à **83,1 Mo** — trop lourd à télécharger sur un téléphone.

| Variante | Taille |
|---|---|
| Gras (3 ABI) | 83,1 Mo |
| **arm64-v8a** (le téléphone de Jay) | **29,6 Mo** |
| armeabi-v7a | 26,5 Mo |
| x86_64 (émulateurs seulement) | 31,4 Mo |

**29,6 Mo Rive inclus, contre 60,9 Mo sans Rive avant.** Le chiffre annoncé le
2026-08-13 (29,3 Mo) se vérifie.

### Le piège de build, deuxième couche

`RSUNA.md` note qu'il faut préfixer le PATH pour `rive_native` (qui appelle
`dart` en le supposant dans le PATH). **Ça n'a pas suffi** : le PATH seul fait
échouer Gradle sur `JAVA_HOME is not set`. La commande complète, déjà écrite en
`RSUNA.md` §230-240, est celle qui marche :

```bash
export JAVA_HOME="C:/Charles/dev/jdk17"
export ANDROID_HOME="C:/Charles/dev/android-sdk"
export PATH="/c/Charles/dev/flutter/bin:/c/Charles/dev/jdk17/bin:$PATH"
flutter build apk --release --split-per-abi
```

---

## Le composant relevé avant modification

| Élément | Valeur |
|---|---|
| Artboard | `Main Board`, 220 × 110 |
| State machine | `State Machine 1` |
| Entrées | `Hover BTN?` et `Hover BG?` (bool), **toutes deux `isPublic: false`** |
| Listeners | enter/exit sur `BTN Outline` (`0-75`) et `BTN BG HoverArea` (`0-895`) |
| ViewModels | **aucun** — le libellé est un `TextValueRun` figé (`0-128`) |
| Décor | 12 orbes en `FollowPathConstraint`, image floutée, dégradés radiaux |

⚠️ **Deux points à signaler à Jay avant qu'il n'investisse du temps** :

1. **Il n'y a pas de survol sur un écran tactile.** Les listeners enter/exit ne
   se déclencheront qu'au doigt posé : l'animation devient un état *pressé*, pas
   *survolé*.
2. **Aucun ViewModel** : le texte du bouton et ses couleurs sont figés dans le
   binaire. C'est exactement le point de l'étude du 2026-08-13 — « une couleur
   codée en dur dans l'animation est figée ». Les quatre écrans ont quatre
   libellés différents (« Envoyer », « Publier ma story », « Publier »,
   « Ajouter à la bibliothèque ») : sans ViewModel, il faudra soit un seul
   libellé, soit poser le texte en Flutter par-dessus.

Point de montage prêt : `SendActionBar` dans `send_common.dart` — **un seul**,
partagé par les quatre écrans.

---

# Réflexion demandée par Jay — « la card sauvegardée est presque un autre format »

Jay propose de **créer la Card au clic sur Sauvegarder**, puis soit de la
partager telle quelle, soit de la remplacer par la card partagée. Et il ajoute :
« cela me fait réfléchir sur le fait que la card sauvegardée est presque un
autre format non ? Car les règles de visionnage ne s'appliquent pas sur les
sauvegardées. »

**Sa dernière phrase est la bonne réponse à sa propre question, et elle est
déjà appliquée.** Une sauvegarde n'est pas une Card avec des réglages
désactivés : c'est un **autre objet**, exactement au sens de la règle 5 de
`CLAUDE.md` (« un objet appartient à un seul contexte de diffusion »).

| | Card (cercle) | **Sauvegarde** |
|---|---|---|
| Octets | scellés, coffre `cards` | **en clair, sur l'appareil** |
| Clé | retenue par le serveur, rendue par `open_card_media` | **aucune — inutile** |
| Règles de visionnage | **c'est l'objet même** | **n'existent pas** |
| Lignes serveur | `cards` + `card_media_keys` + `card_deliveries` | **aucune** |
| Cycle de vie | TTL, épuisement, révocation | **permanent** (décision du 2026-08-11) |

## Pourquoi créer la Card au clic coûterait plus que ça ne rapporte

1. **La sauvegarde cesserait d'être instantanée.** Créer une Card = sceller +
   téléverser les deux faces + trois lignes. Sur 4G c'est plusieurs secondes, et
   **hors ligne c'est impossible**. Une copie locale est une recopie de fichier.
   C'est la consigne « l'affichage de MES contenus ne doit jamais attendre le
   réseau », appliquée à l'écriture.
2. **Des Cards orphelines.** Sauvegarder puis abandonner laisserait une ligne
   `cards` et des médias scellés **sans aucune `card_deliveries`**. Rien ne les
   purge aujourd'hui — s'ajoute à la fuite déjà notée en `RAPPELS` avant-prod #16.
3. **Les règles n'existent pas encore au moment du clic** — Jay l'a vu lui-même.
   Il faudrait créer la Card avec des règles provisoires puis les **modifier**.
   Or aujourd'hui les règles sont figées à la création et le serveur les applique
   dans la transaction qui rend la clé. Les rendre modifiables après coup
   **ramollit la garantie** du 2026-08-10.
4. **Les octets ne sont pas les mêmes.** Le clair de la sauvegarde est
   délibéré : c'est ce qui la rend indépendante du serveur. Fusionner les deux
   objets obligerait soit à mettre du clair là où vit du contenu contrôlé, soit
   à faire dépendre une sauvegarde d'une clé serveur — ce qui casse la promesse
   « gardé ».

## Ce que son option 2 décrit existe déjà, en moins cher

« On remplace la sauvegardée par la partagée » = `SavedStore.rekey`, sans le
re-téléversement : après un envoi réussi, la copie locale **adopte le Content ID**
de la Card. Les octets restent locaux et en clair ; seul le **nom** est partagé,
et il ne sert qu'à une chose — permettre à la révocation de modération de la
retrouver. C'est un pointeur, pas une fusion.

Et son objectif — « on sauvegarde la card même si le partage est abandonné » —
est **déjà le comportement livré** : le clic sauvegarde, l'envoi est
indépendant.

⚠️ **Asymétrie à assumer, signalée** : une Vibe sauvegardée **puis** partagée
devient joignable par la révocation ; sauvegardée et jamais partagée, non. C'est
inévitable — une prise jamais envoyée n'a aucune existence serveur à modérer.

## ✅ Acté par Jay le 2026-08-14

La sauvegarde est le **cinquième contexte de diffusion**. Consigné à deux
endroits :

- **`docs/stockage-et-acces.md`** — nouvelle section, à la suite des six
  buckets : le tableau comparatif, les quatre raisons de ne jamais refusionner,
  et ce qu'est exactement `rekey` (un pointeur, pas une fusion), plus
  l'asymétrie de révocation assumée.
- **`CLAUDE.md`** — ajouté aux **décisions verrouillées**, avec l'interdiction
  explicite de créer la Card au clic sur « Enregistrer pour moi ».

Non consigné en mémoire longue **à dessein** : c'est désormais dans `CLAUDE.md`,
qui est relu à chaque session.

---

## Vérifications

- `dart format` + `flutter analyze` : **propres, 0 problème**.
- **56 tests Dart** passent.
- **Aucun fichier natif touché** — `docs/parties-natives-par-os.md` reste exact.
- **Aucune migration** : les deux changements de règles sont côté client, la
  base acceptait déjà `max_views = null`.
- Suppression de `card_send_screen.dart` — **deux sens relevés** (règle 8) :
  *entrant*, un seul appelant (`card_capture_screen.dart`) plus une mention en
  commentaire dans `card.dart:13`, corrigée ; *sortant*, tout ce qu'il appelait
  (`savedStoreProvider`, les quatre dépôts, `cardsExplainerShownProvider`) est
  repris par les nouveaux écrans — aucun orphelin.
- **Inventaire** (pas le diff) : plus aucune occurrence de `_Destination`,
  `Visionnages`, ni de `CardSendScreen` dans `lib/`.

## Livraison — v0.9.72+162

- Commit `456578d`, tag `v0.9.72`, **release GitHub publiée** avec deux APK :
  `neovibe-v0.9.72-arm64.apk` (30 Mo, celui du téléphone de Jay) et
  `neovibe-v0.9.72-universel.apk` (84 Mo, secours).
- `dart format` + `flutter analyze` : **propres**. **56 tests** passent.
- **Aucun secret** dans le diff (motifs JWT / `service_role` / clés Supabase
  cherchés sur `lib/`, `docs/`, `CLAUDE.md` et le rapport : rien).
- `docs/parties-natives-par-os.md` **mis à jour** : nouvelle section « natif
  fourni par un paquet » pour `rive_native` — l'initialisation obligatoire, le
  coût réel par ABI, et la dette KGP signalée par Flutter. Aucun fichier `.kt`
  du projet touché de la session.

---

# Retours de test de Jay sur la v0.9.72 — v0.9.73+163

> « La nouvelle séparation du menu avec les étapes me convient. »

Trois demandes, plus une remarque de fond sur le geste.

## 1. Le bouton était trop petit — et j'ai trouvé pourquoi

Ce n'était pas la hauteur, c'était le **cadrage**. En `Fit.contain` sur un
artboard 220 × 110 dans une barre pleine largeur haute de 62 px, le graphique
se dessinait à **124 × 62** au milieu d'une barre de ~360 px. Il occupait un
tiers de la place qu'on lui donnait.

Corrigé par un `AspectRatio(220/110)` centré, et la hauteur passée de 62 à
**96** — le graphique fait donc 192 × 96.

## 2. 🎯 La remarque de Jay sur le geste, et le vrai défaut qu'elle a révélé

> « Il peut être pressé mais pas relâché et cela ne doit pas compter comme une
> action, si le doigt relâche en dehors de la zone de clic c'est comme un
> abandon. »

`GestureDetector.onTap` fait déjà exactement ça — il ne se déclenche que si le
doigt se pose **et** se relève dans la zone, et `onTapCancel` prend le relais
sinon. Il n'y avait donc rien à écrire… **sauf que « la zone » était fausse.**

Le `GestureDetector` couvrait **toute la largeur de la barre** alors que le
bouton visible n'en occupait que le milieu. Relâcher le doigt **à côté du
bouton dessiné** déclenchait quand même l'envoi. La remarque de Jay portait sur
un comportement qu'il n'avait peut-être pas encore vu, et elle a mis le doigt
sur un défaut réel.

L'`AspectRatio` du point 1 le corrige **par construction** : la zone sensible
est désormais le graphique, au pixel près. Une correction, deux défauts.

## 3. Le bouton devient son propre loader

> « Je voudrais que l'état où il devient rond se conserve après qu'il ait été
> définitivement cliqué comme un loader le temps de la publication. »

`RiveSendButton.busy` : pendant l'envoi, le Dart **maintient** `pressedBg` et
`pressedBtn` à vrai. Le rond de progression posé par-dessus a été retiré — il
doublait le message et cachait l'animation.

⚠️ **Le conflit évident, supprimé plutôt que surveillé.** Ces deux booléens
sont aussi écrits par les listeners `up`/`exit` : si le doigt repassait sur le
bouton pendant l'envoi, ils auraient défait ce que le Dart maintient. Pendant
`busy`, le graphique passe en **`RiveHitTestBehavior.none`** — Rive ne reçoit
plus aucun pointeur, donc ne peut plus rien écrire. La cause disparaît ; il n'y
a pas de garde-fou à maintenir.

## 4. La vague rosée — reprise du fichier « Apple Intelligence »

Jay a ouvert un second fichier Rive et demandé de récupérer l'animation de la
vague rosée pour la déclencher après l'envoi.

### Ce que le relevé a montré

`colorsplash` (`0-96`) est une **ellipse avec un dégradé et un flou. Rien
d'autre.** Pas d'image, pas de `Mesh`, pas d'os — contrairement au reste de
l'artboard (le mockup de téléphone, lui, est une image riggée sur quatre os
`ripple-*`).

Tout est relevé, valeur par valeur :

| Élément | Relevé |
|---|---|
| Forme | ellipse 328 × 328, origine centrée |
| Dégradé | linéaire `#28ffffff` → **`#fff55ac9`** |
| `Feather` | force 20, espace monde |
| Échelle | 0 → **300 %** sur 18 images |
| Opacité | 100 % jusqu'à l'image 7, **0** à l'image 26 |
| Durée | 30 images (0,5 s) |

### 🛑 Deux obstacles à la réutilisation directe du `.riv`

1. **Les six assets du fichier sont en `includeInExport: false`** (relevé dans
   `listAssets`). Ils ne sont pas embarqués : le fichier exporté attendrait
   qu'on les lui fournisse à l'exécution. Le mockup de téléphone ne
   s'afficherait pas.
2. **Le MCP n'ouvre qu'un fichier à la fois.** Reconstruire la vague dans le
   fichier du bouton aurait demandé à Jay de rouvrir ce fichier, puis de le
   réexporter — deux allers-retours de plus, alors qu'il voulait clôturer.

### La décision, assumée et réversible

**Implémentée en Flutter** (`lib/core/widgets/send_wave.dart`), à partir des
chiffres ci-dessus. Ce n'est pas une interprétation : c'est une spécification
recopiée. Et une troisième raison a pesé — **l'effet doit survivre à la
navigation** : l'envoi dépile jusqu'à la racine, un `RiveWidget` monté dans
l'écran d'envoi mourrait avec lui. L'overlay vit dans l'`Overlay` **racine**.

⚠️ **Si Jay préfère la vraie ressource Rive, tout est prêt** : les chiffres sont
dans le fichier Dart, il suffit qu'il rouvre le fichier du bouton.

Détail d'implémentation qui aurait mordu : le retrait de l'`OverlayEntry` est
**différé d'une image**. Le `AnimationStatus.completed` arrive pendant la phase
de ticks, avant le build — retirer l'entrée à cet instant revient à modifier
l'arbre en cours de frame.

---

# Format de livraison — l'échange qui a suivi, et une erreur de méthode

## ❌ Erreur de méthode : j'ai changé la manière de release sans demander

Le rapport du 2026-08-13 disait « **à proposer** au prochain build ». Je ne l'ai
pas proposé, je l'ai fait. Jay s'est retrouvé avec **deux fichiers au lieu d'un**
et **un APK plus petit qu'avant alors qu'il venait d'ajouter des choses** — sans
explication.

**La façon dont Jay reçoit ses builds est sa décision, pas la mienne.** Le choix
lui a été rendu ; en attendant sa réponse, le format reste celui de la v0.9.72
(arm64 + universel en secours).

## Ce qui l'a perturbé, et pourquoi c'était légitime

J'avais annoncé **trois** variantes puis publié **deux** fichiers. Les deux
listes sont différentes et je les ai mélangées :

- la compilation par ABI **produit** trois APK (arm64-v8a, armeabi-v7a, x86_64) ;
- j'en ai **attaché** deux, et pas les mêmes : l'arm64 et l'**universel**.

Et surtout : « 30 Mo contre 60 Mo » n'est **pas** « avec Rive contre sans Rive ».
**Deux axes ont changé en même temps** et je n'ai montré que le résultat.

## La mesure, faite sur les APK produits

| | 3 architectures | 1 architecture (arm64) |
|---|---|---|
| **Sans Rive** | 60,9 Mo | ~22 Mo |
| **Avec Rive** | 83,1 Mo | **29,6 Mo** |

Contenu réel du paquet arm64, relevé par `unzip -l` :

```
bibliotheques natives (.so) : 27,3 Mo   (92 % de l'APK)
  libflutter.so              11,0
  libapp.so                   8,8
  librive_native.so           7,3
le .riv de Jay              :  0,13 Mo
tout le reste               :  ~2,2 Mo
```

**Un APK Android est à ~92 % du code machine**, et le code machine n'est pas
portable : un APK « universel » en porte **trois copies**, dont l'appareil en
jette deux à l'installation. L'ancien 60,9 Mo était déjà gras — il portait trois
moteurs Flutter depuis toujours. Rive n'a rien créé : il a rendu le problème
assez gros pour qu'on le voie.

⭐ **À retenir pour la refonte UI** : le `.riv` pèse **130 Ko**. Ce qui coûte,
c'est le moteur (7,3 Mo), payé **une fois**. La deuxième animation est quasi
gratuite.

## La question de Jay sur la prod, et sa vraie réponse

> « Pour la production quel apk on doit release ? […] les émulateurs on s'en
> fout et les téléphones 32 bits sont une minorité, on peut passer outre non ? »

**Aucun des deux APK.** En production, le Play Store veut un **AAB**
(`flutter build appbundle`), obligatoire pour toute nouvelle app depuis août
2021 : un seul fichier, et Google fabrique le paquet de chaque appareil.
Construit et mesuré : **78,6 Mo d'AAB, ~30 Mo téléchargés par l'utilisateur**.

**Sa question d'arbitrage disparaît donc.** Avec un AAB on inclut **toutes** les
architectures : un appareil arm64 ne reçoit que l'arm64, garder armeabi-v7a et
x86_64 (Chromebooks) ne coûte rien à personne. L'arbitrage n'existe **que** pour
les APK de test distribués à la main.

🔴 **Et un blocage relevé au passage** : `android/app/build.gradle.kts:34` →
`signingConfig = signingConfigs.getByName("debug")`, `android/key.properties`
**absent**. **Aucun build de production n'est possible aujourd'hui.** C'est
`RAPPELS` #13, ressorti au bon moment.

Consigné en **`RAPPELS.md` #17** (nouveau, avant-prod), avec le renvoi croisé
vers #13 qui le bloque.

---

## Ce qui reste

1. **Le relevé de mesures vidéo**, toujours en attente depuis la v0.9.67.
2. **À juger au test** : l'animation du bouton ne dure que le temps de l'appui.
   Si Jay la veut jouée en entier au tap → `exitTime` sur
   `HoverState → Default State`.
3. **Dette KGP** : `rive_native` et `flutter_foreground_task` appliquent
   l'ancien plugin Kotlin ; Flutter annonce que les futures versions
   refuseront de compiler.
4. La refonte UI proprement dite — les vues de Jay dans `docs/`.

## Difficultés rencontrées ou erreurs commises

### Un correctif qui ouvrait une panne silencieuse ailleurs

Le premier jet sauvegardait sous un identifiant local **sans toucher à
`purgeRevoked`**. Rien n'aurait échoué visiblement : l'exception du cast `uuid[]`
est avalée par le `catch` prévu pour le hors-ligne, et la purge des
sauvegardes révoquées se serait arrêtée pour tout le monde, sans un log. Trouvé
en relisant ce que la nouvelle clé **traverse**, pas en relisant le diff — même
famille que les leçons du 2026-08-13.

### Une identité tirée au mauvais endroit

`VibeDraft` tirait son identifiant local dans son constructeur, et le récap
construit un draft à chaque appui sur « Continuer ». Deux allers-retours =
deux identités = deux copies possibles. Corrigé en faisant porter l'identité par
l'état du récap.

## Notes et consignes de Jay

- Demande d'ouverture de session : vérifier que le MCP Rive est fonctionnel et
  connecté.
- **Cette session vise à modifier et intégrer des composants UI** pour la
  refonte. On commence par ce qui ne demande pas de composant Rive.
- **Envoi d'une Vibe en deux étapes** : « d'abord l'utilisateur choisit quel
  format il souhaite créer, ensuite il arrive sur l'interface dédiée au
  paramétrage de ce format ».
- **« Enregistrer pour moi »** : « n'est plus cochable, c'est un bouton
  cliquable et lorsqu'il est cliqué la vibe est directement enregistrée dans les
  sauvegardes de l'utilisateur, avec une petite notification (genre popup pas
  notification réelle) dans un bandeau en haut ».
- **Paramétrage du cercle** : « déplacer les curseurs et paramètres de
  visionnage dans un bouton réglage en haut à droite ».
- **Nouvelles règles** : « par défaut c'est temps de lecture illimité et
  visionnage à 2 […] possibilité de paramétrer visionnage illimité ».
- **🔑 Un visionnage est une OUVERTURE** : « on ne compte pas le retournement de
  face : si un utilisateur ouvre une card et change de face, la première face
  est toujours visionnable pendant que la card est ouverte. […] L'utilisateur ne
  peut ouvrir la carte que 2 fois par défaut. » Jay a lui-même signalé que
  c'était plus qu'un changement d'UI.
- **Durée limitée + retournement libre** (cas non tranché, arbitré en séance) :
  **budget par face, en pause**.
- **Bouton envoyer** : « utilise le composant ouvert que j'ai modifié dans
  rive ».
- **Rive, déclenchement** : « j'aimerai que tu modifies l'élément pour que
  l'animation se déclenche au clic et pas au hover ». Puis, sur proposition :
  **« oui vas-y pour le ViewModel »**.
- **✅ « acte le cinquième contexte »** — la sauvegarde est un contexte de
  diffusion à part entière. Écrit dans `docs/stockage-et-acces.md` et dans les
  décisions verrouillées de `CLAUDE.md`.
- **Livraison** : « on attend le riv » — pas d'APK avant que le fichier ne soit
  déposé.
- **⚠️ Format de release — décision RENDUE à Jay, en attente.** Il a signalé
  être perturbé d'avoir vu changer la manière de release sans qu'on lui
  demande. Deux options sur la table : arm64 + universel (30 Mo), ou un seul
  universel comme avant (84 Mo). **Ne pas retoucher ce format sans sa réponse.**
- **« Enregistre cela »** (2026-08-14, après l'explication sur les APK) :
  consigné en `RAPPELS.md` #17 et dans ce rapport.
- **Validation du découpage** : « la nouvelle séparation du menu avec les
  étapes me convient ».
- **Bouton d'envoi** : « trop petit » ; « je voudrais que l'état où il devient
  rond se conserve après qu'il ait été définitivement cliqué comme un loader le
  temps de la publication ».
- **🎯 Règle du geste** : « il peut être pressé mais pas relâché et cela ne doit
  pas compter comme une action, si le doigt relâche en dehors de la zone de
  clic c'est comme un abandon ».
- **Vague rosée** : « j'aime particulièrement l'animation de la vague rosée, tu
  peux la récupérer pour la déclencher par exemple après avoir cliqué sur le
  bouton envoyer ».
