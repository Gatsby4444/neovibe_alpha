name: neovibe
description: "NeoVibe â€” le rÃ©seau social de la prÃ©sence physique."
publish_to: 'none'

# versionName+versionCode. Ã€ INCRÃ‰MENTER Ã  chaque release Ã  tester : sans un
# versionCode plus Ã©levÃ©, Android refuse de mettre Ã  jour un APK sideloadÃ© (Jay
# est restÃ© bloquÃ© en v0.9.2 en croyant installer la v0.9.3). Convention :
# 0.9.4+94 â†’ versionName 0.9.4, versionCode 94 (suffixe = version sans points).
version: 0.9.106+196

environment:
  sdk: ^3.12.2

dependencies:
  flutter:
    sdk: flutter

  # Ã‰tat & backend
  flutter_riverpod: ^3.0.0
  supabase_flutter: ^2.10.0

  # Capture & mÃ©dias â€” la camÃ©ra est NATIVE (NativeCamera.kt + platform
  # channel), le plugin `camera` a Ã©tÃ© remplacÃ© (chantier Jay 2026-07-13)
  image_picker: ^1.1.0
  video_player: ^2.9.0

  # ProximitÃ© BLE â€” le lien (advertising/scan/GATT) est NATIF (NativeBle.kt),
  # le protocole (poignÃ©e de main chiffrÃ©e, certificats) vit en Dart
  permission_handler: ^12.0.0
  flutter_foreground_task: ^9.1.0
  cryptography: ^2.7.0
  flutter_secure_storage: ^9.2.0

  # Notifications locales
  flutter_local_notifications: ^19.0.0
  timezone: ^0.10.0

  intl: ^0.20.0
  shared_preferences: ^2.3.0
  path_provider: ^2.1.0
  cupertino_icons: ^1.0.8
  rive: ^0.14.11
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    # Les .riv sont les binaires de SORTIE de Rive. Les .rev éditables vivent
    # dans design/rive/, hors assets/ : ils ne doivent pas partir dans l'APK.
    - assets/rive/
